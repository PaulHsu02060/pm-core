/*! PM-Core (c) 2026 Paul Hsu. All Rights Reserved. Proprietary — see LICENSE. */
(function(){"use strict";App.registerModule({id:"transcripts",page:"transcripts",cat:"tools",title:"會議逐字稿",icon:"ti-microphone",desc:"會議錄音轉出的逐字稿存這裡：自動依專案歸檔，一鍵整理成可貼 Excel 的會議紀錄草稿。",badge:()=>{const n=(DATA.transcripts||[]).filter(t=>t&&!t._deleted).length;return n?n+" 筆":""}});const trAllRaw=()=>DATA.transcripts||[];const trAll=()=>trAllRaw().filter(t=>t&&!t._deleted);const trTrash=()=>trAllRaw().filter(t=>t&&t._deleted);function trPurgeTrash(){const cutoff=Date.now()-30*864e5;const before=trAllRaw().length;DATA.transcripts=trAllRaw().filter(t=>!t._deleted||new Date(t._deletedAt||0).getTime()>cutoff);if(DATA.transcripts.length!==before)Store.transcripts.save()}const trFolders=()=>DATA.folders||[];const trFind=id=>trAll().find(t=>t&&t.id===id);const fmtN=n=>(n||0).toLocaleString("zh-Hant");const byDateDesc=(a,b)=>String(b.date||"").localeCompare(String(a.date||""));const TR_ST={wait:"待整理",draft:"已生成草稿",done:"已完成"};const ST_ORDER=["未開始","進行中","暫緩","已完成"];const ST_CLS={"未開始":"no","進行中":"go","暫緩":"hold","已完成":"done"};function trCurRec(){return trFind(App._trCur)||(App._trDraft&&App._trDraft.id===App._trCur?App._trDraft:null)}function trEnsureStored(rec){if(!trFind(rec.id))Store.transcripts.add(rec)}function trCalcStatus(rec){const mm=rec.minutes||{};if(String(mm.text||"").trim()||rec.minutesTouched)return"done";const hasTable=(mm.table||[]).some(r=>((r.task||"")+(r.owner||"")+(r.due||"")).trim());return hasTable?"draft":"wait"}function trSave(rec){rec.status=trCalcStatus(rec);rec.updatedAt=new Date().toISOString();if(trFind(rec.id))Store.transcripts.save();trListRefresh()}let _trT=null,_trPendingRec=null;function trSaveDebounced(rec){const ind=document.getElementById("tr-save");if(ind)ind.textContent="…儲存中";clearTimeout(_trT);_trPendingRec=rec;_trT=setTimeout(()=>{_trT=null;_trPendingRec=null;trSave(rec);const el=document.getElementById("tr-save");if(el)el.textContent="✓ 已自動儲存"},500)}function trTrashRowHtml(){const n=trTrash().length;return n?`<div class="tr-trash-row" onclick="App._trTrashOpen()">🗑 回收桶（${n}）· 30 天內可還原</div>`:""}function trListRefresh(){trPurgeTrash();const el=document.getElementById("tr-list");if(el&&App.currentPage==="transcripts")el.innerHTML=trListHtml()+trTrashRowHtml()}App.renderTranscripts=function(){const el=document.getElementById("page-transcripts");if(!el)return;el.innerHTML=`<div class="tr-wrap">
      <div class="tr-head">
        <div class="tr-hico">🎙</div>
        <div><h1>會議逐字稿</h1><p class="tr-hdesc">依專案自動歸檔＋自建資料夾整理；搜尋、檢視、整理成會議紀錄</p></div>
      </div>
      <div class="tr-toolbar">
        <div class="tr-search"><input id="tr-q" placeholder="搜尋標題或內容…" value="${U.esc(App._trQ||"")}" oninput="App._trSetQ(this.value)"></div>
        <select class="tr-sel tr-viewsel" onchange="App._trSetView(this.value)">
          <option value="group"${App._trView!=="flat"?" selected":""}>檢視：分組</option>
          <option value="flat"${App._trView==="flat"?" selected":""}>檢視：平鋪（依日期）</option>
        </select>
        <button class="tb-action ghost" onclick="App._trNewFolder(null)">📁 新增資料夾</button>
        <button class="tb-action" onclick="App._trNew()">＋ 新增逐字稿</button>
      </div>
      <div id="tr-list">${trPurgeTrash(),trListHtml()+trTrashRowHtml()}</div>
    </div>`};App._trSetQ=function(v){App._trQ=v;trListRefresh()};App._trSetView=function(v){App._trView=v;trListRefresh()};function trListHtml(){const q=String(App._trQ||"").trim().toLowerCase();const match=t=>!q||[t.title,t.transcript,t.minutes&&t.minutes.text||""].some(s=>String(s||"").toLowerCase().includes(q));const items=trAll().filter(match);if(!trAll().length){return trEmptyHtml()+(trFolders().length?trFolderSection({},false):"")}if(App._trView==="flat"){const sorted=items.slice().sort(byDateDesc);return sorted.length?`<div class="tr-gbody tr-flat">${sorted.map(trCardHtml).join("")}</div>`:`<div class="tr-nohit">找不到符合「${U.esc(App._trQ||"")}」的逐字稿。</div>`}const byProj={},byFolder={},loose=[];items.forEach(t=>{const p=t.project?App.getProj(t.project):null;if(p)(byProj[p.id]||(byProj[p.id]=[])).push(t);else if(t.folderId&&trFolders().some(f=>f.id===t.folderId))(byFolder[t.folderId]||(byFolder[t.folderId]=[])).push(t);else loose.push(t)});const projs=(DATA.projects||[]).filter(p=>byProj[p.id]);const ordered=[...projs.filter(p=>!p.ecnType),...projs.filter(p=>p.ecnType)];const projGrp=p=>{const its=byProj[p.id].slice().sort(byDateDesc);return`<details class="tr-grp" open><summary><span class="tr-caret">▶</span><span class="tr-fico">📁</span><span class="tr-gname">${U.esc(p.name)}</span><span class="tr-tag ${p.ecnType?"tr-tag-ecn":"tr-tag-npi"}">${p.ecnType?"ECN":"NPI"}</span><span class="tr-cnt">${its.length} 筆</span></summary><div class="tr-gbody">${its.map(trCardHtml).join("")}</div></details>`};let html=`<div class="tr-treehead">依專案（自動歸檔）</div>`;html+=ordered.length?ordered.map(projGrp).join(""):`<div class="tr-secempty">（逐字稿在編輯視窗掛上專案後，會自動歸到這裡）</div>`;html+=trFolderSection(byFolder,!!q);if(loose.length){const its=loose.slice().sort(byDateDesc);html+=`<details class="tr-grp" open><summary><span class="tr-caret">▶</span><span class="tr-fico">📂</span><span class="tr-gname">未歸類</span><span class="tr-tag tr-tag-none">待整理歸檔</span><span class="tr-cnt">${its.length} 筆</span></summary><div class="tr-gbody">${its.map(trCardHtml).join("")}</div></details>`}if(q&&!ordered.length&&!loose.length&&!Object.keys(byFolder).length){html+=`<div class="tr-nohit">找不到符合「${U.esc(App._trQ||"")}」的逐字稿。</div>`}return html}function trEmptyHtml(){return`<div class="tr-empty" onclick="App._trNew()">
      <div class="tr-plus">＋</div><div class="tr-empty-t">新增第一份逐字稿</div>
      <div class="tr-empty-h">會議錄音用 Buzz 轉出 .txt／.srt 匯入（或直接貼上），系統幫你歸檔＋整理成會議紀錄草稿</div>
    </div>`}function trFolderSection(byFolder,searching){const roots=trFolders().filter(f=>!f.parentId);let html=`<div class="tr-treehead">手動資料夾（可自建·最多 3 層）</div>`;if(!roots.length)return html+`<div class="tr-secempty">（還沒有自建資料夾——右上「📁 新增資料夾」建立第一個）</div>`;const body=roots.map(f=>trFolderHtml(f,byFolder,searching)).join("");return html+(body||`<div class="tr-secempty">（資料夾裡沒有符合搜尋的逐字稿）</div>`)}function trFolderDeep(fid,byFolder){const own=(byFolder[fid]||[]).length;return own+trFolders().filter(x=>x.parentId===fid).reduce((s,k)=>s+trFolderDeep(k.id,byFolder),0)}function trFolderEmptyDeep(fid){if(trAll().some(t=>t.folderId===fid))return false;return trFolders().filter(x=>x.parentId===fid).every(k=>trFolderEmptyDeep(k.id))}function trFolderHtml(f,byFolder,searching){const kids=trFolders().filter(x=>x.parentId===f.id);const own=(byFolder[f.id]||[]).slice().sort(byDateDesc);const deep=trFolderDeep(f.id,byFolder);if(searching&&!deep)return"";const del=trFolderEmptyDeep(f.id)?`<button class="tr-fdel" onclick="event.preventDefault();event.stopPropagation();App._trDelFolder('${f.id}')" title="刪除空資料夾">🗑</button>`:"";const kidsHtml=kids.map(k=>trFolderHtml(k,byFolder,searching)).join("");return`<details class="tr-grp${f.depth>1?" tr-sub":""}" open>
      <summary><span class="tr-caret">▶</span><span class="tr-fico">📁</span><span class="tr-gname">${U.esc(f.name)}</span><span class="tr-cnt">${deep} 筆</span>${del}</summary>
      ${kidsHtml?`<div class="tr-nest">${kidsHtml}</div>`:""}
      ${own.length?`<div class="tr-gbody">${own.map(trCardHtml).join("")}</div>`:""}
      ${f.depth<3?`<button class="tr-mkdir" onclick="App._trNewFolder('${f.id}')">＋ 在此新增子資料夾</button>`:""}
    </details>`}function trCardHtml(t){const st=TR_ST[t.status]?t.status:"wait";const prev=String(t.transcript||"").replace(/\s+/g," ").trim().slice(0,60);const hasProj=!!(t.project&&App.getProj(t.project));const moveLabel=t.folderId?"↪ 移動":"↪ 移動到資料夾";return`<div class="tr-card">
      <div class="tr-ctop"><h3 class="tr-ctitle">${U.esc(t.title||"未命名會議")}</h3><span class="tr-cdate">${U.esc(t.date||"")}</span></div>
      <p class="tr-cprev">${prev?U.esc(prev)+"…":"（還沒有逐字稿內容）"}</p>
      <div class="tr-chips"><span class="tr-st tr-st-${st}">${TR_ST[st]}</span><span class="tr-cmeta">· ${fmtN(String(t.transcript||"").length)} 字</span></div>
      <div class="tr-cacts">
        <button class="tr-ico" onclick="App._trCopy('${t.id}')">📋 複製</button>
        <button class="tr-ico" onclick="App._trOpen('${t.id}')">✎ 檢視/編輯</button>
        ${hasProj?"":`<button class="tr-ico tr-move" onclick="App._trMove('${t.id}')">${moveLabel}</button>`}
        <button class="tr-ico tr-del" onclick="App._trDel('${t.id}')">🗑</button>
      </div>
    </div>`}App._trCopy=function(id){const rec=trFind(id);if(!rec)return;if(!String(rec.transcript||"").trim())return U.toast("這筆還沒有逐字稿內容","warning");U.copy(rec.transcript,"✓ 已複製逐字稿全文（"+fmtN(rec.transcript.length)+" 字）","複製失敗，請開啟後手動選取")};App._trDel=function(id){const rec=trFind(id);if(!rec)return;const _chars=String(rec.transcript||"").length+String(rec.minutes&&rec.minutes.text||"").length;App.confirmModal({icon:"ti-trash",iconBg:"--rose-l",iconColor:"--rose-ink",title:"刪除逐字稿？",msg:`「${U.esc(rec.title||"未命名會議")}」${_chars?"（含約 "+_chars.toLocaleString()+" 字轉譯/紀錄內容）":""}會連同右側會議紀錄一起移到<b>回收桶</b>，30 天內可從清單底部「🗑 回收桶」還原，逾期自動永久清除。`,okText:"移到回收桶",okClass:"danger",onConfirm:()=>{rec._deleted=true;rec._deletedAt=new Date().toISOString();Store.transcripts.save();trListRefresh();U.toast("已移入回收桶（30 天內可還原）")}})};App._trTrashOpen=function(){const items=trTrash().slice().sort((a,b)=>String(b._deletedAt||"").localeCompare(String(a._deletedAt||"")));const rows=items.length?items.map(t=>{const left=Math.max(0,30-Math.floor((Date.now()-new Date(t._deletedAt||0).getTime())/864e5));return`<div class="tr-trash-item"><div class="tr-trash-nm">${U.esc(t.title||"未命名會議")}<span class="tr-trash-sub">${U.esc(t.date||"")} · 剩 ${left} 天自動清除</span></div>
        <button class="tb-action ghost" onclick="App._trTrashRestore('${t.id}')">↺ 還原</button>
        <button class="tb-action danger" onclick="App._trTrashPurge('${t.id}')">永久刪除</button></div>`}).join(""):'<div class="tr-secempty">回收桶是空的。</div>';App.openModal({title:"🗑 逐字稿回收桶（30 天內可還原）",body:`<div class="tr-trash-list">${rows}</div>`,footer:'<button class="tb-action ghost" onclick="App.closeModal()">關閉</button>'})};App._trTrashRestore=function(id){const rec=trAllRaw().find(t=>t&&t.id===id);if(!rec)return;delete rec._deleted;delete rec._deletedAt;Store.transcripts.save();App.closeModal();trListRefresh();U.toast("↺ 已還原「"+(rec.title||"未命名會議")+"」","success")};App._trTrashPurge=function(id){const rec=trAllRaw().find(t=>t&&t.id===id);if(!rec)return;App.confirmModal({icon:"ti-trash-x",iconBg:"--rose-l",iconColor:"--rose-ink",okClass:"danger",title:"永久刪除？",msg:"「"+U.esc(rec.title||"未命名會議")+"」將<b>永久刪除、救不回來</b>。",okText:"永久刪除",cancelText:"取消",onConfirm:()=>{Store.transcripts.remove(id);App.closeModal();trListRefresh();App._trTrashOpen();U.toast("已永久刪除")}})};App._trMove=function(id){const rec=trFind(id);if(!rec)return;const opts=[`<option value=""${!rec.folderId?" selected":""}>（未歸類）</option>`];const walk=(pid,depth)=>trFolders().filter(f=>(f.parentId||null)===pid).forEach(f=>{opts.push(`<option value="${f.id}"${rec.folderId===f.id?" selected":""}>${"　".repeat(depth)}📁 ${U.esc(f.name)}</option>`);walk(f.id,depth+1)});walk(null,0);if(opts.length===1)return App.confirmModal({title:"還沒有資料夾",msg:"先用右上「📁 新增資料夾」建一個，再把逐字稿搬進去。",okText:"知道了",cancelText:null});App.openModal({title:"↪ 移動到資料夾",body:`<div class="tr-field"><label>把「${U.esc(rec.title||"未命名會議")}」放到</label><select id="tr-move-sel" class="tr-sel">${opts.join("")}</select></div>`,footer:`<button class="tb-action ghost" onclick="App.closeModal()">取消</button><button class="tb-action" onclick="App._trDoMove('${id}')">移動</button>`})};App._trDoMove=function(id){const rec=trFind(id);const sel=document.getElementById("tr-move-sel");if(rec&&sel){rec.folderId=sel.value||null;Store.transcripts.save()}App.closeModal();trListRefresh();U.toast("已移動")};App._trNewFolder=function(parentId){const parent=parentId?trFolders().find(f=>f.id===parentId):null;if(parent&&parent.depth>=3)return U.toast("最多三層（甲＞乙＞丙），這層不能再建子資料夾","warning");App.promptModal({title:parent?`在「${U.esc(parent.name)}」下新增子資料夾`:"新增資料夾",label:"資料夾名稱",rows:1,okText:"建立",onSubmit:v=>{const name=String(v||"").trim();if(!name)return;Store.folders.add({id:U.id(),name,parentId:parent?parent.id:null,depth:parent?parent.depth+1:1});trListRefresh();U.toast("已建立資料夾「"+U.esc(name)+"」")}})};App._trDelFolder=function(id){const f=trFolders().find(x=>x.id===id);if(!f)return;if(!trFolderEmptyDeep(id))return App.confirmModal({title:"資料夾還有東西",msg:`「${U.esc(f.name)}」（或它的子資料夾）裡還有逐字稿，先把它們搬走再刪。`,okText:"知道了",cancelText:null});App.confirmModal({title:"刪除資料夾",msg:`刪除空資料夾「${U.esc(f.name)}」（含其下空的子資料夾）？`,okText:"刪除",okClass:"danger",onConfirm:()=>{const rm=fid=>{trFolders().filter(x=>x.parentId===fid).forEach(k=>rm(k.id));DATA.folders=trFolders().filter(x=>x.id!==fid)};rm(id);Store.folders.save();trListRefresh();U.toast("已刪除")}})};App._trNew=function(){const now=new Date().toISOString();const rec={id:U.id(),title:"",date:D.fmt(D.today(),"iso"),project:null,folderId:null,transcript:"",minutes:{table:[],text:""},status:"wait",createdAt:now,updatedAt:now};App._trDraft=rec;App._trCur=rec.id;App._trTab="table";trOpenModal(rec)};App._trOpen=function(id){const rec=trFind(id);if(!rec)return;rec.minutes=rec.minutes||{table:[],text:""};rec.minutes.table=(rec.minutes.table||[]).filter(r=>((r.task||"")+(r.owner||"")+(r.due||"")).trim());App._trDraft=null;App._trCur=id;App._trTab="table";trOpenModal(rec)};function trOpenModal(rec){if(App._trRec)App._trRec.stop();App._trSrcOpen=null;App.openModal({title:`${U.esc(rec.title||"會議逐字稿")} <span class="tr-mode">離線·手動</span>`,body:trModalHtml(rec),footer:`<span class="tr-foothint">左右兩邊都可以直接編輯，改了就自動儲存（右側貼了或改了會議紀錄 → 狀態自動變「已完成」）</span>
        <button class="tb-action ghost" onclick="App.closeModal()">關閉</button>`})}function trModalHtml(rec){const lw=App._trSplit||32;return`<div class="tr-2col" style="--tr-cols:${lw}fr 6px ${100-lw}fr"
        ondragover="event.preventDefault();this.classList.add('tr-dropping')" ondragleave="this.classList.remove('tr-dropping')" ondrop="App._trDrop(event)">
      <div class="tr-col-l">${trLeftHtml(rec)}</div>
      <div class="tr-split" onmousedown="App._trSplitStart(event)" title="拖曳調整左右寬度"><span class="tr-split-bar"></span></div>
      <div class="tr-col-r" id="tr-rwrap">${trRightHtml(rec)}</div>
    </div>`}function trProjOptions(sel){const live=(DATA.projects||[]).filter(p=>App._isLiveProject(p));const npi=live.filter(p=>!p.ecnType),ecn=live.filter(p=>p.ecnType);const cur=sel&&!live.some(p=>p.id===sel)?App.getProj(sel):null;const opt=p=>`<option value="${p.id}"${p.id===sel?" selected":""}>${U.esc(p.name)}</option>`;return`<option value=""${!sel?" selected":""}>— 不指定 —</option>`+(npi.length?`<optgroup label="NPI">${npi.map(opt).join("")}</optgroup>`:"")+(ecn.length?`<optgroup label="ECN">${ecn.map(opt).join("")}</optgroup>`:"")+(cur?`<optgroup label="已結案／封存">${opt(cur)}</optgroup>`:"")}function trLeftHtml(rec){return`<div class="tr-row2">
        <div class="tr-field"><label>日期</label><input type="date" class="tr-input" value="${U.esc(rec.date||"")}" onchange="App._trField('date', this.value)"></div>
        <div class="tr-field"><label>專案 <span class="tr-opt">（歸檔用）</span></label>
          <select class="tr-sel" onchange="App._trField('project', this.value)">${trProjOptions(rec.project)}</select></div>
      </div>
      <div class="tr-field"><label>標題</label><input class="tr-input" value="${U.esc(rec.title||"")}" placeholder="例：G3 電控可靠度週會" oninput="App._trField('title', this.value)"></div>
      <div class="tr-subt"><span class="tr-n">1</span>逐字稿 <span class="tr-opt">（可直接改）</span></div>
      <div class="tr-pathbar">
        <button class="tr-sm ghost" onclick="App._trBuzzHelp()">🖥 怎麼用 Buzz 轉？</button>
        ${trWhisperCfg().url?`<button class="tr-sm ghost so-online" id="tr-recbtn" onclick="App._trRecToggle()" title="按一下開始錄音·再按一下停止並自動轉逐字稿">🎙 錄音</button>
             <button class="tr-sm ghost so-online" id="tr-cloudbtn" onclick="App._trCloudClick()" title="選手機錄好的音檔（.aac/.m4a/.mp3…）自動轉逐字稿">🎵 匯入音檔</button>`:`<button class="tr-sm ghost" id="tr-recbtn" onclick="App._trRecToggle()" title="按一下開始錄音·停止後自動下載音檔（丟給 Buzz 轉文字·乙案 2026-07-19）">🎙 錄音</button>`}
        <button class="tr-sm ghost" onclick="document.getElementById('tr-file').click()">📄 匯入逐字稿（.txt / .srt）</button>
        <span class="tr-info" title="錄音檔用 Buzz 轉成文字檔（.txt/.srt）再匯入；檔案直接拖進這個視窗也可以。未設雲端轉譯時「🎙 錄音」＝錄完下載音檔（給 Buzz）；設定好雲端轉譯（設定 → 資料來源）則錄完自動轉逐字稿、並多「🎵 匯入音檔」。">i</span>
        <input type="file" id="tr-file" accept=".txt,.srt,.vtt" style="display:none" onchange="App._trOnTxt(this)">
        <input type="file" id="tr-audio" accept="audio/*,.m4a,.mp3,.webm,.wav,.mp4,.ogg,.oga,.flac,.aac" style="display:none" onchange="App._trOnAudio(this)">
        <span id="tr-cloudst" style="font-size:12px;color:var(--ink3)"></span>
      </div>
      ${trSrcCardHtml(rec)}
      <div class="tr-runbar"><button class="tr-sm ghost" onclick="App._trSummarize()">🔎 重點整理 →</button></div>
      <div class="tr-relnote">改逐字稿後按「重點整理」會<b>重生右側的會議紀錄表格</b>（會先提醒、你確認才覆蓋）；反過來，右側改了<b>不會動到逐字稿</b>。</div>`}function trSrcCardHtml(rec){const txt=String(rec.transcript||"");const nchar=txt.length,nseg=trSegsPos(txt).length,nspk=trSpeakers(txt).length;const open=App._trSrcOpen!=null?App._trSrcOpen:nchar<500;const prev=txt.replace(/\s+/g," ").trim().slice(0,140);return`<div class="tr-src${open?" open":""}" id="tr-src">
        <div class="tr-src-h" onclick="App._trSrcToggle()">
          <span class="tr-src-car">▶</span><span class="tr-src-ttl">📝 逐字稿</span>
          <span class="tr-src-meta">${fmtN(nchar)} 字 · ${nseg} 段${nspk?" · 偵測到 "+nspk+" 位發言人":""}</span>
        </div>
        <div class="tr-src-prev">${prev?U.esc(prev):'<span class="tr-src-ph">還沒有逐字稿——用上面按鈕匯入，或展開直接貼上。</span>'}</div>
        <div class="tr-src-body">
          <div class="tr-src-tools"><div class="tr-src-qwrap"><input class="tr-src-q" placeholder="搜尋逐字稿…" oninput="App._trSrcSearch(this.value)"></div></div>
          <div class="tr-srcdoc" id="tr-srcdoc" contenteditable="plaintext-only" data-ph="貼上會議逐字稿，或用上面「📄 匯入 .txt」…" oninput="App._trSrcInput(this)">${trSrcDocHtml(txt,"")}</div>
          <div class="tr-src-foot">在這裡直接改字即時儲存 · 內容多時這區自己捲動、不撐開視窗 · 搜尋命中會標黃</div>
        </div>
      </div>`}function trSpeakers(text){const out=[];trSegsPos(String(text||"")).forEach(sg=>{const m=sg.text.replace(TS_RE,"").match(SPK_RE);if(m&&!out.includes(m[1].trim()))out.push(m[1].trim())});return out}function trSrcDocHtml(text,query){text=String(text||"");const segs=trSegsPos(text),q=String(query||"").trim(),ql=q.toLowerCase();const spkIdx={};trSpeakers(text).forEach((n,i)=>{spkIdx[n]=i%3+1});const mk=s=>{if(!q)return U.esc(s);let out="",lo=s.toLowerCase(),i=0,idx;while((idx=lo.indexOf(ql,i))>=0){out+=U.esc(s.slice(i,idx))+'<mark class="tr-mk">'+U.esc(s.slice(idx,idx+q.length))+"</mark>";i=idx+q.length}return out+U.esc(s.slice(i))};const mkSeg=s=>{let h="";const tm=s.match(TS_RE);if(tm){h+='<span class="tr-ts">'+U.esc(tm[0])+"</span>";s=s.slice(tm[0].length)}const pm=s.match(SPK_RE);if(pm&&spkIdx[pm[1].trim()]){h+='<span class="tr-spk tr-spk-c'+spkIdx[pm[1].trim()]+'">'+mk(pm[0])+"</span>";s=s.slice(pm[0].length)}return h+mk(s)};let html="",prev=0;for(let k=0;k<segs.length;k++){const sg=segs[k];if(sg.start>prev)html+=U.esc(text.slice(prev,sg.start));html+='<span class="tr-seg" data-seg="'+k+'">'+mkSeg(sg.text)+"</span>";prev=sg.end}if(prev<text.length)html+=U.esc(text.slice(prev));return html}function trRightHtml(rec){const tab=App._trTab==="text"?"text":"table";const mm=rec.minutes||{table:[],text:""};let body;if(tab==="table"){const rows=mm.table||[];const cell=(i,k,v,cls,extra)=>`<td><span class="tr-cell${cls?" "+cls:""}" contenteditable data-i="${i}" data-k="${k}" oninput="App._trCell(this)"${extra||""}>${U.esc(v||"")}</span></td>`;const taskCell=(i,r)=>`<td><span class="tr-cell" contenteditable data-i="${i}" data-k="task" oninput="App._trCell(this)">${trKwHtml(r.task)}</span></td>`;const ownerCell=(i,r)=>cell(i,"owner",r.owner,r.ownerGuess?"tr-own-guess":"",r.ownerGuess?' title="系統用發言人推斷的（句子裡比不到名冊人名）——點格子可改，改了就當確認"':"");const rowsHtml=rows.map((r,i)=>`<tr><td class="tr-kdcell"><button class="tr-kind tr-kind-${r.kind==="decide"?"dec":"todo"}" onclick="App._trCycleKind(${i})" title="點擊切換 待辦↔決議">${r.kind==="decide"?"決議":"待辦"}</button></td>${taskCell(i,r)}${ownerCell(i,r)}${cell(i,"due",r.due,"tr-num")}<td class="tr-stcell"><button class="tr-stchip tr-stc-${ST_CLS[r.status]||"no"}" onclick="App._trCycleSt(${i})" title="點擊切換進度">${U.esc(r.status||"未開始")}</button></td><td class="tr-jcell">${r.srcSeg!=null?`<button class="tr-jump" onclick="App._trJump(${r.srcSeg})" title="跳到逐字稿出處並高亮">↖ 原文</button>`:""}</td></tr>`).join("");body=`<div class="tr-rtop"><span class="tr-rtip">點任一格即可改，Tab 跳下一格</span>
          <button class="tr-sm ghost" onclick="App._trAddRow()">＋ 加一列</button>
          <button class="tr-sm ghost" onclick="App._trCopyTable()">📋 複製表格</button>
          <button class="tr-sm ghost" onclick="App._trRulesHelp()">? 整理規則</button></div>
        <table class="tr-tbl" onkeydown="App._trCellKey(event)">
          <thead><tr><th class="tr-kth">類型</th><th>事項</th><th>負責人</th><th>預期完成日</th><th>進度</th><th class="tr-jth"></th></tr></thead>
          <tbody>${rowsHtml||`<tr><td colspan="6" class="tr-tbl-empty">還沒有內容——按左邊「🔎 重點整理」生成候選，或點「＋ 加一列」自己填。</td></tr>`}</tbody>
        </table>
        <div class="tr-editnote">系統規則式抽的候選（<b>非 AI·可能不準</b>·底線＝觸發字，「? 整理規則」看白話規則）——格子隨時可改（右上淡淡 ✎ 表示可編輯），改完自動存。點欄位旁的「<b>↖ 原文</b>」，左側會自動跳轉並高亮對應的會議發言。「複製表格」以 TSV 貼 Excel 零跑格；要更漂亮，把左邊逐字稿丟 Claude chat 精修，再貼回「純文字」頁。</div>`}else{body=`<textarea class="tr-free" rows="14" placeholder="貼上 Claude 精修後的會議紀錄，或直接在這裡自己寫…" oninput="App._trText(this.value)">${U.esc(mm.text||"")}</textarea>
        <div class="tr-editnote">這頁＝定稿用的純文字會議紀錄；貼了內容，狀態就自動變「已完成」。</div>`}return`<div class="tr-rhead">
        <span class="tr-rtitle">📋 會議紀錄</span>
        <span class="tr-editchip">✎ 可直接編輯</span>
        <span class="tr-saveind" id="tr-save">✓ 已自動儲存</span>
        <div class="tr-tabs">
          <button class="tr-tab${tab==="table"?" active":""}" onclick="App._trSwitchTab('table')">表格</button>
          <button class="tr-tab${tab==="text"?" active":""}" onclick="App._trSwitchTab('text')">純文字</button>
        </div></div>
      <div class="tr-rbody">${body}</div>`}function trRepaintRight(){const el=document.getElementById("tr-rwrap");const rec=trCurRec();if(el&&rec)el.innerHTML=trRightHtml(rec)}App._trField=function(field,val){const rec=trCurRec();if(!rec)return;if(field==="project")rec.project=val||null;else rec[field]=val;trEnsureStored(rec);trSaveDebounced(rec)};App._trSwitchTab=function(t){App._trTab=t;trRepaintRight()};App._trSrcToggle=function(){const c=document.getElementById("tr-src");if(!c)return;App._trSrcOpen=!c.classList.contains("open");c.classList.toggle("open",App._trSrcOpen)};App._trSrcInput=function(el){const rec=trCurRec();if(!rec)return;rec.transcript=el.innerText;trEnsureStored(rec);trSaveDebounced(rec)};App._trSrcSearch=function(q){const doc=document.getElementById("tr-srcdoc");if(!doc)return;doc.innerHTML=trSrcDocHtml(doc.innerText,q);const first=doc.querySelector(".tr-mk");if(first)trScrollDocTo(first)};function trScrollDocTo(el){const doc=document.getElementById("tr-srcdoc");if(!doc||!el)return;doc.scrollTo({top:Math.max(0,el.offsetTop-doc.clientHeight/2+el.offsetHeight/2),behavior:"smooth"})}App._trJump=function(i){const c=document.getElementById("tr-src");if(c&&!c.classList.contains("open")){c.classList.add("open");App._trSrcOpen=true}const doc=document.getElementById("tr-srcdoc");if(!doc)return;const seg=doc.querySelector('[data-seg="'+i+'"]');if(!seg)return U.toast("原文已改動，找不到對應段落","warning",{soft:true});void doc.offsetHeight;trScrollDocTo(seg);seg.classList.remove("tr-flash");void seg.offsetWidth;seg.classList.add("tr-flash")};App._trSplitStart=function(e){const cols=document.querySelector(".tr-2col");if(!cols)return;const rect=cols.getBoundingClientRect();const move=ev=>{let l=(ev.clientX-rect.left)/rect.width*100;App._trSplit=Math.max(22,Math.min(72,Math.round(l)));cols.style.setProperty("--tr-cols",App._trSplit+"fr 6px "+(100-App._trSplit)+"fr")};const up=()=>{document.removeEventListener("mousemove",move);document.removeEventListener("mouseup",up);document.body.classList.remove("tr-dragging")};document.addEventListener("mousemove",move);document.addEventListener("mouseup",up);document.body.classList.add("tr-dragging");e.preventDefault()};App._trText=function(v){const rec=trCurRec();if(!rec)return;rec.minutes=rec.minutes||{table:[],text:""};rec.minutes.text=v;trEnsureStored(rec);trSaveDebounced(rec)};App._trCell=function(el){const rec=trCurRec();if(!rec)return;const i=+el.getAttribute("data-i"),k=el.getAttribute("data-k");const row=rec.minutes&&rec.minutes.table&&rec.minutes.table[i];if(!row)return;row[k]=el.textContent;if(k==="owner"&&row.ownerGuess){delete row.ownerGuess;el.classList.remove("tr-own-guess");el.removeAttribute("title")}rec.minutesTouched=true;trEnsureStored(rec);trSaveDebounced(rec)};App._trCellKey=function(e){if(e.key!=="Tab")return;const cur=e.target&&e.target.closest?e.target.closest(".tr-cell"):null;if(!cur)return;const cells=Array.prototype.slice.call(document.querySelectorAll("#modal .tr-cell"));const i=cells.indexOf(cur);if(i<0)return;const nxt=cells[i+(e.shiftKey?-1:1)];if(!nxt)return;e.preventDefault();nxt.focus();const r=document.createRange();r.selectNodeContents(nxt);const s=window.getSelection();s.removeAllRanges();s.addRange(r)};App._trCycleSt=function(i){const rec=trCurRec();if(!rec)return;const row=rec.minutes&&rec.minutes.table&&rec.minutes.table[i];if(!row)return;row.status=ST_ORDER[(ST_ORDER.indexOf(row.status)+1+ST_ORDER.length)%ST_ORDER.length];rec.minutesTouched=true;trEnsureStored(rec);trSave(rec);trRepaintRight()};App._trCycleKind=function(i){const rec=trCurRec();if(!rec)return;const row=rec.minutes&&rec.minutes.table&&rec.minutes.table[i];if(!row)return;row.kind=row.kind==="decide"?"todo":"decide";rec.minutesTouched=true;trEnsureStored(rec);trSave(rec);trRepaintRight()};App._trAddRow=function(){const rec=trCurRec();if(!rec)return;rec.minutes=rec.minutes||{table:[],text:""};(rec.minutes.table=rec.minutes.table||[]).push({task:"",owner:"",due:"",kind:"todo",status:"未開始"});trRepaintRight()};App._trCopyTable=function(){const rec=trCurRec();if(!rec)return;const rows=(rec.minutes&&rec.minutes.table||[]).filter(r=>((r.task||"")+(r.owner||"")+(r.due||"")).trim());if(!rows.length)return U.toast("表格還是空的，先按「重點整理」或自己填","warning");const clean=v=>String(v||"").replace(/[\t\r\n]+/g," ").trim();const tsv=["類型	事項	負責人	預期完成日	進度",...rows.map(r=>[r.kind==="decide"?"決議":"待辦",clean(r.task),clean(r.owner),clean(r.due),clean(r.status||"未開始")].join("	"))].join("\n");U.copy(tsv,"✓ 已複製表格（TSV，貼進 Excel 不跑格）","複製失敗，請直接框選表格複製")};App._trRulesHelp=function(){App.confirmModal({icon:"ti-list-search",title:"重點整理是怎麼抓的？（規則式·非 AI）",msg:`<div class="tr-rules">
        <b>怎麼切句</b>：遇到句號、問號、驚嘆號、分號或換行就算一句。<br>
        <b>哪些句子會被抓</b>：①含待辦動詞 → 收進「待辦」　②含決議詞 → 收進「決議」　③講了日期＋「前」（例：25 號前交）→ 收進「待辦」。句子裡的觸發字會畫底線（綠＝待辦、琥珀＝決議）。<br>
        <b>負責人</b>：先比對全公司人員名冊（長名字優先）；比不到就用發言人推斷（虛線加「?」，點格子改了就當確認）。<br>
        <b>日期</b>：認得西元／民國／幾月幾日／幾號、今天明天後天、（下）週幾、月中、月底。<br>
        候選最多 30 列；規則式可能不準——每列都可以直接改或刪。<br><br>
        <span class="tr-rules-kws"><b>待辦詞</b>：${TODO_WORDS.join("、")}、follow up<br><b>決議詞</b>：${DECIDE_WORDS.join("、")}</span>
      </div>`,okText:"知道了",cancelText:null})};App._trOnTxt=function(input){const f=input.files&&input.files[0];input.value="";if(f)trImportTextFile(f)};App._trDrop=function(e){e.preventDefault();e.stopPropagation();const box=e.currentTarget;if(box)box.classList.remove("tr-dropping");const f=e.dataTransfer&&e.dataTransfer.files&&e.dataTransfer.files[0];if(f)trImportTextFile(f)};function trImportTextFile(f){if(!/\.(txt|srt|vtt)$/i.test(f.name)){return App.confirmModal({title:"格式不支援",msg:"這裡吃純文字 <b>.txt</b> 或字幕檔 <b>.srt</b>（用 Buzz 把錄音轉出這兩種格式再匯入）。",okText:"知道了",cancelText:null})}const rd=new FileReader;rd.onload=e=>{const raw=String(e.target.result||"");if(/\.(srt|vtt)$/i.test(f.name))return trSrtChoice(f,raw);trApplyImport(f.name,raw)};rd.readAsText(f)}function trApplyImport(fname,text){const rec=trCurRec();if(!rec)return;rec.transcript=text;if(!String(rec.title||"").trim())rec.title=fname.replace(/\.(txt|srt|vtt)$/i,"");trEnsureStored(rec);trSave(rec);App._trSrcOpen=null;const mb=document.querySelector("#modal .modal-body");if(mb)mb.innerHTML=trModalHtml(rec);U.toast("已匯入 "+U.esc(fname)+"（"+fmtN(rec.transcript.length)+" 字）")}function trSrtChoice(f,raw){App._trSrtMode="clean";const opt=(v,ttl,desc)=>`<label class="tr-srt-opt"><input type="radio" name="tr-srt-mode" value="${v}"${v==="clean"?" checked":""} onchange="App._trSrtMode=this.value"><span><b>${ttl}</b><br><span class="tr-srt-d">${desc}</span></span></label>`;App.confirmModal({icon:"ti-file-import",title:"匯入字幕檔",msg:`「${U.esc(f.name)}」是帶時間軸的字幕檔，要怎麼收？<div class="tr-srt-opts">`+opt("clean","轉成乾淨段落（推薦）","去掉時間軸、被切在半句的字幕自動接回同一句——好讀、適合直接整理。")+opt("ts","保留時間標記","每句前面留 [12:34] 淡灰時間，方便對回錄音檔的位置。")+"</div>",okText:"匯入",cancelText:"取消",onConfirm:()=>trApplyImport(f.name,trParseSrt(raw,App._trSrtMode==="ts"))})}function trParseSrt(raw,keepTs){const blocks=String(raw||"").replace(/^﻿/,"").replace(/^WEBVTT[^\n]*\n/,"").split(/\r?\n\r?\n+/);const cues=[];for(const b of blocks){const lines=b.split(/\r?\n/).filter(l=>l.trim()!=="");if(!lines.length)continue;const ti=lines.findIndex(l=>/-->/.test(l));if(ti<0)continue;const txt=lines.slice(ti+1).join(" ").replace(/<[^>]+>/g,"").trim();if(!txt)continue;const tm=lines[ti].match(/(\d{1,2}):(\d{2})(?::(\d{2}))?[.,]\d{1,3}\s*-->/);let ts="";if(tm){const h=tm[3]!=null?+tm[1]:0,mn=tm[3]!=null?+tm[2]:+tm[1],sc=tm[3]!=null?+tm[3]:+tm[2];ts=h?h+":"+String(mn).padStart(2,"0")+":"+String(sc).padStart(2,"0"):mn+":"+String(sc).padStart(2,"0")}if(cues.length&&cues[cues.length-1].txt===txt)continue;cues.push({ts,txt})}if(keepTs)return cues.map(c=>(c.ts?"["+c.ts+"] ":"")+c.txt).join("\n");let out="";for(const c of cues){if(!out){out=c.txt;continue}if(/[。！？!?…；;]["」』)）]?$/.test(out))out+="\n"+c.txt;else out+=(/[A-Za-z0-9]$/.test(out)&&/^[A-Za-z0-9]/.test(c.txt)?" ":"")+c.txt}return out}App._buzzStepsHtml=function(){return`<div class="tr-buzz-steps">
      <b>特點：</b>免費開源的單機轉譯工具。錄音檔直接匯入、文字直接輸出，全程於您的電腦在地端運算，<b>絕不向外上傳任何內容</b>。
      <div class="bz-h">1. 下載與檔案準備</div>
      <ul>
        <li><b>官方下載連結：</b>前往 <a href="https://github.com/chidiwilliams/buzz/releases/latest" target="_blank">GitHub 釋出頁面</a>，並向下滾動至「<b>Assets</b>」區塊。</li>
        <li><b>【Windows】三個檔案皆須下載，並放於同一資料夾（共約 2.5GB）：</b><br>
          <code>Buzz-…-windows.exe</code>　<code>…windows-1.bin</code>　<code>…windows-2.bin</code><br>
          ⚠ <b>重要說明：</b>兩個 <code>.bin</code> 檔案<b>不需開啟也不需解壓縮</b>，只需與安裝檔放置在同一個資料夾即可。</li>
        <li><b>【Mac】僅需下載一個對應晶片的檔案：</b><br>
          M 系列晶片：請選擇 <code>…mac-ARM64.dmg</code>　／　舊款 Intel 機型：請選擇 <code>…mac-X64.dmg</code><br>
          下載後打開並拖移至「應用程式」資料夾即可完成安裝。</li>
      </ul>
      <div class="bz-h">2. 安裝與執行提示（Windows）</div>
      <ul>
        <li>按兩下執行 <code>Buzz-…-windows</code>（類型＝應用程式）進行安裝，系統會自動讀取同資料夾內的兩個 <code>.bin</code> 檔案。</li>
        <li>若遇到系統跳出警告，請點選「<b>其他資訊</b>」→「<b>仍要執行</b>」（此為未簽章的正常現象）。</li>
      </ul>
      <div class="bz-h">3. 使用操作指南（首次設定後，Buzz 會自動記憶，往後只需點擊「開始執行」）</div>
      <ul>
        <li><b>匯入檔案：</b>將錄音檔直接拖曳至 Buzz 視窗中。</li>
        <li><b>參數設定（彈出視窗，每一欄照下面選）：</b>
          <ul class="bz-sub">
            <li><b>模型：</b><code>Whisper</code>＋<code>Large</code>（實測＝正式紀錄等級·轉譯最慢、模型檔最大）；電腦跑不動或趕時間改 <code>Medium</code>（平衡）／<code>Small</code>（快·草稿）；<b>預設的 <code>Tiny</code> 中文幾乎不能用，務必更改</b>。</li>
            <li><b>任務：</b><code>轉錄</code></li>
            <li><b>語言：</b><code>中文</code>（Buzz 記住的是「上次的選擇」——要轉英文或其他語言的錄音時，每次轉譯前在這個視窗把「語言」改成該語言即可）</li>
            <li><b>匯出格式：</b>勾選 <code>TXT</code>＋<code>SRT</code></li>
          </ul></li>
        <li><b>開始轉譯：</b>點擊「開始執行」。（首次轉譯時會自動下載模型檔，此時需要網路，之後即可完全離線使用）<br>
          <span style="color:var(--ink3)">準確度不只看模型——錄音收音品質影響很大：手機／錄音筆盡量靠近發言者、減少環境雜音。</span></li>
      </ul>
      <div class="bz-h">4. 自動與手動匯入</div>
      <ul>
        <li><b>自動：</b>勾選匯出後，轉完會<b>自動在錄音檔旁</b>生成同名的 <code>.txt</code>／<code>.srt</code> 檔，直接將此文字檔拖進本系統的逐字稿視窗即可。</li>
        <li><b>手動：</b>亦可點選本系統的「📄 匯入逐字稿」按鈕來手動選取檔案。</li>
        <li>⚡ <b>更自動（進階）：</b>本系統的「🎙 錄音」在未設定雲端轉譯時，停止後會直接下載音檔——固定存到同一個資料夾，搭配 Buzz 的「監看資料夾」功能（資料夾內新增的音檔自動轉譯），流程即近全自動。</li>
      </ul></div>`};App._trBuzzHelp=function(){App.confirmModal({icon:"ti-device-desktop",title:"用 Buzz 把錄音變逐字稿（免費·單機）",msg:App._buzzStepsHtml(),okText:"知道了",cancelText:null})};App._trSummarize=function(){const rec=trCurRec();if(!rec)return;const doc=document.getElementById("tr-srcdoc");if(doc)rec.transcript=doc.innerText;const txt=String(rec.transcript||"").trim();if(!txt)return U.toast("逐字稿還是空的，先貼上或匯入 .txt","warning");const run=()=>{const rows=trExtract(txt);rec.minutes=rec.minutes||{table:[],text:""};rec.minutes.table=rows;rec.minutesTouched=false;trEnsureStored(rec);trSave(rec);App._trTab="table";trRepaintRight();U.toast(rows.length?"已生成 "+rows.length+" 列候選（非 AI·可能不準，請逐列確認）":"規則式沒抓到可整理的句子，可點「＋ 加一列」自己填",rows.length?"success":"warning",{soft:true})};const mm=rec.minutes||{};const hasOld=(mm.table||[]).some(r=>((r.task||"")+(r.owner||"")+(r.due||"")).trim())||rec.minutesTouched;if(hasOld)App.confirmModal({title:"重生會議紀錄表格",msg:"會用左邊逐字稿<b>重新產生</b>右側表格，表格裡的手動修改會被覆蓋（「純文字」頁不受影響）。確定重生？",okText:"重生",cancelText:"先不要",onConfirm:run});else run()};const TR_SR=16e3,TR_CHUNK_SEC=480;function trWhisperCfg(){const s=DATA.settings||{};return{url:String(s.whisperUrl||"").trim()}}function trIdToken(){return typeof Auth!=="undefined"&&Auth._idToken||""}App._trCloudClick=function(){if(!trWhisperCfg().url)return App._trCloudSetup();if(!trIdToken())return U.toast("要先用 Google 登入才能用雲端轉譯（後端會驗白名單）","warning");const a=document.getElementById("tr-audio");if(a)a.click()};App._trCloudSetup=function(){App.confirmModal({title:"還沒設定雲端轉譯",icon:"ti-cloud",msg:"到「設定 → 資料來源」的「<b>🎙 會議錄音轉逐字稿</b>」展開「方式二：雲端自動轉譯」貼 Apps Script 網址（只要網址·誰能用由後端白名單管），儲存後回來錄音／匯入音檔鈕就會出現。",okText:"前往設定",cancelText:"先不要",onConfirm:()=>{App.closeModal();App.showPage("settings");setTimeout(()=>{const btn=Array.from(document.querySelectorAll("#page-settings .tab-btn")).find(b=>(b.getAttribute("onclick")||"").includes("資料來源"));if(btn)App.showSettingsTab(btn,"資料來源");const sec=document.getElementById("ss-whisper");if(sec){sec.scrollIntoView({behavior:"smooth",block:"center"});sec.style.outline="2px solid var(--sig-red)";sec.style.outlineOffset="3px";setTimeout(()=>{sec.style.outline="";sec.style.outlineOffset=""},4e3)}},80)}})};function trRecSaveLocal(f){const url=URL.createObjectURL(f);const a=document.createElement("a");a.href=url;a.download=f.name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),4e3);U.toast("✓ 錄音已下載「"+U.esc(f.name)+"」——丟給 Buzz 轉出 .txt/.srt 再匯入（點「🖥 怎麼用 Buzz 轉？」看教學）","success",{duration:7e3})}App._trRecToggle=async function(){if(App._trRec){App._trRec.stop();return}const cloud=!!trWhisperCfg().url;if(cloud&&!trIdToken())return U.toast("要先用 Google 登入才能用雲端轉譯（後端會驗白名單）","warning");const rec0=trCurRec();if(!rec0)return;let stream;try{stream=await navigator.mediaDevices.getUserMedia({audio:true})}catch(e){return App.confirmModal({title:"拿不到麥克風",msg:"瀏覽器擋了麥克風權限——點網址列右邊的 🎤 圖示改「允許」，再按一次錄音。",okText:"知道了",cancelText:null})}const mr=new MediaRecorder(stream);const chunks=[];mr.ondataavailable=e=>{if(e.data&&e.data.size)chunks.push(e.data)};mr.onstop=()=>{stream.getTracks().forEach(t=>t.stop());clearInterval(App._trRecTimer);App._trRec=null;App._modalCleanup=null;trRecBtnUi(false);const mime=mr.mimeType||"audio/webm";const ext=/mp4/.test(mime)?"mp4":/ogg/.test(mime)?"ogg":"webm";const d=new Date,p2=x=>String(x).padStart(2,"0");const stamp=d.getFullYear()+p2(d.getMonth()+1)+p2(d.getDate())+"-"+p2(d.getHours())+p2(d.getMinutes());const f=new File([new Blob(chunks,{type:mime})],"會議錄音-"+stamp+"."+ext,{type:mime});if(f.size<2e3)return U.toast("錄音太短，沒有內容","warning");if(cloud)App._trTranscribe(f,rec0);else trRecSaveLocal(f)};mr.start(1e3);App._trRec=mr;App._trRecT0=Date.now();App._modalCleanup=()=>{if(App._trRec)App._trRec.stop()};trRecBtnUi(true);App._trRecTimer=setInterval(()=>trRecBtnUi(true),1e3)};function trRecBtnUi(on){const b=document.getElementById("tr-recbtn");if(!b)return;if(!on){b.innerHTML="🎙 錄音";b.classList.remove("tr-recing");return}const s=Math.floor((Date.now()-App._trRecT0)/1e3);b.innerHTML="⏹ 停止錄音 "+String(Math.floor(s/60)).padStart(2,"0")+":"+String(s%60).padStart(2,"0");b.classList.add("tr-recing")}App._trOnAudio=function(input){const f=input.files&&input.files[0];input.value="";if(f)App._trTranscribe(f)};window.addEventListener("beforeunload",function(e){if(_trT){clearTimeout(_trT);_trT=null;const r=_trPendingRec;_trPendingRec=null;if(r)trSave(r)}const cloudPending=typeof CloudSync!=="undefined"&&(CloudSync._uploadTimer||CloudSync._isUploading);if(App._trRec||App._trBusy||cloudPending){e.preventDefault();e.returnValue=""}});App._trTranscribe=async function(f,recArg){const c=trWhisperCfg();const rec=recArg||trCurRec();if(!rec||!c.url)return;App._trBusy=true;if(!trIdToken())return U.toast("要先用 Google 登入才能用雲端轉譯（後端會驗白名單）","warning");const st=t=>{const el=document.getElementById("tr-cloudst");if(el)el.textContent=t};const btn=document.getElementById("tr-cloudbtn");if(btn)btn.disabled=true;try{const okExt=/\.(m4a|mp3|webm|wav|mp4|mpga|mpeg|ogg|oga|flac)$/i.test(f.name);let dur=null;try{dur=await trAudioDuration(f)}catch(e){}if(dur!=null&&!isFinite(dur))dur=null;let parts;if(okExt&&f.size<=15*1048576&&dur&&dur<=1200){st("直接上傳（約 "+Math.round(dur/60)+" 分鐘）…");parts=[{name:f.name,mime:f.type||"audio/mpeg",b64:await trFileB64(f)}]}else{if(dur&&dur>95*60){if(btn)btn.disabled=false;st("");return App.confirmModal({title:"檔案太長",msg:"這版單檔支援約 90 分鐘（"+Math.round(dur/60)+" 分鐘超過了）。請分段錄音再各自轉譯，或用 Buzz 離線轉 .txt 匯入。",okText:"知道了",cancelText:null})}st("解碼音檔中…（大檔要一點時間，別關頁面）");const samples=await trDecode16k(f);const n=Math.max(1,Math.ceil(samples.length/(TR_CHUNK_SEC*TR_SR)));parts=[];for(let i=0;i<n;i++){const s0=i*TR_CHUNK_SEC*TR_SR,s1=Math.min(samples.length,s0+TR_CHUNK_SEC*TR_SR);parts.push({name:"chunk"+i+".wav",mime:"audio/wav",b64:trWavB64(samples,s0,s1)})}}const texts=[];for(let i=0;i<parts.length;i++){st("轉譯中 第 "+(i+1)+"/"+parts.length+" 段…（每段約半分鐘）");const res=await fetch(c.url,{method:"POST",mode:"cors",redirect:"follow",headers:{"Content-Type":"text/plain;charset=utf-8"},body:JSON.stringify({action:"transcribe",id_token:trIdToken(),name:parts[i].name,mime:parts[i].mime,audio_b64:parts[i].b64})}).then(r=>r.json());if(res&&res.error)throw new Error((res.stage==="auth"?"登入／白名單問題：":"")+res.error);if(!res||res.ok!==true)throw new Error("後端回應格式不對（網址貼錯？）");texts.push(String(res.text||""))}rec.transcript=(rec.transcript?rec.transcript+"\n":"")+texts.join("\n");trEnsureStored(rec);trSave(rec);const stillOpen=trCurRec()===rec&&document.getElementById("modalOverlay").classList.contains("open")&&document.getElementById("tr-srcdoc");if(stillOpen){App._trSrcOpen=null;const mb=document.querySelector("#modal .modal-body");if(mb)mb.innerHTML=trModalHtml(rec);U.toast("✓ 轉譯完成（"+parts.length+" 段·共 "+fmtN(rec.transcript.length)+" 字），自動整理中…");App._trSummarize()}else{const hasOld=(rec.minutes&&rec.minutes.table||[]).some(r=>((r.task||"")+(r.owner||"")+(r.due||"")).trim())||rec.minutesTouched;if(!hasOld){const rows=trExtract(String(rec.transcript||"").trim());rec.minutes=rec.minutes||{table:[],text:""};rec.minutes.table=rows;rec.minutesTouched=false;trEnsureStored(rec);trSave(rec)}U.toast("✓ 轉譯完成，已存回「"+U.esc(rec.title||"會議逐字稿")+"」","success",{duration:6e3});trListRefresh()}}catch(e){U.toast("雲端轉譯失敗："+U.esc(String(e&&e.message||e)),"error",{duration:6e3});st("")}finally{App._trBusy=false;const b=document.getElementById("tr-cloudbtn");if(b)b.disabled=false}};function trFileB64(f){return new Promise((res,rej)=>{const rd=new FileReader;rd.onerror=()=>rej(new Error("讀檔失敗，請重新選一次"));rd.onload=()=>res(String(rd.result).split(",")[1]||"");rd.readAsDataURL(f)})}function trAudioDuration(f){return new Promise((res,rej)=>{const u=URL.createObjectURL(f);const a=new Audio;a.preload="metadata";a.onloadedmetadata=()=>{URL.revokeObjectURL(u);res(a.duration)};a.onerror=()=>{URL.revokeObjectURL(u);rej(new Error("讀不出音檔長度"))};a.src=u})}async function trDecode16k(f){const ab=await f.arrayBuffer();const ctx=new OfflineAudioContext(1,1,TR_SR);let buf;try{buf=await ctx.decodeAudioData(ab)}catch(e){throw new Error("這個格式瀏覽器解不開（"+f.name+"）——請改用 .m4a／.mp3，或用 Buzz 轉 .txt 匯入")}const ch0=buf.getChannelData(0);if(buf.numberOfChannels===1)return ch0;const ch1=buf.getChannelData(1);const out=new Float32Array(buf.length);for(let i=0;i<buf.length;i++)out[i]=(ch0[i]+ch1[i])/2;return out}function trWavB64(samples,s0,s1){const n=s1-s0;const bytes=new Uint8Array(44+n*2);const dv=new DataView(bytes.buffer);const ws=(o,s)=>{for(let i=0;i<s.length;i++)dv.setUint8(o+i,s.charCodeAt(i))};ws(0,"RIFF");dv.setUint32(4,36+n*2,true);ws(8,"WAVE");ws(12,"fmt ");dv.setUint32(16,16,true);dv.setUint16(20,1,true);dv.setUint16(22,1,true);dv.setUint32(24,TR_SR,true);dv.setUint32(28,TR_SR*2,true);dv.setUint16(32,2,true);dv.setUint16(34,16,true);ws(36,"data");dv.setUint32(40,n*2,true);for(let i=0;i<n;i++){const v=Math.max(-1,Math.min(1,samples[s0+i]));dv.setInt16(44+i*2,v<0?v*32768:v*32767,true)}let bin="";const CH=32768;for(let i=0;i<bytes.length;i+=CH)bin+=String.fromCharCode.apply(null,bytes.subarray(i,i+CH));return btoa(bin)}function trSegsPos(text){text=String(text||"");const out=[],re=/[^。；;！!？?\r\n]+/g;let m;while(m=re.exec(text)){const raw=m[0],t=raw.trim();if(!t)continue;const start=m.index+(raw.length-raw.replace(/^\s+/,"").length);out.push({text:t,start,end:start+t.length})}return out}const TODO_WORDS=["請","要","負責","追蹤","跟催","確認","回覆","回報","提供","安排","完成","處理","提出","交付","補齊","更新","寄","交","給"];const DECIDE_WORDS=["決定","拍板","同意","結論","定案","敲定","通過"];const TODO_RE=new RegExp("("+TODO_WORDS.join("|")+"|follow\\s*up|\\bby\\b)","i");const DECIDE_RE=new RegExp("("+DECIDE_WORDS.join("|")+")");const TS_RE=/^\[\d{1,3}:\d{2}(?::\d{2})?\]\s*/;const SPK_RE=/^([^：:\s]{1,12})[：:]\s*/;function trExtract(text){const roster=typeof Portfolio!=="undefined"&&Portfolio.personRoster?Portfolio.personRoster():[];const names=roster.map(r=>r.name).filter(n=>n&&n.length>=2).sort((a,b)=>b.length-a.length);const today=D.today();const segs=trSegsPos(text);const rows=[],seen=new Set;let speaker="";for(let si=0;si<segs.length;si++){let s=segs[si].text.replace(TS_RE,"").trim();const pm=s.match(SPK_RE);if(pm){speaker=pm[1].trim();s=s.replace(SPK_RE,"").trim()}if(rows.length>=30)break;if(s.length<6)continue;const due=trParseDate(s,today);const hitTodo=TODO_RE.test(s),hitDecide=DECIDE_RE.test(s),hitDate=!!(due&&/前/.test(s));if(!hitTodo&&!hitDecide&&!hitDate)continue;const kind=hitTodo||hitDate?"todo":"decide";let owner="",ownerGuess=false;for(const n of names){if(s.includes(n)){owner=n;break}}if(!owner&&speaker){owner=speaker;ownerGuess=true}const task=s.length>60?s.slice(0,60)+"…":s;if(seen.has(task))continue;seen.add(task);rows.push({task,owner,ownerGuess,due,kind,status:"未開始",srcSeg:si})}return rows}function trKwHtml(text){let h=U.esc(String(text||""));h=h.replace(new RegExp("("+DECIDE_WORDS.join("|")+")","g"),'<u class="tr-kw tr-kw-a">$1</u>');h=h.replace(new RegExp("("+TODO_WORDS.join("|")+"|follow\\s*up)","gi"),m=>/<|>/.test(m)?m:'<u class="tr-kw">'+m+"</u>");h=h.replace(/((?:\d{1,2}\s*[月\/]\s*)?\d{1,2}\s*[日號]?\s*前)/g,'<u class="tr-kw">$1</u>');return h}function trParseDate(s,today){const Y=today.getFullYear(),M=today.getMonth(),TD=today.getDate();const mk=(y,mo,d)=>{const dt=new Date(y,mo-1,d);return dt.getMonth()===mo-1&&dt.getDate()===d?D.fmt(dt,"iso"):""};let m;if(m=s.match(/(\d{4})\s*[年\/\-.]\s*(\d{1,2})\s*[月\/\-.]\s*(\d{1,2})/))return mk(+m[1],+m[2],+m[3]);if(m=s.match(/(1[01]\d)\s*年\s*(\d{1,2})\s*月\s*(\d{1,2})/))return mk(+m[1]+1911,+m[2],+m[3]);if(m=s.match(/(\d{1,2})\s*[月\/]\s*(\d{1,2})\s*[日號]?/)){let y=Y;const dt=new Date(y,+m[1]-1,+m[2]);if((dt-today)/864e5<-180)y++;return mk(y,+m[1],+m[2])}if(m=s.match(/(\d{1,2})\s*[號日]/)){let mo=M+1,y=Y;if(+m[1]<TD){mo++;if(mo>12){mo=1;y++}}return mk(y,mo,+m[1])}if(/今天/.test(s))return D.fmt(today,"iso");if(/明天/.test(s))return D.fmt(D.addDays(today,1),"iso");if(/後天/.test(s))return D.fmt(D.addDays(today,2),"iso");if(m=s.match(/(下下|下)?(?:週|星期|禮拜)([一二三四五六日天])/)){const map={"一":1,"二":2,"三":3,"四":4,"五":5,"六":6,"日":0,"天":0};const tw=map[m[2]];const off=tw===0?6:tw-1;const curMon=D.monday(today);let target;if(!m[1]){target=D.addDays(curMon,off);if(target<=today)target=D.addDays(target,7)}else if(m[1]==="下")target=D.addDays(curMon,7+off);else target=D.addDays(curMon,14+off);return D.fmt(target,"iso")}if(/下(個)?月底/.test(s))return D.fmt(new Date(Y,M+2,0),"iso");if(/月底/.test(s))return D.fmt(new Date(Y,M+1,0),"iso");if(/月中/.test(s))return D.fmt(TD>15?new Date(Y,M+1,15):new Date(Y,M,15),"iso");return""}})();
