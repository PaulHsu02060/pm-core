/*! PM-Core (c) 2026 Paul Hsu. All Rights Reserved. Proprietary — see LICENSE. */
App._reschedulePreview=function(tasks,variants,warnings){const variantStart={},variantEnd={},variantDir={};variants.forEach(v=>{variantStart[v.id]=v.schedule.startDate||"";variantEnd[v.id]=v.schedule.endDate||"";variantDir[v.id]=v.schedule.direction||"forward"});const effDir=vid=>App._effScheduleDir(variantStart[vid],variantEnd[vid],variantDir[vid]);const detectOverflow6b=(vid,vname)=>{const endLimit=variantEnd[vid];if(!endLimit)return;const vts=tasks.filter(t=>t.variant===vid&&t.plannedEnd);if(!vts.length)return;let binding=vts[0];vts.forEach(t=>{if(t.plannedEnd>binding.plannedEnd)binding=t});const computedEnd=binding.plannedEnd;if(computedEnd>endLimit){const overDays=Math.max(0,D.workdaysBetween(endLimit,computedEnd)-1);warnings.push("「"+vname+"」排程溢出：最晚「"+binding.name+"」需排到 "+computedEnd+"，超過設定結束日 "+endLimit+"（約 "+overDays+" 工作天）")}};if(variants.every(v=>effDir(v.id)==="forward")){tasks.forEach(t=>{if(!t.predecessor)t.plannedStart=variantStart[t.variant]||""});const sch=computeSchedule(tasks);const schById=new Map;sch.results.forEach(r=>schById.set(r.taskId,r));tasks.forEach(t=>{const r=schById.get(t.id);if(r&&r.suggestedStart){t.plannedStart=r.suggestedStart;t.plannedEnd=r.suggestedEnd}else{t.plannedStart="";t.plannedEnd="";warnings.push("「"+t.name+"」未能排入（無起算日或循環依賴）")}});variants.forEach(v=>detectOverflow6b(v.id,v.name));return}variants.forEach(v=>{const vid=v.id;const vtasks=tasks.filter(t=>t.variant===vid);if(!vtasks.length)return;const dir=effDir(vid);if(dir==="forward"){vtasks.forEach(t=>{if(!t.predecessor)t.plannedStart=variantStart[vid]||""});const sch2=computeSchedule(vtasks);const m2=new Map;sch2.results.forEach(r=>m2.set(r.taskId,r));vtasks.forEach(t=>{const r=m2.get(t.id);if(r&&r.suggestedStart){t.plannedStart=r.suggestedStart;t.plannedEnd=r.suggestedEnd}else{t.plannedStart="";t.plannedEnd="";warnings.push("「"+t.name+"」未能排入（無起算日或循環依賴）")}});detectOverflow6b(vid,v.name);return}const endDate=variantEnd[vid];let sch;try{vtasks.forEach(t=>{t.targetEnd=endDate});sch=computeScheduleBackward(vtasks)}finally{vtasks.forEach(t=>{delete t.targetEnd})}const m=new Map;sch.results.forEach(r=>m.set(r.taskId,r));vtasks.forEach(t=>{const r=m.get(t.id);if(r&&r.lateStart!=null){t.plannedStart=r.lateStart;t.plannedEnd=r.lateFinish}else{t.plannedStart="";t.plannedEnd="";warnings.push("「"+t.name+"」未能排入（無目標可販日或循環依賴）")}});if(dir==="interval"){const slack=App._computeSlack(sch.results,variantStart[vid],endDate);if(slack&&slack.light==="red"){warnings.push("「"+v.name+"」時間不足：最快 "+slack.earliestFinish+" 完成，超出結束日 "+endDate+" 約 "+slack.overDays+" 工作天")}}})};App._computeSlack=function(results,startDate,endDate){if(!startDate||!endDate)return null;let minStart=null,maxFinish=null;(results||[]).forEach(r=>{if(r.lateStart!=null&&(minStart===null||r.lateStart<minStart))minStart=r.lateStart;if(r.lateFinish!=null&&(maxFinish===null||r.lateFinish>maxFinish))maxFinish=r.lateFinish});const available=D.workdaysBetween(startDate,endDate);const needed=minStart&&maxFinish?D.workdaysBetween(minStart,maxFinish):0;const slack=available-needed;const light=slack>=5?"green":slack>=0?"yellow":"red";const earliestFinish=needed>0?D.fmt(D.addWorkdays(startDate,needed-1),"iso"):"";const overDays=slack<0?Math.max(0,D.workdaysBetween(endDate,earliestFinish)-1):0;return{available,needed,slack,light,earliestFinish,overDays}};App._effScheduleDir=function(startDate,endDate,direction){if(startDate&&endDate)return"interval";if(endDate)return"backward";if(startDate)return"forward";return direction==="backward"?"backward":"forward"};App.applyTemplate=function(template,userInput){const ui=userInput||{};const project={id:U.id(),name:(ui.projectName||"").trim(),color:ui.color||PROJ_COLORS[0],note:(ui.note||"").trim(),synced:false,createdAt:new Date().toISOString(),targetUnitCostBase:ui.targetUnitCostBase||0,unitSellPriceBase:ui.unitSellPriceBase||0,targetUnitCost:ui.targetUnitCostBase||0,unitSellPrice:ui.unitSellPriceBase||0};ensurePdcaData(project);const variants=[];const variantNameToId={};(ui.cases||[]).forEach(c=>{const id=U.id();const name=(c.variantName||"").trim();variants.push({id,name,schedule:{startDate:c.startDate||"",endDate:c.endDate||"",direction:c.direction||"forward"},stages:c.selectedStages?c.selectedStages.slice():[]});variantNameToId[name]=id});const depts=[];(ui.depts||[]).forEach(d=>{const name=(d.name||"").trim();if(!name)return;const members=(d.members||[]).map(m=>(m.name||"").trim()).filter(Boolean).map(nm=>({id:U.id(),name:nm}));if(!members.length)return;depts.push({id:U.id(),name,members})});const roleToDeptId={};depts.forEach(d=>{roleToDeptId[d.name]=d.id});const dailyHours=typeof DATA!=="undefined"&&DATA.settings&&DATA.settings.dailyHours||6;const tasks=[];const excludedByVariant={};const variantKey=v=>v==null?"":v;(ui.cases||[]).forEach(uiCase=>{const srcKey=(uiCase.templateVariant||uiCase.variantName||"").trim();const tc=(template&&template.cases?template.cases:[]).find(t=>(t.variant||"").trim()===srcKey);if(!tc)return;const variantId=variantNameToId[(uiCase.variantName||"").trim()]||null;const selected=uiCase.selectedStages||null;(tc.modules||[]).forEach(mod=>{const included=!selected||selected.indexOf(mod.stage)>=0;(mod.tasks||[]).forEach(tk=>{if(!included){const _vk=variantKey(variantId);(excludedByVariant[_vk]||(excludedByVariant[_vk]=new Set)).add(tk.n);return}tasks.push({id:U.id(),project:project.id,wbs:tk.n,parentWbsId:"",name:tk.name||"",desc:mod.stage?mod.stage+" / "+(tk.subgroup||""):tk.subgroup||"",category:(tk.type||"").indexOf("里程碑")>=0?"meeting":"deep",taskType:tk.type||"任務",predecessor:tk.predecessor||"",durationDays:tk.durationDays,owner:"",dept:roleToDeptId[(tk.role||"").trim()]||"",role:(tk.role||"").trim(),variant:variantId,start:"",end:"",plannedStart:"",plannedEnd:"",actualStart:"",actualEnd:"",progress:0,status:"pending",urgency:"med",estHours:parseFloat(tk.durationDays||0)*dailyHours||4,method:"",canSplit:false,completedAt:null,createdAt:new Date().toISOString(),scheduledStart:"",scheduledEnd:"",synced:false,stage:mod.stage||"",subgroup:tk.subgroup||"",mustDeliver:false,deliverableType:"",requiredTask:true,mustIssue:false,deliverable:tk.deliverable||"",riskIssue:"",delivered:"",deliverableLink:"",note:"",effortRatio:tk.effortRatio!=null?tk.effortRatio:defaultEffort(mod.stage,(tk.role||"").trim()==="PM"),taskAttr:tk.taskAttr||"baseline"})})})});const excludedNs=[].concat(...Object.values(excludedByVariant).map(s=>[...s]));const wbsToIdMapByVariant={};{const tasksByVariant={};tasks.forEach(t=>{const k=variantKey(t.variant);(tasksByVariant[k]||(tasksByVariant[k]=[])).push(t)});Object.keys(tasksByVariant).forEach(k=>{wbsToIdMapByVariant[k]=buildWbsToIdMap(tasksByVariant[k])})}const nToName={};(template&&template.cases?template.cases:[]).forEach(tc=>{(tc.modules||[]).forEach(mod=>{(mod.tasks||[]).forEach(tk=>{nToName[tk.n]=tk.name||""})})});const warnings=[];function relinkPred(rawPred,selfName,vMap,vExcluded){const parts=String(rawPred||"").split(/[,，;；]/).map(p=>p.trim()).filter(Boolean);const kept=[];for(const part of parts){const m=part.match(/^(\d+)/);if(m&&vExcluded&&vExcluded.has(parseInt(m[1],10))){const depName=nToName[m[1]]||"#"+m[1];warnings.push("「"+selfName+"」的前置「"+depName+"」因所在階段未選，已自動移除");continue}if(m&&vMap&&!vMap.get(String(parseInt(m[1],10)))){const depName=nToName[m[1]]||"#"+m[1];warnings.push("「"+selfName+"」的前置「"+depName+"」已不存在（範本中被刪除），已自動移除");continue}kept.push(part)}return translatePredToId(kept.join(","),vMap)}tasks.forEach(t=>{const k=variantKey(t.variant);t.predecessor=relinkPred(t.predecessor,t.name,wbsToIdMapByVariant[k],excludedByVariant[k])});App._reschedulePreview(tasks,variants,warnings);return{project,variants,variantNameToId,depts,tasks,excludedNs,warnings}};App._stagePickHtml=function(stages){const _tpl=tplNpi();if(!_tpl)return"";const cn={};(_tpl.stageDefaults||[]).forEach(s=>{cn[s.stage]=s.stageNameCN});const list=stages||(_tpl.cases[0]?_tpl.cases[0].stages:[])||[];const pills=list.map(st=>`<button type="button" class="stage-pick on" data-stage="${st}" onclick="this.classList.toggle('on')">${cn[st]||st}</button>`).join("");return`<div class="form-field"><label>選擇階段（不選=不建該階段）</label><div class="stage-pick-row">${pills}</div></div>`};App._applyStagePicks=function(cardEl,selectedStages){if(!cardEl)return;const want=new Set(selectedStages||[]);cardEl.querySelectorAll(".stage-pick").forEach(b=>{b.classList.toggle("on",want.has(b.dataset.stage))})};App._tplAddOtherCase=function(){const box=document.getElementById("pf-otherCases");if(!box)return;const _tpl=tplNpi();const otherStages=_tpl&&_tpl.cases[1]?_tpl.cases[1].stages:void 0;const card=document.createElement("div");card.className="case-card case-other";card.dataset.case="other";card.innerHTML=`<div class="case-card-head"><div class="form-field"><label>案別名稱</label><input type="text" class="case-variant-name" placeholder="案別名稱（例：2.2kW）"></div><button type="button" class="tb-action ghost case-del" onclick="this.closest('.case-card').remove()">刪除</button></div><div class="form-row"><div class="form-field"><label>開始日</label><input type="date" class="case-start"></div><div class="form-field"><label>結束日</label><input type="date" class="case-end"></div></div><div class="form-field"><label>排程方向</label><select class="case-direction"><option value="forward">順推（從開始日）</option><option value="backward">逆推（從結束日）</option></select></div>`+App._stagePickHtml(otherStages);box.appendChild(card)};App._deptEditorHtml=function(){return`        <div class="form-field"><label>部門與負責人（支援彈性增減）</label>
          <div class="dept-editor-head"><span class="dept-head-name">部門名稱</span><span class="dept-head-members">負責人姓名</span></div>
          <div class="dept-edit-list" id="deptEditorList">${App.buildDeptRowsHtml(App._tplDepts,"tpl",null)}</div>
          <button class="tb-action ghost dept-add-btn" onclick="App.deptUI.addDept('tpl', '')">＋ 新增部門</button>
        </div>`};App._stage1FormHtml=function(){return`      <div id="pf-tplBox">
        <div class="form-field">
          <label>選擇範本</label>
          <select id="pf-tpl">${tplAll("npi").map(t=>`<option value="${U.esc(t.templateId)}">${U.esc(t.templateName)}</option>`).join("")}</select>
        </div>
        <div class="case-card case-main" data-case="main">
          <div class="case-card-head">
            <div class="form-field">
              <label>案別名稱</label>
              <input type="text" class="case-variant-name" id="pf-mainName" placeholder="主案名稱（例：7.3kW）" value="" oninput="this.dataset.touched='1'">
              <div class="case-name-hint">已帶入專案名，可自行修改</div>
            </div>
          </div>
          <div class="form-row">
            <div class="form-field"><label>主案開始日</label><input type="date" id="pf-start" class="case-start"></div>
            <div class="form-field"><label>主案結束日</label><input type="date" id="pf-end" class="case-end"></div>
          </div>
          <div class="form-field">
            <label>排程方向</label>
            <select id="pf-direction" class="case-direction">
              <option value="forward">順推（從開始日）</option>
              <option value="backward">逆推（從結束日）</option>
            </select>
          </div>
          ${App._stagePickHtml()}
        </div>
        <div id="pf-otherCases"></div>
        <button type="button" class="tb-action ghost" onclick="App._tplAddOtherCase()">＋ 新增另案</button>
${App._deptEditorHtml()}
      </div>`};App._ecnTplForSize=function(size,tplId){const src=tplId?tplResolve(tplId)||tplEcn():tplEcn();if(!src)return null;const filterMods=mods=>(mods||[]).map(m=>({stage:m.stage,stageNameCN:m.stageNameCN,tasks:(m.tasks||[]).filter(tk=>String(tk.sizes||"SML").indexOf(size)>=0).map(tk=>{const t=Object.assign({},tk);if(tk.predBySize&&tk.predBySize[size]!=null)t.predecessor=tk.predBySize[size];delete t.predBySize;delete t.sizes;return t})})).filter(m=>m.tasks.length);return{templateId:src.templateId+"-"+size,templateName:src.templateName,stageDefaults:src.stageDefaults,roles:src.roles,sizeMeta:src.sizeMeta,cases:(src.cases||[]).map(c=>({variant:c.variant,stages:(src.sizeMeta[size]?src.sizeMeta[size].stages:[]).slice(),modules:filterMods(c.modules)}))}};App._s1Tpl=function(){if(App._s1Ecn)return App._ecnTplForSize(App._s1Ecn.size,App._s1Ecn.tplId);return tplResolve(App._s1TplId)||tplNpi()};App._flowStartEcn=function(size){App._createFlow={step:1,mode:"ecn",stage1Data:null};App._s1Ecn={size:size||"S"};App._s1Cases=null;App.closeModal();const _ecnT=tplEcn();const _roles=_ecnT&&_ecnT.roles?_ecnT.roles:[""];App._tplDepts=_roles.map(r=>({id:U.id(),name:r,members:[{id:U.id(),name:""}]}));App._renderStage1Preview()};App._s1Back=function(){App.confirmModal({icon:"ti-alert-triangle",iconBg:"--amber-l",iconColor:"--amber-ink",title:"返回上一步？",msg:"本頁已填寫的資料（名稱、日期、各項設定）<b>將不會保留</b>，確定要返回？",okText:"返回上一步",cancelText:"留在本頁",onConfirm:function(){const tb=document.querySelector(".main > .topbar");if(tb)tb.classList.remove("topbar-hidden");App.showPage(App.currentPage||"workspace",null);if(App._s1Ecn)App._flowStep1();else App._scheduleEduCard()}})};App._flowStep1=function(){App._s2EcnMode=null;App._createFlow={step:1,mode:"template",stage1Data:null};App.openModal({title:"建立新案 · 選擇類型",wide:true,body:`<div class="wiz">
      <div class="wiz-warn"><i class="ti ti-alert-triangle wiz-warn-ic"></i><span>「新產品開發」與「工程設變」的管理重點與介面完全不同，<b>立案後無法互轉</b>，請依任務性質謹慎選擇。（設變案的 S/M/L 規模，開案後仍可隨進度調整）</span></div>
      <div class="wiz-cols">
        <div class="wiz-col wiz-npi">
          <div class="wiz-colhead"><span class="wiz-dot"></span>開發專案 · NPI</div>
          <div class="wiz-colnote">適合「從無到有」的新產品開發，提供甘特圖、WBS 與月曆等多種排程檢視。</div>
          <div class="wiz-card" onclick="App._flowPickMode('template')">
            <div class="wiz-ct"><i class="ti ti-template wiz-ci"></i><span class="wiz-cn">套用範本</span></div>
            <div class="wiz-cd">載入標準開發流程，自動帶入各部門負責人與預設工期，最快建立完整骨架。</div>
            <div class="wiz-ch">→ 產出：一般專案 Dashboard（含甘特／WBS）</div>
          </div>
          <div class="wiz-card" onclick="App._flowPickMode('excel')">
            <div class="wiz-ct"><i class="ti ti-table-import wiz-ci"></i><span class="wiz-cn">從 Excel 匯入</span></div>
            <div class="wiz-cd">匯入既有 WBS Excel，自動解析並轉為任務排程，無縫接軌。</div>
            <div class="wiz-ch">→ 產出：一般專案 Dashboard（依 Excel 長甘特）</div>
          </div>
          <div class="wiz-card" onclick="App._flowBlankChoice()">
            <div class="wiz-ct"><i class="ti ti-file wiz-ci"></i><span class="wiz-cn">空白專案</span></div>
            <div class="wiz-cd">從零開始建立，無預設流程框架，適合階段特殊或不適用標準範本的微型專案。</div>
            <div class="wiz-ch">→ 先畫時程骨架，或直接完全空白</div>
          </div>
        </div>
        <div class="wiz-col wiz-ecn">
          <div class="wiz-colhead"><span class="wiz-dot"></span>設變案 · ECN</div>
          <div class="wiz-colnote">啟用設變專屬戰情室，精準追蹤「進度落差」、「重工次數」與「成本調降效益（ROI）」。</div>
          <div class="wiz-card" onclick="App._flowEcnStart()">
            <div class="wiz-ct"><i class="ti ti-replace wiz-ci"></i><span class="wiz-cn">套用設變範本</span></div>
            <div class="wiz-cd">載入標準設變流程，啟用戰情室。<b>下一步依風險選 S／M／L 規模</b>（換料 3 階段／結構 6 階段／改模含 DR 審查·開案後仍可調）。</div>
            <div class="wiz-ch">→ 產出：ECN 戰情室（S/M/L 於下一步選）</div>
          </div>
          <div class="wiz-card" onclick="App._flowBlankSkelStart('ecn')">
            <div class="wiz-ct"><i class="ti ti-file wiz-ci"></i><span class="wiz-cn">空白設變案</span></div>
            <div class="wiz-cd">不套範本，自己劃分設變階段、抓粗工期，系統排里程碑甘特，下一步再填任務。適合非標準流程的設變案。</div>
            <div class="wiz-ch">→ 先畫時程骨架 → 檢視任務</div>
          </div>
          <div class="wiz-prot"><i class="ti ti-shield-check wiz-prot-ic"></i>防過勞機制：全級別皆強制掛載「PM 協調工時」以計算跨案負載。</div>
        </div>
      </div>
    </div>`,footer:`<button class="tb-action ghost" onclick="App.closeModal()">取消</button>`})};App._flowPickMode=function(mode){if(App._createFlow){App._createFlow.mode=mode;App._createFlow.stage1Data=null}if(App._s1Ecn){App._s1Ecn=null;App._s1Cases=null}if(mode==="template"){App._scheduleEduCard();return}App._flowStep2()};App._flowStep2=function(){if(App._createFlow)App._createFlow.step=2;const mode=App._createFlow?App._createFlow.mode:"template";if(!App._createFlow||!App._createFlow.stage1Data){if(mode==="blank"){App._tplDepts=[{id:U.id(),name:"",members:[{id:U.id(),name:""}]}]}else{const _npiT=tplNpi();const _roles=_npiT&&_npiT.roles&&_npiT.roles.length?_npiT.roles:[""];App._tplDepts=_roles.map(r=>({id:U.id(),name:r,members:[{id:U.id(),name:""}]}))}}App.openModal({title:mode==="blank"?"新增空白專案":"填寫專案資料",body:`<div class="form-field"><label>專案名稱 *</label><input type="text" id="pf-name" placeholder="e.g. ${CFG("PROJECT_INPUT_EXAMPLE","範例品項")}" oninput="App._syncMainName()"></div>
      <div class="form-field"><label>備註</label><input type="text" id="pf-note" placeholder="簡短描述"></div>
      ${mode==="template"?App._stage1FormHtml():""}
      ${mode==="blank"?App._deptEditorHtml():""}
      <div class="form-field excel-upload" style="${mode==="excel"?"":"display:none"}">
        <label>WBS Excel 檔</label>
        <label class="eu-filebtn"><i class="ti ti-table-import"></i> 選擇檔案<span id="pf-excelName" class="eu-filename">尚未選擇</span><input type="file" id="pf-excelFile" accept=".xlsx,.xls" onchange="App._flowExcelPick(event)"></label>
        <div id="pf-excelStatus" class="excel-status"></div>
        <div id="pf-excelMap" style="display:none"></div>
      </div>`,footer:`<button class="tb-action ghost" onclick="${mode==="blank"?"App._flowBlankChoice()":"App._flowStep1()"}">上一步</button>
      <button class="tb-action" onclick="App._flowStage2Next()">${mode==="blank"?"建立":"下一步：檢視任務"}</button>`})};App._flowViewonlyPreview=function(){App._createFlow={step:2,mode:"template",stage1Data:null};const _npiT2=tplNpi();const _roles=_npiT2&&_npiT2.roles&&_npiT2.roles.length?_npiT2.roles:[""];App._tplDepts=_roles.map(r=>({id:U.id(),name:r,members:[{id:U.id(),name:""}]}));App.openModal({title:"範本預覽（唯讀）",body:`<div class="form-field"><label>專案名稱 *</label><input type="text" id="pf-name"></div>${App._stage1FormHtml()}`,footer:`<button class="tb-action ghost" onclick="App.closeModal()">關閉</button>`});const nameEl=document.getElementById("pf-name");if(nameEl)nameEl.value=CFG("PROJECT_INPUT_EXAMPLE","範例品項");const mainNameEl=document.getElementById("pf-mainName");if(mainNameEl)mainNameEl.value=CFG("PROJECT_INPUT_EXAMPLE","範例品項");const startEl=document.getElementById("pf-start");if(startEl)startEl.value=D.fmt(new Date,"iso");["pf-name","pf-note","pf-mainName","pf-start","pf-end","pf-direction","pf-tpl"].forEach(id=>{const el=document.getElementById(id);if(el)el.disabled=true});document.querySelectorAll("#pf-tplBox .stage-pick, #pf-tplBox #deptEditorList input, #pf-tplBox #deptEditorList button, #pf-tplBox .dept-add-btn").forEach(el=>el.disabled=true);const addCaseBtn=document.querySelector('#pf-tplBox button[onclick*="_tplAddOtherCase"]');if(addCaseBtn)addCaseBtn.style.display="none"};App._setExcelStatus=function(text,kind){const st=document.getElementById("pf-excelStatus");if(st){st.className="excel-status "+(kind||"");st.textContent=text}};App._flowExcelPick=async function(e){const file=e.target.files&&e.target.files[0];if(!file)return;App._setExcelStatus("解析中…","wait");await App._flowExcelParse(file,null)};App._flowExcelParse=async function(file,mapping){try{const parsed=await parseWbsExcel(file,mapping?{mapping}:void 0);if(parsed&&parsed.needMapping){if(App._createFlow)App._createFlow.excelParsed=null;App._setExcelStatus("⚠ 部分欄位對不上，請在下方指定對應","err");App.renderImportMapping({containerId:"pf-excelMap",specs:WBS_COLUMNS,headerCells:parsed.headerCells,resolved:parsed.resolved,requiredKeys:WBS_REQUIRED_KEYS,domain:"wbs",onConfirm:m=>{App._setExcelStatus("解析中…","wait");App._flowExcelParse(file,m)}});return}if(!parsed||!parsed.ok){if(App._createFlow)App._createFlow.excelParsed=null;App._setExcelStatus("⚠ 解析失敗："+(parsed&&parsed.errors&&parsed.errors[0]||"檔案格式不符"),"err");return}if(App._createFlow)App._createFlow.excelParsed=parsed;App._setExcelStatus("✓ 已讀取「"+(parsed.projectName||"未命名")+"」共 "+parsed.rows.length+" 筆任務，按下一步檢視","ok");const nameSpan=document.getElementById("pf-excelName");if(nameSpan&&file)nameSpan.textContent=file.name;const nameEl=document.getElementById("pf-name");if(nameEl&&!nameEl.value.trim()&&parsed.projectName)nameEl.value=parsed.projectName}catch(err){if(App._createFlow)App._createFlow.excelParsed=null;App._setExcelStatus("⚠ 解析錯誤："+(err.message||err),"err")}};App._flowStage2Next=function(){const name=document.getElementById("pf-name").value.trim();if(!name){U.toast("⚠ 請填專案名稱","warning");return}const color=PROJ_COLORS[0];const note=document.getElementById("pf-note").value.trim();const mode=App._createFlow?App._createFlow.mode:"template";if(mode==="blank"){return App._flowBlankCommit(name,color,note)}if(mode==="excel"){const parsed=App._createFlow?App._createFlow.excelParsed:null;if(!parsed||!parsed.ok){U.toast("⚠ 請先選擇 Excel 檔","warning");return}this._tplPreview=buildWbsPreview(parsed);this._tplPreview.project.name=name;this.closeModal();App._s2From="excel";this._renderStage2New();return}const _pfSel=document.getElementById("pf-tpl");const tpl=_pfSel&&_pfSel.value?tplResolve(_pfSel.value)||tplNpi():tplNpi();if(!tpl){U.toast("⚠ 找不到範本","warning");return}const cards=document.querySelectorAll("#pf-tplBox .case-card");const cases=[];for(const card of cards){const isMain=card.dataset.case==="main";const vnEl=card.querySelector(".case-variant-name");const variantName=vnEl?vnEl.value.trim():"";if(!variantName){U.toast(isMain?"⚠️請填主案的案別名稱":"⚠️請填另案的案別名稱","warning");return}const startEl=card.querySelector(".case-start");const startDate=startEl?startEl.value:"";if(isMain&&!startDate){U.toast("⚠ 套用範本請填主案開始日","warning");return}const stages=[...card.querySelectorAll(".stage-pick.on")].map(b=>b.dataset.stage);if(!stages.length){U.toast("⚠️請為「"+variantName+"」至少選一個階段","warning");return}cases.push({variantName,templateVariant:isMain?"主案":"另案",startDate,endDate:(card.querySelector(".case-end")||{}).value||"",direction:(card.querySelector(".case-direction")||{}).value||"forward",selectedStages:stages})}App._createFlow.stage1Data={name,color,note,mode:"template",cases:JSON.parse(JSON.stringify(cases)),depts:JSON.parse(JSON.stringify(App._tplDepts||[]))};const userInput={projectName:name,color,note,cases,depts:App._tplDepts||[]};this._tplPreview=App.applyTemplate(tpl,userInput);App._attachStageCoord(this._tplPreview,tpl,!!(App._createFlow&&App._createFlow.ecn));this.closeModal();this._renderStage2()};App._flowBlankCommit=function(name,color,note){if(App._roGuard())return;const np={id:U.id(),name,color,note,depts:JSON.parse(JSON.stringify(App._tplDepts||[])),synced:false,createdAt:new Date().toISOString()};const dup=DATA.projects.filter(p=>p.name===name);const _commit=()=>{np.version=dup.length?Math.max(...dup.map(p=>p.version||1))+1:1;ensurePdcaData(np);DATA.projects.push(np);App.currentProjectId=np.id;Store.projects.save();App._createFlow=null;App.closeModal();App.refreshAll();U.toast("✓ 專案已建立");App.showPage("project",null)};if(dup.length){App.confirmModal({icon:"ti-copy",iconBg:"--amber-l",iconColor:"--amber-ink",title:`已有 ${dup.length} 個同名專案「${name}」`,msg:"要建立新版本嗎？兩者並存，可在側邊欄辨識版號。",okText:"建立新版本",cancelText:"返回修改",onConfirm:_commit})}else{_commit()}};App.openProjectDialog=function(projId){const editing=projId?this.getProj(projId):null;const isEdit=!!editing;if(!isEdit&&document.body.classList.contains("viewonly"))return App._flowViewonlyPreview();if(!isEdit)return App._flowStep1();this.openModal({title:isEdit?"編輯專案":"新增專案",body:`
      <div class="form-field">
        <label>專案名稱 *</label>
        <input type="text" id="pf-name" value="${editing?U.esc(editing.name):""}" placeholder="e.g. ${CFG("PROJECT_INPUT_EXAMPLE","範例品項")}" oninput="App._syncMainName()">
      </div>
      <div class="form-field">
        <label>備註</label>
        <input type="text" id="pf-note" value="${editing?U.esc(editing.note||""):""}" placeholder="簡短描述">
      </div>
      <div class="form-field">
        <label>專案狀態</label>
        <select id="pf-status" onchange="App._pfStatusChange()" style="max-width:220px;">
          <option value="active"${!editing.archived&&!App._isProjectDone(editing)?" selected":""}>進行中</option>
          <option value="done"${!editing.archived&&App._isProjectDone(editing)?" selected":""}>已完成</option>
          <option value="archived"${editing.archived?" selected":""}>已封存</option>
        </select>
        <div id="pf-status-reason" class="form-field" style="margin-top:10px;${editing.archived?"":"display:none;"}">
          <label>封存原因（必填）</label>
          <textarea id="pf-arch-reason" rows="2" style="width:100%;resize:vertical;" placeholder="例：專案損益不佳被喊停 / 需求變更暫停 / 已被新案取代…" oninput="App._pfStatusChange()">${editing.archiveReason?U.esc(editing.archiveReason):""}</textarea>
          <div id="pf-arch-hint" style="margin-top:4px;font-size:12px;color:var(--rose-ink);display:none;">選「已封存」需填原因——一定有原因才會喊停一個專案。</div>
        </div>
        <div style="margin-top:4px;font-size:11.5px;color:var(--ink3);">完成或封存的專案會移到「專案檔案室」、不在側欄顯示；改回「進行中」即退回側欄。</div>
      </div>
        ${isEdit?`
        <div class="form-field">
          <label>部門負責人</label>
          <div class="dept-editor-head"><span class="dept-head-name">部門名稱</span><span class="dept-head-members">負責人姓名</span></div>
          <div class="dept-edit-list" id="deptEditorList">${App.buildDeptRowsHtml(editing.depts||[],"edit",projId)}</div>
          <button class="tb-action ghost dept-add-btn" onclick="App.deptUI.addDept('edit', '${projId}')">＋ 新增部門</button>
        </div>
        ${editing.ecnType?`
        <div class="form-field" style="max-width:300px;">
          <label>本案呆滯特批門檻覆寫（NTD $，選填）</label>
          <input type="number" id="pf-scrapthreshold" min="0" step="1000" value="${editing.scrapThresholdOverride!=null?editing.scrapThresholdOverride:""}" placeholder="留空＝沿用全域 ${DATA.settings.ecnScrapThreshold??3e4}">
          <div style="margin-top:4px;font-size:11.5px;color:var(--ink3);">結案時舊料報廢金額超過此值就擋下、需特批；留空＝沿用系統設定的全域門檻。</div>
        </div>
        `:""}
        `:""}
    `,footer:`
      ${isEdit?`<button class="tb-action danger" data-edit-hide onclick="App.deleteProject('${projId}')" style="margin-right:auto;">刪除專案</button>`:""}
      <button class="tb-action ghost" onclick="App.closeModal()">取消</button>
      <button class="tb-action pf-btn-create" id="pf-submitBtn" data-edit-hide onclick="App.saveProject('${projId||""}')">${isEdit?"儲存":"建立"}</button>
    `});setTimeout(()=>{if(App._pfStatusChange)App._pfStatusChange()},30)};App._pfStatusChange=function(){const sel=document.getElementById("pf-status");if(!sel)return;const box=document.getElementById("pf-status-reason");const ta=document.getElementById("pf-arch-reason");const hint=document.getElementById("pf-arch-hint");const submit=document.getElementById("pf-submitBtn");const isArch=sel.value==="archived";if(box)box.style.display=isArch?"":"none";const needReason=isArch&&!(ta&&ta.value.trim());if(submit)submit.disabled=needReason;if(hint)hint.style.display=needReason?"":"none"};App.editProject=function(id){this.openProjectDialog(id)};App._syncMainName=function(){const proj=document.getElementById("pf-name");const main=document.getElementById("pf-mainName");if(!proj||!main)return;if(!main.dataset.touched)main.value=proj.value};App._showTplWarnings=function(warnings){if(!warnings||!warnings.length)return;const MAX=12;const shown=warnings.slice(0,MAX).map(w=>"<li>"+U.esc(w)+"</li>").join("");const more=warnings.length>MAX?'<li class="tpl-warn-more">…另 '+(warnings.length-MAX)+" 項</li>":"";App.confirmModal({title:"套用範本提醒（"+warnings.length+" 項自動調整）",icon:"ti-info-circle",iconBg:"--amber-l",iconColor:"--amber-ink",msg:'<div class="tpl-warn-modal-body">建立時系統自動處理了下列項目（多為「前置已刪除／階段未選 → 自動移除該前置」）：<ul class="tpl-warn-list">'+shown+more+'</ul><div class="tpl-warn-tip">📌 請到<b>甘特圖／任務大表</b>確認排程是否符合預期；要改流程母版請回<b>設定 → 範本</b>編修後重新開案。</div></div>',okText:"我知道了",cancelText:null})};App.saveProject=function(id){const name=document.getElementById("pf-name").value.trim();if(!name){U.toast("⚠ 請填專案名稱","warning");return}const note=document.getElementById("pf-note").value.trim();if(App._roGuard())return;const p=this.getProj(id);if(p){p.name=name;p.note=note;const stEl=document.getElementById("pf-scrapthreshold");if(stEl){const raw=stEl.value.trim();p.scrapThresholdOverride=raw===""?void 0:Math.max(0,parseInt(raw)||0)}const statusSel=document.getElementById("pf-status");if(statusSel){const v=statusSel.value;if(v==="archived"){const reason=(document.getElementById("pf-arch-reason")?.value||"").trim();if(!reason){App._pfStatusChange();return}p.archived=true;p.archiveReason=reason;if(!p.archivedAt)p.archivedAt=new Date().toISOString()}else if(v==="done"){p.archived=false;delete p.archiveReason;delete p.archivedAt;if(!App._isProjectDone(p)){p.status="done";p.completedAt=new Date().toISOString()}}else{p.archived=false;delete p.archiveReason;delete p.archivedAt;p.status="active";delete p.completedAt}}}Store.projects.save();this.closeModal();this.refreshAll();U.toast("✓ 專案已更新")};App._renderStage2=function(){const res=this._tplPreview;if(!res){U.toast("⚠ 無範本預覽資料，請重新套用範本","warning");return}const variants=res.variants||[];const tasks=res.tasks||[];if(!this._s2Stage)this._s2Stage={};variants.forEach(v=>{const g=this._s2GroupByStage(v.id);if(g.order.length&&g.order.indexOf(this._s2Stage[v.id])<0)this._s2Stage[v.id]=g.order[0]});const fmtD=s=>s?String(s).replace(/-/g,"/"):"";const caseRange=vid=>{const ts=tasks.filter(t=>t.variant===vid);const starts=ts.map(t=>t.plannedStart).filter(Boolean).sort();const ends=ts.map(t=>t.plannedEnd).filter(Boolean).sort();const a=starts[0],b=ends[ends.length-1];return a||b?fmtD(a)+" → "+fmtD(b):"（待排程）"};const help='<div class="stage2-help"><div class="stage2-help-head">❓ 填寫說明</div><div class="stage2-help-block"><b>日期（起訖）</b>：系統自動計算，不直接填；請以「前置任務 ＋ 工期」調整。</div><div class="stage2-help-block"><b>需交付</b>：此任務是否須繳交付件（如報告、樣品）。可逐筆勾或整階段全選。</div><div class="stage2-help-block"><b>前置任務</b>三種設定：<br>・接在《A》後 — 等 A 做完，隔天才開始。例：樣機組裝 接在《零件到料》後<br>・接在《A》後 ＋2天 — 等 A 做完，再多等 2 天才開始。例：塗裝 接在《組裝》後 ＋2天（等乾）<br>・無前置 — 不用等其他項目，從專案開始日就排入。例：規格訂定</div></div>';const blocks=variants.map((v,i)=>{const isMain=i===0;return'<div class="s2-case '+(isMain?"s2-case-main":"s2-case-other")+'" data-variant="'+v.id+'"><div class="s2-case-head"><span class="stage-cap-pill cap-'+i%3+'">'+(isMain?"主案":"另案")+'</span><span class="s2-case-name">'+U.esc(v.name||"")+'</span><span class="s2-case-range">'+caseRange(v.id)+'</span></div><div class="s2-slack-wrap" data-variant="'+v.id+'">'+this._s2SlackHtml(v.id)+'</div><div class="s2-gantt" data-variant="'+v.id+'">'+this._s2GanttHtml(v.id)+'</div><div class="s2-list" data-variant="'+v.id+'">'+this._s2ListHtml(v.id)+"</div></div>"}).join("");document.querySelectorAll(".page").forEach(pg=>pg.classList.remove("active"));const page=document.getElementById("page-stage2");page.classList.add("active");page.innerHTML='<div class="stage2-wrap"><div class="stage2-head"><span class="s2-num">2</span>編輯任務骨架</div>'+help+blocks+(n=>n>0?'<div class="s2-unassigned-bar">⚠ 還有 '+n+" 個任務未指派負責人</div>":"")((res.tasks||[]).filter(t=>!t.owner).length)+'<div class="stage2-foot"><button class="tb-action ghost" onclick="App._flowStage3Back()">上一步</button><button class="tb-action" data-edit-hide onclick="App._stage2Commit()">建立專案</button></div></div>';if(document.body.classList.contains("viewonly")){document.querySelectorAll("#page-stage2 input, #page-stage2 select, #page-stage2 .s2-del, #page-stage2 .dt-insert-btn").forEach(el=>{el.disabled=true})}window.scrollTo({top:0,behavior:"smooth"})};App._flowStage1Next=function(){const tpl=App._s1Tpl();if(!tpl){U.toast("⚠ 找不到範本","warning");return}const input=App._s1CollectInput();if(!input||!input.cases.length){U.toast("⚠ 無案別資料，請重新套用範本","warning");return}const main=input.cases[0];if(!main.startDate&&!main.endDate){U.toast("⚠ 主案請至少填開始日或上市日期","warning");return}input.depts=App._tplDepts||[];if(App._s1Ecn){App._createFlow=App._createFlow||{};App._createFlow.ecn={size:App._s1Ecn.size,changeReason:((document.getElementById("s1-ecn-reason")||{}).value||"").trim(),roiType:(document.getElementById("s1-ecn-roi")||{}).value||"benefit",sourceNo:((document.getElementById("s1-ecn-src")||{}).value||"").trim(),targetSavePerUnit:parseFloat((document.getElementById("s1-ecn-target")||{}).value)||0,ecnNo:"",pmEffort:function(){const t=tplEcn();return t&&t.sizeMeta&&t.sizeMeta[App._s1Ecn.size]?t.sizeMeta[App._s1Ecn.size].pmEffort:20}()}}else if(App._createFlow){App._createFlow.ecn=null}this._tplPreview=App.applyTemplate(tpl,input);App._attachStageCoord(this._tplPreview,tpl,!!(App._createFlow&&App._createFlow.ecn));const reds=(this._tplPreview.variants||[]).filter(v=>{const s=App._s2VariantSlack(v.id);return s&&s.light==="red"});if(reds.length){let maxOver=0;reds.forEach(v=>{const s=App._s2VariantSlack(v.id);if(s&&s.overDays>maxOver)maxOver=s.overDays});App.confirmModal({icon:"ti-chart-bar",iconBg:"--rose-l",iconColor:"--rose",title:"偵測到時程衝突！已為您開啟智慧排程引導",msg:"目前排程規劃尚缺 <b>"+maxOver+"</b> 個工作天"+(reds.length>1?"（共 "+reds.length+" 個案別時程不足）":"")+"。為了協助您快速順時程，系統已將此專案導入「智慧排程衝突處理面板」。<br><br>您可以透過系統建議一鍵微調，或透過精選的長工時任務快速進行扣減。",okText:"開始智慧排程 →",cancelText:"返回上一步",onConfirm:function(){App._renderOverflowFlow()}});return}if(App._s1Ecn){App._stage2Commit();return}App._s2From="stage1";this._renderStage2New()};App._s2BackToStage1=function(){if(App._s2From==="overflow"&&App._ovfState){App._ovfRender();return}if(App._s2From==="excel"){App._flowStep2();return}document.querySelectorAll(".page").forEach(pg=>pg.classList.remove("active"));const p=document.getElementById("page-stage1");if(p)p.classList.add("active");window.scrollTo({top:0,behavior:"smooth"})};App._s2CommitNew=function(){const res=this._tplPreview;const reds=res?(res.variants||[]).filter(v=>{const s=App._s2VariantSlack(v.id);return s&&s.light==="red"}):[];if(reds.length){App.confirmModal({icon:"ti-tool",iconBg:"--rose-l",iconColor:"--rose",title:"工期仍不足，確定建立？",msg:"尚有 <b>"+reds.length+"</b> 個案別時程不足（餘裕 < 0）。可先用上方紅燈引導調整上市日或壓縮工期；仍要強制建立嗎？",okText:"仍要強制建立",okClass:"danger",cancelText:"返回調整",onConfirm:function(){App._s2DoCommit()}});return}App._s2DoCommit()};App._s2DoCommit=function(){const tb=document.querySelector(".main > .topbar");if(tb)tb.classList.remove("topbar-hidden");App._stage2Commit()};App._s2DeptPanelHtml=function(variantId){const res=this._tplPreview;if(!res)return"";const depts=res.depts||[];const tasks=(res.tasks||[]).filter(t=>t.variant===variantId);const unassigned=tasks.filter(t=>!t.owner).length;let rows=depts.map(d=>{const cnt=tasks.filter(t=>t.dept===d.id).length;const names=(d.members||[]).map(m=>U.esc(m.name)).filter(Boolean).join("、")||'<span class="s2n-dp-empty">無成員</span>';return'<div class="s2n-dp-row"><div class="s2n-dp-name">'+U.esc(d.name)+'</div><div class="s2n-dp-members">'+names+'</div><div class="s2n-dp-cnt">'+cnt+" 件</div></div>"}).join("");if(!depts.length)rows='<div class="s2n-dp-none">尚未設定部門，點右上「新增/編輯部門」建立</div>';const unRow='<div class="s2n-dp-row s2n-dp-unassigned'+(unassigned?"":" ok")+'"><div class="s2n-dp-name">未指派</div><div class="s2n-dp-members"></div><div class="s2n-dp-cnt">'+unassigned+" 件</div></div>";return'<div class="s2n-deptpanel"><div class="s2n-dp-head"><span class="s2n-dp-title">部門與負責人</span><button class="s2n-dp-btn" onclick="App._s2OpenDeptModal()"><i class="ti ti-pencil"></i> 新增/編輯部門</button></div>'+rows+unRow+"</div>"};App._s2OpenDeptModal=function(){const res=this._tplPreview;if(!res)return;const roles=[];(res.tasks||[]).forEach(t=>{const r=(t.role||"").trim();if(r&&roles.indexOf(r)<0)roles.push(r)});const existing={};(res.depts||[]).forEach(d=>{existing[d.name]=d});const working=roles.map(r=>existing[r]?JSON.parse(JSON.stringify(existing[r])):{id:U.id(),name:r,members:[{id:U.id(),name:""}]});(res.depts||[]).forEach(d=>{if(roles.indexOf(d.name)<0)working.push(JSON.parse(JSON.stringify(d)))});App._tplDepts=working;const body='<div class="form-field"><label>部門與負責人（可自由增減）</label><div class="s2n-dept-hint">先建立部門與成員；儲存後系統會依任務角色自動指派負責人，之後可在任務表逐筆微調。</div><div class="dept-edit-list" id="deptEditorList">'+App.buildDeptRowsHtml(App._tplDepts,"tpl",null)+`</div><button class="dept-add-btn" onclick="App.deptUI.addDept('tpl', '')">＋增加部門</button></div>`;const footer='<button class="tb-action ghost" onclick="App.closeModal()">取消</button><button class="tb-action" onclick="App._s2ApplyDepts()">儲存並套用</button>';App.openModal({title:"部門與負責人",body,footer})};App._s2ApplyDepts=function(){const res=this._tplPreview;const edited=JSON.parse(JSON.stringify(App._tplDepts||[]));if(!res){App.closeModal();return}const depts=[];edited.forEach(d=>{const name=(d.name||"").trim();if(!name)return;const members=(d.members||[]).map(m=>({id:m.id,name:(m.name||"").trim()})).filter(m=>m.name);if(!members.length)return;depts.push({id:d.id,name,members})});res.depts=depts;const roleToDeptId={};const deptById={};depts.forEach(d=>{roleToDeptId[d.name]=d.id;deptById[d.id]=d});(res.tasks||[]).forEach(t=>{t.dept=roleToDeptId[(t.role||"").trim()]||"";if(!t.owner&&t.dept){const d=deptById[t.dept];if(d&&d.members&&d.members[0])t.owner=d.members[0].name}});App.closeModal();if(App._s2EcnMode){const proj=App.getProj(App._s2EcnMode);if(proj)proj.depts=depts;App._s2EcnPersist();return}this._renderStage2New()};App._renderStage2New=function(){const res=this._tplPreview;if(!res){U.toast("⚠ 無範本預覽資料，請重新套用範本","warning");return}const variants=res.variants||[];const tasks=res.tasks||[];if(!this._s2Stage)this._s2Stage={};variants.forEach(v=>{const g=this._s2GroupByStage(v.id);if(g.order.length&&g.order.indexOf(this._s2Stage[v.id])<0)this._s2Stage[v.id]=g.order[0]});const fmtD=s=>s?String(s).replace(/-/g,"/"):"";const caseRange=vid=>{const ts=tasks.filter(t=>t.variant===vid);const starts=ts.map(t=>t.plannedStart).filter(Boolean).sort();const ends=ts.map(t=>t.plannedEnd).filter(Boolean).sort();const a=starts[0],b=ends[ends.length-1];return a||b?fmtD(a)+" → "+fmtD(b):"（待排程）"};const topGuide=App.buildHintBox({key:"s2-guide",icon:"ti-info-circle",title:"骨架與階段切換指南",summary:"先建名冊自動帶負責人（手動優先）；點甘特切換階段",collapsed:false,bodyHtml:'<div class="s2n-gd-row"><i class="ti ti-speakerphone"></i><span><b>先建名冊再排程</b>：請先至左側「部門與負責人」區塊建立清單，系統會自動將人員帶入下方任務大表的負責人欄。手動修改的指派結果將受系統保護，不會被覆蓋。</span></div><div class="s2n-gd-row"><i class="ti ti-chart-bar"></i><span><b>快速切換開發階段</b>：點擊上方甘特圖的綠色進度條（如：設計、手工機），下方大表即會同步切換至該階段的專屬任務清單。</span></div>'});const _hasCoord=!!(res.project&&res.project.stageCoordDefaults);const predHelpHtml=i=>App.buildHintBox({key:"s2-pred-help-"+i,icon:"ti-list-details",title:"任務大表填寫指南",summary:_hasCoord?"前置防呆／投入%負荷／PM 隱形負荷":"前置防呆／投入%負荷",collapsed:true,bodyHtml:'<div class="s2n-gd-row"><i class="ti ti-link"></i><span><b>前置連動與防呆</b>：依序設定「序號 → 銜接方式 → 緩衝天數」。<b>系統防呆</b>：前置任務僅能綁定「當前階段」或「過去 3 個階段」內的項目，嚴禁綁定未來任務以防排程死鎖。</span></div><div class="s2n-gd-row s2n-gd-warn"><i class="ti ti-gauge"></i><span><b>負荷警示與投入級別</b>：投入%＝這件事吃掉負責人一天的幾成，六檔（100% 獨佔／75% 大半天／50% 半天／25% 零星盯／10% 點一下／0% 掛名未出力）。同人跨案單日疊加破百＝爆單，會在總覽 PM 負荷／部門負載轉紅示警。</span></div>'+(_hasCoord?'<div class="s2n-gd-row"><i class="ti ti-user-cog"></i><span><b>PM 隱形負荷保護</b>：每階段最底部的「PM 階段協調（常駐）」專門記錄你的跨部門溝通心血，請依該階段的實際硬度自由調整投入%（其餘各部門常規任務預設為 100%）。</span></div>':"")});const blocks=variants.map((v,i)=>{const isMain=i===0;return'<div class="s2n-case s2-case '+(isMain?"s2-case-main":"s2-case-other")+'" data-variant="'+v.id+'"><div class="s2-case-head"><span class="stage-cap-pill cap-'+i%3+'">'+(isMain?"主案":"子案")+'</span><span class="s2-case-name">'+U.esc(v.name||"")+'</span><span class="s2-case-range">'+caseRange(v.id)+'</span></div><div class="s2-slack-wrap" data-variant="'+v.id+'">'+this._s2SlackHtml(v.id)+'</div><div class="s2n-body"><div class="s2n-left">'+this._s2DeptPanelHtml(v.id)+'</div><div class="s2-gantt s2n-right" data-variant="'+v.id+'">'+this._s2GanttHtml(v.id)+'</div></div><div class="s2n-banner-wrap" data-variant="'+v.id+'">'+this._s2BannerHtml(v.id)+"</div>"+predHelpHtml(i)+'<div class="s2-list" data-variant="'+v.id+'">'+this._s2ListHtml(v.id)+"</div></div>"}).join("");const unbar=(n=>n>0?'<div class="s2-unassigned-bar">⚠ 還有 '+n+" 個任務未指派負責人</div>":"")(tasks.filter(t=>!t.owner).length);document.querySelectorAll(".page").forEach(pg=>pg.classList.remove("active"));const page=document.getElementById("page-stage2");page.classList.add("active");page.innerHTML='<div class="s2n-wrap"><div class="s2n-pagehd"><div class="s1-crumb">總儀表板 <span class="s1-crumb-sep">/</span> 新增專案 <span class="s1-crumb-sep">/</span> 編輯任務</div><div class="s2n-head"><span class="s2-num">2</span>編輯任務骨架</div></div>'+topGuide+blocks+unbar+'<div class="stage2-foot"><button class="tb-action ghost" onclick="App._s2BackToStage1()">上一步</button><button class="tb-action" data-edit-hide onclick="App._s2CommitNew()">建立專案</button></div></div>';if(document.body.classList.contains("viewonly")){document.querySelectorAll("#page-stage2 input, #page-stage2 select, #page-stage2 .s2-del, #page-stage2 .dt-insert-btn").forEach(el=>{el.disabled=true})}window.scrollTo({top:0,behavior:"smooth"})};App._scheduleEduCard=function(nextFn,backFn,kind){App._eduNext=nextFn||function(){App._renderStage1Preview()};App._eduBack=backFn||function(){App._flowStep1()};const k=kind==="ecn"?"ecn":"npi";const cards=[{icon:"ti-calendar",tag:"填開始日",tagcls:"s1-tag-start",title:"已指定開工日",desc:"採「正向順推」機制，由您的開工日往後自動推算各階段預計完工日。",secret:"適合已有明確開工時間，想精算「最終何時能完工/上市」的專案。"},{icon:"ti-flag",tag:"填上市日期",tagcls:"s1-tag-end",title:"有指定上市日",desc:"採「逆向倒推」機制，依據目標上市日自動反推最晚必須開工的時間。",secret:"若倒推出的時間不夠，系統將自動啟動智慧排程，為您建議最快完工的黃金時程。"},{icon:"ti-arrows-left-right",tag:"都填",tagcls:"s1-tag-both",title:"雙端日期皆鎖定",desc:"採「雙端對比」機制，同時鎖定開工與上市日，嚴格精算專案時間彈性。",secret:"當時間出現衝突時，系統將自動開啟「時程引導面板」，協助您無痛化解時程衝突。"}];const cardsHtml=cards.map(c=>'<div class="s1-edu-card"><div class="s1-edu-coretop"><div class="s1-edu-cardhd"><i class="ti '+c.icon+' s1-edu-ico"></i><span class="s1-edu-tag '+c.tagcls+'">'+c.tag+'</span></div><div class="s1-edu-cardtitle">'+c.title+'</div><div class="s1-edu-carddesc">'+c.desc+'</div></div><div class="s1-edu-secret"><span class="s1-edu-skey">小秘訣：</span>'+c.secret+"</div></div>").join("");App.openModal({title:'<i class="ti ti-book-open"></i> 排程模式說明指南',wide:true,body:'<div class="s1-edu s1-edu-'+k+'"><div class="s1-edu-lead">本頁為排程模式導覽（僅供功能理解），下一頁直接輸入日期，系統即會依據您的填寫自動切換對應模式。</div><div class="s1-edu-cards">'+cardsHtml+'</div><div class="s1-edu-bulb"><i class="ti ti-bulb"></i><span><b>排程小貼士：</b>不論您在下一頁填寫哪一個欄位，系統皆會智慧判斷並自動切換排程方向。若手頭已有完整時間規劃，建議兩者皆填，算出來的時程容裕度與預算最為精準。</span></div></div>',footer:'<button class="tb-action ghost" onclick="App._eduGoBack()">上一步</button><button class="tb-action edu-go-'+k+'" onclick="App._eduGoNext()">我懂了，開始填寫 →</button>'})};App._eduGoNext=function(){const f=App._eduNext;App.closeModal();if(f)f()};App._eduGoBack=function(){const f=App._eduBack;App.closeModal();if(f)f()};App._renderStage1Preview=function(){const _tpl0=App._s1Tpl();if(!App._s1Cases){const tplMain=_tpl0&&_tpl0.cases&&_tpl0.cases[0]?_tpl0.cases[0].stages.slice():App._s1FallbackCase().stages.map(s=>s.stage);App._s1Cases=[{key:"main",templateVariant:"主案",stages:tplMain,renames:{}}]}const casesHtml=App._s1Cases.map((c,i)=>App._s1CaseColHtml(c,i===0)).join("");const addCase=App._s1Ecn?"":'<div class="s1-add-case" onclick="App._s1AddSubcase()"><i class="ti ti-plus"></i><span>新增子案</span></div>';const tips=App._s1Ecn?App.buildHintBox({key:"s1-ecn-helper",icon:"ti-bulb",title:"開案小幫手",summary:"點擊了解類型、時程與分級邏輯",collapsed:true,bodyHtml:'<ol class="s1-ecn-helpol"><li><b>設變類型</b>：「效益型」（如 Cost Down）結案時系統自動對比新舊 BOM 算出省下的成本；「被迫型」（如停產／法規）系統會著重追蹤舊料消耗率、卡控生效日以降低報廢損失。</li><li><b>時程推算</b>：填「開始日」系統順推預估完工日；只填「上市日」回推最晚必須動工的日期；若算出工期超出上市死線，進度條將亮紅燈警示。</li><li><b>設變分級（S/M/L）</b>：S 為單純換料（免認證）；M 涉及結構變更（需重新認定）；L 涉及改模。開案時依初步評估選擇，執行中若範圍擴大，系統會自動提醒您升級。</li><li><b>PM 協調工時（防呆保護）</b>：設變不是只有 RD 在忙——每張單都預設保留 PM 的跨部門協調時間。多案疊加總負載超過 100% 時，系統會發出排擠告警，避免隱形過勞。</li></ol>'}):App.buildHintBox({key:"s1-sched-tips",icon:"ti-info-circle",title:"排程小秘訣",summary:"怎麼填日期決定排程方向",collapsed:false,bodyHtml:'<div class="s1-tips-line"><span class="s1-tips-dot"></span>只填開始日：自動順推，算出預計完工日。</div><div class="s1-tips-line"><span class="s1-tips-dot"></span>只填上市日期：自動倒推最晚開工日<b class="s1-tips-warn">（若發現來不及，會自動改為建議最快完工日）</b>。</div><div class="s1-tips-line"><span class="s1-tips-dot"></span>兩者皆填齊：精算時間是否足夠。<b class="s1-tips-warn">若發生超時衝突（如目前顯示：時程不足），點擊「下一步」後系統將引導您透過智慧排程面板一鍵優化。</b></div><div class="s1-tips-line s1-tips-cap"><i class="ti ti-info-circle"></i>若有多個產品規格（如 7.3kW ／ 2.2kW），點擊主案右側 ＋【新增子案】即可獨立排程。</div>'});const helpBody='<div class="slack-help-ambox"><i class="ti ti-alert-triangle"></i><span>須在<span class="slack-help-hl">開始與上市日期皆填齊</span>時，各開發階段才會觸發以下顏色；只填單一日期，甘特圖維持預設單色。</span></div><div class="slack-help-grid"><div class="slack-help-cell"><span class="slack-dot sd-green"></span><div><div class="slack-help-ct">綠色 ── 安全</div><div class="slack-help-cd">完工日比該段 Deadline 提前 5 天以上。</div></div></div><div class="slack-help-cell"><span class="slack-dot sd-yellow"></span><div><div class="slack-help-ct">黃色 ── 警告</div><div class="slack-help-cd">完工日離該段 Deadline 僅差 0～4 天。</div></div></div><div class="slack-help-cell"><span class="slack-dot sd-red"></span><div><div class="slack-help-ct">紅色 ── 延誤</div><div class="slack-help-cd">完工日已落在該段 Deadline 之後。</div></div></div></div>';const hint=App.buildHintBox({key:"s1-slack-help",icon:"ti-info-circle",title:"甘特圖階段顏色說明",summary:"一分鐘看懂各階段排程緊迫度",bodyHtml:helpBody,collapsed:true});const top='<div class="s1-top"><div class="s1-top-col"><span class="s1-top-label">專案名稱</span><input type="text" class="s1-proj-name" value="範例專案" placeholder="專案名稱" oninput="App._s1SyncMainName();App._s1RefreshPreview()"></div>'+(App._s1Ecn?tplAll("ecn").length>1?'<div class="s1-top-col"><span class="s1-top-label">選擇範本</span><select class="s1-tpl-sel" onchange="App._s1Ecn.tplId=this.value;App._s1RefreshPreview()">'+tplAll("ecn").map(t=>'<option value="'+U.esc(t.templateId)+'"'+(t.templateId===App._s1Ecn.tplId?" selected":"")+">"+U.esc(t.templateName)+"</option>").join("")+"</select></div>":"":'<div class="s1-top-col"><span class="s1-top-label">選擇範本</span><select class="s1-tpl-sel" onchange="App._s1TplId=this.value;App._s1RefreshPreview()">'+tplAll("npi").map(t=>'<option value="'+U.esc(t.templateId)+'"'+(t.templateId===App._s1TplId?" selected":"")+">"+U.esc(t.templateName)+"</option>").join("")+"</select></div>")+(!App._s1Ecn?'<div class="s1-top-col"><span class="s1-top-label">目標 BOM 成本 / 台（選填）</span><input type="number" class="s1-src-in" id="s1-npi-tuc" min="0" step="1" placeholder="例：1200（元/台）"></div><div class="s1-top-col"><span class="s1-top-label">預估售價 / 台（選填 · 開毛利）</span><input type="number" class="s1-src-in" id="s1-npi-usp" min="0" step="1" placeholder="例：2000（元/台）"></div>':"")+"</div>";const ecnMetaTop=App._s1Ecn?App._s1EcnMetaTopHtml():"";const ecnMetaBottom=App._s1Ecn?App._s1EcnMetaBottomHtml():"";const rosterHint=App._s1Ecn?App.buildHintBox({key:"s1-roster-help",icon:"ti-info-circle",title:"為什麼要先填名冊？",summary:"先填人，後面全自動帶入",collapsed:false,bodyHtml:'<ol class="s1-ecn-helpol"><li><b>自動帶負責人</b>：套範本時系統依「部門 → 人」把名冊自動填進各任務的負責人欄，不用一筆一筆指派。</li><li><b>大表下拉直選</b>：後續任務大表的「負責人」欄＝直接下拉選名冊，改人一鍵完成。</li><li><b>負荷自動計算</b>：每個人的投入比例會自動疊進部門／個人負荷，跨案同人同日超過 100% 立即紅旗示警。</li></ol>'}):"";const roster=App._s1Ecn?'<div class="s1-roster"><div class="s1-prev-title">部門 / 負責人名冊</div>'+rosterHint+App._deptEditorHtml()+"</div>":"";const previewBlocks='<div id="s1-prev-blocks">'+App._s1PreviewBlocksHtml()+"</div>";const tb=document.querySelector(".main > .topbar");if(tb)tb.classList.add("topbar-hidden");document.querySelectorAll(".page").forEach(pg=>pg.classList.remove("active"));const page=document.getElementById("page-stage1");page.classList.add("active");page.innerHTML='<div class="s1-preview'+(App._s1Ecn?" s1-ecnpage":"")+'"><div class="s1-pagehd"><div class="s1-crumb">總儀表板 <span class="s1-crumb-sep">/</span> '+(App._s1Ecn?"新增設變案":"新增專案")+'</div><div class="s1-pagetitle">'+(App._s1Ecn?"套用範本創建 · 設變案":"套用範本創建")+"</div>"+(App._s1Ecn?'<div class="s1-pagesub">點 S / M / L 看時程與 PM 負荷即時變化。排程模式由你填的日期系統自判。</div>':"")+"</div>"+top+tips+ecnMetaTop+(App._s1Ecn?'<div class="s1-sched-frame"><div class="s1-sched-lbl">排程設定（開始日/上市日期＋設變分級，共同決定下方甘特）</div><div class="s1-cases">'+casesHtml+addCase+"</div>"+ecnMetaBottom+"</div>":'<div class="s1-cases">'+casesHtml+addCase+"</div>")+'<div class="s1-prev-section"><div class="s1-prev-title">階段區間預覽</div>'+hint+previewBlocks+"</div>"+roster+'<div class="stage2-foot"><button class="tb-action ghost" onclick="App._s1Back()">上一步</button><button class="tb-action" onclick="App._flowStage1Next()">'+(App._s1Ecn?"建立專案":"下一步：檢視任務")+"</button></div></div>";App._s1SyncMainName();App._s1RefreshPreview();window.scrollTo({top:0,behavior:"smooth"})};App._S1_SZ_HINT={S:"單純零件替代或文件修改，免安規重測——「評估 → 改圖 → 結案」3 階段快速放行。PM 常駐 Effort 15%。",M:"牽涉結構變更的中型設變案，標準 6 階段流程，含實體打樣、品保測試與安規驗證檢核。PM 常駐 Effort 20%。",L:"改模或高風險大型設變案，6 階段外強制追加跨部門「DR 設計審查」關卡，嚴格卡控。PM 常駐 Effort 40%。"};App._s1RoiCardsHtml=function(sel){const benefitOn=sel!=="forced";const card=(type,on)=>{const cls="s1-roicard"+(on?" on":"");const check=on?'<span class="s1-rc-check">✓</span>':"";if(type==="benefit"){return'<div class="'+cls+`" onclick="App._s1SelectRoi('benefit')">`+check+'<div class="s1-rc-h">效益型·主動成本調降</div><div class="s1-rc-sec"><span class="s1-rc-lbl">適用情境</span>針對<b>換料、議價</b>等主動降低 BOM 成本之變更案。</div><div class="s1-rc-sec"><span class="s1-rc-lbl">系統結算模式</span>將啟用 ROI 計算，結算面板顯示「<b>回本期</b>」與「<b>N 年淨效益</b>」，評估每台成本降幅。</div></div>'}return'<div class="'+cls+`" onclick="App._s1SelectRoi('forced')">`+check+'<div class="s1-rc-h">被迫型·不可抗力</div><div class="s1-rc-sec"><span class="s1-rc-lbl">適用情境</span>針對料件<b>停產 (EOL)</b>、<b>法規安規變更</b>或設計品質修正之設變案。</div><div class="s1-rc-sec"><span class="s1-rc-lbl">系統結算模式</span>不適用回本計算，結算面板轉為評估「<b>合約／變更總代價</b>」與每台成本最小化損失。</div></div>'};return card("benefit",benefitOn)+card("forced",!benefitOn)};App._s1SelectRoi=function(v){const h=document.getElementById("s1-ecn-roi");if(h)h.value=v;const c=document.getElementById("s1-roi-cards");if(c)c.innerHTML=App._s1RoiCardsHtml(v)};App._s1EcnMetaTopHtml=function(){return'<div class="s1-ecnmeta"><div class="s1-ecnmeta-col"><span class="s1-top-label">需求單號（選填）<span class="s1-qmark">?<span class="s1-qtip">此單號往後會與正式 ECN 單號關聯，記錄在專案內頁中，作為開案與結案的追溯紀錄。</span></span></span><input type="text" id="s1-ecn-src" class="s1-src-in" placeholder="觸發此案的客訴單或異常單號"></div><div class="s1-ecnmeta-col"><span class="s1-top-label">設變背景與原因（簡述 2～3 句即可）</span><textarea id="s1-ecn-reason" class="s1-reason-in" rows="2" placeholder="請描述變更背景，例：客戶反映壓縮機外殼異音，需增加緩衝墊片"></textarea></div><div class="s1-ecnmeta-col"><span class="s1-top-label">設變類型（點卡片選擇，決定結案怎麼算錢）</span><input type="hidden" id="s1-ecn-roi" value="benefit"><div class="s1-roicards" id="s1-roi-cards">'+App._s1RoiCardsHtml("benefit")+'</div></div><div class="s1-ecnmeta-col"><span class="s1-top-label">目標成本調降 / 台（選填 · 效益型結案達成率基準）</span><input type="number" id="s1-ecn-target" class="s1-src-in" min="0" step="1" placeholder="例：120（單位：元/台）"></div></div>'};App._s1EcnMetaBottomHtml=function(){const size=App._s1Ecn.size;const szBtn=(s,lbl)=>'<button type="button" class="s1-szbtn'+(s===size?" on":"")+'" data-size="'+s+`" onclick="App._s1SetEcnSize('`+s+`')"><b>`+s+"</b><span>"+lbl+"</span></button>";return'<div class="s1-ecnmeta"><div class="s1-ecnmeta-col"><span class="s1-top-label">設變分級（S/M/L，點選看時程變化）</span><div class="s1-szrow">'+szBtn("S","換料·免認證")+szBtn("M","結構·需認定")+szBtn("L","改模·認證")+'</div><div class="s1-szpanel" id="s1-sz-hint">'+App._S1_SZ_HINT[size]+"</div></div></div>"};App._s1SetEcnSize=function(size){if(!App._s1Ecn)return;App._s1Ecn.size=size;const _et2=tplEcn();const meta=_et2&&_et2.sizeMeta&&_et2.sizeMeta[size]||null;(App._s1Cases||[]).forEach(c=>{if(meta)c.stages=meta.stages.slice()});document.querySelectorAll("#page-stage1 .s1-szbtn").forEach(b=>b.classList.toggle("on",b.dataset.size===size));const h=document.getElementById("s1-sz-hint");if(h)h.textContent=App._S1_SZ_HINT[size]||"";App._s1RefreshPreview()};App._s1FallbackCase=function(){return{name:"7.3kW 主案",start:"2026-03-02",end:"2026-12-28",light:"green",slack:12,overDays:0,stages:[{stage:"設計",start:"2026-03-02",end:"2026-05-15"},{stage:"手工機",start:"2026-05-18",end:"2026-07-31"},{stage:"性試",start:"2026-08-03",end:"2026-10-09"},{stage:"量試",start:"2026-10-12",end:"2026-12-28"}]}};App._s1CaseByKey=function(key){return(App._s1Cases||[]).find(x=>x.key===key)||null};App._s1CaseColHtml=function(c,isMain){const pill=isMain?'<span class="stage-cap-pill cap-0">主案</span>':'<span class="stage-cap-pill cap-1">子案</span>';const del=isMain?"":`<button class="s1-case-del" title="刪除子案" onclick="App._s1DelSubcase('`+c.key+`')"><i class="ti ti-x"></i></button>`;const nameVal=isMain?"":c.dname||"";const ph=isMain?"主案名稱":"子案名稱（例：2.2kW）";return'<div class="s1-case-col case-card '+(isMain?"s2-case-main":"s2-case-other")+'" data-case="'+(isMain?"main":c.key)+'" data-case-key="'+c.key+'" data-tplvariant="'+c.templateVariant+'">'+(App._s1Ecn?"":'<div class="s1-case-head">'+pill+'<input type="text" class="s1-case-name" value="'+U.esc(nameVal)+'" placeholder="'+ph+'" oninput="App._s1RefreshPreview()">'+del+"</div>")+'<div class="s1-case-dates"><label>開始日<input type="date" class="s1-in-start" value="" onchange="App._s1RefreshPreview()"></label><label>上市日期<input type="date" class="s1-in-end" value="" onchange="App._s1RefreshPreview()"></label></div><div class="s1-dynhint s1-dynhint-init">'+App._s1DynHintHtml("","","")+"</div>"+(App._s1Ecn?"":'<div class="s1-stage-hd">開發階段</div><div class="s1-stage-note"><i class="ti ti-info-circle"></i>此處直接決定專案流程（由左至右）：點擊膠囊可重新命名和刪除階段，按 ＋ 可增加階段。<br><b class="s1-stage-note-em">調整後系統將自動重排下方甘特圖、並重新精算時間餘裕與燈號。</b></div><div class="s1-stagelist">'+App._s1StageChipsHtml(c)+"</div>")+"</div>"};App._s1AddSubcase=function(){if(!App._s1Cases)return;const _tpl=App._s1Tpl();const tplCases=_tpl&&_tpl.cases?_tpl.cases:[];const tplSub=(tplCases[1]?tplCases[1].stages:tplCases[0]?tplCases[0].stages:[]).slice();App._s1SubSeq=(App._s1SubSeq||0)+1;const c={key:"c"+App._s1SubSeq,templateVariant:"另案",stages:tplSub,renames:{},dname:"子案 "+App._s1SubSeq};App._s1Cases.push(c);const wrap=document.querySelector("#page-stage1 .s1-cases");const addBox=wrap?wrap.querySelector(".s1-add-case"):null;if(wrap&&addBox){const tmp=document.createElement("div");tmp.innerHTML=App._s1CaseColHtml(c,false);if(tmp.firstChild)wrap.insertBefore(tmp.firstChild,addBox)}App._s1RefreshPreview()};App._s1DelSubcase=function(key){App._s1Cases=(App._s1Cases||[]).filter(x=>x.key!==key);const col=document.querySelector('#page-stage1 .s1-case-col[data-case-key="'+key+'"]');if(col)col.remove();App._s1RefreshPreview()};App._s1PreviewBlocksHtml=function(real){const fmtD=x=>x?String(x).replace(/-/g,"/"):"";const miniGantt=stages=>{const toNum=d=>d?Date.parse(d):NaN;const allS=stages.map(s=>toNum(s.start)).filter(n=>!isNaN(n));const allE=stages.map(s=>toNum(s.end)).filter(n=>!isNaN(n));const minN=allS.length?Math.min.apply(null,allS):0;const maxN=allE.length?Math.max.apply(null,allE):0;const span=maxN-minN||1;const shortD=x=>{const p=String(x).split("-");return(p[1]||"")+"/"+(p[2]||"")};let rows="";stages.forEach(r=>{const a=toNum(r.start),b=toNum(r.end);const left=(a-minN)/span*100;const width=Math.max((b-a)/span*100,1.5);rows+='<div class="s2-grow"><div class="s2-gname">'+U.esc(r.label||r.stage)+'</div><div class="s2-gbar-track"><div class="s2-gbar'+(r.light?" s2-gbar-"+r.light:"")+'" style="left:'+left+"%;width:"+width+'%"></div></div><div class="s2-gdate">'+shortD(r.start)+" → "+shortD(r.end)+"</div></div>"});return'<div class="s2-gantt-axis">'+rows+"</div>"};const lightTxt=(light,slack,overDays)=>light==="green"?"時程充足·餘裕 "+slack+" 天":light==="yellow"?"時程偏緊·餘裕 "+slack+" 天":"時程不足·超出 "+overDays+" 工作天";if(real===void 0)real=App._s1ComputePreview();if(!real||!real.length)return"";const PILL_IC={green:"ti-circle-check",yellow:"ti-alert-circle",red:"ti-alert-triangle"};const PILL_LB={green:"時程充足",yellow:"時程偏緊",red:"時程不足"};const PILL_TIP={green:"時程非常安全！整體進度皆在上市截止日控制範圍內。",yellow:"勉強能如期完成，但毫無緩衝。時程扣得很死，後續階段將非常緊湊。",red:"依照現有範本工期排下去將會超過上市截止日，建議重新調整人力或壓縮工期。"};const blockOf=(v,isMain)=>{const cls=isMain?"s2-case-main":"s2-case-other";if(!v.stages||!v.stages.length){const skel=(v.skelStages||[]).map(lb=>'<div class="s2-grow"><div class="s2-gname">'+U.esc(lb)+'</div><div class="s2-gbar-track"><div class="s2-gbar s2-gbar-none"></div></div><div class="s2-gdate"></div></div>').join("");return'<div class="s1-prev-case case-card '+cls+' s1-prev-empty"><div class="s1-prev-head"><span class="s1-prev-name">'+U.esc(v.name||"")+'</span></div><div class="s2-gantt-axis">'+skel+'</div><div class="s1-prev-empty-hint">請於上方輸入日期以產生進度預覽</div></div>'}const pill=v.light?'<span class="s1-pill-wrap"><span class="slack-pill slack-pill-'+v.light+'"><i class="ti '+PILL_IC[v.light]+'"></i>'+lightTxt(v.light,v.slack,v.overDays)+'</span><span class="s1-pill-tip"><span class="s1-pill-tip-hd s1-tiphd-'+v.light+'"><i class="ti '+PILL_IC[v.light]+'"></i>'+PILL_LB[v.light]+"</span>"+PILL_TIP[v.light]+"</span></span>":"";const _etPm=App._s1Ecn?tplEcn():null;const pmRow=App._s1Ecn&&_etPm&&_etPm.sizeMeta&&_etPm.sizeMeta[App._s1Ecn.size]?'<div class="s1-pm-bar"><div class="s2-gname">PM 協調</div><div class="s1-pm-track">常駐 · 全程鎖定 · Effort '+_etPm.sizeMeta[App._s1Ecn.size].pmEffort+'%</div><div class="s2-gdate">全程</div></div>':"";return'<div class="s1-prev-case case-card '+cls+'"><div class="s1-prev-head"><span class="s1-prev-name">'+U.esc(v.name||"")+'</span><span class="s1-prev-range">'+fmtD(v.start)+" → "+fmtD(v.end)+"</span>"+pill+"</div>"+miniGantt(v.stages)+pmRow+"</div>"};return real.map((v,i)=>blockOf(v,i===0)).join("")};App._s1DynHintHtml=function(startDate,endDate,meta){if(!startDate&&!endDate){return'<i class="ti ti-calendar-event"></i><span>請輸入或選擇日期：填入「開始日」或「上市日期」，系統將自動為您啟動智慧時程推算。</span>'}const fmt=x=>x?String(x).replace(/-/g,"/"):"";const eff=App._effScheduleDir(startDate,endDate,"forward");let ic="ti-arrow-narrow-right",txt;if(eff==="interval"){txt="雙向精算中：已幫您比對開工與上市日期，中間的彈性天數已呈現在下方的「餘裕燈號」中。"}else if(eff==="backward"&&meta&&meta.backFallback){ic="ti-alert-triangle";const d=fmt(meta.forwardFinish);txt="時空警報！上市日期太緊，最晚開工日已過。系統已自動改以「今天」開工順推"+(d?"，最快完工日為 "+d+"。":"。")}else if(eff==="backward"){const d=meta?fmt(meta.backStart):"";txt=d?"逆向倒推中：已為您推算出最晚必須在 "+d+" 前開工，才趕得上上市。":"逆向倒推中：系統正從您的上市日期往前推算最晚開工日。"}else{txt="正向排程中：系統正從您的開工日往後順推，自動算出最後的預計完工日。"}return'<i class="ti '+ic+'"></i><span>'+txt+"</span>"};App._s1RefreshPreview=function(){const real=App._s1ComputePreview();const c=document.getElementById("s1-prev-blocks");if(c)c.innerHTML=App._s1PreviewBlocksHtml(real);const cols=document.querySelectorAll("#page-stage1 .s1-case-col");cols.forEach((col,i)=>{const hintBox=col.querySelector(".s1-dynhint");if(!hintBox)return;const s=(col.querySelector(".s1-in-start")||{}).value||"";const e=(col.querySelector(".s1-in-end")||{}).value||"";const meta=real&&real[i]?real[i]:null;hintBox.innerHTML=App._s1DynHintHtml(s,e,meta);hintBox.classList.toggle("s1-dynhint-init",!s&&!e);hintBox.classList.toggle("s1-dynhint-alert",!!(meta&&meta.backFallback))})};App._s1SyncMainName=function(){const page=document.getElementById("page-stage1");if(!page)return;const proj=page.querySelector(".s1-proj-name");const main=page.querySelector('.s1-case-col[data-case="main"] .s1-case-name');if(!proj||!main)return;main.value=proj.value};App._s1CollectInput=function(){const page=document.getElementById("page-stage1");if(!page)return null;const projectName=(page.querySelector(".s1-proj-name")||{}).value||"";const color=PROJ_COLORS[0]||"";const cases=[];page.querySelectorAll(".s1-case-col").forEach(col=>{const key=col.dataset.caseKey;const cs=App._s1CaseByKey(key);const nameEl=col.querySelector(".s1-case-name");const rawName=nameEl?(nameEl.value||"").trim():App._s1Ecn?(projectName||"").trim():"";const variantName=rawName||"案-"+(key||"");const templateVariant=col.dataset.tplvariant||"主案";const startDate=(col.querySelector(".s1-in-start")||{}).value||"";const endDate=(col.querySelector(".s1-in-end")||{}).value||"";const direction=App._effScheduleDir(startDate,endDate,"forward");const selectedStages=cs?cs.stages.slice():[];cases.push({variantName,templateVariant,startDate,endDate,direction,selectedStages,renames:cs?cs.renames:{}})});const tucBase=parseFloat((page.querySelector("#s1-npi-tuc")||{}).value)||0;const uspBase=parseFloat((page.querySelector("#s1-npi-usp")||{}).value)||0;return{projectName,color,cases,targetUnitCostBase:tucBase,unitSellPriceBase:uspBase}};App._s1ColorStagesForward=function(res,v,stages,startDate,endDate){if(!startDate||!endDate)return;const fwd=res.tasks.filter(t=>t.variant===v.id).map(t=>Object.assign({},t));fwd.forEach(t=>{if(!t.predecessor)t.plannedStart=startDate});const fsch=computeSchedule(fwd);const fm=new Map;fsch.results.forEach(r=>fm.set(r.taskId,r));const fStage={};fwd.forEach(t=>{const r=fm.get(t.id);if(!r||!r.suggestedStart)return;const stage=(t.desc||"").split(" / ")[0]||"其他";if(!fStage[stage])fStage[stage]={start:r.suggestedStart,end:r.suggestedEnd};else{if(r.suggestedStart<fStage[stage].start)fStage[stage].start=r.suggestedStart;if(r.suggestedEnd>fStage[stage].end)fStage[stage].end=r.suggestedEnd}});stages.forEach(s=>{const f=fStage[s.stage];if(f){s.start=f.start;s.end=f.end}});App._chainStages(stages);stages.forEach(s=>{const margin=s.end<=endDate?D.workdaysBetween(s.end,endDate)-1:-(D.workdaysBetween(endDate,s.end)-1);s.light=margin>=5?"green":margin>=0?"yellow":"red"})};App._chainStages=function(stages){let prevEnd="";(stages||[]).forEach(s=>{if(!s||!s.start||!s.end)return;if(prevEnd&&s.start<prevEnd){const span=Math.max(D.workdaysBetween(s.start,s.end)-1,0);s.start=D.fmt(D.addWorkdays(prevEnd,1),"iso");s.end=D.fmt(D.addWorkdays(s.start,span),"iso")}prevEnd=s.end})};App._chainStagesBackward=function(stages,deadline){let nextStart="";for(let i=(stages||[]).length-1;i>=0;i--){const s=stages[i];if(!s||!s.start||!s.end)continue;const span=Math.max(D.workdaysBetween(s.start,s.end)-1,0);s.end=nextStart?D.fmt(D.addWorkdays(nextStart,-1),"iso"):deadline;s.start=D.fmt(D.addWorkdays(s.end,-span),"iso");nextStart=s.start}};App._s1ComputePreview=function(){const tpl=App._s1Tpl();if(!tpl)return null;const input=App._s1CollectInput();if(!input||!input.cases.length)return null;const res=App.applyTemplate(tpl,input);const byVariant=[];res.variants.forEach((v,i)=>{const vtasks=res.tasks.filter(t=>t.variant===v.id&&t.plannedStart&&t.plannedEnd);const stageMap={};vtasks.forEach(t=>{const stage=(t.desc||"").split(" / ")[0]||"其他";if(!stageMap[stage])stageMap[stage]={stage,start:t.plannedStart,end:t.plannedEnd};else{if(t.plannedStart<stageMap[stage].start)stageMap[stage].start=t.plannedStart;if(t.plannedEnd>stageMap[stage].end)stageMap[stage].end=t.plannedEnd}});const stages=(v.stages||[]).map(s=>stageMap[s]).filter(Boolean);const _rn=input.cases[i]&&input.cases[i].renames||{};stages.forEach(s=>{s.label=_rn[s.stage]!=null?_rn[s.stage]:s.stage});const skelStages=(input.cases[i]&&input.cases[i].selectedStages||[]).map(id=>_rn[id]!=null?_rn[id]:id);const vsch=v.schedule||{};const pseudoResults=vtasks.map(t=>({lateStart:t.plannedStart,lateFinish:t.plannedEnd}));const sl=App._computeSlack(pseudoResults,vsch.startDate,vsch.endDate);const eff=App._effScheduleDir(vsch.startDate,vsch.endDate,vsch.direction);let backStart="",backFallback=false,forwardFinish="";if(eff==="interval"){App._s1ColorStagesForward(res,v,stages,vsch.startDate,vsch.endDate)}else if(eff==="backward"&&vsch.endDate){App._chainStagesBackward(stages,vsch.endDate);const todayIso=D.fmt(D.today(),"iso");backStart=stages.length?stages[0].start||"":"";if(backStart&&backStart<todayIso){App._s1ColorStagesForward(res,v,stages,todayIso,vsch.endDate);backStart=stages.length?stages[0].start||backStart:backStart;backFallback=true;forwardFinish=stages.reduce((m,s)=>s.end>m?s.end:m,"")}else{stages.forEach(s=>{if(!s.end)return;const margin=s.end<=vsch.endDate?D.workdaysBetween(s.end,vsch.endDate)-1:-(D.workdaysBetween(vsch.endDate,s.end)-1);s.light=margin>=5?"green":margin>=0?"yellow":"red"})}}else{App._chainStages(stages)}const allS=stages.map(s=>s.start).filter(Boolean);const allE=stages.map(s=>s.end).filter(Boolean);const caseEnd=allE.length?allE.reduce((a,b)=>a>b?a:b):"";let light=sl?sl.light:"",slack=sl?sl.slack:null,overDays=sl?sl.overDays:null;if(caseEnd&&vsch.endDate&&(eff==="interval"||eff==="backward")){const m=caseEnd<=vsch.endDate?D.workdaysBetween(caseEnd,vsch.endDate)-1:-(D.workdaysBetween(vsch.endDate,caseEnd)-1);light=m>=5?"green":m>=0?"yellow":"red";slack=m;overDays=m<0?Math.abs(m):0}byVariant.push({id:v.id,name:v.name,start:allS.length?allS.reduce((a,b)=>a<b?a:b):"",end:caseEnd,stages,light,slack,overDays,dir:eff,backStart,backFallback,forwardFinish,skelStages})});return byVariant};App._s1StageChipsHtml=function(c){const arr=c&&c.stages||[];const rn=c&&c.renames||{};return arr.map((id,i)=>{const label=rn[id]!=null?rn[id]:id;return'<span class="s1-stage-chip" data-idx="'+i+'"><span class="chip-text">'+U.esc(label)+'</span><span class="chip-del" title="刪除">×</span></span>'}).join('<i class="ti ti-chevron-right s1-stage-arrow"></i>')+'<span class="s1-stage-add" title="新增階段"><i class="ti ti-plus"></i></span>'};App._renderStageChips=function(key){const col=document.querySelector('#page-stage1 .s1-case-col[data-case-key="'+key+'"]');const c=App._s1CaseByKey(key);if(col&&c){const box=col.querySelector(".s1-stagelist");if(box)box.innerHTML=App._s1StageChipsHtml(c)}};App._stageChipToInput=function(textEl){if(!textEl)return;const chip=textEl.closest(".s1-stage-chip");if(!chip)return;const col=chip.closest(".s1-case-col");if(!col)return;const key=col.dataset.caseKey;const c=App._s1CaseByKey(key);if(!c)return;const idx=+chip.getAttribute("data-idx");const id0=c.stages[idx];const cur=c.renames[id0]!=null?c.renames[id0]:id0!=null?id0:"";const inp=document.createElement("input");inp.type="text";inp.className="chip-edit";inp.value=cur;chip.classList.add("editing");textEl.style.display="none";const delEl=chip.querySelector(".chip-del");if(delEl)delEl.style.display="none";chip.insertBefore(inp,textEl);inp.focus();inp.select();let done=false;const commit=()=>{if(done)return;done=true;const v=inp.value.trim();const id=c.stages[idx];if(v===""){c.stages.splice(idx,1);if(id)delete c.renames[id]}else if(!id){c.stages[idx]=v}else if(v!==id){c.renames[id]=v}else{delete c.renames[id]}App._renderStageChips(key);App._s1RefreshPreview()};inp.addEventListener("blur",commit);inp.addEventListener("keydown",ev=>{if(ev.key==="Enter"){ev.preventDefault();inp.blur()}else if(ev.key==="Escape"){inp.value=cur;inp.blur()}})};App._bindStageChipEvents=function(){if(App._stageChipDelegated)return;App._stageChipDelegated=true;document.addEventListener("click",e=>{const page=document.getElementById("page-stage1");if(!page||!page.classList.contains("active"))return;const del=e.target.closest(".chip-del");if(del){const chip=del.closest(".s1-stage-chip");const col=chip&&chip.closest(".s1-case-col");const c=col&&App._s1CaseByKey(col.dataset.caseKey);if(chip&&c){const di=+chip.getAttribute("data-idx");const id0=c.stages[di];c.stages.splice(di,1);if(id0)delete c.renames[id0];App._renderStageChips(col.dataset.caseKey);App._s1RefreshPreview()}return}const add=e.target.closest(".s1-stage-add");if(add){const col=add.closest(".s1-case-col");const c=col&&App._s1CaseByKey(col.dataset.caseKey);if(c){c.stages.push("");App._renderStageChips(col.dataset.caseKey);const chips=col.querySelectorAll(".s1-stage-chip");const last=chips[chips.length-1];if(last)App._stageChipToInput(last.querySelector(".chip-text"))}return}const txt=e.target.closest(".chip-text");if(txt){App._stageChipToInput(txt);return}})};App._bindStageChipEvents();App._flowStage3Back=function(){if(App._createFlow&&App._createFlow.mode==="excel"){this.showPage("workspace");App._flowStep2();const parsed=App._createFlow.excelParsed;const st=document.getElementById("pf-excelStatus");if(st&&parsed&&parsed.ok){st.textContent="✓ 已讀取「"+(parsed.projectName||"未命名")+"」共 "+parsed.rows.length+" 筆任務，按下一步檢視"}return}const snap=App._createFlow?App._createFlow.stage1Data:null;this.showPage("workspace");if(!snap){return App._flowStep1()}App._tplDepts=JSON.parse(JSON.stringify(snap.depts||[]));App._flowStep2();const nameEl=document.getElementById("pf-name");if(nameEl)nameEl.value=snap.name||"";const noteEl=document.getElementById("pf-note");if(noteEl)noteEl.value=snap.note||"";const cs=snap.cases||[];const mainCard=document.querySelector("#pf-tplBox .case-card.case-main");if(cs[0]&&mainCard){const m=cs[0];const mn=document.getElementById("pf-mainName");if(mn){mn.value=m.variantName||"";mn.dataset.touched="1"}const ms=document.getElementById("pf-start");if(ms)ms.value=m.startDate||"";const me=document.getElementById("pf-end");if(me)me.value=m.endDate||"";const md=document.getElementById("pf-direction");if(md)md.value=m.direction||"forward";App._applyStagePicks(mainCard,m.selectedStages)}for(let i=1;i<cs.length;i++){const c=cs[i];App._tplAddOtherCase();const cards=document.querySelectorAll("#pf-otherCases .case-card.case-other");const card=cards[cards.length-1];if(!card)continue;const vn=card.querySelector(".case-variant-name");if(vn)vn.value=c.variantName||"";const st=card.querySelector(".case-start");if(st)st.value=c.startDate||"";const en=card.querySelector(".case-end");if(en)en.value=c.endDate||"";const dr=card.querySelector(".case-direction");if(dr)dr.value=c.direction||"forward";App._applyStagePicks(card,c.selectedStages)}};App._stage2Commit=function(){if(App._roGuard())return;const ecnGate=App._createFlow&&App._createFlow.ecn;if(ecnGate&&!App._createFlow._ecnWarned){App.confirmModal({icon:"ti-settings",iconBg:"--amber-l",iconColor:"--amber-ink",title:"此案將建立為工程設變案（ECN）",msg:"建立後將使用設變專屬儀表板，<b>無法轉為一般開發案</b>。（S/M/L 分級之後仍可調整）確定建立？",okText:"確定建立",cancelText:"返回修改",onConfirm:function(){App._createFlow._ecnWarned=true;App._stage2Commit()}});return}const res=this._tplPreview;if(!res){U.toast("⚠ 無範本預覽資料，請重新套用範本","warning");return}res.project.depts=res.depts;res.project.variants=res.variants;const ecn=App._createFlow&&App._createFlow.ecn;if(ecn&&!res.project.ecnType){Object.assign(res.project,{ecnType:true,size:ecn.size,changeReason:ecn.changeReason,roiType:ecn.roiType,sourceNo:ecn.sourceNo,ecnNo:ecn.ecnNo,targetSavePerUnit:ecn.targetSavePerUnit,status:"active",loopCount:0,scopeGrowthCount:0,reopenCount:0});const ds=res.tasks.map(t=>t.plannedStart).filter(Boolean).sort();const de=res.tasks.map(t=>t.plannedEnd).filter(Boolean).sort();if(ds.length&&de.length){const s0=ds[0],e0=de[de.length-1];const span=Math.max(D.workdaysBetween(s0,e0),1);const dailyHours=DATA.settings&&DATA.settings.dailyHours||6;const pmDept=(res.depts||[]).find(d=>d.name==="PM");res.tasks.push({id:U.id(),project:res.project.id,wbs:99,parentWbsId:"",name:"PM 設變協調／文件彙整（常駐）",desc:"全程 / PM 協調",category:"deep",taskType:"任務",predecessor:"",durationDays:span,owner:"",dept:pmDept?pmDept.id:"",role:"PM",variant:(res.variants[0]||{}).id||null,start:"",end:"",plannedStart:s0,plannedEnd:e0,actualStart:"",actualEnd:"",progress:0,status:"pending",urgency:"med",estHours:Math.round(span*dailyHours*(ecn.pmEffort/100)*10)/10,method:"",canSplit:false,completedAt:null,createdAt:new Date().toISOString(),scheduledStart:"",scheduledEnd:"",synced:false,stage:"",subgroup:"",mustDeliver:false,deliverableType:"",requiredTask:true,mustIssue:false,deliverable:"",riskIssue:"",delivered:"",deliverableLink:"",note:"",effortRatio:ecn.pmEffort,taskAttr:"baseline",isPmCoord:true})}App._ecnForwardVariants(res.variants,res.tasks);App._reschedulePreview(res.tasks,res.variants,[]);const _dh=DATA.settings&&DATA.settings.dailyHours||6;res.project.baselineHours=Math.round(res.tasks.reduce((s,t)=>s+(t.effortRatio!=null?t.effortRatio:100)/100*_dh*(t.durationDays||0),0)*10)/10;res.project.ecnEvents=[{type:"open",date:D.fmt(new Date,"iso"),label:"開案（"+ecn.size+" 級）",cause:""}]}else if(res.project.stageCoordDefaults){App._materializeNpiStageCoord(res)}const dup=DATA.projects.filter(p=>p.name===res.project.name&&!!p.ecnType===!!res.project.ecnType);const unassigned=res.tasks.filter(t=>!t.owner).length;const _commit=()=>{const tb=document.querySelector(".main > .topbar");if(tb)tb.classList.remove("topbar-hidden");res.project.version=dup.length?Math.max(...dup.map(p=>p.version||1))+1:1;res.project.importedAt=D.fmt(new Date,"iso");DATA.projects.push(res.project);res.tasks.forEach(t=>DATA.tasks.push(t));App.currentProjectId=res.project.id;Store.projects.save();Store.tasks.save();App._tplPreview=null;App._createFlow=null;App.refreshAll();if(res.warnings.length){console.warn("套範本提醒:",res.warnings);App._showTplWarnings(res.warnings);U.toast("✓ 已建立 "+res.tasks.length+" 筆（"+res.warnings.length+" 項提醒見上方）","warning",{soft:true})}else{U.toast("✓ 已建立 "+res.tasks.length+" 筆任務","success")}App.showPage("project",null)};const _checkDup=()=>{if(dup.length){App.confirmModal({icon:"ti-copy",iconBg:"--amber-l",iconColor:"--amber-ink",title:`已有 ${dup.length} 個同名專案「${res.project.name}」`,msg:"要建立新版本嗎？兩者並存，可在側邊欄辨識版號。",okText:"建立新版本",cancelText:"返回修改",onConfirm:_commit})}else{_commit()}};if(unassigned>0){App.confirmModal({icon:"ti-alert-triangle",iconBg:"--amber-l",iconColor:"--amber-ink",title:`還有 ${unassigned} 個任務未指派負責人`,msg:"確定建立？",okText:"確定建立",cancelText:"取消",onConfirm:_checkDup})}else{_checkDup()}};App._s2GroupByStage=function(variantId){const res=this._tplPreview;const order=[],byStage={};(res&&res.tasks||[]).filter(t=>t.variant===variantId).forEach(t=>{const st=t.stage||"（未分階段）";if(!byStage[st]){byStage[st]=[];order.push(st)}byStage[st].push(t)});return{order,byStage}};App._s2OwnerOptions=function(t){const res=this._tplPreview;if(!res)return"";const depts=res.depts||[];const cur=t.owner||"";const ownDeptId=t.dept||"";let html='<option value=""'+(cur===""?" selected":"")+">未指派</option>";const ordered=depts.slice().sort((a,b)=>a.id===ownDeptId?-1:b.id===ownDeptId?1:0);ordered.forEach(d=>{const members=d.members||[];if(!members.length)return;html+='<optgroup label="'+U.esc((d.id===ownDeptId?"本部門·":"")+d.name)+'">';members.forEach(m=>{html+='<option value="'+U.esc(m.name)+'"'+(m.name===cur?" selected":"")+">"+U.esc(m.name)+"</option>"});html+="</optgroup>"});return html};App._s2PredHlOn=function(td){(td.dataset.preds||"").split(",").filter(Boolean).forEach(id=>{const r=document.querySelector('[data-taskid="'+id+'"]');if(r)r.classList.add("s2-pred-hl")})};App._s2PredHlOff=function(){document.querySelectorAll(".s2-pred-hl").forEach(e=>e.classList.remove("s2-pred-hl"))};App._s2PredText=function(t){const res=this._tplPreview;if(!res)return"無";const parts=String(t.predecessor||"").split(/[,，;；]/).map(x=>x.trim()).filter(Boolean);if(!parts.length)return"無";if(parts.length===1){const pid=parts[0].split("#")[0];const dep=(res.tasks||[]).find(x=>x.id===pid);return dep?"接在《"+dep.name+"》後":"接在 1 項後"}return"接在 "+parts.length+" 項後"};App._s2ListHtml=function(variantId){const res=this._tplPreview;if(!res)return"";const _idToName={};(res.depts||[]).forEach(d=>{_idToName[d.id]=d.name});const g=this._s2GroupByStage(variantId);if(!g.order.length)return'<div class="s2-ph">此案別無任務</div>';const sel=this._s2Stage&&this._s2Stage[variantId]||g.order[0];const selIdx=g.order.indexOf(sel);const group=g.byStage[sel]||[];const fmtD=x=>x?String(x).replace(/-/g,"/"):"";const mmdd=x=>{const p=String(x||"").split("-");return p.length>=3?p[1]+"/"+p[2]:""};let seqBase=0;for(let i=0;i<selIdx;i++)seqBase+=(g.byStage[g.order[i]]||[]).length;const allDeliver=group.length>0&&group.every(t=>t.mustDeliver);let rows="";const gains=App._effectiveGains(variantId);group.forEach((t,gi)=>{const seq=seqBase+gi+1;const isCrit=(gains.get(t.id)||0)>0;rows+='<tr data-taskid="'+t.id+'" class="'+(seq%2===0?"s2-rz ":"")+(isCrit?"s2-crit":"")+'"><td class="col-num">'+seq+'</td><td class="col-flex s2-namecell" title="'+U.esc(t.name)+'"><div class="s2-nameflex"><input class="s2-name-inp" value="'+U.esc(t.name)+`" onchange="App._s2SetName('`+t.id+`', this.value)">`+(isCrit?'<span class="s2-crit-tag">關鍵路徑</span>':"")+'</div></td><td class="col-mid s2-deptcell" title="此任務所屬部門（在「新增/編輯部門」設定負責人）">'+(U.esc(_idToName[t.dept]||t.role)||'<span class="s2-dept-none">—</span>')+'</td><td class="col-mid s2-ownercell"><select class="s2-owner-sel'+(t.owner?"":" s2-owner-unassigned")+`" onchange="App._s2SetOwner('`+t.id+`', this.value)">`+this._s2OwnerOptions(t)+"</select></td>"+this._s2PredCells(t,variantId)+'<td class="col-mid s2-dur"><input class="s2-dur-inp" type="number" min="0" value="'+(t.durationDays!=null?t.durationDays:"")+`" onchange="App._s2SetDuration('`+t.id+`', this.value)"></td><td class="col-mid s2-date" title="`+(t.plannedStart?fmtD(t.plannedStart)+" → "+fmtD(t.plannedEnd):"")+'">'+(t.plannedStart?mmdd(t.plannedStart)+" → "+mmdd(t.plannedEnd):"（待排）")+`</td><td class="col-mid s2-pct-cell"><select class="s2-pct-sel" onchange="App._s2SetEffort('`+t.id+`', this.value)">`+App._ecnEffortOptions(t.effortRatio)+'</select></td><td class="col-mid s2-deliver"><input type="checkbox"'+(t.mustDeliver?" checked":"")+` onchange="App._s2SetDeliver('`+t.id+`', this.checked)"></td><td class="col-action s2-del-cell"><button class="s2-del" title="刪除此列" onclick="App._s2DelRow('`+t.id+`')">✕</button></td></tr><tr class="dt-insert-row"><td colspan="12" class="dt-insert-cell"><div class="dt-insert"><button class="dt-insert-btn" title="在此列後插入" onclick="App._s2InsertRow('`+t.id+"', '"+variantId+`')">＋</button></div></td></tr>`});const _pmDeptS2=(res.depts||[]).find(d=>d.name==="PM");const _hasExplPmS2=(res.tasks||[]).some(t=>t.variant===variantId&&t.stage===sel&&!t.isPmCoord&&(t.role==="PM"||_pmDeptS2&&t.dept===_pmDeptS2.id));if(_hasExplPmS2&&res.project&&res.project.stageCoordDefaults&&res.project.stageCoordDefaults[sel]!=null){rows+='<tr class="s2-coord-row"><td class="col-num"></td><td colspan="11" class="s2-coord-label" style="opacity:.62"><span class="s2-coord-note">此階段已有顯性 PM 任務——不另列常駐協調（同階段不雙算）</span></td></tr>'}else if(res.project&&res.project.stageCoordDefaults&&res.project.stageCoordDefaults[sel]!=null){const vv=(res.variants||[]).find(x=>x.id===variantId);const cur=vv&&vv.stageCoord&&vv.stageCoord[sel]!=null?vv.stageCoord[sel]:res.project.stageCoordDefaults[sel];rows+='<tr class="s2-coord-row"><td class="col-num"></td><td colspan="8" class="s2-coord-label"><b>PM 階段協調（常駐）</b><span class="s2-coord-note">「'+U.esc(sel)+`」全程背景協調</span></td><td class="col-mid s2-pct-cell"><select class="s2-pct-sel" onchange="App._s2SetStageCoord('`+variantId+"', '"+U.esc(sel)+`', this.value)">`+App._ecnEffortOptions(cur)+'</select></td><td class="col-mid"></td><td class="col-action"></td></tr>'}return'<table class="data-table s2-tbl"><thead><tr><th class="col-num" rowspan="2">序</th><th class="col-flex" rowspan="2">任務名</th><th class="col-mid" rowspan="2">部門</th><th class="col-mid" rowspan="2">負責人</th><th class="col-pred-group" colspan="3">前置任務</th><th class="col-mid s2-dur-th" rowspan="2">工期</th><th class="col-mid" rowspan="2">日期（起訖）</th><th class="col-mid s2-pct-th" rowspan="2">投入%</th><th class="col-mid s2-deliver-th" rowspan="2">需交付<label class="s2-all-th"><input type="checkbox"'+(allDeliver?" checked":"")+` onchange="App._s2DeliverAll('`+variantId+"', "+selIdx+', this.checked)"> 全選</label></th><th class="col-action" rowspan="2"></th></tr><tr><th class="col-pred-sub">序號</th><th class="col-pred-sub">銜接方式</th><th class="col-pred-sub">緩衝</th></tr></thead><tbody>'+rows+"</tbody></table>"};App._s2SetDuration=function(taskId,value){const res=this._tplPreview;if(!res)return;const t=res.tasks.find(t2=>t2.id===taskId);if(!t)return;t.durationDays=Math.max(0,parseInt(value)||0);App._reschedulePreview(res.tasks,res.variants,[]);res.variants.forEach(v=>this._s2RefreshCase(v.id))};App._s2SetEffort=function(taskId,val){if(val==="")return;const res=this._tplPreview;if(!res)return;const t=res.tasks.find(t2=>t2.id===taskId);if(!t)return;t.effortRatio=parseInt(val,10)||0;this._s2RefreshCase(t.variant)};App._s2SetStageCoord=function(variantId,stage,val){const res=this._tplPreview;if(!res)return;const v=(res.variants||[]).find(x=>x.id===variantId);if(!v)return;v.stageCoord=v.stageCoord||{};v.stageCoord[stage]=parseInt(val,10)||0;this._s2RefreshCase(variantId)};App._attachStageCoord=function(res,tpl,isEcn){if(isEcn||!res||!res.project||!tpl||!tpl.stageDefaults)return;const m={};tpl.stageDefaults.forEach(s=>{if(s.pmCoordEffort!=null)m[s.stage]=s.pmCoordEffort});if(Object.keys(m).length)res.project.stageCoordDefaults=m};App._materializeNpiStageCoord=function(res){const defs=res.project&&res.project.stageCoordDefaults||{};const dailyHours=DATA.settings&&DATA.settings.dailyHours||6;const pmDept=(res.depts||[]).find(d=>d.name==="PM");(res.variants||[]).forEach(v=>{const byStage={};res.tasks.filter(t=>t.variant===v.id&&t.stage&&!t.isPmCoord).forEach(t=>{(byStage[t.stage]=byStage[t.stage]||[]).push(t)});Object.keys(byStage).forEach(st=>{if(byStage[st].some(t=>t.role==="PM"||pmDept&&t.dept===pmDept.id))return;const eff=v.stageCoord&&v.stageCoord[st]!=null?v.stageCoord[st]:defs[st];if(eff==null)return;const ss=byStage[st].map(t=>t.plannedStart).filter(Boolean).sort();const ee=byStage[st].map(t=>t.plannedEnd).filter(Boolean).sort();if(!ss.length||!ee.length)return;const s0=ss[0],e0=ee[ee.length-1];const span=Math.max(D.workdaysBetween(s0,e0),1);res.tasks.push({id:U.id(),project:res.project.id,wbs:0,parentWbsId:"",name:"PM 階段協調（常駐）",desc:st+" / PM 協調",category:"deep",taskType:"任務",predecessor:"",durationDays:span,owner:"",dept:pmDept?pmDept.id:"",role:"PM",variant:v.id,start:"",end:"",plannedStart:s0,plannedEnd:e0,actualStart:"",actualEnd:"",progress:0,status:"pending",urgency:"med",estHours:Math.round(span*dailyHours*(eff/100)*10)/10,method:"",canSplit:false,completedAt:null,createdAt:new Date().toISOString(),scheduledStart:"",scheduledEnd:"",synced:false,stage:st,subgroup:"",mustDeliver:false,deliverableType:"",requiredTask:true,mustIssue:false,deliverable:"",riskIssue:"",delivered:"",deliverableLink:"",note:"",effortRatio:eff,taskAttr:"baseline",isPmCoord:true})})})};App._s2SetOwner=function(taskId,value){const res=this._tplPreview;if(!res)return;const t=(res.tasks||[]).find(x=>x.id===taskId);if(t)t.owner=value};App._s2SetName=function(taskId,value){const res=this._tplPreview;if(!res)return;const t=(res.tasks||[]).find(x=>x.id===taskId);if(!t)return;t.name=value;this._s2RefreshCase(t.variant)};App._s2DelRow=function(taskId){const res=this._tplPreview;if(!res)return;const t=(res.tasks||[]).find(x=>x.id===taskId);if(!t)return;App.confirmModal({title:"刪除此列？",msg:"「"+U.esc(t.name||"")+"」將自清單移除（後續引用它當前置的任務會落待排）。",okText:"刪除",cancelText:"取消",okClass:"danger",onConfirm:()=>{const variantId=t.variant;res.tasks=res.tasks.filter(x=>x.id!==taskId);App._s2RefreshCase(variantId);U.toast("已刪除「"+(t.name||"")+"」","success",{soft:true})}})};App._s2InsertRow=function(taskId,variantId){const res=this._tplPreview;if(!res)return;const idx=res.tasks.findIndex(x=>x.id===taskId);if(idx<0)return;const ref=res.tasks[idx];const dailyHours=DATA.settings&&DATA.settings.dailyHours||6;const newTask={id:U.id(),project:res.project.id,wbs:"",parentWbsId:"",name:"新任務",desc:ref.stage||"",category:"deep",taskType:"任務",predecessor:"",durationDays:1,owner:"",dept:"",role:"",variant:variantId,start:"",end:"",plannedStart:"",plannedEnd:"",actualStart:"",actualEnd:"",progress:0,status:"pending",urgency:"med",estHours:dailyHours,method:"",canSplit:false,completedAt:null,createdAt:new Date().toISOString(),scheduledStart:"",scheduledEnd:"",synced:false,stage:ref.stage||"",subgroup:"",mustDeliver:false,deliverableType:"",requiredTask:true,mustIssue:false,deliverable:"",riskIssue:"",delivered:"",deliverableLink:"",note:""};res.tasks.splice(idx+1,0,newTask);App._reschedulePreview(res.tasks,res.variants,[]);res.variants.forEach(v=>this._s2RefreshCase(v.id))};App._s2SetDeliver=function(taskId,checked){const res=this._tplPreview;if(!res)return;const t=(res.tasks||[]).find(x=>x.id===taskId);if(t)t.mustDeliver=!!checked};App._s2DeliverAll=function(variantId,si,checked){const g=this._s2GroupByStage(variantId);const st=g.order[si];if(st==null)return;g.byStage[st].forEach(t=>{t.mustDeliver=!!checked});this._s2RefreshCase(variantId)};App._s2StageRanges=function(variantId){const g=this._s2GroupByStage(variantId);const ranges=g.order.map(st=>{const ts=g.byStage[st];const starts=ts.map(t=>t.plannedStart).filter(Boolean).sort();const ends=ts.map(t=>t.plannedEnd).filter(Boolean).sort();return{stage:st,start:starts[0]||"",end:ends[ends.length-1]||""}});return{order:g.order,ranges}};App._s2StageStatuses=function(variantId){const res=this._tplPreview;const data=this._s2StageRanges(variantId);const order=data.order,ranges=data.ranges;const v=res?(res.variants||[]).find(x=>x.id===variantId):null;let ed="";if(res&&v){const vsch=v.schedule||{};ed=vsch.endDate||"";const eff=App._effScheduleDir(vsch.startDate,vsch.endDate,vsch.direction);if(eff==="interval"){App._s1ColorStagesForward(res,v,ranges,vsch.startDate,vsch.endDate)}else if(eff==="backward"&&ed){App._chainStagesBackward(ranges,ed);const todayIso=D.fmt(D.today(),"iso");const backStart=ranges.length?ranges[0].start||"":"";if(backStart&&backStart<todayIso){App._s1ColorStagesForward(res,v,ranges,todayIso,ed)}else{ranges.forEach(s=>{if(!s.end)return;const margin=s.end<=ed?D.workdaysBetween(s.end,ed)-1:-(D.workdaysBetween(ed,s.end)-1);s.light=margin>=5?"green":margin>=0?"yellow":"red"})}}else{App._chainStages(ranges)}}ranges.forEach(r=>{r.lack=ed&&r.end&&r.end>ed?Math.max(0,D.workdaysBetween(ed,r.end)-1):0});return{order,ranges,endDate:ed}};App._s2GotoStage=function(variantId,si){App._s2SelectStage(variantId,si);const list=document.querySelector('.s2-list[data-variant="'+variantId+'"]');if(list)list.scrollIntoView({behavior:"smooth",block:"start"})};App._s2GanttHtml=function(variantId){const data=App._s2StageStatuses(variantId);const order=data.order,ranges=data.ranges,ed=data.endDate;if(!order.length)return"";const fmtD=x=>x?String(x).replace(/-/g,"/"):"";const toNum=d=>d?Date.parse(d):NaN;const sel=this._s2Stage&&this._s2Stage[variantId]||order[0];const allStarts=ranges.map(r=>toNum(r.start)).filter(n=>!isNaN(n));const allEnds=ranges.map(r=>toNum(r.end)).filter(n=>!isNaN(n));const todayN=Date.parse(D.fmt(D.today(),"iso"));let minN=allStarts.length?Math.min.apply(null,allStarts):isNaN(todayN)?0:todayN;let maxN=allEnds.length?Math.max.apply(null,allEnds):isNaN(todayN)?0:todayN;if(!isNaN(todayN)){minN=Math.min(minN,todayN);maxN=Math.max(maxN,todayN)}const span=maxN-minN||1;const _todayLeft=(todayN-minN)/span*100;const _todayOn=!isNaN(todayN)&&maxN>minN;const todayMark=_todayOn?'<div class="s2-gtoday" style="left:'+_todayLeft+'%"></div>':"";const _flagStyle=_todayLeft<10?"left:0;transform:none":_todayLeft>90?"left:auto;right:0;transform:none":"";const todayMarkLead=_todayOn?'<div class="s2-gtoday" style="left:'+_todayLeft+'%"><span class="s2-gtoday-flag"'+(_flagStyle?' style="'+_flagStyle+'"':"")+">今天</span></div>":"";let rows="";ranges.forEach((r,si)=>{const isSel=r.stage===sel;const a=toNum(r.start),b=toNum(r.end);const light=r.light||"green";const tMark=si===0?todayMarkLead:todayMark;let bar;if(isNaN(a)||isNaN(b)){bar='<div class="s2-gbar-track">'+tMark+'<div class="s2-gbar s2-gbar-none"></div></div>'}else{const left=(a-minN)/span*100;const width=Math.max((b-a)/span*100,1.5);bar='<div class="s2-gbar-track">'+tMark+'<div class="s2-gbar s2-gbar-'+light+'" style="left:'+left+"%;width:"+width+'%"></div></div>'}const shortD=x=>{if(!x)return"";const p=String(x).split("-");return(p[1]||"")+"/"+(p[2]||"")};const sameYr=r.start&&r.end&&r.start.slice(0,4)===r.end.slice(0,4);const oneD=x=>x?sameYr?shortD(x):String(x).slice(2,4)+"/"+shortD(x):"";const dateLbl=r.start||r.end?oneD(r.start)+" → "+oneD(r.end):"待排";const dateFull=r.start||r.end?fmtD(r.start)+" → "+fmtD(r.end):"待排";const dot=isNaN(a)||isNaN(b)?'<span class="s2-gdot"></span>':'<span class="s2-gdot s2-gdot-'+light+'"></span>';const stat=!ed?"":r.lack>0?`<span class="s2-gstat bad" onclick="event.stopPropagation();App._s2GotoStage('`+variantId+"', "+si+')"><i class="ti ti-alert-triangle"></i> 尚缺 '+r.lack+" 天</span>":'<span class="s2-gstat ok"><i class="ti ti-circle-check"></i> 正常</span>';rows+='<div class="s2-grow'+(isSel?" on":"")+`" onclick="App._s2SelectStage('`+variantId+"', "+si+')">'+dot+'<div class="s2-gname">'+U.esc(r.stage)+"</div>"+bar+'<div class="s2-gdate" title="'+dateFull+'">'+dateLbl+"</div>"+stat+"</div>"});return'<div class="s2-gantt-axis">'+rows+"</div>"};App._s2BannerHtml=function(variantId){const data=App._s2StageStatuses(variantId);if(!data.order.length)return"";const sel=this._s2Stage&&this._s2Stage[variantId]||data.order[0];const r=data.ranges.find(x=>x.stage===sel)||{};const fmtD=x=>x?String(x).replace(/-/g,"/"):"";const dl=r.start||r.end?fmtD(r.start)+" → "+fmtD(r.end):"（待排）";const status=data.endDate&&r.lack>0?'<span class="s2n-bn-bad">（⚠ 排程尚缺 '+r.lack+" 個工作天，請縮減下方關鍵路徑工期）</span>":data.endDate?'<span class="s2n-bn-ok">（時程充足）</span>':"";return'<div class="s2n-banner"><i class="ti ti-tool s2n-bn-ico"></i><span class="s2n-bn-name">當前階段：'+U.esc(sel)+status+'</span><span class="s2n-bn-dl"><i class="ti ti-calendar"></i> 階段 Deadline：'+dl+"</span></div>"};App._s2ParsePred=function(t){const parts=String(t.predecessor||"").split(/[,，;；]/).map(x=>x.trim()).filter(Boolean);if(parts.length>=2)return{multi:true};if(!parts.length)return{id:"",type:"FS",lag:0};const p=parts[0];const h=p.indexOf("#");const id=h>=0?p.slice(0,h).trim():p.trim();const tail=h>=0?p.slice(h+1).trim():"";const m=tail.match(/^([A-Za-z]{2})?\s*([+-]\s*\d+)?$/)||[];let type=(m[1]||"FS").toUpperCase();if(["FS","SS","FF","SF"].indexOf(type)<0)type="FS";let lag=m[2]?parseInt(m[2].replace(/\s+/g,""),10):0;if(isNaN(lag))lag=0;return{id,type,lag}};App._s2PredSeqInput=function(t,variantId,curId){const g=this._s2GroupByStage(variantId);const stageIdxOf={};g.order.forEach((st,si)=>(g.byStage[st]||[]).forEach(x=>{stageIdxOf[x.id]=si}));const flat=g.order.reduce((a,st)=>a.concat(g.byStage[st]||[]),[]);const myIdx=flat.findIndex(x=>x.id===t.id);const mySi=stageIdxOf[t.id];let curSeq="";if(curId){const ci=flat.findIndex(x=>x.id===curId);if(ci>=0)curSeq=String(ci+1)}let opts="";for(let i=0;i<myIdx;i++){const x=flat[i],xsi=stageIdxOf[x.id];if(xsi<=mySi&&xsi>=mySi-3)opts+='<option value="'+(i+1)+'">'+U.esc(i+1+"·"+x.name)+"</option>"}const did="s2pl-"+t.id;return'<input class="s2-pc-seq" type="text" list="'+did+'" value="'+curSeq+`" placeholder="序號" title="輸入或下拉前置序號（可手動輸入更早項目；建議僅當前或過去 3 階段）" onchange="App._s2SetPredCombo('`+t.id+`')"><datalist id="`+did+'">'+opts+"</datalist>"};App._s2PredTypeOptions=function(type){const opts=[["FS","完成後才開始"],["SS","同一天開始"],["FF","同一天完成"],["SF","開始才完成"]];return opts.map(o=>'<option value="'+o[0]+'"'+(o[0]===type?" selected":"")+">"+o[1]+"</option>").join("")};App._s2SetPredCombo=function(taskId){const res=this._tplPreview;if(!res)return;const t=res.tasks.find(x=>x.id===taskId);if(!t)return;const row=document.querySelector('[data-taskid="'+taskId+'"]');if(!row)return;const seqRaw=((row.querySelector(".s2-pc-seq")||{}).value||"").trim();const type=(row.querySelector(".s2-pc-type")||{}).value||"FS";let lag=parseInt((row.querySelector(".s2-pc-lag")||{}).value,10);if(isNaN(lag))lag=0;let predId="";if(seqRaw){const g=this._s2GroupByStage(t.variant);const flat=g.order.reduce((a,st)=>a.concat(g.byStage[st]||[]),[]);const myIdx=flat.findIndex(x=>x.id===t.id);const n=parseInt(String(seqRaw).split("·")[0],10);if(!isNaN(n)&&n>=1&&n-1<myIdx)predId=flat[n-1].id}if(!predId){t.predecessor=""}else{const lagStr=lag>0?"+"+lag:lag<0?String(lag):"";t.predecessor=predId+"#"+type+lagStr}App._reschedulePreview(res.tasks,res.variants,[]);res.variants.forEach(v=>this._s2RefreshCase(v.id))};App._s2PredCells=function(t,variantId){const preds=String(t.predecessor||"").split(/[,，;；]/).map(x=>x.split("#")[0].trim()).filter(Boolean).join(",");const pp=this._s2ParsePred(t);if(pp.multi){return'<td class="col-pred s2-pred s2-pred-multi" colspan="3" data-preds="'+preds+'" onmouseenter="App._s2PredHlOn(this)" onmouseleave="App._s2PredHlOff()">'+U.esc(this._s2PredText(t))+"</td>"}return'<td class="col-pred s2-pred s2-pc-seqcell" data-preds="'+preds+'" onmouseenter="App._s2PredHlOn(this)" onmouseleave="App._s2PredHlOff()">'+this._s2PredSeqInput(t,variantId,pp.id)+`</td><td class="col-pred s2-pc-typecell"><select class="s2-pc-type" title="銜接方式" onchange="App._s2SetPredCombo('`+t.id+`')">`+this._s2PredTypeOptions(pp.type)+'</select></td><td class="col-pred s2-pc-lagcell"><input class="s2-pc-lag" type="number" value="'+pp.lag+`" title="緩衝天數：+2 多等兩天、-1 提前一天" onchange="App._s2SetPredCombo('`+t.id+`')"></td>`};App._s2VariantSlack=function(variantId){const res=this._tplPreview;if(!res)return null;const v=(res.variants||[]).find(x=>x.id===variantId);if(!v)return null;const dir=App._effScheduleDir(v.schedule.startDate,v.schedule.endDate,v.schedule.direction);if(dir==="interval"&&v.schedule.startDate&&v.schedule.endDate){const g=App._s2GroupByStage(variantId);const stages=g.order.map(st=>({stage:st}));App._s1ColorStagesForward(res,v,stages,v.schedule.startDate,v.schedule.endDate);let fin="";stages.forEach(s=>{if(s.end&&s.end>fin)fin=s.end});if(!fin)return null;const start=v.schedule.startDate,end=v.schedule.endDate;const available=D.workdaysBetween(start,end);const needed=D.workdaysBetween(start,fin);const slack=available-needed;return{available,needed,slack,light:slack>=5?"green":slack>=0?"yellow":"red",earliestFinish:fin,overDays:slack<0?Math.max(0,D.workdaysBetween(end,fin)-1):0}}if(dir==="backward"&&v.schedule.endDate){const g=App._s2GroupByStage(variantId);const stages=g.order.map(st=>({stage:st}));App._s1ColorStagesForward(res,v,stages,D.fmt(D.today(),"iso"),v.schedule.endDate);let fin="";stages.forEach(s=>{if(s.end&&s.end>fin)fin=s.end});if(!fin)return null;const end=v.schedule.endDate;const m=fin<=end?D.workdaysBetween(fin,end)-1:-(D.workdaysBetween(end,fin)-1);return{available:null,needed:null,slack:m,light:m>=5?"green":m>=0?"yellow":"red",earliestFinish:fin,overDays:m<0?Math.abs(m):0}}return null};App._s2SlackHtml=function(variantId){const res=this._tplPreview;if(!res)return"";const v=(res.variants||[]).find(x=>x.id===variantId);if(!v)return"";const s=App._s2VariantSlack(variantId);if(!s)return"";const fmtD=x=>x?String(x).replace(/-/g,"/"):"";const wkd=iso=>iso?"（週"+["日","一","二","三","四","五","六"][new Date(iso).getDay()]+"）":"";const sd=v.schedule.startDate,ed=v.schedule.endDate;const msg=s.light==="green"?"時間充足，還有 "+s.slack+" 個工作天緩衝":s.light==="yellow"?"時間偏緊，只剩 "+s.slack+" 個工作天緩衝，任何延誤都可能時程延誤、緩衝期較少":"時間不足，照專案範本的工期排，比需求的最快完成日晚 "+s.overDays+" 個工作天";const diffTxt=s.light==="green"?"多出 "+s.slack:s.light==="yellow"?"只多 "+s.slack:"少了 "+Math.abs(s.slack);const fastest=s.light==="red"&&s.earliestFinish?'<span class="s2-slack-fastest">實際最快完成 '+fmtD(s.earliestFinish)+wkd(s.earliestFinish)+"（晚 "+s.overDays+" 個工作天）</span>":"";return'<div class="s2-slack s2-slack-'+s.light+'"><span class="s2-slack-dot"></span><span class="s2-slack-msg">'+msg+'</span><span class="s2-slack-period">需求專案週期 '+fmtD(sd)+wkd(sd)+" → "+fmtD(ed)+wkd(ed)+'</span><span class="s2-slack-nums">可排工作天 '+s.available+" ／ 任務需要 "+s.needed+" ／ "+diffTxt+"</span>"+fastest+"</div>"};App._s2SelectStage=function(variantId,si){const g=this._s2GroupByStage(variantId);const st=g.order[si];if(st==null)return;if(!this._s2Stage)this._s2Stage={};this._s2Stage[variantId]=st;this._s2RefreshCase(variantId)};App._s2EcnPersist=function(){const pid=App._s2EcnMode;if(!pid)return;const proj=App.getProj(pid);if(!proj||!proj.ecnType){App._s2EcnMode=null;return}const res=App._tplPreview;if(!res)return;DATA.tasks=DATA.tasks.filter(t=>!(t.project===pid&&!t._deleted)).concat(res.tasks);Store.tasks.save();App.renderEcnDashboard(proj)};App._s2RefreshCase=function(variantId){if(App._s2EcnMode){App._s2EcnPersist();return}const slack=document.querySelector('.s2-slack-wrap[data-variant="'+variantId+'"]');if(slack)slack.innerHTML=this._s2SlackHtml(variantId);const gantt=document.querySelector('.s2-gantt[data-variant="'+variantId+'"]');if(gantt)gantt.innerHTML=this._s2GanttHtml(variantId);const bn=document.querySelector('.s2n-banner-wrap[data-variant="'+variantId+'"]');if(bn)bn.innerHTML=this._s2BannerHtml(variantId);const list=document.querySelector('.s2-list[data-variant="'+variantId+'"]');if(list)list.innerHTML=this._s2ListHtml(variantId)};App._ovfState=null;App._renderOverflowFlow=function(){const res=this._tplPreview;if(!res){U.toast("⚠ 無範本預覽資料，請重新套用範本","warning");return}const variants=res.variants||[];const baseTask={};(res.tasks||[]).forEach(t=>{baseTask[t.id]={s:t.plannedStart||null,e:t.plannedEnd||null}});const baseEnd={},baseOver={};variants.forEach(v=>{baseEnd[v.id]=v.schedule.endDate;const s=App._s2VariantSlack(v.id);baseOver[v.id]=s&&s.light==="red"?s.overDays:0});let firstRed=null;variants.forEach(v=>{if(firstRed)return;const s=App._s2VariantSlack(v.id);if(s&&s.light==="red")firstRed=v.id});App._ovfState={tab:firstRed||variants[0]&&variants[0].id||null,sel:{},modified:{},topN:{},baseTask,baseEnd,baseOver};App._s2From="overflow";App._ovfRender()};App._ovfRender=function(){const page=document.getElementById("page-stage2");document.querySelectorAll(".page").forEach(pg=>pg.classList.remove("active"));page.classList.add("active");page.innerHTML='<div class="s2n-wrap ovf-wrap"><div class="s2n-pagehd"><div class="s1-crumb">總儀表板 <span class="s1-crumb-sep">/</span> 新增專案 <span class="s1-crumb-sep">/</span> 智慧排程衝突處理</div><div class="s2n-head"><span class="s2-num"><i class="ti ti-wand"></i></span>智慧排程衝突處理面板</div></div><div class="ovf-tabs" id="ovf-tabs">'+App._ovfTabsInner()+'</div><div class="ovf-casebox" id="ovf-casebox">'+App._ovfCaseHtml(App._ovfState.tab)+'</div><div class="stage2-foot"><button class="tb-action ghost" onclick="App._ovfBack()">上一步</button><button class="tb-action ovf-next-btn" onclick="App._ovfGotoStage2()">下一步：進入任務大表逐項微調 →</button></div></div>';window.scrollTo({top:0,behavior:"smooth"})};App._ovfRefresh=function(){const tabs=document.getElementById("ovf-tabs");if(tabs)tabs.innerHTML=App._ovfTabsInner();const box=document.getElementById("ovf-casebox");if(box)box.innerHTML=App._ovfCaseHtml(App._ovfState.tab)};App._ovfTabsInner=function(){const res=this._tplPreview;const st=App._ovfState;return(res.variants||[]).map((v,i)=>{const s=App._s2VariantSlack(v.id);const sub=s&&s.light==="red"?'<span class="ovf-tab-lack">● 尚缺 '+s.overDays+" 天</span>":s?'<span class="ovf-tab-ok">✓ 已足夠</span>':"";return'<button class="ovf-tab'+(v.id===st.tab?" on":"")+`" onclick="App._ovfSelectTab('`+v.id+`')"><span class="ovf-tab-name">`+(i===0?"主案":"子案")+" "+U.esc(v.name||"")+"</span>"+sub+"</button>"}).join("")};App._ovfSelectTab=function(vid){App._ovfState.tab=vid;App._ovfRefresh()};App._ovfPickLayer=function(vid,n){App._ovfState.sel[vid]=n;if(n==="2"){const s=App._s2VariantSlack(vid);App._ovfState.baseOver[vid]=s&&s.light==="red"?s.overDays:0}App._ovfRefresh()};App._ovfCaseHtml=function(vid){const res=this._tplPreview;const v=(res.variants||[]).find(x=>x.id===vid);if(!v)return"";const isMain=(res.variants||[])[0]&&(res.variants||[])[0].id===vid;const sel=App._ovfState.sel[vid]||null;const s=App._s2VariantSlack(vid);const fmtD=x=>x?String(x).replace(/-/g,"/"):"";const resolved=!s||s.light!=="red";const head='<div class="ovf-case-head"><span class="stage-cap-pill cap-0">'+(isMain?"主案":"子案")+'</span><span class="ovf-case-name">'+U.esc(v.name||"")+"</span>"+App._ovfRangeBadge(vid,v)+"</div>";const banner=resolved?'<div class="ovf-banner ok"><i class="ti ti-circle-check"></i> 時程已足夠：已成功解決排程衝突'+(s?"（餘裕 "+s.slack+" 個工作天）":"")+"。</div>":'<div class="ovf-banner"><span class="ovf-bdot"></span> 排程時間不足：照範本工期排，比需求上市日晚 <b>'+s.overDays+"</b> 個工作天（尚缺 "+s.overDays+" 個工作天）</div>";if(sel==="2"){return head+banner+App._ovfLayer2Panel(vid,s,v)}if(sel==="1"){return head+banner+App._ovfLayer1Html(vid,s,v,true)+App._ovfLayer2CardHtml(vid,v,true)}if(resolved){return head+banner+'<div class="ovf-resolved-hint"><i class="ti ti-arrow-down-circle"></i> 可直接點右下角「下一步」進任務大表，或切換上方其他案別繼續處理。</div>'}return head+banner+'<div class="ovf-hd">排程時間不足（尚缺 '+s.overDays+" 個工作天），請用以下方式處理；仍不足可按右下角「下一步」進任務大表逐項微調：</div>"+App._ovfLayer1Html(vid,s,v)+App._ovfLayer2CardHtml(vid,v)};App._ovfLayer1Html=function(vid,s,v,selected){const fmtD=x=>x?String(x).replace(/-/g,"/"):"";if(selected){return'<div class="ovf-plan ovf-p1 on"><div class="ovf-radio on"><span></span></div><div class="ovf-pbody"><div class="ovf-pt">已採用系統建議的最快可行上市日 <span class="ovf-tag easy">已套用</span></div><div class="ovf-pd">上市日已改為 <b>'+fmtD(v.schedule.endDate)+"</b>，系統已重排並點亮綠燈。如需改用其他方式，按左下「上一步」可退回重選。</div></div></div>"}return`<div class="ovf-plan ovf-p1"><div class="ovf-radio" onclick="App._ovfAdoptFastest('`+vid+`')"></div><div class="ovf-pbody"><div class="ovf-pt">採用系統建議的最快可行上市日 <span class="ovf-tag easy">最省力</span></div><div class="ovf-pd">依現有工期與前置，最快 <b>`+fmtD(s.earliestFinish)+"</b> 可上市，比原定 "+fmtD(v.schedule.endDate)+" 順延 "+s.overDays+` 個工作天。</div><button class="tb-action ovf-p1btn" onclick="App._ovfAdoptFastest('`+vid+`')">把上市日期改成 `+fmtD(s.earliestFinish)+"</button></div></div>"};App._ovfLayer2CardHtml=function(vid,v,locked){const fmtD=x=>x?String(x).replace(/-/g,"/"):"";const click=locked?"":` onclick="App._ovfPickLayer('`+vid+`','2')"`;return'<div class="ovf-plan ovf-p2'+(locked?" locked":"")+'"><div class="ovf-radio"'+click+'></div><div class="ovf-pbody"><div class="ovf-pt"'+click+'>延後需求上市日 <span class="ovf-tag mid">中度微調 · 核心主線</span></div><div class="ovf-pd">重新指定一個您可以接受的較晚日期（須晚於 '+fmtD(v.schedule.endDate)+"），或在內部精選長工時任務快速縮減。</div></div></div>"};App._ovfLayer2Panel=function(vid,s,v){const fmtD=x=>x?String(x).replace(/-/g,"/"):"";return'<div class="ovf-plan ovf-p2 on"><div class="ovf-radio on"><span></span></div><div class="ovf-pbody"><div class="ovf-pt">延後需求上市日 <span class="ovf-tag mid">中度微調 · 核心主線</span></div><div class="ovf-pd">重新指定一個您可以接受的較晚日期（須晚於 '+fmtD(v.schedule.endDate)+'），由系統重算時間差。</div><div class="ovf-l2-row"><input class="ovf-l2-date" type="date" min="'+v.schedule.endDate+`"><button class="ovf-l2-recalc-btn" onclick="App._ovfRecalc('`+vid+`')">重新計算餘裕</button></div>`+App._ovfTop3Html(vid,s)+"</div></div>"};App._effectiveGains=function(variantId){const res=this._tplPreview;if(!res)return new Map;const ts=(res.tasks||[]).filter(t=>t.variant===variantId);if(!ts.length)return new Map;const todayIso=D.fmt(D.today(),"iso");const finishOf=tasks=>{const fwd=tasks.map(t=>Object.assign({},t,{start:""}));fwd.forEach(t=>{if(!t.predecessor)t.plannedStart=todayIso});const r=computeSchedule(fwd);let fin="";r.results.forEach(x=>{if(x.suggestedEnd&&x.suggestedEnd>fin)fin=x.suggestedEnd});return fin};const baseFin=finishOf(ts);const gains=new Map;ts.forEach(t=>{if((t.durationDays||0)<=1){gains.set(t.id,0);return}const sim=ts.map(x=>x.id===t.id?Object.assign({},x,{durationDays:1}):x);const f=finishOf(sim);const g=f&&baseFin?Math.max(0,Math.round((new Date(baseFin)-new Date(f))/864e5)):0;gains.set(t.id,g)});return gains};App._ovfTopTasks=function(vid){const res=this._tplPreview;const st=App._ovfState;if(st&&st.topN&&st.topN[vid]){return st.topN[vid].map(id=>(res.tasks||[]).find(t=>t.id===id)).filter(Boolean)}const gains=App._effectiveGains(vid);const byStage={};(res.tasks||[]).filter(t=>t.variant===vid).forEach(t=>{const s=(t.stage||"").trim()||"其他";(byStage[s]=byStage[s]||[]).push(t)});const reps=[];Object.keys(byStage).forEach(s=>{const cand=byStage[s].filter(t=>(gains.get(t.id)||0)>0).sort((a,b)=>(gains.get(b.id)||0)-(gains.get(a.id)||0));if(cand.length)reps.push(cand[0])});reps.sort((a,b)=>(gains.get(b.id)||0)-(gains.get(a.id)||0));const ts=reps.slice(0,5);if(st){if(!st.topN)st.topN={};st.topN[vid]=ts.map(t=>t.id)}return ts};App._taskCap=function(vid,taskId){const res=this._tplPreview;if(!res)return{dur:0,cap:0,minDur:0};const ts=(res.tasks||[]).filter(t2=>t2.variant===vid);const t=ts.find(x=>x.id===taskId);if(!t)return{dur:0,cap:0,minDur:0};const dur=t.durationDays||0;if(dur<=1)return{dur,cap:0,minDur:dur};const todayIso=D.fmt(D.today(),"iso");const finWith=d=>{const fwd=ts.map(x=>Object.assign({},x,{start:"",durationDays:x.id===taskId?d:x.durationDays||0}));fwd.forEach(x=>{if(!x.predecessor)x.plannedStart=todayIso});const r=computeSchedule(fwd);let f="";r.results.forEach(z=>{if(z.suggestedEnd&&z.suggestedEnd>f)f=z.suggestedEnd});return f};const baseFin=finWith(dur),minFin=finWith(1);if(!baseFin||!minFin||minFin>=baseFin)return{dur,cap:0,minDur:dur};let lo=1,hi=dur-1,cap=dur-1;while(lo<=hi){const mid=lo+hi>>1;if(finWith(dur-mid)<=minFin){cap=mid;hi=mid-1}else lo=mid+1}return{dur,cap,minDur:dur-cap}};App._ovfTop3Html=function(vid,s){const ts=App._ovfTopTasks(vid);const rows=ts.map((t,i)=>{const dur=t.durationDays||0;const cp=App._taskCap(vid,t.id);const cap=cp.cap,minDur=cp.minDur;let d1=Math.min(3,Math.max(1,Math.round(dur*.15)));let d2=Math.min(5,Math.max(2,Math.round(dur*.25)));d1=Math.min(d1,cap);d2=Math.min(d2,cap);const meta=[(t.stage||"").trim(),(t.role||"").trim()].filter(Boolean).join(" · ")||"未分階段";const capNote=cap>0?'<span class="ovf-t3cap">工期 '+dur+" 天，但最多有效縮 <b>"+cap+"</b> 天（縮到 "+minDur+" 天就見底，再縮會被並行任務接手、省不了總時程）</span>":'<span class="ovf-t3cap none">此任務已不在瓶頸上，縮了不影響總時程</span>';const pills=cap>0?`<button class="ovf-pill" onclick="App._ovfTrim('`+vid+"','"+t.id+"',"+d1+')">−'+d1+" 天</button>"+(d2>d1?`<button class="ovf-pill" onclick="App._ovfTrim('`+vid+"','"+t.id+"',"+d2+')">−'+d2+" 天</button>":"")+'<span class="ovf-t3or">或</span><span class="ovf-t3man">手動 <input type="number" min="'+minDur+'" class="ovf-t3inp" value="'+dur+`" onkeydown="if(event.key==='Enter'){event.preventDefault();this.blur();}" onchange="App._ovfSetDur('`+vid+"','"+t.id+`',this.value)"> 天</span>`:"";return'<div class="ovf-t3row"><span class="ovf-t3n">'+String.fromCharCode(65+i)+'</span><span class="ovf-t3nmwrap"><span class="ovf-t3nm" title="'+U.esc(t.name)+'">'+U.esc(t.name)+'</span><span class="ovf-t3meta">'+U.esc(meta)+'</span></span><span class="ovf-t3dur">目前工期 <b>'+dur+'</b> 天</span><span class="ovf-t3arr">→</span>'+pills+capNote+"</div>"}).join("");const resolved=!s||s.light!=="red";const hd=resolved?'<div class="ovf-t3hd ok"><i class="ti ti-circle-check"></i> 時間已足夠！可直接建立，或繼續微調以下各階段瓶頸任務：</div>':'<div class="ovf-t3hd"><i class="ti ti-alert-triangle"></i> 時間仍不足 '+s.overDays+" 個工作天！系統為您精選各階段「改了最能縮短總時程」的瓶頸任務（每階段一個·避免並行互拖白改），請嘗試縮減：</div>";return'<div class="ovf-t3box">'+hd+rows+App._ovfMiniBattleHtml(vid,s)+`<div class="ovf-t3foot"><button class="tb-action ovf-recalc" onclick="App._ovfReeval('`+vid+`')">再次重算</button>`+(resolved?"":'<span class="ovf-t3tip"><i class="ti ti-bulb"></i> 小祕訣：每按一次重算，系統會自動撈出下一批<b>不重複</b>的瓶頸任務讓您繼續縮減，可一路扣到<b>尚缺歸零</b>；仍不足也可按右下角「下一步」進大表微調。</span>')+"</div></div>"};App._ovfMiniBattleHtml=function(vid,s){const st=App._ovfState;const v=(this._tplPreview.variants||[]).find(x=>x.id===vid);const fmtD=x=>x?String(x).replace(/-/g,"/"):"";const nowOver=s&&s.light==="red"?s.overDays:0;const cut=Math.max(0,(st.baseOver[vid]||0)-nowOver);const enough=nowOver<=0;return'<div class="ovf-mini"><div class="ovf-mini-hd"><i class="ti ti-chart-line"></i> 層二微調即時戰報</div><div class="ovf-mini-row"><span class="ovf-mini-k">目標上市日對齊</span><span class="ovf-mini-old">預期 '+fmtD(v.schedule.endDate)+'</span><span class="ovf-mini-arr">➔</span>'+(enough?'<b class="ovf-mini-ok">時間已足夠，可直接套用</b>':'<b class="ovf-mini-lack">還差 '+nowOver+' 個工作天</b><span class="ovf-mini-cut">（已縮短 '+cut+" 天）</span>")+'</div><div class="ovf-mini-judge">'+(enough?'<i class="ti ti-circle-check"></i> 已解決，按上方「建立專案」或「再次重算」確認。':'<i class="ti ti-alert-triangle"></i> <b>仍超出目標</b>，建議繼續扣減瓶頸任務，或按右下角「下一步」進任務大表逐項微調。')+"</div></div>"};App._ovfAdoptFastest=function(vid){const res=this._tplPreview;const v=(res.variants||[]).find(x=>x.id===vid);if(!v)return;const s=App._s2VariantSlack(vid);if(!s||!s.earliestFinish)return;const fmtD=x=>x?String(x).replace(/-/g,"/"):"";App.confirmModal({icon:"ti-circle-check",iconBg:"--sage-50",iconColor:"--sage-600",title:"確認更換為系統建議上市日？",msg:"系統已為您重新規劃最佳排程。確認後，本案的需求上市日將改為 <b>"+fmtD(s.earliestFinish)+"</b>（順延 "+s.overDays+" 個工作天），系統將自動點亮綠燈並套用排程。",okText:"確認套用並關閉",onConfirm:function(){v.schedule.endDate=s.earliestFinish;App._reschedulePreview(res.tasks,res.variants,[]);App._ovfState.sel[vid]="1";App._ovfRefresh()}})};App._ovfAfterResolve=function(vid){const res=App._tplPreview;if(!res)return;const reds=(res.variants||[]).filter(x=>{const ss=App._s2VariantSlack(x.id);return ss&&ss.light==="red"});if(!reds.length){App._renderStage2New()}else{App._ovfState.tab=reds[0].id;App._ovfState.sel[reds[0].id]=null;App._ovfRefresh();U.toast("✓ 本案已解決，請繼續處理下一個時程不足的案別："+(reds[0].name||""),"success")}};App._ovfGotoStage2=function(){const res=App._tplPreview;const reds=res?(res.variants||[]).filter(v=>{const s=App._s2VariantSlack(v.id);return s&&s.light==="red"}):[];if(reds.length){const names=reds.map(v=>{const s=App._s2VariantSlack(v.id);const isMain=(res.variants||[])[0]&&(res.variants||[])[0].id===v.id;return"·"+(isMain?"主案":"子案")+" "+U.esc(v.name||"")+"（尚缺 "+(s?s.overDays:0)+" 個工作天）"}).join("<br>");App.confirmModal({icon:"ti-alert-triangle",iconBg:"--rose-l",iconColor:"--rose",title:"還有案別時程未處理",msg:"以下案別仍時程不足、尚未在本面板處理：<br><br>"+names+"<br><br>可切換上方頁籤逐案處理，或仍要直接進入任務大表逐項微調？",okText:"仍要進入大表 →",cancelText:"留在此繼續處理",onConfirm:function(){App._renderStage2New()}});return}App._renderStage2New()};App._ovfRangeBadge=function(vid,v){const fmtD=x=>x?String(x).replace(/-/g,"/"):"";const st=App._ovfState;const baseEnd=st&&st.baseEnd?st.baseEnd[vid]:"";const curEnd=v.schedule.endDate;const sd=v.schedule.startDate;const sFmt=sd?fmtD(sd):"（自動起算）";if(baseEnd&&curEnd&&curEnd!==baseEnd){const delay=curEnd>baseEnd?Math.max(0,D.workdaysBetween(baseEnd,curEnd)-1):0;return'<span class="ovf-rangebadge"><span class="ovf-rb-old"><i class="ti ti-history"></i> 原始 '+sFmt+" → "+fmtD(baseEnd)+'</span><span class="ovf-rb-arr">➔</span><span class="ovf-rb-new">新時程 '+sFmt+" → "+fmtD(curEnd)+(delay>0?' <span class="ovf-rb-delay">順延 '+delay+" 個工作天</span>":"")+"</span></span>"}return'<span class="ovf-case-range">'+sFmt+" → "+fmtD(curEnd)+"（需求上市日）</span>"};App._ovfRecalc=function(vid){const res=this._tplPreview;const v=(res.variants||[]).find(x=>x.id===vid);if(!v)return;const box=document.getElementById("ovf-casebox");const inp=box?box.querySelector(".ovf-l2-date"):null;const val=inp?inp.value:"";if(val&&val>v.schedule.endDate){v.schedule.endDate=val;App._reschedulePreview(res.tasks,res.variants,[])}if(App._ovfState&&App._ovfState.topN)delete App._ovfState.topN[vid];App._ovfRefresh();App._ovfResultModal(vid)};App._ovfTrim=function(vid,taskId,days){const res=this._tplPreview;const t=(res.tasks||[]).find(x=>x.id===taskId);if(!t)return;const cp=App._taskCap(vid,taskId);const target=(t.durationDays||0)-days;const clamped=Math.max(cp.minDur,target);if(clamped>target)U.toast("已達有效上限（縮到 "+cp.minDur+" 天），再縮也省不了總時程——瓶頸已轉移到並行任務","warning",{soft:true});t.durationDays=Math.max(0,clamped);App._ovfState.modified[taskId]=true;App._reschedulePreview(res.tasks,res.variants,[]);App._ovfRefresh()};App._ovfSetDur=function(vid,taskId,value){const res=this._tplPreview;const t=(res.tasks||[]).find(x=>x.id===taskId);if(!t)return;const cp=App._taskCap(vid,taskId);const target=Math.max(0,parseInt(value)||0);const clamped=Math.max(cp.minDur,target);if(clamped>target)U.toast("已達有效上限（縮到 "+cp.minDur+" 天），再縮也省不了總時程——瓶頸已轉移到並行任務","warning",{soft:true});t.durationDays=clamped;App._ovfState.modified[taskId]=true;App._reschedulePreview(res.tasks,res.variants,[]);App._ovfRefresh()};App._ovfResultModal=function(vid){const res=App._tplPreview;const v=(res.variants||[]).find(x=>x.id===vid);if(!v)return;const s=App._s2VariantSlack(vid);const fmtD=x=>x?String(x).replace(/-/g,"/"):"";const resolved=!s||s.light!=="red";if(resolved){App.confirmModal({icon:"ti-calendar-check",iconBg:"--sage-50",iconColor:"--sage-600",title:"時間已足夠！",msg:"新排程已可在 <b>"+fmtD(v.schedule.endDate)+"</b> 前完成"+(s?"，餘裕 "+s.slack+" 個工作天":"")+"。下一步前往任務細節編輯頁（指派負責人、調整任務）。",okText:"確認並前往調整任務細節",cancelText:"留在此頁微調",onConfirm:function(){App._ovfAfterResolve(vid)}})}else{App.confirmModal({icon:"ti-calendar",iconBg:"--amber-l",iconColor:"--amber-accent",title:"重新計算完成：時間仍不足",msg:"目前仍差 <b>"+s.overDays+"</b> 個工作天。您可以繼續扣減上方瓶頸任務、改更晚的上市日，或按右下角「下一步」進 Stage 2 大表逐項微調。",okText:"我知道了",cancelText:null})}};App._ovfReeval=function(vid){if(App._ovfState&&App._ovfState.topN)delete App._ovfState.topN[vid];App._ovfRefresh();App._ovfResultModal(vid)};App._ovfBack=function(){const tab=App._ovfState?App._ovfState.tab:null;if(tab&&App._ovfState.sel[tab]){App._ovfState.sel[tab]=null;App._ovfRefresh();return}App._s2BackToStage1()};App.buildDeptRowsHtml=function(depts,mode,projId){const pid=projId||"";return(depts||[]).map(d=>`
      <div class="dept-edit-row" data-dept-id="${d.id}">
        <div class="dept-pill">
          <input class="dept-edit-name" value="${U.esc(d.name)}" placeholder="例：研發部" onchange="App.deptUI.renameDept('${mode}','${pid}','${d.id}',this.value)">
          <span class="dept-pill-sep"></span>
          <div class="dept-members">
            ${(d.members||[]).map(m=>`<span class="dept-member-chip"><input class="dept-member-name" data-member-id="${m.id}" value="${U.esc(m.name)}" placeholder="例：王小明" onchange="App.deptUI.renameMember('${mode}','${pid}','${d.id}','${m.id}',this.value)"><button class="dept-member-del" title="刪除負責人" onclick="App.deptUI.removeMember('${mode}','${pid}','${d.id}','${m.id}')">×</button></span>`).join("")}
            <button class="dept-member-add" onclick="App.deptUI.addMember('${mode}','${pid}','${d.id}')">＋ 新增負責人</button>
          </div>
        </div>
        <button class="dept-del-btn" title="刪除部門" onclick="App.deptUI.removeDept('${mode}','${pid}','${d.id}')">×</button>
      </div>`).join("")};App.deptUI={_store(mode,projId){if(mode==="edit"){const p=App.getProj(projId);if(!p)return null;if(!p.depts)p.depts=[];return p.depts}if(!App._tplDepts)App._tplDepts=[];return App._tplDepts},_after(mode,projId,focusSel){if(mode==="edit")Store.projects.save();this._rerender(mode,projId,focusSel)},_rerender(mode,projId,focusSel){const box=document.getElementById("deptEditorList-"+projId)||document.getElementById("deptEditorList");if(!box)return;box.innerHTML=App.buildDeptRowsHtml(this._store(mode,projId),mode,projId);if(focusSel){const el=box.querySelector(focusSel);if(el)el.focus()}},addDept(mode,projId){const store=this._store(mode,projId);if(!store)return;const id=U.id();store.push({id,name:"",members:[{id:U.id(),name:""}]});this._after(mode,projId,'.dept-edit-row[data-dept-id="'+id+'"] .dept-edit-name')},renameDept(mode,projId,deptId,val){const store=this._store(mode,projId);if(!store)return;const d=store.find(x=>x.id===deptId);if(!d)return;const v=(val||"").trim();if(mode==="edit"&&!v){U.toast("部門名不可空白");return}d.name=v;this._after(mode,projId)},removeDept(mode,projId,deptId){const store=this._store(mode,projId);if(!store)return;const self=this;const _doRemove=()=>{const i=store.findIndex(x=>x.id===deptId);if(i>=0)store.splice(i,1);if(mode==="tpl"&&store.length===0)store.push({id:U.id(),name:"",members:[{id:U.id(),name:""}]});self._after(mode,projId)};if(mode==="edit"){const n=DATA.tasks.filter(t=>t.dept===deptId).length;if(n>0){App.openDeptReassign(projId,deptId);return}const d0=store.find(x=>x.id===deptId);App.confirmModal({icon:"ti-trash",iconBg:"--rose-l",iconColor:"--rose-ink",title:`確定刪除部門「${d0?d0.name:deptId}」？`,okText:"刪除",cancelText:"取消",okClass:"danger",onConfirm:_doRemove});return}_doRemove()},addMember(mode,projId,deptId){const store=this._store(mode,projId);if(!store)return;const d=store.find(x=>x.id===deptId);if(!d)return;if(!d.members)d.members=[];const mid=U.id();d.members.push({id:mid,name:""});this._after(mode,projId,'[data-member-id="'+mid+'"]')},renameMember(mode,projId,deptId,memberId,val){const store=this._store(mode,projId);if(!store)return;const d=store.find(x=>x.id===deptId);if(!d||!d.members)return;const m=d.members.find(x=>x.id===memberId);if(!m)return;m.name=(val||"").trim();this._after(mode,projId)},removeMember(mode,projId,deptId,memberId){const store=this._store(mode,projId);if(!store)return;const d=store.find(x=>x.id===deptId);if(!d||!d.members)return;d.members=d.members.filter(x=>x.id!==memberId);this._after(mode,projId)}};App.openDeptReassign=function(projId,deptId){const p=App.getProj(projId);if(!p||!p.depts)return;const delDept=p.depts.find(x=>x.id===deptId);const affected=DATA.tasks.filter(t=>t.dept===deptId);const optDepts=p.depts.filter(x=>x.id!==deptId);const rows=affected.map(t=>{const label=t.wbs!==void 0&&t.wbs!==null&&String(t.wbs).trim()!==""?U.esc(String(t.wbs))+" "+U.esc(t.name||""):U.esc(t.name||"");const opts=['<option value="">— 請選擇 —</option>'].concat(optDepts.map(d=>'<option value="'+d.id+'">'+U.esc(d.name)+"</option>")).concat(['<option value="__UNASSIGN__">未指派</option>']).join("");return'<div class="reassign-row" data-task-id="'+t.id+'"><span class="reassign-task">'+label+'</span><select class="reassign-select" onchange="App.checkReassignReady()">'+opts+"</select></div>"}).join("");const body='<div class="reassign-list">'+rows+"</div>";const footer=`<button class="tb-action ghost" onclick="App.openProjectDialog('`+projId+`')">取消</button><button id="reassign-del-btn" class="tb-action danger" disabled onclick="App.confirmDeptReassign('`+projId+"','"+deptId+`')">刪除部門</button>`;App.openModal({title:"刪除部門「"+(delDept?U.esc(delDept.name):deptId)+"」— 改派 "+affected.length+" 個任務",body,footer})};App.checkReassignReady=function(){const sels=document.querySelectorAll(".reassign-select");const btn=document.getElementById("reassign-del-btn");if(!btn)return;const allChosen=Array.from(sels).every(s=>s.value!=="");btn.disabled=!allChosen};App.confirmDeptReassign=function(projId,deptId){const p=App.getProj(projId);if(!p||!p.depts)return;const rows=Array.from(document.querySelectorAll(".reassign-row"));if(!rows.every(r=>{const s=r.querySelector(".reassign-select");return s&&s.value!==""}))return;rows.forEach(r=>{const taskId=r.getAttribute("data-task-id");const val=r.querySelector(".reassign-select").value;const t=DATA.tasks.find(x=>x.id===taskId);if(!t)return;t.dept=val==="__UNASSIGN__"?"未指派":val});p.depts=p.depts.filter(x=>x.id!==deptId);Store.tasks.save();App.deptUI._after("edit",projId)};App.deleteProject=function(id){if(App._roGuard())return;const p=this.getProj(id);if(!p)return;const taskCnt=this.getTasksOf(id).length;App.confirmModal({icon:"ti-trash",iconBg:"--rose-l",iconColor:"--rose-ink",title:`刪除專案「${p.name}」？`,msg:`含 ${taskCnt} 個任務會一起移到「專案檔案室 · 回收桶」，30 天內可還原。`,okText:"刪除",cancelText:"取消",okClass:"danger",onConfirm:()=>{p.deletedAt=new Date().toISOString();DATA.tasks.forEach(t=>{if(t.project===id&&!t._deleted){t._deleted=true;t._deletedAt=p.deletedAt;t._delWithProj=true}});DATA.projects=DATA.projects.filter(x=>x.id!==id);(DATA.projectsTrash=DATA.projectsTrash||[]).push(p);if(App.currentProjectId===id)App.currentProjectId=null;Store.projects.save();Store.projectsTrash.save();Store.tasks.save();App.closeModal();App.showPage("workspace",document.querySelector("[data-page=workspace]"));App.refreshAll();U.toast("已移到回收桶（30 天內可在專案檔案室還原）","success")}})};App.restoreProject=function(id){const trash=DATA.projectsTrash||[];const i=trash.findIndex(x=>x.id===id);if(i<0)return;const p=trash[i];delete p.deletedAt;DATA.tasks.forEach(t=>{if(t.project===id&&t._delWithProj){t._deleted=false;delete t._deletedAt;delete t._delWithProj}});trash.splice(i,1);DATA.projects.push(p);Store.projects.save();Store.projectsTrash.save();Store.tasks.save();App.refreshAll();U.toast("已還原專案","success")};App.purgeProject=function(id){const p=(DATA.projectsTrash||[]).find(x=>x.id===id);if(!p)return;App.confirmModal({icon:"ti-trash",iconBg:"--rose-l",iconColor:"--rose-ink",title:`永久刪除「${p.name}」？`,msg:"永久刪除後無法還原，該案任務也一併清除。",okText:"永久刪除",cancelText:"取消",okClass:"danger",onConfirm:()=>{DATA.projectsTrash=(DATA.projectsTrash||[]).filter(x=>x.id!==id);DATA.tasks=DATA.tasks.filter(t=>t.project!==id);Store.projectsTrash.save();Store.tasks.save();App.closeModal();App.refreshAll();U.toast("已永久刪除","info")}})};App.purgeExpiredTrash=function(){const trash=DATA.projectsTrash||[];if(!trash.length)return;const cutoff=D.addDays(D.today(),-30);const keep=trash.filter(p=>!p.deletedAt||new Date(p.deletedAt)>cutoff);if(keep.length===trash.length)return;const goneIds=new Set(trash.filter(p=>!keep.includes(p)).map(p=>p.id));DATA.projectsTrash=keep;DATA.tasks=DATA.tasks.filter(t=>!goneIds.has(t.project));Store.projectsTrash.save();Store.tasks.save()};App.reopenProject=function(id){const p=this.getProj(id);if(!p)return;p.status="active";delete p.completedAt;Store.projects.save();App.closeModal();App.refreshAll();U.toast("已重新開啟","success")};App.unarchiveProject=function(id){const p=this.getProj(id);if(!p)return;p.archived=false;delete p.archiveReason;delete p.archivedAt;Store.projects.save();App.refreshAll();U.toast("已取消封存","success")};App.archiveProject=function(id){const p=this.getProj(id);if(!p)return;App.openModal({title:`收進封存 · ${U.esc(p.name)}`,body:`<div class="form-field">
      <label>封存原因（必填）</label>
      <textarea id="pf-arch-reason" rows="3" style="width:100%;resize:vertical;" placeholder="例：專案損益不佳被喊停 / 需求變更暫停 / 已被新案取代…" oninput="App._archReasonCheck()">${U.esc(p.archiveReason||"")}</textarea>
      <div id="pf-arch-hint" style="margin-top:6px;font-size:12px;color:var(--rose-ink);display:none;">請填封存原因才能封存——一定有原因才會喊停一個專案。</div>
      <div style="margin-top:6px;font-size:12px;color:var(--ink3);">封存的專案移到「專案檔案室 · 已封存」，不在側欄顯示；日後可取消封存退回。</div>
    </div>`,footer:`<button class="tb-action ghost" onclick="App.closeModal()">取消</button>
             <button class="tb-action" id="pf-arch-ok" disabled onclick="App._archiveConfirm('${id}')">收進封存</button>`});setTimeout(()=>{const i=document.getElementById("pf-arch-reason");if(i)i.focus();App._archReasonCheck()},30)};App._archReasonCheck=function(){const ta=document.getElementById("pf-arch-reason"),ok=document.getElementById("pf-arch-ok"),hint=document.getElementById("pf-arch-hint");if(!ta||!ok)return;const v=ta.value.trim();ok.disabled=!v;if(hint)hint.style.display=v?"none":""};App._archiveConfirm=function(id){const p=this.getProj(id);if(!p)return;const ta=document.getElementById("pf-arch-reason");const reason=ta?ta.value.trim():"";if(!reason)return;p.archived=true;p.archiveReason=reason;p.archivedAt=new Date().toISOString();Store.projects.save();App.closeModal();if(App.currentPage==="project"&&App.currentProjectId===id)App.showPage("workspace",document.querySelector("[data-page=workspace]"));App.refreshAll();U.toast("已收進封存","success")};App._archTab="done";App._archSwitchTab=function(t){App._archTab=t;App.renderArchive()};App.renderArchive=function(){const el=document.getElementById("page-archive");if(!el)return;const c=this._archCounts();const tab=this._archTab||"done";const badge=p=>p.ecnType?'<span class="arch-badge arch-badge-ecn">設變 ECN</span>':'<span class="arch-badge arch-badge-npi">NPI 開發</span>';const liveTaskCnt=pid=>DATA.tasks.filter(t=>t.project===pid&&!t._deleted).length;const allTaskCnt=pid=>DATA.tasks.filter(t=>t.project===pid).length;const doneList=(DATA.projects||[]).filter(p=>!p.archived&&this._isProjectDone(p));const archList=(DATA.projects||[]).filter(p=>p.archived);const trashList=(DATA.projectsTrash||[]).slice().sort((a,b)=>(b.deletedAt||"").localeCompare(a.deletedAt||""));const rowDone=p=>`<div class="arch-pc arch-done">
    <div class="arch-body"><div class="arch-name">${U.esc(p.name)} ${badge(p)}</div>
      <div class="arch-meta">${p.ecnType?"結案":"完成"}於 ${p.completedAt?D.fmt(p.completedAt,"ymd"):"—"} · ${liveTaskCnt(p.id)} 任務 · <span class="arch-pill">${p.ecnType?"已結案":"進度 100%"}</span></div></div>
    <div class="arch-acts">
      <button class="tb-action ghost" onclick="App.reopenProject('${p.id}')">重新開啟</button>
      <button class="tb-action ghost" onclick="App.archiveProject('${p.id}')">收進封存</button>
      <button class="tb-action ghost arch-icon" title="移到回收桶" onclick="App.deleteProject('${p.id}')"><i class="ti ti-trash"></i></button>
    </div></div>`;const rowArch=p=>`<div class="arch-pc arch-arch">
    <div class="arch-body"><div class="arch-name arch-dim">${U.esc(p.name)} ${badge(p)}</div>
      <div class="arch-meta">封存於 ${p.archivedAt?D.fmt(p.archivedAt,"ymd"):"—"} · ${liveTaskCnt(p.id)} 任務</div>
      ${p.archiveReason?`<div class="arch-reason">封存原因：${U.esc(p.archiveReason)}</div>`:""}</div>
    <div class="arch-acts">
      <button class="tb-action" onclick="App.unarchiveProject('${p.id}')">取消封存</button>
      <button class="tb-action ghost arch-icon" title="移到回收桶" onclick="App.deleteProject('${p.id}')"><i class="ti ti-trash"></i></button>
    </div></div>`;const rowTrash=p=>{const until=p.deletedAt?D.addDays(new Date(p.deletedAt),30):null;const left=until?Math.max(0,D.daysBetween(D.today(),until)):"—";return`<div class="arch-pc arch-trash">
    <div class="arch-body"><div class="arch-name arch-dim">${U.esc(p.name)} ${badge(p)}</div>
      <div class="arch-meta">刪除於 ${p.deletedAt?D.fmt(p.deletedAt,"ymd"):"—"} · <span class="arch-left">保留至 ${until?D.fmt(until,"ymd"):"—"}（剩 ${left} 天）</span> · ${allTaskCnt(p.id)} 任務一併刪除</div></div>
    <div class="arch-acts">
      <button class="tb-action" onclick="App.restoreProject('${p.id}')">↺ 還原</button>
      <button class="tb-action danger" onclick="App.purgeProject('${p.id}')">永久刪除</button>
    </div></div>`};const empty=m=>`<div class="arch-empty">${m}</div>`;let body;if(tab==="done")body=doneList.length?doneList.map(rowDone).join(""):empty("目前沒有已完成的專案。在專案編輯視窗按「標記完成」即可收尾。");else if(tab==="arch")body=archList.length?archList.map(rowArch).join(""):empty("目前沒有已封存的專案。");else body=trashList.length?`<div class="arch-hint arch-hint-warn">🗑 已刪除的專案保留 30 天可還原，過期自動永久清除。</div>`+trashList.map(rowTrash).join(""):empty("回收桶是空的。");const tbtn=(k,label)=>`<button class="arch-tab ${tab===k?"on":""}${k==="trash"?" arch-tab-trash":""}" onclick="App._archSwitchTab('${k}')">${label}<span class="arch-tn">${k==="done"?c.done:k==="arch"?c.arch:c.trash}</span></button>`;el.innerHTML=`
    <div class="arch-hd">
      <h1 class="arch-title"><i class="ti ti-archive"></i> 專案檔案室</h1>
      <p class="arch-sub">已收尾、已封存與已刪除的專案都在這裡。側欄只保留進行中的活案，讓正在追的專案一眼看完。</p>
    </div>
    <div class="arch-tabs">${tbtn("done","已完成")}${tbtn("arch","已封存")}${tbtn("trash","🗑 回收桶")}</div>
    <div class="arch-cards">${body}</div>`};App._flowBlankChoice=function(){App.openModal({title:"空白專案 · 建立方式",wide:true,body:`<div class="bs-choice"><div class="bs-choice-card bs-choice-skel" onclick="App._flowBlankSkelStart('npi')"><div class="bsc-hd"><i class="ti ti-timeline-event bsc-ic"></i><span class="bsc-title">自訂時程骨架</span><span class="bsc-rec">推薦</span></div><div class="bsc-body">不套用複雜範本，僅需快速定義大階段與粗估工期。系統將自動換算工作天並產出里程碑甘特圖，隨後再填入細部任務。</div><div class="bsc-fit"><span class="bsc-fit-ic">🎯</span>適合：已有專案大方向、需優先確認大時間軸與截止日的情境。</div><div class="bsc-foot">預設結構：≥ 1 個階段　｜　流程：階段骨架 → 細部任務</div></div><div class="bs-choice-card bs-choice-plain" onclick="App._flowPickMode('blank')"><div class="bsc-hd"><i class="ti ti-file bsc-ic"></i><span class="bsc-title">完全空白專案</span></div><div class="bsc-body">跳過所有預設結構與階段劃分，直接建立一個乾淨的空白看板。您可以隨時手動新增臨時任務，彈性記錄時程與交辦事項。</div><div class="bsc-fit"><span class="bsc-fit-ic">🎯</span>適合：臨時交辦小案、單純記事或無需分階段的扁平化專案。</div><div class="bsc-foot">預設結構：0 個階段　｜　流程：直接進入任務面板</div></div></div>`,footer:'<button class="tb-action ghost" onclick="App._flowStep1()">上一步</button>'})};App._flowEcnStart=function(){App._scheduleEduCard(function(){App._flowStartEcn("M")},function(){App._flowStep1()},"ecn")};App._flowBlankSkelStart=function(kind){kind=kind==="ecn"?"ecn":"npi";App._scheduleEduCard(function(){App._flowBlankSkeleton(kind)},function(){if(kind==="ecn")App._flowStep1();else App._flowBlankChoice()},kind)};App._bsDefaultStages=function(){return[{id:U.id(),name:"P1 規劃 / 立項",months:2,wd:0,driver:"months"},{id:U.id(),name:"EVT 工程驗證",months:3,wd:0,driver:"months"},{id:U.id(),name:"DVT 設計驗證",months:3,wd:0,driver:"months"},{id:U.id(),name:"PVT / 量產導入",months:2,wd:0,driver:"months"}]};App._bsDefaultDepts=function(){return[{id:U.id(),name:"研發部",members:[{id:U.id(),name:""}]},{id:U.id(),name:"採購",members:[{id:U.id(),name:""}]},{id:U.id(),name:"品保",members:[{id:U.id(),name:""}]},{id:U.id(),name:"專案管理 PM",members:[{id:U.id(),name:""}]}]};App._flowBlankSkeleton=function(kind){kind=kind==="ecn"?"ecn":"npi";App.closeModal();const today=D.fmt(new Date,"iso");App._bs={kind,name:"",color:typeof PROJ_COLORS!=="undefined"&&PROJ_COLORS[0]||"",start:today,end:"",roiType:"benefit",sourceNo:"",reason:"",stages:App._bsDefaultStages()};App._tplDepts=App._bsDefaultDepts();App._renderBsStage1()};App._bsAddMonths=function(d,n){const x=new Date(d);x.setHours(0,0,0,0);x.setMonth(x.getMonth()+Math.round(n||0));return x};App._bsFirstWorkOnAfter=function(d){return D.isWorkday(d)?new Date(d):D.addWorkdays(d,1)};App._bsLastWorkOnBefore=function(d){return D.isWorkday(d)?new Date(d):D.addWorkdays(d,-1)};App._bsComputeStages=function(){const bs=App._bs;if(!bs)return{dir:"forward",stages:[],totalStart:"",totalEnd:""};const dir=App._effScheduleDir(bs.start,bs.end,"forward");const list=(bs.stages||[]).map(s=>Object.assign({},s));const out=[];const durEnd=(startD,s)=>{let endW;if(s.driver==="wd"&&s.wd>0)endW=D.addWorkdays(startD,Math.max(1,s.wd)-1);else{let endCal=D.addDays(App._bsAddMonths(startD,s.months||0),-1);endW=App._bsLastWorkOnBefore(endCal);if(endW<startD)endW=new Date(startD)}return endW};const durStart=(endD,s)=>{let startW;if(s.driver==="wd"&&s.wd>0)startW=D.addWorkdays(endD,-(Math.max(1,s.wd)-1));else{let startCal=D.addDays(App._bsAddMonths(endD,-(s.months||0)),1);startW=App._bsFirstWorkOnAfter(startCal);if(startW>endD)startW=new Date(endD)}return startW};if(dir==="backward"){let cursor=App._bsLastWorkOnBefore(new Date(bs.end));for(let i=list.length-1;i>=0;i--){const endW=cursor,startW=durStart(endW,list[i]);out[i]={start:D.fmt(startW,"iso"),end:D.fmt(endW,"iso"),wd:D.workdaysBetween(startW,endW)};cursor=D.addWorkdays(startW,-1)}}else{let cursor=App._bsFirstWorkOnAfter(new Date(bs.start||D.fmt(new Date,"iso")));for(let i=0;i<list.length;i++){const startW=cursor,endW=durEnd(startW,list[i]);out[i]={start:D.fmt(startW,"iso"),end:D.fmt(endW,"iso"),wd:D.workdaysBetween(startW,endW)};cursor=D.addWorkdays(endW,1)}}list.forEach((s,i)=>{const c=out[i];if(!c)return;c.name=s.name;c.months=s.months;c.driverWd=s.driver;c.approxMonths=Math.max(1,Math.round((D.daysBetween(c.start,c.end)+1)/30.44));if(!bs.end){c.light="green";c.margin=null;return}let margin;if(c.end<=bs.end)margin=D.workdaysBetween(c.end,bs.end)-1;else margin=-(D.workdaysBetween(bs.end,c.end)-1);c.margin=margin;c.light=margin>=5?"green":margin>=0?"yellow":"red"});const totalStart=out.length?out[0].start:"";const totalEnd=out.length?out[out.length-1].end:"";return{dir,stages:out,totalStart,totalEnd}};App._renderBsStage1=function(){const bs=App._bs;if(!bs)return;const isEcn=bs.kind==="ecn";const accentCls=isEcn?"bs-ecn":"bs-npi";const typeBadge='<span class="bs-typebadge">'+(isEcn?"ECN 設變":"NPI 開發")+"</span>";const npiTips=App.buildHintBox({key:"bs-sched-tips",icon:"ti-bulb",title:"排程導航秘訣",summary:"怎麼填日期決定排程方向",collapsed:false,bodyHtml:'<ul class="bs-tiplist"><li><b>正向推演</b>（僅填開始日）：系統將自動順推各階段，算出預計完工日。</li><li><b>逆向倒推</b>（僅填上市日）：系統將自動逆推最晚需開工日（若時程來不及，將建議最快完工日）。</li><li><b>雙向精算</b>（兩者皆填）：系統自動精算時程容裕度。若產生衝突，下一步將引導您進行一鍵時程優化。</li></ul>'});const ecnHelper=App.buildHintBox({key:"bs-ecn-helper",icon:"ti-bulb",title:"開案小幫手",summary:"點擊了解類型與時程邏輯",collapsed:true,bodyHtml:'<ol class="bs-tiplist bs-tiplist-ol"><li><b>設變類型</b>：「效益型」（如 Cost Down）結案時系統自動對比新舊 BOM 算出省下的成本；「被迫型」（如停產／法規）系統會著重追蹤舊料消耗率、卡控生效日以降低報廢損失。</li><li><b>時程推算</b>：填「開始日」系統順推預估完工日；只填「上市日」回推最晚必須動工的日期；若算出工期超出上市死線，進度條將亮紅燈警示。</li><li><b>PM 協調工時（防呆保護）</b>：設變不是只有 RD 在忙——每張單都預設保留 PM 的跨部門協調時間。多案疊加總負載超過 100% 時，系統會發出排擠告警，避免隱形過勞。</li></ol>'});const rosterHint=App.buildHintBox({key:"bs-roster-help",icon:"ti-info-circle",title:"為什麼要先建立部門與人名？",summary:"先填人，後面全自動帶入",collapsed:true,bodyHtml:'<ol class="bs-tiplist bs-tiplist-ol"><li><b>自動配對</b>：套用範本時，系統會自動將部門成員配對至對應任務，省去逐筆指派時間。</li><li><b>直選下拉選單</b>：後續任務大表的「負責人」欄將自動帶入此名冊，改人、派工一鍵搞定。</li><li><b>人均負荷防線</b>：系統將自動統計成員的專案投入度，若跨案同人同日負荷超過 100% 將主動紅旗警示。</li></ol>'});const ecnBox=isEcn?App._bsEcnBoxHtml():"";const dateCard='<div class="bs-datecard"><label class="bs-datefield"><span class="bs-dlabel">開始日</span><input type="date" class="bs-in-start" value="'+U.esc(bs.start||"")+`" onchange="App._bsSetDate('start',this.value)"></label><label class="bs-datefield"><span class="bs-dlabel">上市日期<span class="bs-dopt">（可留空）</span></span><input type="date" class="bs-in-end" value="`+U.esc(bs.end||"")+`" onchange="App._bsSetDate('end',this.value)"></label></div>`;const tb=document.querySelector(".main > .topbar");if(tb)tb.classList.add("topbar-hidden");document.querySelectorAll(".page").forEach(pg=>pg.classList.remove("active"));const page=document.getElementById("page-bs1");page.className="page active "+accentCls;page.innerHTML='<div class="bs-wrap"><div class="bs-pagehd"><div class="bs-crumb">總儀表板 <span class="bs-crumb-sep">/</span> '+(isEcn?"新增設變案":"新增專案")+"</div>"+typeBadge+'</div><div class="bs-title">自訂階段骨架'+(isEcn?" · 設變案":"")+'</div><div class="bs-namecard"><div class="bs-field"><span class="bs-flabel">專案名稱 <span class="bs-req">*</span></span><input type="text" class="bs-proj-name" value="'+U.esc(bs.name||"")+'" placeholder="'+U.esc(CFG("PROJECT_INPUT_EXAMPLE","範例品項"))+'" oninput="App._bsSetName(this.value)"></div></div>'+(isEcn?ecnHelper:npiTips)+ecnBox+dateCard+'<div id="bs-dyn">'+App._bsDynSectionHtml()+'</div><div class="bs-roster"><div class="bs-sectitle">部門 / 負責人名冊</div>'+rosterHint+App._deptEditorHtml()+'</div><div class="bs-foot"><button class="tb-action ghost" onclick="App._bsBack()">上一步</button><button class="tb-action bs-next" onclick="App._bsNext()">下一步：檢視任務</button></div></div>';window.scrollTo({top:0,behavior:"smooth"})};App._bsDynSectionHtml=function(){const bs=App._bs;if(!bs)return"";const comp=App._bsComputeStages();const bothEmpty=!bs.start&&!bs.end;let dynhint=App._s1DynHintHtml(bs.start,bs.end,App._bsDynMeta(comp));const dhIcon=bothEmpty?"ti-calendar-event":comp.dir==="backward"?"ti-arrow-narrow-left":"ti-circle-check bs-dyn-ok";dynhint=dynhint.replace(/^<i[^>]*><\/i>/,'<i class="ti '+dhIcon+'"></i>');dynhint='<div class="bs-dynhint'+(bothEmpty?" bs-dynhint-init":"")+'">'+dynhint+"</div>";return dynhint+App._bsStageSecHtml(comp)+App._bsGanttSecHtml(comp)};App._bsDynMeta=function(comp){if(!comp||!comp.stages.length)return null;return{backStart:comp.totalStart,forwardFinish:comp.totalEnd,backFallback:false}};App._bsRefresh=function(){const c=document.getElementById("bs-dyn");if(c)c.innerHTML=App._bsDynSectionHtml()};App._bsStageSecHtml=function(comp){return'<div class="bs-stagesec"><div class="bs-sechd"><span class="bs-sectitle">專案階段<span class="bs-sechint">（自己劃分·每階段抓幾個月，系統換算工作天）</span></span><button type="button" class="tb-action ghost bs-addstage" onclick="App._bsAddStage()">＋ 新增階段</button></div><div class="bs-stagenote">此處直接決定專案流程（由上而下）：點階段名可重新命名、按 ✕ 刪除、按 ＋ 新增階段。<b class="bs-note-em">調整後系統將自動重排下方甘特圖、並重新精算時間餘裕與燈號。</b></div>'+App._bsStageTableHtml(comp)+'<div class="bs-stagetip"><i class="ti ti-bulb"></i>耗時(月) 與 工作天 二擇一填：填了月數，工作天自動換算且反灰（反之亦然）。要改另一欄，先把目前這欄清空。</div></div>'};App._bsStageTableHtml=function(comp){const bs=App._bs;const rows=bs.stages||[];const fmtMD=iso=>{if(!iso)return"";const p=String(iso).split("-");return+p[1]+"/"+ +p[2]};let body="";rows.forEach((s,i)=>{const c=comp.stages[i]||{};const isM=s.driver==="months";const isW=s.driver==="wd";const monthsCell=isW?'<span class="bs-lockval"><span class="bs-approx">約</span> '+(c.approxMonths||0)+" 個月</span>":'<span class="bs-numwrap"><input type="number" class="bs-months bs-driverin" min="0" step="1" value="'+(s.months||"")+'" onchange="App._bsMonths('+i+',this.value)"><span class="bs-unit">個月</span></span>';const wdCell=isM?'<span class="bs-lockval"><span class="bs-approx">約</span> '+(c.wd||0)+" 天</span>":'<span class="bs-numwrap"><input type="number" class="bs-wd bs-driverin" min="0" step="1" value="'+(s.wd||"")+'" onchange="App._bsWd('+i+',this.value)"><span class="bs-unit">天</span></span>';const range=c.start&&c.end?fmtMD(c.start)+" → "+fmtMD(c.end):"—";const del=rows.length>1?'<button type="button" class="bs-delstage" title="刪除階段" onclick="App._bsDelStage('+i+')"><i class="ti ti-x"></i></button>':'<span class="bs-delstage bs-del-off"></span>';body+='<div class="bs-strow" draggable="true" data-idx="'+i+'" ondragstart="App._bsDragStart(event,'+i+')" ondragover="App._bsDragOver(event)" ondrop="App._bsDrop(event,'+i+')" ondragend="App._bsDragEnd(event)"><span class="bs-drag" title="拖曳排序">⠿</span><input type="text" class="bs-stagename" value="'+U.esc(s.name||"")+'" placeholder="階段名稱" onchange="App._bsRenameStage('+i+',this.value)"><span class="bs-cell bs-cell-m">'+monthsCell+'</span><span class="bs-cell bs-cell-w">'+wdCell+'</span><span class="bs-cell bs-cell-r">'+range+"</span>"+del+"</div>"});return'<div class="bs-stagetbl"><div class="bs-strow bs-sthead"><span class="bs-drag"></span><span class="bs-hname">階段名稱</span><span class="bs-cell bs-cell-m">耗時(月)</span><span class="bs-cell bs-cell-w">工作天</span><span class="bs-cell bs-cell-r">起 → 迄</span><span class="bs-delstage bs-del-off"></span></div>'+body+"</div>"};App._bsGanttSecHtml=function(comp){const help=App.buildHintBox({key:"bs-slack-help",icon:"ti-info-circle",title:"甘特圖顏色定義",summary:"一分鐘看懂各階段時程緊迫度",collapsed:true,bodyHtml:'<div class="bs-slackhelp-warn">須在開始與上市日期皆填齊時，各開發階段才會觸發以下顏色；只填單一日期，甘特圖維持預設單色。</div><div class="bs-slackrow"><span class="bs-slackdot bs-sd-green"></span><b>安全（綠）</b>：完工日較截止日（Deadline）提前 5 天以上。</div><div class="bs-slackrow"><span class="bs-slackdot bs-sd-yellow"></span><b>緊繃（黃）</b>：完工日已臨近截止日，緩衝時間僅剩 0～4 天。</div><div class="bs-slackrow"><span class="bs-slackdot bs-sd-red"></span><b>逾期（紅）</b>：預計完工日已超出設定之截止日。</div>'});return'<div class="bs-ganttsec"><div class="bs-sectitle">階段區間預覽</div>'+help+App._bsGanttHtml(comp)+App._bsFitHintHtml(comp)+"</div>"};App._bsGanttHtml=function(comp){const stages=comp.stages||[];if(!stages.length)return'<div class="bs-gantt-empty">尚無階段，按「＋ 新增階段」開始。</div>';const nums=stages.flatMap(s=>[Date.parse(s.start),Date.parse(s.end)]).filter(n=>!isNaN(n));const minN=nums.length?Math.min.apply(null,nums):0;const maxN=nums.length?Math.max.apply(null,nums):1;const span=maxN-minN||1;const fmtFull=(iso,forceYear)=>{if(!iso)return"";const p=String(iso).split("-");return forceYear?p[0].slice(2)+"/"+ +p[1]+"/"+ +p[2]:+p[1]+"/"+ +p[2]};let rows="";stages.forEach((s,i)=>{const a=Date.parse(s.start),b=Date.parse(s.end);const left=(a-minN)/span*100;const width=Math.max((b-a)/span*100,1.5);const forceYear=i===0||i===stages.length-1;rows+='<div class="s2-grow bs-grow"><span class="s2-gname bs-gname" title="'+U.esc(s.name||"")+'">'+U.esc(s.name||"")+'</span><span class="s2-gbar-track"><span class="s2-gbar s2-gbar-'+(s.light||"green")+'" style="left:'+left+"%;width:"+width+'%"></span></span><span class="s2-gdate bs-gdate">'+fmtFull(s.start,forceYear)+" → "+fmtFull(s.end,forceYear)+'<span class="bs-gwd">'+(s.wd||0)+" 工作天</span></span></div>"});const totWd=stages.reduce((n,s)=>n+(s.wd||0),0);const chip=comp.totalStart?'<div class="bs-gantt-total">總期程 '+fmtFull(comp.totalStart,true)+" → "+fmtFull(comp.totalEnd,true)+" · "+totWd+" 工作天</div>":"";return'<div class="bs-gantt">'+chip+'<div class="s2-gantt-axis bs-gantt-axis">'+rows+"</div></div>"};App._bsFitHintHtml=function(comp){const bs=App._bs;if(!bs)return"";if(comp.dir==="interval"){if(!comp.totalEnd||!bs.end)return"";if(comp.totalEnd<=bs.end){const slack=D.workdaysBetween(comp.totalEnd,bs.end)-1;return'<div class="bs-fit bs-fit-ok"><i class="ti ti-circle-check"></i>時程充足 · 餘裕 '+slack+" 個工作天</div>"}const over=D.workdaysBetween(bs.end,comp.totalEnd)-1;return'<div class="bs-fit bs-fit-over"><i class="ti ti-alert-triangle"></i>時程不足，差 '+over+" 個工作天（下一步將引導智慧排程面板優化）</div>"}if(comp.dir==="backward"){if(!comp.totalStart)return"";const p=String(comp.totalStart).split("-");return'<div class="bs-fit bs-fit-back"><i class="ti ti-arrow-narrow-left"></i>最晚 '+(p[0].slice(2)+"/"+ +p[1]+"/"+ +p[2])+" 就得開工，才趕得上上市。</div>"}return""};App._bsEcnBoxHtml=function(){const bs=App._bs;const card=type=>{const on=(bs.roiType||"benefit")===type;const cls="bs-roicard"+(on?" on":"");const check=on?'<span class="bs-rc-check">✓</span>':"";if(type==="benefit"){return'<div class="'+cls+`" onclick="App._bsSelectRoi('benefit')">`+check+'<div class="bs-rc-h">效益型·主動成本調降</div><div class="bs-rc-sec"><span class="bs-rc-lbl">適用情境</span>針對<b>換料、議價</b>等主動降低 BOM 成本之變更案。</div><div class="bs-rc-sec"><span class="bs-rc-lbl">系統結算模式</span>將啟用 ROI 計算，結算面板顯示「<b>回本期</b>」與「<b>N 年淨效益</b>」，評估每台成本降幅。</div></div>'}return'<div class="'+cls+`" onclick="App._bsSelectRoi('forced')">`+check+'<div class="bs-rc-h">被迫型·不可抗力</div><div class="bs-rc-sec"><span class="bs-rc-lbl">適用情境</span>針對料件<b>停產 (EOL)</b>、<b>法規安規變更</b>或設計品質修正之設變案。</div><div class="bs-rc-sec"><span class="bs-rc-lbl">系統結算模式</span>不適用回本計算，結算面板轉為評估「<b>合約／變更總代價</b>」與每台成本最小化損失。</div></div>'};return'<div class="bs-ecnbox"><div class="bs-ecnbox-hd"><i class="ti ti-replace"></i>設變（ECN）專屬欄位</div><div class="bs-field"><span class="bs-flabel">需求單號（選填）</span><input type="text" class="bs-ecn-src" value="'+U.esc(bs.sourceNo||"")+'" placeholder="觸發此案的客訴單或異常單號" onchange="App._bs.sourceNo=this.value"></div><div class="bs-field"><span class="bs-flabel">設變背景與原因（簡述 2～3 句即可）</span><textarea class="bs-ecn-reason" rows="2" placeholder="請描述變更背景，例：客戶反映壓縮機外殼異音，需增加緩衝墊片" onchange="App._bs.reason=this.value">'+U.esc(bs.reason||"")+'</textarea></div><div class="bs-field"><span class="bs-flabel">設變類型（點卡片選擇，決定結案怎麼算錢）</span><div class="bs-roicards">'+card("benefit")+card("forced")+"</div></div></div>"};App._bsSelectRoi=function(v){if(App._bs){App._bs.roiType=v}App._renderBsStage1()};App._bsSetName=function(v){if(App._bs)App._bs.name=v};App._bsSetDate=function(which,v){if(!App._bs)return;if(which==="end")App._bs.end=v;else App._bs.start=v;App._bsRefresh()};App._bsRenameStage=function(i,v){const s=App._bs&&App._bs.stages[i];if(s){s.name=v;App._bsRefresh()}};App._bsMonths=function(i,v){const s=App._bs&&App._bs.stages[i];if(!s)return;const n=parseFloat(v)||0;if(n<=0){s.months=0;if(s.driver==="months")s.driver=null}else{s.months=n;s.driver="months"}App._bsRefresh()};App._bsWd=function(i,v){const s=App._bs&&App._bs.stages[i];if(!s)return;const n=parseInt(v)||0;if(n<=0){s.wd=0;if(s.driver==="wd")s.driver=null}else{s.wd=n;s.driver="wd"}App._bsRefresh()};App._bsAddStage=function(){if(!App._bs)return;App._bs.stages.push({id:U.id(),name:"新階段",months:1,wd:0,driver:"months"});App._bsRefresh()};App._bsDelStage=function(i){if(!App._bs||App._bs.stages.length<=1)return;const s=App._bs.stages[i]||{};const tn=(s.tasks||[]).length;if(!tn){App._bs.stages.splice(i,1);App._bsRefresh();return}App.confirmModal({icon:"ti-alert-triangle",iconBg:"--rose-l",iconColor:"--rose-ink",okClass:"danger",title:"刪除階段「"+U.esc(s.name||"")+"」？",msg:"此階段底下有 <b>"+tn+" 個任務</b>會一併刪除、無法復原。",okText:"連任務一起刪",cancelText:"取消",onConfirm:()=>{App._bs.stages.splice(i,1);App._bsRefresh();U.toast("已刪除階段「"+(s.name||"")+"」（含 "+tn+" 任務）","success")}})};App._bsDragStart=function(e,i){App._bsDragIdx=i;if(e.dataTransfer)e.dataTransfer.effectAllowed="move";const row=e.target.closest(".bs-strow");if(row)row.classList.add("bs-dragging")};App._bsDragOver=function(e){e.preventDefault();if(e.dataTransfer)e.dataTransfer.dropEffect="move"};App._bsDrop=function(e,j){e.preventDefault();const i=App._bsDragIdx;if(i==null||i===j||!App._bs)return;const arr=App._bs.stages;const[moved]=arr.splice(i,1);arr.splice(j,0,moved);App._bsDragIdx=null;App._bsRefresh()};App._bsDragEnd=function(){App._bsDragIdx=null;document.querySelectorAll("#page-bs1 .bs-dragging").forEach(x=>x.classList.remove("bs-dragging"))};App._bsBack=function(){App.confirmModal({icon:"ti-alert-triangle",iconBg:"--amber-l",iconColor:"--amber-ink",title:"返回上一步？",msg:"本頁已填寫的資料（名稱、日期、階段、名冊）<b>將不會保留</b>，確定要返回？",okText:"返回上一步",cancelText:"留在本頁",onConfirm:function(){const tb=document.querySelector(".main > .topbar");if(tb)tb.classList.remove("topbar-hidden");App.showPage(App.currentPage||"workspace",null);App._flowBlankChoice()}})};App._bsNext=function(){const bs=App._bs;if(!bs||!(bs.name||"").trim()){U.toast("⚠ 請填專案名稱","warning");return}(bs.stages||[]).forEach(s=>{if(!s.tasks)s.tasks=[];if(!s.tasks.length)s.tasks.push({id:U.id(),name:"",who:"",dur:5,effort:100,so:null,eo:null});if(s.pmCoord===void 0)s.pmCoord=null});App._renderBsStage2()};App._bsTaskSeq=function(){const seq={};let n=0;(App._bs.stages||[]).forEach(s=>(s.tasks||[]).forEach(t=>{seq[t.id]=++n}));return seq};App._bsChainTasks=function(stageStartIso,tasks){const anchor=App._bsFirstWorkOnAfter(new Date(stageStartIso));const out=[];(tasks||[]).forEach((t,i)=>{let startW;if(t.so)startW=App._bsFirstWorkOnAfter(new Date(t.so));else if(i===0)startW=new Date(anchor);else startW=D.addWorkdays(new Date(out[i-1].endD),1);let endW,dur;if(t.eo){endW=App._bsLastWorkOnBefore(new Date(t.eo));if(endW<startW)endW=new Date(startW);dur=D.workdaysBetween(startW,endW)}else{dur=Math.max(1,parseInt(t.dur)||1);endW=D.addWorkdays(startW,dur-1)}out.push({start:D.fmt(startW,"iso"),end:D.fmt(endW,"iso"),dur,startD:startW,endD:endW})});return out};App._bsOwnerOptions=function(cur){let html='<option value=""'+(cur?"":" selected")+">未指派</option>";(App._tplDepts||[]).forEach(d=>{const nm=(d.name||"").trim();if(!nm)return;const members=(d.members||[]).map(m=>(m.name||"").trim()).filter(Boolean);if(!members.length)return;html+='<optgroup label="'+U.esc(nm)+'">'+members.map(m=>'<option value="'+U.esc(m)+'"'+(m===cur?" selected":"")+">"+U.esc(m)+"</option>").join("")+"</optgroup>"});return html};App._bsTaskNameList=function(){const t=App._bs&&App._bs.kind==="ecn"?tplEcn():tplNpi();const names=new Set;(t&&t.cases||[]).forEach(c=>(c.modules||[]).forEach(m=>(m.tasks||[]).forEach(tk=>{if(tk.name)names.add(tk.name)})));return[...names].map(n=>'<option value="'+U.esc(n)+'">').join("")};App._bsPmCoordOptions=function(cur){return'<option value=""'+(cur==null?" selected":"")+">— 選 PM 協調% —</option>"+App._EFFORT_TIERS.map(o=>'<option value="'+o.v+'"'+(o.v===cur?" selected":"")+">"+o.v+"% "+o.short+"</option>").join("")};App._renderBsStage2=function(){const bs=App._bs;if(!bs)return;const isEcn=bs.kind==="ecn";const accentCls=isEcn?"bs-ecn":"bs-npi";const guide=App.buildHintBox({key:"bs-s2-guide",icon:"ti-info-circle",title:"檢視任務（Stage 2）排程指引",summary:"點擊看基準線對比與連動邏輯",collapsed:true,bodyHtml:'<ol class="bs-tiplist bs-tiplist-ol"><li><b>基準線對比</b>：每個階段頂部皆顯示「湛藍色基準線」（含時間區間與總工作天），即使未排任務也會維持長條，方便您隨時掌握可排時程空間。</li><li><b>負責人依部門分組</b>：負責人下拉依部門（optgroup）分組，只在各部門標題下顯示該部門成員（依上一步名冊設定）。</li><li><b>智慧接續連動</b>：第二筆任務起，開始日預設自動接續「前一筆結束日 + 1 天」。若手動修改單筆日期（釘住），後續未釘住的任務將隨之整串連動。</li><li><b>負荷警示與投入級別</b>：投入%＝這件事吃掉負責人一天的幾成，六檔（100% 獨佔／75% 大半天／50% 半天／25% 零星盯／10% 點一下／0% 掛名未出力）。同人跨案單日疊加破百＝爆單，會在總覽 PM 負荷／部門負載轉紅示警。</li><li><b>PM 協調（每階段一格）</b>：代表 PM 對整個階段的跨部門協調負荷（撐滿階段跨度、非某張任務）。自訂階段吃不到範本標準值，請自行選一檔；此值會另計進 PM 負荷、不與任務投入% 重複。</li></ol>'});const comp=App._bsComputeStages();const seq=App._bsTaskSeq();const cards=bs.stages.map((s,si)=>App._bsStageCardHtml(s,si,comp.stages[si]||{},seq)).join("");const tb=document.querySelector(".main > .topbar");if(tb)tb.classList.add("topbar-hidden");document.querySelectorAll(".page").forEach(pg=>pg.classList.remove("active"));const page=document.getElementById("page-bs1");page.className="page active bs-s2page "+accentCls;page.innerHTML='<div class="bs-wrap"><div class="bs-pagehd"><div class="bs-crumb">總儀表板 <span class="bs-crumb-sep">/</span> '+(isEcn?"新增設變案":"新增專案")+' <span class="bs-crumb-sep">/</span> 檢視任務</div><span class="bs-typebadge">'+(isEcn?"ECN 設變":"NPI 開發")+'</span></div><div class="bs-title">檢視任務（Stage 2）</div>'+guide+'<div id="bs-s2cards">'+cards+'</div><div class="bs-foot"><button class="tb-action ghost" onclick="App._renderBsStage1()">上一步</button><button class="tb-action bs-next" onclick="App._bsCommit()">建立專案</button></div><datalist id="bs-tasknames">'+App._bsTaskNameList()+"</datalist></div>";window.scrollTo({top:0,behavior:"smooth"})};App._bsS2Refresh=function(){const c=document.getElementById("bs-s2cards");if(!c||!App._bs)return;const comp=App._bsComputeStages();const seq=App._bsTaskSeq();c.innerHTML=App._bs.stages.map((s,si)=>App._bsStageCardHtml(s,si,comp.stages[si]||{},seq)).join("")};App._bsStageCardHtml=function(stage,si,sComp,seq){const fmtFull=iso=>{if(!iso)return"";const p=String(iso).split("-");return p[0].slice(2)+"/"+ +p[1]+"/"+ +p[2]};const dated=App._bsChainTasks(sComp.start||(App._bs.start||D.fmt(new Date,"iso")),stage.tasks);const baseWd=sComp.wd||0;const k=stage.tasks?stage.tasks.length:0;const mLabel=sComp.months||sComp.approxMonths||0;const pill='<span class="bs-base-pill">基準線 '+mLabel+" 個月（"+baseWd+" 工作天）· "+(k?"已排 "+k+" 任務":"尚無任務")+"</span>";const pmSel='<span class="bs-pmcoord"><span class="bs-pm-lbl">PM 協調</span><select onchange="App._bsStagePmCoord('+si+',this.value)">'+App._bsPmCoordOptions(stage.pmCoord)+"</select></span>";const range=sComp.start&&sComp.end?fmtFull(sComp.start)+" → "+fmtFull(sComp.end):"";const head='<div class="bs-s2hd"><span class="bs-s2name">'+U.esc(stage.name||"")+"</span>"+(range?'<span class="bs-s2range">'+range+"</span>":"")+pmSel+pill+"</div>";const bars=App._bsTkBarsHtml(sComp,stage.tasks,dated);const rows=App._bsTaskRowsHtml(stage,si,dated,seq);const totWd=dated.reduce((n,d)=>n+(d.dur||0),0);const over=totWd-baseWd;const overHint=k&&over>0?'<div class="bs-s2over"><i class="ti ti-alert-triangle"></i>已排任務共 '+totWd+" 工作天，超過基準線 "+baseWd+" 天（超 "+over+" 天）… 後段時程將順延，不強制擋，你自己評估要不要壓。</div>":"";return'<div class="bs-s2card">'+head+bars+rows+'<button type="button" class="tb-action ghost bs-addtask" onclick="App._bsAddTask('+si+')">＋ 新增任務'+(k?"（接在最後一筆之後）":"（第一筆接階段起日）")+"</button>"+overHint+"</div>"};App._bsTkBarsHtml=function(sComp,tasks,dated){const bStart=Date.parse(sComp.start),bEnd=Date.parse(sComp.end);if(isNaN(bStart)||isNaN(bEnd))return"";const taskEnds=dated.map(d=>Date.parse(d.end)).filter(n=>!isNaN(n));const maxEnd=Math.max(bEnd,taskEnds.length?Math.max.apply(null,taskEnds):bEnd);const span=maxEnd-bStart||1;const baseW=(bEnd-bStart)/span*100;const baseline='<div class="bs-baseline" style="width:'+baseW+'%">基準線 '+(sComp.wd||0)+" 天</div>";let over=false;const rows=(tasks||[]).map((t,i)=>{const d=dated[i];const a=Date.parse(d.start),b=Date.parse(d.end);const left=(a-bStart)/span*100,width=Math.max((b-a)/span*100,1.5);const margin=d.end<=sComp.end?D.workdaysBetween(d.end,sComp.end)-1:-(D.workdaysBetween(sComp.end,d.end)-1);const light=margin>=5?"green":margin>=0?"yellow":"red";if(margin<0)over=true;return'<div class="bs-tkrow"><span class="bs-tkname" title="'+U.esc(t.name||"")+'">'+U.esc(t.name||"任務 "+(i+1))+'</span><span class="bs-tktrack"><span class="bs-tkbar bs-tkbar-'+light+(margin<0?" over":"")+'" style="left:'+left+"%;width:"+width+'%"></span></span></div>'}).join("");const baseend=over?'<div class="bs-baseend" style="left:'+baseW+'%"></div>':"";return'<div class="bs-tkbars">'+baseline+rows+baseend+"</div>"};App._bsTaskRowsHtml=function(stage,si,dated,seq){const tasks=stage.tasks||[];const head='<div class="bs-tkrowform bs-tkform-head"><span class="bs-tc-seq">序</span><span class="bs-tc-name">任務名稱</span><span class="bs-tc-d">開始日</span><span class="bs-tc-dur">工期（工作天）</span><span class="bs-tc-d">結束日</span><span class="bs-tc-who">負責人</span><span class="bs-tc-eff">投入%</span><span class="bs-tc-del"></span></div>';const body=tasks.map((t,ti)=>{const d=dated[ti]||{};const soPin=t.so?" bs-pinned":"";const eoPin=t.eo?" bs-pinned":"";return'<div class="bs-tkrowform"><span class="bs-tc-seq"><span class="bs-seqpill">'+(seq[t.id]||"")+'</span></span><span class="bs-tc-name"><input type="text" list="bs-tasknames" value="'+U.esc(t.name||"")+'" placeholder="任務名稱" onchange="App._bsTaskField('+si+","+ti+`,'name',this.value)"></span><span class="bs-tc-d"><input type="date" class="bs-tkdate`+soPin+'" value="'+U.esc(t.so||d.start||"")+'" onchange="App._bsTaskField('+si+","+ti+`,'so',this.value)"></span><span class="bs-tc-dur"><input type="number" min="1" step="1" value="`+(d.dur||t.dur||1)+'" onchange="App._bsTaskField('+si+","+ti+`,'dur',this.value)"></span><span class="bs-tc-d"><input type="date" class="bs-tkdate`+eoPin+'" value="'+U.esc(t.eo||d.end||"")+'" onchange="App._bsTaskField('+si+","+ti+`,'eo',this.value)"></span><span class="bs-tc-who"><select onchange="App._bsTaskField(`+si+","+ti+`,'who',this.value)">`+App._bsOwnerOptions(t.who||"")+'</select></span><span class="bs-tc-eff"><select onchange="App._bsTaskField('+si+","+ti+`,'effort',this.value)">`+App._ecnEffortOptions(t.effort!=null?t.effort:100)+'</select></span><span class="bs-tc-del"><button type="button" class="bs-delstage" title="刪除任務" onclick="App._bsDelTask('+si+","+ti+')"><i class="ti ti-x"></i></button></span></div>'}).join("");return'<div class="bs-tkform">'+head+body+"</div>"};App._bsAddTask=function(si){const s=App._bs&&App._bs.stages[si];if(!s)return;s.tasks=s.tasks||[];s.tasks.push({id:U.id(),name:"",who:"",dur:5,effort:100,so:null,eo:null});App._bsS2Refresh()};App._bsDelTask=function(si,ti){const s=App._bs&&App._bs.stages[si];if(!s||!s.tasks)return;s.tasks.splice(ti,1);App._bsS2Refresh()};App._bsTaskField=function(si,ti,field,val){const s=App._bs&&App._bs.stages[si];if(!s||!s.tasks||!s.tasks[ti])return;const t=s.tasks[ti];if(field==="name"){t.name=val;return}if(field==="who"){t.who=val;return}if(field==="effort"){t.effort=parseInt(val);return}if(field==="so"){t.so=val||null}else if(field==="eo"){t.eo=val||null}else if(field==="dur"){t.dur=Math.max(1,parseInt(val)||1);t.eo=null}App._bsS2Refresh()};App._bsStagePmCoord=function(si,val){const s=App._bs&&App._bs.stages[si];if(!s)return;s.pmCoord=val===""?null:parseInt(val)};App._bsCommit=function(){if(App._roGuard())return;const bs=App._bs;if(!bs)return;const name=(bs.name||"").trim();if(!name){U.toast("⚠ 請填專案名稱","warning");return}const isEcn=bs.kind==="ecn";if(isEcn&&!App._bsPmCoordAsked&&(bs.stages||[]).length&&(bs.stages||[]).every(s=>s.pmCoord==null||s.pmCoord<=0)){const _defEff=function(){const t=tplEcn();return t&&t.sizeMeta&&t.sizeMeta.M?t.sizeMeta.M.pmEffort:20}();App.confirmModal({icon:"ti-user-cog",iconBg:"--amber-l",iconColor:"--amber-ink",title:"尚未設定 PM 常駐協調",msg:"設變案建議每階段至少帶 PM 常駐（追蹤/文件/協調工時不落帳＝PM 負荷會被低估）。<br><br>要幫每個階段帶入建議值 <b>"+_defEff+"%</b> 嗎？（建立後仍可逐階段調整或上調）",okText:"帶入 "+_defEff+"% 建立",cancelText:"不帶，直接建立",onConfirm:()=>{(bs.stages||[]).forEach(s=>{s.pmCoord=_defEff});App._bsPmCoordAsked=true;App._bsCommit();App._bsPmCoordAsked=false},onCancel:()=>{App._bsPmCoordAsked=true;App._bsCommit();App._bsPmCoordAsked=false}});return}const comp=App._bsComputeStages();const dailyHours=DATA.settings&&DATA.settings.dailyHours||6;const depts=[];(App._tplDepts||[]).forEach(d=>{const nm=(d.name||"").trim();if(!nm)return;const members=(d.members||[]).map(m=>(m.name||"").trim()).filter(Boolean).map(x=>({id:U.id(),name:x}));if(!members.length)return;depts.push({id:U.id(),name:nm,members})});const ownerDept=who=>{const d=depts.find(dd=>(dd.members||[]).some(m=>m.name===who));return d?d.id:""};const isPmName=who=>{const d=depts.find(dd=>(dd.members||[]).some(m=>m.name===who));return!!(d&&/PM/i.test(d.name))};const pmDept=depts.find(d=>/PM/i.test(d.name));const proj={id:U.id(),name,color:bs.color||PROJ_COLORS[0],note:"",synced:false,createdAt:new Date().toISOString(),targetUnitCostBase:0,unitSellPriceBase:0,targetUnitCost:0,unitSellPrice:0};ensurePdcaData(proj);const vId=U.id();const variant={id:vId,name,schedule:{startDate:bs.start||"",endDate:bs.end||"",direction:comp.dir},stages:bs.stages.map(s=>s.name),stageCoord:{}};bs.stages.forEach(s=>{if(s.pmCoord!=null)variant.stageCoord[s.name]=s.pmCoord});proj.depts=depts;proj.variants=[variant];const tasks=[];const mkTask=o=>Object.assign({id:U.id(),project:proj.id,wbs:0,parentWbsId:"",name:"",desc:"",category:"deep",taskType:"任務",predecessor:"",durationDays:1,owner:"",dept:"",role:"",variant:vId,start:"",end:"",plannedStart:"",plannedEnd:"",actualStart:"",actualEnd:"",progress:0,status:"pending",urgency:"med",estHours:4,method:"",canSplit:false,completedAt:null,createdAt:new Date().toISOString(),scheduledStart:"",scheduledEnd:"",synced:false,stage:"",subgroup:"",mustDeliver:false,deliverableType:"",requiredTask:true,mustIssue:false,deliverable:"",riskIssue:"",delivered:"",deliverableLink:"",note:"",effortRatio:100,taskAttr:"baseline"},o);let seqN=0;bs.stages.forEach((s,si)=>{const dated=App._bsChainTasks((comp.stages[si]||{}).start||bs.start,s.tasks);(s.tasks||[]).forEach((t,ti)=>{const d=dated[ti];const who=(t.who||"").trim();seqN++;const eff=t.effort!=null?t.effort:100;tasks.push(mkTask({wbs:seqN,name:(t.name||"").trim()||"任務 "+seqN,desc:s.name+" / ",durationDays:d.dur,owner:who,dept:ownerDept(who),role:isPmName(who)?"PM":"",plannedStart:d.start,plannedEnd:d.end,stage:s.name,effortRatio:eff,estHours:Math.round(d.dur*dailyHours*(eff/100)*10)/10}))});if((s.tasks||[]).some(t=>isPmName((t.who||"").trim()))){}else if(s.pmCoord!=null&&s.pmCoord>0){const sc=comp.stages[si]||{};const span=Math.max(sc.wd||0,1);tasks.push(mkTask({wbs:0,name:"PM 階段協調（常駐）",desc:s.name+" / PM 協調",durationDays:span,dept:pmDept?pmDept.id:"",role:"PM",plannedStart:sc.start,plannedEnd:sc.end,stage:s.name,effortRatio:s.pmCoord,estHours:Math.round(span*dailyHours*(s.pmCoord/100)*10)/10,isPmCoord:true}))}});if(isEcn){Object.assign(proj,{ecnType:true,size:"M",changeReason:bs.reason||"",roiType:bs.roiType||"benefit",sourceNo:bs.sourceNo||"",ecnNo:"",targetSavePerUnit:0,status:"active",loopCount:0,scopeGrowthCount:0,reopenCount:0});proj.baselineHours=Math.round(tasks.reduce((a,t)=>a+(t.effortRatio!=null?t.effortRatio:100)/100*dailyHours*(t.durationDays||0),0)*10)/10;proj.ecnEvents=[{type:"open",date:D.fmt(new Date,"iso"),label:"開案（空白設變）",cause:""}]}const dup=DATA.projects.filter(p=>p.name===proj.name&&!!p.ecnType===!!proj.ecnType);const unassigned=tasks.filter(t=>!t.owner&&!t.isPmCoord).length;const _commit=()=>{const tb=document.querySelector(".main > .topbar");if(tb)tb.classList.remove("topbar-hidden");proj.version=dup.length?Math.max(...dup.map(p=>p.version||1))+1:1;proj.importedAt=D.fmt(new Date,"iso");DATA.projects.push(proj);tasks.forEach(t=>DATA.tasks.push(t));App.currentProjectId=proj.id;Store.projects.save();Store.tasks.save();App._bs=null;App._createFlow=null;App.refreshAll();U.toast("✓ 已建立 "+tasks.length+" 筆任務","success");App.showPage("project",null)};const _checkDup=()=>{if(dup.length)App.confirmModal({icon:"ti-copy",iconBg:"--amber-l",iconColor:"--amber-ink",title:"已有 "+dup.length+" 個同名專案「"+proj.name+"」",msg:"要建立新版本嗎？兩者並存，可在側邊欄辨識版號。",okText:"建立新版本",cancelText:"返回修改",onConfirm:_commit});else _commit()};if(unassigned>0)App.confirmModal({icon:"ti-alert-triangle",iconBg:"--amber-l",iconColor:"--amber-ink",title:"還有 "+unassigned+" 個任務未指派負責人",msg:"確定建立？",okText:"確定建立",cancelText:"取消",onConfirm:_checkDup});else _checkDup()};
