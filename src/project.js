/*! PM-Core (c) 2026 Paul Hsu. All Rights Reserved. Proprietary — see LICENSE. */
App.buildProjectHeaderHtml=function(){const proj=this.getProj(this.currentProjectId);if(!proj)return"";const prevNames={home:"首頁",workspace:"個人工作台",portfolio:"全專案總覽",materials:"物料分析",gantt:"跨專案時程",reportgen:"報表產出",transcripts:"會議逐字稿",settings:"設定",archive:"專案檔案室"};const back=this._prevPage&&this._prevPage!=="project"&&prevNames[this._prevPage]?`<button class="tb-action ghost proj-back" onclick="App.showPage('${this._prevPage}')" title="回到剛才的頁面繼續看其他專案">← 回${prevNames[this._prevPage]}</button>`:"";return`<div class="proj-header">
        ${back}
        <div style="flex:1; min-width:0;">
          <div class="proj-name">
            ${U.esc(proj.name)}
          </div>
        </div>
        <span id="projExportSlot" style="display:contents">${this._projExportBtnHtml(proj)}</span>
        <button class="tb-action ghost" data-edit onclick="App.editProject('${proj.id}')">編輯專案</button>
        <span class="hdr-menu-wrap">
          <button class="tb-action ghost hdr-menu-toggle" data-edit onclick="App.toggleMoreMenu(event, '${proj.id}')">⋯</button>
          <div class="hdr-menu hdr-menu-right" id="hdrMoreMenu">
            <button class="hdr-menu-item hdr-menu-danger" onclick="App.openWbsImport('${proj.id}'); App.closeHdrMenus();">覆蓋匯入<span class="hdr-menu-note hdr-menu-danger-note">危險</span></button>
          </div>
        </span>
      </div>`};App._projExportBtnHtml=function(proj){const view=this.projectView,pid=proj.id;const dl='<i class="ti ti-download" style="font-size:15px; vertical-align:-2px; margin-right:5px; color:var(--sage-600);"></i>';const wrap=(label,body)=>`<span class="hdr-menu-wrap"><button class="tb-action ghost hdr-menu-toggle" data-edit-hide onclick="App.toggleExportMenu(event, '`+pid+`')">`+dl+label+' ▾</button><div class="hdr-menu hdr-menu-right hdr-menu-export" id="hdrExportMenu">'+body+'</div></span><span class="hdr-divider"></span>';const rt=o=>'<span class="hdr-menu-rt"><i class="ti '+o.icon+'"></i>'+o.title+(o.tag?'<span class="hdr-menu-rtag">'+o.tag+"</span>":"")+"</span>";const item=o=>o.enabled?'<button class="hdr-menu-item rich" onclick="'+o.onclick+' App.closeHdrMenus();">'+rt(o)+'<span class="hdr-menu-rr">結果：'+o.result+'</span><span class="hdr-menu-rc">情境：'+o.scene+"</span></button>":'<div class="hdr-menu-item rich disabled">'+rt(o)+'<span class="hdr-menu-rr">結果：'+o.result+'</span><span class="hdr-menu-rc">情境：'+o.scene+'</span><span class="hdr-menu-hint">＊'+o.disabledHint+"</span></div>";if(view==="dashboard"){return`<button class="tb-action ghost" data-edit-hide onclick="App.exportProjectWbs('`+pid+`', null, 'data');">`+dl+'匯出任務清單</button><span class="hdr-divider"></span>'}if(view==="gantt"){const sch=(g,icon,title,tag,coarse,scene)=>item({enabled:true,icon,onclick:"App.exportProjectWbs('"+pid+"','"+g+"','gantt');",title,tag,result:"甘特圖分頁（"+coarse+"）",scene});return wrap("匯出甘特圖",'<div class="hdr-menu-title">匯出甘特圖</div><div class="hdr-menu-sub">只含甘特分頁（任務清單／專案資訊改在「儀表板」頁匯出）</div>'+sch("week","ti-calendar-week","週刻度","最常用","每週一欄","例行週會報告，分析預期與實際進度差異")+sch("day","ti-calendar","日刻度","","每日一欄，最細","專案短期衝刺、細部排程檢視")+sch("month","ti-calendar-stats","月刻度","","每月一欄，最粗","跨月長專案評估，高階主管"))}if(view==="bom"){const models=proj.bomModels||[];const hasModels=models.length>0;const bs=proj.bomSheets;const hasSingle=!!(bs&&bs.new&&bs.old);return wrap("匯出成本差異報告",'<div class="hdr-menu-title">匯出成本差異報告</div><div class="hdr-menu-sub">系統不留存 Excel，按即時資料產生</div>'+item({enabled:hasModels,icon:"ti-table",onclick:"App.exportBomModels(App.getProj('"+pid+"'));",title:"跨機種成本比較報告",tag:"全型號·分析報表",result:"全系列型號的「成本價差」與「降幅成效總表」",scene:"主管開會、採購總覽全系列「省了多少錢」時",disabledHint:"先在上方上傳新舊 BOM、按「全機種自動比對」"})+item({enabled:hasSingle,icon:"ti-file-invoice",onclick:"App._bomExportWizard('"+pid+"');",title:"單一機種設變效益分析",tag:"當前型號·含試算公式",result:"當前型號的「設變回本期」與「損耗效益估算表（帶 Excel 公式）」",scene:"需要精算「特定這台冷氣」改了之後多久能回本時",disabledHint:"先在上方上傳新舊 BOM 並比對（選一個機種）"})+item({enabled:hasModels,icon:"ti-list-details",onclick:"App.exportBomModelsFullBom(App.getProj('"+pid+"'));",title:"各機種完整新 BOM 料表",tag:"全型號·純生產料表",result:"變更後的「全新完整乾淨料表」（非數據分析表）",scene:"工程師、採購需要拿去「直接建檔或回填 ERP」時",disabledHint:"先在上方上傳新舊 BOM、按「全機種自動比對」"}))}return""};App.toggleExportMenu=function(ev,projId){ev.stopPropagation();this._ensureHdrMenuClose();const m=document.getElementById("hdrExportMenu");const open=m&&m.classList.contains("open");this.closeHdrMenus();if(m&&!open)m.classList.add("open")};App.toggleMoreMenu=function(ev,projId){ev.stopPropagation();this._ensureHdrMenuClose();const m=document.getElementById("hdrMoreMenu");const open=m&&m.classList.contains("open");this.closeHdrMenus();if(m&&!open)m.classList.add("open")};App.closeHdrMenus=function(){document.querySelectorAll(".hdr-menu.open").forEach(m=>m.classList.remove("open"))};App._ensureHdrMenuClose=function(){if(this._hdrMenuCloseBound)return;this._hdrMenuCloseBound=true;document.addEventListener("click",e=>{if(!e.target.closest(".hdr-menu"))App.closeHdrMenus()})};App.renderProject=function(){if(!this.currentProjectId){if(DATA.projects.length>0){this.currentProjectId=DATA.projects[0].id}else{document.getElementById("page-project").innerHTML='<div class="empty-task-list"><div class="empty-task-list-icon">📁</div>請先建立專案</div>';return}}const proj=this.getProj(this.currentProjectId);if(!proj){document.getElementById("page-project").innerHTML='<div class="empty-task-list">專案不存在</div>';return}document.getElementById("page-project").classList.toggle("proj-npi",!proj.ecnType);if(proj.ecnType)return App.renderEcnDashboard(proj);App._s2EcnMode=null;const _body=this.projectView==="list"?this.buildProjectTaskListHtml(proj):this.renderProjectDashboard(proj);const html=`
    ${this.buildProjectHeaderHtml()}
    <div class="view-tabs-bar">${this.buildProjectViewTabsHtml()}</div>

    ${_body}
  `;document.getElementById("page-project").innerHTML=html;if(this.projectView!=="list"){App._maybePromptUnscheduled(proj);App._maybePromptPmCoord(proj)}};App._maybePromptUnscheduled=function(proj){if(!proj||proj.ecnType)return;this._unschedPrompted=this._unschedPrompted||{};if(this._unschedPrompted[proj.id])return;const ts=DATA.tasks.filter(t=>t.project===proj.id&&!t._deleted&&!t.isPmCoord);const bad=ts.filter(t=>!t.plannedStart&&!t.actualStart);if(!bad.length)return;this._unschedPrompted[proj.id]=true;App.confirmModal({title:"有 "+bad.length+" 項任務排不進時程",icon:"ti-alert-triangle",iconBg:"--amber-l",iconColor:"--amber-ink",msg:'<div style="text-align:left;font-size:12.5px;line-height:1.6;color:var(--ink2);">本案有 <b>'+bad.length+"</b> 項任務算不出計畫日期（多為前置指向已刪除的任務、或循環依賴）。<br>建議到<b>設定 → 範本</b>把該範本的前置修好後<b>重新開案</b>，或在本案甘特圖／任務大表逐筆調整前置。</div>",okText:"我知道了",cancelText:null})};App.renderProjectDashboard=function(proj){return`    ${this.buildProjKpiHtml(proj)}

    <div class="proj-dash-grid">
      ${this.buildProjStagesHtml(proj)}
      <div class="proj-load-col">${this.buildProjLoadCol(proj)}</div>
    </div>
`};App.buildProjectTaskListHtml=function(proj){const allTasks=this.getTasksOf(proj.id);const today=D.today();const ordered=this.orderedProjectTasks(proj.id);const taskFilter=this.getTaskFilter(proj.id);const q=(this._taskSearch&&this._taskSearch[proj.id]||"").trim();const npiTabs=this._npiVariantTabs(proj);const activeVid=npiTabs?this._npiActiveVariant(proj):null;const hasFilter=["stages","owners","urg","status"].some(k=>taskFilter[k]&&taskFilter[k].size>0)||!!q;let filtered=applyTaskFilter(ordered,taskFilter);if(npiTabs){const firstId=npiTabs[0].id;filtered=filtered.filter(t=>{const tv=t.variant||"";return activeVid===firstId?tv===firstId||tv==="":tv===activeVid})}if(q){const ql=q.toLowerCase();filtered=filtered.filter(t=>(t.name||"").toLowerCase().includes(ql)||(t.owner||"").toLowerCase().includes(ql))}const activeCount=filtered.filter(t=>t.status!=="done").length;const doneCount=filtered.length-activeCount;const deletedTasks=allTasks.filter(t=>t._deleted).sort((a,b)=>(b._deletedAt||"").localeCompare(a._deletedAt||""));const PREVIEW_ACTIVE_LIMIT=15;this._projectExpanded=this._projectExpanded||{};const isExpanded=!!this._projectExpanded[proj.id];let activeSeen=0,cutIdx=filtered.length-1;for(let p=0;p<filtered.length;p++){if(filtered[p].status!=="done"){activeSeen++;if(activeSeen===PREVIEW_ACTIVE_LIMIT){cutIdx=p;break}}}const overflow=activeCount>PREVIEW_ACTIVE_LIMIT;const showAll=hasFilter||isExpanded||!overflow;const visible=showAll?filtered:filtered.slice(0,cutIdx+1);this._doneVisible=this._doneVisible||{};const doneVisible=!!this._doneVisible[proj.id];this._toScheduleVisible=this._toScheduleVisible||{};const toScheduleVisible=this._toScheduleVisible[proj.id]!==false;const firstUndated=visible.findIndex(t=>getEffectiveSchedule(t).start==="");const tsCollapsed=toScheduleVisible?"":"collapsed";const bulk=!!(this._bulkMode||{})[proj.id];const COLS=bulk?11:10;let activeListInner;if(visible.length===0){activeListInner=hasFilter?`<tr class="empty-task-list bar-row"><td colspan="${COLS}"><div class="empty-task-list-icon">🔍</div>無符合篩選條件的任務</td></tr>`:`<tr class="empty-task-list bar-row"><td colspan="${COLS}"><div class="empty-task-list-icon">📝</div>尚無待辦任務</td></tr>`}else if(firstUndated<0){activeListInner=visible.map(t=>this.buildTaskRowHtml(t)).join("")}else{const datedRows=visible.slice(0,firstUndated).map(t=>this.buildTaskRowHtml(t)).join("");const undatedRows=visible.slice(firstUndated).map(t=>this.buildTaskRowHtml(t,"undated")).join("");const undatedCount=visible.length-firstUndated;activeListInner=datedRows+`<tr class="toschedule-bar bar-row ${tsCollapsed}" onclick="App.toggleToScheduleVisible('${proj.id}')"><td colspan="${COLS}"><div class="bar-inner">
            <span class="done-head-chevron">▼</span>
            <span class="done-head-title">待排</span>
            <span class="done-head-count">${undatedCount}</span>
            <span class="done-toggle-note">${toScheduleVisible?"未填開始日（補開始日或前置即排入）":"已收合"}</span>
          </div></td></tr>`+undatedRows}return`
    <div class="proj-grid">
      <div>
        <!-- Active tasks -->
        <div class="task-list-card">
          <div class="tlc-head">
            <span class="tlc-title">待辦任務</span>
            <span class="tlc-count">${activeCount}</span>
            <button class="tb-action" onclick="App.toggleBulkEdit('${proj.id}')" style="margin-left:auto;">${bulk?"完成批量編輯":"☑ 批量編輯"}</button>
            <button class="tb-action" onclick="App.openNewTaskDialog('${proj.id}')">＋ 新增任務</button>
          </div>
          ${this._npiVariantTabsHtml(proj)}
          ${this.buildTaskFilterBar(proj.id)}
          ${bulk?this._bulkBarHtml(proj):""}
          <!-- 第二刀-A 已接線：applyTaskFilter(ordered, getTaskFilter) 四 Set 過濾 → filtered，下游 counts／預覽／visible 全吃 filtered。 -->
          <!-- subgrid 步2：單一 .task-grid 父，header/done-bar/各列直屬，欄軌共用自動算；hide-done/ts-collapsed 摺疊 class 烤在父上。 -->
          <table id="activeTaskList" class="data-table task-table${doneVisible?"":" hide-done"}${toScheduleVisible?"":" ts-collapsed"}">
            <thead>
              <tr class="task-row-header">
                ${bulk?`<th class="col-chk"><input type="checkbox" onclick="event.stopPropagation()" onchange="App._bulkSelectAll('${proj.id}', this.checked)" data-tip="全選|勾選目前顯示的全部未完成任務"></th>`:""}
                <th class="col-num">序</th>
                <th class="col-mid">階段</th>
                <th class="col-flex">任務</th>
                <th class="col-mid">進度%</th>
                <th class="col-mid">負責人</th>
                <th class="col-mid">前置任務</th>
                <th class="col-num">狀態</th>
                <th class="col-mid">預計時程（開始→結束）</th>
                <th class="col-num">餘裕（天）</th>
                <th class="col-num">截止日</th>
              </tr>
            </thead>
            <tbody>
            ${doneCount>0?`
            <tr class="done-toggle-bar bar-row ${doneVisible?"":"collapsed"}" onclick="App.toggleDoneVisible('${proj.id}')"><td colspan="${COLS}"><div class="bar-inner">
              <span class="done-head-chevron">▼</span>
              <span class="done-head-title">已完成</span>
              <span class="done-head-count">${doneCount}</span>
              <span class="done-toggle-note">${doneVisible?"原位顯示（灰字刪除線）":"已收合"}</span>
            </div></td></tr>`:""}
            ${activeListInner}
            </tbody>
          </table>
          ${!showAll?`
          <div style="padding:10px 16px; border-top:1px solid var(--rule); text-align:center; background:var(--surface2);">
            <button class="tb-action ghost" onclick="App.toggleProjectExpanded('${proj.id}')" style="font-size:11.5px; padding:5px 14px;">
              展開全部（還有 ${activeCount-PREVIEW_ACTIVE_LIMIT} 筆）▼
            </button>
          </div>`:isExpanded&&overflow?`
          <div style="padding:10px 16px; border-top:1px solid var(--rule); text-align:center; background:var(--surface2);">
            <button class="tb-action ghost" onclick="App.toggleProjectExpanded('${proj.id}')" style="font-size:11.5px; padding:5px 14px;">
              收起（只顯示前 ${PREVIEW_ACTIVE_LIMIT} 個未完成）▲
            </button>
          </div>`:""}
          <div class="list-foot">
            <input id="quickAddTask" placeholder="＋ 快速新增任務（按 Enter 完成）" data-edit
                   onkeydown="if(event.key==='Enter') App.quickAddTask('${proj.id}', this)">
            <button data-edit onclick="App.quickAddTask('${proj.id}', document.getElementById('quickAddTask'))">新增</button>
          </div>
        </div>


        ${deletedTasks.length>0?`
        <div class="done-section deleted-section collapsed" id="deletedSection">
          <div class="done-head" onclick="document.getElementById('deletedSection').classList.toggle('collapsed')">
            <span class="done-head-title">🗑 已刪除</span>
            <span class="done-head-count" style="background:var(--terracotta-l); color:var(--terracotta);">${deletedTasks.length}</span>
            <span class="done-head-chevron">▼</span>
          </div>
          <div class="done-list">
            ${deletedTasks.map(t=>`<div class="deleted-row" style="display:flex; align-items:center; gap:10px; padding:9px 14px; border-bottom:1px solid var(--rule);">
              <div style="flex:1; min-width:0;">
                <div style="font-size:12.5px; text-decoration:line-through; color:var(--ink3);">${U.esc(t.name)}</div>
                <div style="font-size:10.5px; color:var(--ink4); margin-top:2px;">刪除於 ${t._deletedAt?D.fmt(t._deletedAt,"ymd"):"—"}</div>
              </div>
              <button class="tb-action ghost" onclick="App.restoreTask('${t.id}')" style="font-size:10.5px; padding:3px 10px; color:var(--sage-700);">↺ 還原</button>
              <button class="tb-action ghost" onclick="App.permanentDeleteTask('${t.id}')" style="font-size:10.5px; padding:3px 10px; color:var(--terracotta);">永久刪除</button>
            </div>`).join("")}
          </div>
          <div class="done-clear-tip">
            💡 已刪除任務保留 14 天，過期自動清除
          </div>
        </div>`:""}
      </div>
    </div>
`};App.getTaskFilter=function(projId){this._taskFilter=this._taskFilter||{};if(!this._taskFilter[projId])this._taskFilter[projId]={stages:new Set,owners:new Set,urg:new Set,status:new Set};return this._taskFilter[projId]};App.taskOwnerOptions=function(projectId){const set=new Set;(DATA.tasks||[]).forEach(t=>{if(t.project===projectId&&!t._deleted&&typeof t.owner==="string"&&t.owner.trim())set.add(t.owner.trim())});return[...set].sort((a,b)=>a.localeCompare(b,"zh-Hant"))};App._npiVariantTabs=function(proj){const vs=proj.variants||[];return vs.length<2?null:vs};App._npiActiveVariant=function(proj){const vs=this._npiVariantTabs(proj);if(!vs)return null;this._npiVariantTab=this._npiVariantTab||{};const cur=this._npiVariantTab[proj.id];return cur&&vs.some(v=>v.id===cur)?cur:vs[0].id};App._npiVariantTabsHtml=function(proj){const vs=this._npiVariantTabs(proj);if(!vs)return"";const active=this._npiActiveVariant(proj);return'<div class="npi-vtabs">'+vs.map((v,i)=>'<button class="npi-vtab'+(v.id===active?" on":"")+`" onclick="App._npiSetVariantTab('`+proj.id+"', '"+v.id+`')">`+(i===0?'<i class="ti ti-star" aria-hidden="true"></i> 主案 · ':"")+U.esc(v.name||"案別"+(i+1))+"</button>").join("")+"</div>"};App._npiSetVariantTab=function(projId,vid){this._npiVariantTab=this._npiVariantTab||{};this._npiVariantTab[projId]=vid;this.renderProject()};App.buildTaskFilterBar=function(projId){const seenStage=new Set;const stageOpts=this.getProjectStages(projId).filter(s=>{if(seenStage.has(s.name))return false;seenStage.add(s.name);return true}).map(s=>({value:s.name,label:s.name}));const ownerOpts=this.taskOwnerOptions(projId).map(o=>({value:o,label:o}));const urgOpts=["high","medium","low"].map(v=>({value:v,label:URGENCY_LABELS_ZH[v]}));const statusOpts=["pending","wip","done","hold"].map(v=>({value:v,label:STATUS_LABELS_ZH[v]}));return`<div class="task-filter-bar">
    ${this.buildTaskFilterChip(projId,"stages","階段",stageOpts)}
    ${this.buildTaskFilterChip(projId,"owners","負責人",ownerOpts)}
    ${this.buildTaskFilterChip(projId,"urg","緊急程度",urgOpts)}
    ${this.buildTaskFilterChip(projId,"status","狀態",statusOpts)}
    <div class="tf-search"><i class="ti ti-search" aria-hidden="true"></i><input id="taskSearchInput" placeholder="搜尋任務…" value="${U.esc(this._taskSearch&&this._taskSearch[projId]||"")}" oninput="App.setTaskSearch('${projId}', this.value)"></div>
    <button class="tf-clear-all" onclick="App.clearTaskFilter('${projId}')">全部清除</button>
  </div>`};App.buildTaskFilterChip=function(projId,key,label,options){const sel=this.getTaskFilter(projId)[key];const selArr=[...sel];const chipText=selArr.length===0?label:selArr[0]+(selArr.length>1?` +${selArr.length-1}`:"");const boxes=options.length?options.map(o=>`<label class="tf-opt${sel.has(o.value)?" on":""}">
      <input type="checkbox" value="${U.esc(o.value)}"${sel.has(o.value)?" checked":""}
        onchange="App.toggleTaskFilterOpt('${projId}','${key}',this.value,this.checked)">
      <span>${U.esc(o.label)}</span>
    </label>`).join(""):'<div class="tf-empty">無選項</div>';return`<div class="tf-chip-wrap" data-key="${key}">
    <button class="tf-chip tf-${key}${selArr.length?" active":""}" data-label="${U.esc(label)}"
      onclick="App.toggleTaskFilterPanel('${projId}','${key}')">
      <span class="tf-chip-label">${U.esc(chipText)}</span><span class="tf-caret">▾</span>
    </button>
    <div class="tf-panel">
      <div class="tf-opts">${boxes}</div>
      <div class="tf-panel-foot">
        <button onclick="App.clearTaskFilterKey('${projId}','${key}')">清除</button>
        <button onclick="App.toggleTaskFilterPanel('${projId}','${key}')">套用</button>
      </div>
    </div>
  </div>`};App.toggleTaskFilterPanel=function(projId,key){const wrap=document.querySelector(`.tf-chip-wrap[data-key="${key}"]`);if(!wrap)return;const willOpen=!wrap.classList.contains("open");document.querySelectorAll(".tf-chip-wrap.open").forEach(w=>w.classList.remove("open"));if(willOpen)wrap.classList.add("open");else this.renderProject()};App.toggleTaskFilterOpt=function(projId,key,value,checked){const sel=this.getTaskFilter(projId)[key];checked?sel.add(value):sel.delete(value);const wrap=document.querySelector(`.tf-chip-wrap[data-key="${key}"]`);if(!wrap)return;wrap.querySelectorAll(".tf-opt").forEach(l=>l.classList.toggle("on",l.querySelector("input").checked));const chip=wrap.querySelector(".tf-chip");const selArr=[...sel];chip.classList.toggle("active",selArr.length>0);wrap.querySelector(".tf-chip-label").textContent=selArr.length===0?chip.dataset.label:selArr[0]+(selArr.length>1?` +${selArr.length-1}`:"")};App.clearTaskFilterKey=function(projId,key){this.getTaskFilter(projId)[key].clear();const wrap=document.querySelector(`.tf-chip-wrap[data-key="${key}"]`);if(wrap)wrap.querySelectorAll("input[type=checkbox]").forEach(cb=>{cb.checked=false;cb.dispatchEvent(new Event("change"))});this.renderProject()};App.clearTaskFilter=function(projId){const f=this.getTaskFilter(projId);["stages","owners","urg","status"].forEach(k=>f[k].clear());if(this._taskSearch)this._taskSearch[projId]="";document.querySelectorAll(".task-filter-bar input[type=checkbox]").forEach(cb=>{cb.checked=false;cb.dispatchEvent(new Event("change"))});this.renderProject()};App.setTaskSearch=function(projId,val){this._taskSearch=this._taskSearch||{};this._taskSearch[projId]=val;this.renderProject();const el=document.getElementById("taskSearchInput");if(el){el.focus();const n=el.value.length;if(el.setSelectionRange)el.setSelectionRange(n,n)}};App.buildProjKpiHtml=function(proj){const tasks=DATA.tasks.filter(t=>t.project===proj.id&&!t._deleted&&t.taskType!=="group");const total=tasks.length;const today=D.today();const done=tasks.filter(t=>isTaskDone(t)).length;const donePct=total>0?Math.round(done/total*100):null;const wip=tasks.filter(t=>t.status==="wip").length;let delayed=0,noEnd=0;tasks.forEach(t=>{if(t.status==="done"||t.status==="hold")return;if(isTaskDelayed(t,today)){delayed++;return}if(!getEffectiveSchedule(t).end)noEnd++});const overall=total>0?weightedProgress(tasks):null;const targetDate=proj.pdcaData&&proj.pdcaData.targetDate||"";let endDate=targetDate;if(!endDate){tasks.forEach(t=>{const e=getEffectiveSchedule(t).end;if(e&&e>endDate)endDate=e})}let wdLeft=null,overdueWd=0;if(endDate){if(D.daysBetween(today,endDate)<0){wdLeft=0;overdueWd=Math.max(0,D.workdaysBetween(endDate,today)-1)}else wdLeft=D.workdaysBetween(today,endDate)}const card=(label,value,sub,dataTip,warn,stack,tone)=>`
    <div class="stat${warn?" kpi-warn":""}${stack&&sub?" kpi-stack":""}${tone?" "+tone:""}"${dataTip?` data-tip="${U.esc(dataTip)}"`:""}>
      <div class="stat-num">${value}</div>
      <div class="stat-label">${label}${sub&&!stack?` <span class="stat-pct">${sub}</span>`:""}</div>
      ${stack&&sub?`<div class="stat-sub">${sub}</div>`:""}
    </div>`;return`<div class="stats-row proj-kpi">
    ${card("任務總數",total,"","任務總數|這個專案的所有工作項目數(不含已刪除)",false,false,"kpi-tone-task")}
    ${card("已完成",done,donePct===null?"—":donePct+"%","完成件數|已完成的工作項目數|完成% = 已完成 ÷ 任務總數",false,true,"kpi-tone-done")}
    ${card("進行中",wip,"","進行中|正在進行、還沒完成的項目數",false,false,"kpi-tone-wip")}
    ${card("延誤",delayed,noEnd>0?`另${noEnd}件無日期`:"","延遲件數|已過結束日但還沒完成的項目數|(暫停的不算;沒設日期的另計)",delayed>0,false,"kpi-tone-delayed")}
    ${card("整體進度",overall===null?"—":overall+"%","","整體完成度|按工作量加權(工期×每日投入程度),工作量大的任務影響較大",false,false,"kpi-tone-overall")}
    ${card("剩餘工作天",wdLeft===null?"—":wdLeft,wdLeft===null?"未設定":overdueWd>0?`已逾期${overdueWd}工作日`:`至${endDate}`,"剩餘工作天|到專案結束日還剩幾個上班日(不含週末假日)",overdueWd>0,false,"kpi-tone-days")}
  </div>`};App.buildProjStagesHtml=function(proj){const stages=this.getProjectStages(proj.id);if(stages.length===0||stages.length===1&&stages[0].name==="未分階段"){return`<div class="proj-stages-card">
      <div class="proj-stages-head">階段進度</div>
      <div class="proj-stages-empty">此專案未分階段(同步專案會自動帶入 PLM 階段)</div>
    </div>`}const stageOf=t=>typeof t.stage==="string"&&t.stage.trim()?t.stage.trim():"未分階段";const tasks=DATA.tasks.filter(t=>t.project===proj.id&&!t._deleted&&!t.isPmCoord);const rangeStr=(s,e)=>{const sLine=s?`<span class="stage-date-lbl">起</span> ${D.fmt(s,"ymd")}`:'<span class="stage-date-lbl">起</span> –';const eLine=e?`<span class="stage-date-lbl">迄</span> ${D.fmt(e,"ymd")}`:'<span class="stage-date-lbl">迄</span> –';return`${sLine}<br>${eLine}`};const rowHtml=st=>{const ts=tasks.filter(t=>stageOf(t)===st.name&&(t.variant||null)===st.variantId);const pct=ts.length>0?Math.round(ts.reduce((s,t)=>s+taskDisplayProgress(t),0)/ts.length):null;const tier=pct===null?"s0":pct>=100?"s100":pct>=50?"s50":pct>0?"s1":"s0";const dateStr=rangeStr(st.earliestStart,st.latestEnd);const dateCls=st.earliestStart||st.latestEnd?"":" stage-date-empty";return`<div class="stage-row">
      <div class="stage-name">
        <div class="stage-name-txt">${U.esc(st.name)}</div>
        <div class="stage-date${dateCls}">${dateStr}</div>
      </div>
      <div class="stage-bar"><div class="stage-bar-fill ${tier}" style="width:${pct||0}%"></div></div>
      <div class="stage-pct ${tier}">${pct===null?"—":pct+"%"}</div>
      <div class="stage-cnt">${st.doneCount}/${st.itemCount}</div>
    </div>`};const colHead=`<div class="stage-colhead">
      <div class="stage-name"></div><div class="stage-bar-spacer"></div>
      <div class="stage-pct-h">完成</div><div class="stage-cnt-h">完成/件數</div>
    </div>`;const variantList=proj.variants&&proj.variants.length?proj.variants:null;let blocks;if(variantList){blocks=variantList.map((vr,i)=>{const gs=stages.filter(st=>st.variantId===vr.id);if(gs.length===0)return"";const cap=`<div class="stage-group-cap"><span class="stage-cap-pill cap-${i%3}">${U.esc(vr.name)}</span><span class="stage-cap-rule"></span></div>`;return cap+colHead+gs.map(rowHtml).join("")}).join("");const noVar=stages.filter(st=>!st.variantId);if(noVar.length)blocks+=colHead+noVar.map(rowHtml).join("")}else{blocks=colHead+stages.map(rowHtml).join("")}return`<div class="proj-stages-card">
    <div class="proj-stages-head">階段進度 <span class="proj-stages-count">${stages.length} 個階段</span></div>
    ${App.buildHintBox({key:"stage-progress",icon:"ti-stairs",title:"階段進度怎麼算",summary:"完成%、件數、日期的計算方式",collapsed:true,bodyHtml:'<div class="ht-rule ht-start"><b>完成%</b><span>將這階段「所有任務的進度」加起來算平均（每個任務的影響力都一樣）。任務有寫進度就依比例計算；沒寫進度的話，已完成的算 100%、沒完成的算 0%。例：階段內有 4 個任務，進度分別是 100 / 50 / 0 / 0，平均下來該階段進度就是 38%。</span></div><div class="ht-rule ht-dur"><b>件數</b><span>顯示「已完成的任務數 / 總任務數」。例：5/16 代表這個階段總共有 16 個任務，目前已經搞定 5 個。</span></div><div class="ht-rule ht-end"><b>日期</b><span>自動抓取這階段裡「最早開始」到「最晚結束」的任務時間，代表整個階段預計要跑的時間跨度。</span></div><div class="ht-rule ht-down"><b>同名階段分開算</b><span>主專案和子專案即使有同名的階段（例如都叫「手工機」），系統也會貼心地把它們各自獨立成一桶、分開計算，絕對不會混在一起。</span></div>'})}
    ${blocks}
  </div>`};App.buildProjLoadCol=function(proj){const daily=DATA.settings.dailyHours||6;const today=D.today();const idToName={};(proj.depts||[]).forEach(d=>{idToName[d.id]=d.name});const dName=v=>v?idToName[v]||v:"未指派";const tasks=DATA.tasks.filter(t=>t.project===proj.id&&!t._deleted);const deptDone={},deptWip={},deptRest={};tasks.forEach(t=>{if(t.measureType==="hours")return;if(isPmTask(t,proj))return;const nm=dName(t.dept);const h=taskIntensity(t)*daily*(t.durationDays||0);if(isTaskDone(t))deptDone[nm]=(deptDone[nm]||0)+h;else if(t.status==="wip")deptWip[nm]=(deptWip[nm]||0)+h;else deptRest[nm]=(deptRest[nm]||0)+h});const deptRows=[...new Set([...Object.keys(deptDone),...Object.keys(deptWip),...Object.keys(deptRest)])].map(nm=>{const done=deptDone[nm]||0,wip=deptWip[nm]||0,rest=deptRest[nm]||0;return{nm,done,wip,rest,tot:done+wip+rest}}).sort((a,b)=>(a.nm==="未指派")-(b.nm==="未指派")||b.tot-a.tot);const pct=(part,tot)=>tot>0?Math.round(part/tot*100):0;const deptCard=`<div class="proj-stages-card pl-card">
    <div class="proj-stages-head dept-head"><span>本案部門負荷<span class="pl-sub">總工時 · 進度＝完工＋進行中（未開始留白）· 不含 PM</span></span><button class="pl-hist-btn" onclick="App.openLoadHistory('${proj.id}')" title="點看「已完工」段的階段×部門明細（與下方各列的完工工時對得起來）">📜 完工帳</button></div>
    ${deptRows.length?deptRows.map(r=>`<div class="pl-drow">
      <div class="pl-dtop"><span class="pl-name">${U.esc(r.nm)}</span><span class="pl-h">${Math.round(r.tot)}h</span></div>
      <div class="pl-bar split"><div class="pl-fill dept-done" style="width:${pct(r.done,r.tot)}%"></div><div class="pl-fill dept-wip" style="width:${pct(r.wip,r.tot)}%"></div></div>
      <div class="pl-dsub"><span class="pl-done-n">完工 ${Math.round(r.done)}h</span><span class="pl-wip-n">進行 ${Math.round(r.wip)}h</span></div>
    </div>`).join("")+`<div class="pl-legend"><span class="pl-lg pl-lg-done">已完工</span><span class="pl-lg pl-lg-wip">進行中</span><span class="pl-lg pl-lg-blank">未開始＝留白</span></div>`:'<div class="proj-stages-empty">本案無其他部門工時</div>'}
  </div>`;const _SNAP=[0,10,25,50,75,100];const snapEff=v=>_SNAP.reduce((a,b)=>Math.abs(b-v)<Math.abs(a-v)?b:a,100);const taskHrs=t=>taskIntensity(t)*daily*(t.durationDays||0);const stageTasks=sm=>tasks.filter(t=>(t.variant||"")===(sm.variantId||"")&&(typeof t.stage==="string"&&t.stage.trim()?t.stage.trim():"未分階段")===sm.name);const varName=vid=>{const v=(proj.variants||[]).find(x=>x.id===vid);return v?v.name:""};const rowObjs=[];this.getProjectStages(proj.id).forEach(sm=>{const all=stageTasks(sm);const pm=all.filter(t=>t.measureType!=="hours"&&isPmTask(t,proj));if(!pm.length){rowObjs.push({sm,vid:sm.variantId||"",gap:true,hrs:0});return}const real=all.filter(t=>!t.isPmCoord);const complete=real.length>0&&real.every(t=>isTaskDone(t));const active=pm.filter(t=>!isTaskDone(t));const hrs=(complete?pm:active).reduce((s,t)=>s+taskHrs(t),0);rowObjs.push({sm,vid:sm.variantId||"",complete,hrs,active})});const anyPm=rowObjs.some(r=>!r.gap);const pmTotal=rowObjs.filter(r=>!r.complete&&!r.gap).reduce((s,r)=>s+r.hrs,0);const maxPm=Math.max(1,...rowObjs.map(r=>r.hrs));const showVarHeads=new Set(rowObjs.map(r=>r.vid)).size>1;let curVar="\0";const stageRows=rowObjs.map(r=>{let head="";if(showVarHeads&&r.vid!==curVar){curVar=r.vid;head=`<div class="pl-vhead">${U.esc(varName(r.vid))}</div>`}if(r.gap){return head+`<div class="pl-crow pl-dim">
      <div class="pl-cstage">${U.esc(r.sm.name)}</div>
      <div class="pl-done-tag" title="此階段目前沒有任何 PM 常駐或顯性 PM 任務——PM 負荷=0 不會是常態，請補設">無 PM 負荷</div>
      <div class="pl-bar"></div>
      <button class="pl-cta-btn pl-cta-sm" onclick="App.seedProjStageCoord('${proj.id}')" title="一鍵補設缺口階段的 PM 常駐協調（已有 PM 的階段不動）">＋設常駐</button>
    </div>`}if(r.complete){return head+`<div class="pl-crow pl-dim">
      <div class="pl-cstage">${U.esc(r.sm.name)}</div>
      <div class="pl-done-tag">已完成</div>
      <div class="pl-bar"><div class="pl-fill done" style="width:${Math.round(r.hrs/maxPm*100)}%"></div></div>
      <div class="pl-h">${Math.round(r.hrs)}h</div>
    </div>`}const expl=r.active.filter(t=>!t.isPmCoord);const cor=r.active.filter(t=>t.isPmCoord);let sel;if(expl.length){const _allNull=expl.every(t=>t.effortRatio==null);const rep=_allNull?null:snapEff(expl.reduce((s,t)=>s+(t.effortRatio!=null?t.effortRatio:App._legacyPct()),0)/expl.length);const _ndTitle=expl.some(t=>t.netWorkDays>0)?' title="此階段有任務已填精算淨工作天——該些任務負荷以淨工作天為準，投入% 不參與計算（§18.10d）"':"";sel=`<select class="pl-pct"${_ndTitle} onchange="App.setProjStagePmEffort('${proj.id}', '${U.esc(r.vid)}', '${U.esc(r.sm.name)}', this.value)">${App._ecnEffortOptions(rep)}</select>`}else if(cor.length){sel=`<select class="pl-pct" onchange="App.setProjStageCoord('${cor[0].id}', this.value)">${App._ecnEffortOptions(cor[0].effortRatio)}</select>`}else{sel='<div class="pl-done-tag">—</div>'}return head+`<div class="pl-crow">
      <div class="pl-cstage">${U.esc(r.sm.name)}</div>
      ${sel}
      <div class="pl-bar"><div class="pl-fill pm" style="width:${Math.round(r.hrs/maxPm*100)}%"></div></div>
      <div class="pl-h">${Math.round(r.hrs)}h</div>
    </div>`}).join("");const pmCard=`<div class="proj-stages-card pl-card">
    <div class="proj-stages-head"><i class="ti ti-user-cog"></i> 本案 PM 負荷<span class="pl-sub">常駐協調 + 顯性任務</span></div>
    ${stageRows}
    ${!anyPm?`<div class="pl-empty-cta">
      <div class="proj-stages-empty">尚未有任何 PM 負荷</div>
      <button class="pl-cta-btn" onclick="App.seedProjStageCoord('${proj.id}')">＋ 一鍵設定 PM 常駐協調</button>
    </div>`:""}
    <div class="pl-total"><span>本案 PM 合計</span><span>${Math.round(pmTotal)}h</span></div>
  </div>`;const SOON_DAYS=7;const risks=App._classifyDueRisk(tasks,t=>!t.isPmCoord&&!t._deleted&&t.status!=="done",SOON_DAYS,today);const issueCard=`<div class="proj-stages-card pl-card">
    <div class="proj-stages-head risk-head">即將到期／延遲<span class="proj-stages-count">${risks.length}</span><button class="pl-risk-all" onclick="App.switchProjectView('list')">查看全部 →</button></div>
    ${risks.length?risks.map(({t,kind,days})=>`<div class="dept-overdue-row" onclick="App.openTaskModal('${t.id}')">
      <div class="dept-overdue-name"><div>${U.esc(t.name)}</div><div class="dept-overdue-sub">${U.esc(t.stage||"")} ${t.subgroup?"/ "+U.esc(t.subgroup):""}</div></div>
      <div class="dept-overdue-days ${kind==="soon"?"soon":""}">${kind==="over"?"逾期 "+days+" 工作天":days===0?"今日到期":days+" 天內到期"}</div>
    </div>`).join(""):'<div class="proj-stages-empty">目前無逾期或即將到期任務</div>'}
  </div>`;return deptCard+pmCard+issueCard};App._classifyDueRisk=function(tasks,filterFn,rangeDays,today){return(tasks||[]).filter(filterFn).map(t=>{const end=getEffectiveSchedule(t).end;if(!end)return null;const dd=D.daysBetween(today,new Date(end));if(isTaskDelayed(t,today))return{t,kind:"over",days:D.workdaysBetween(end,today)-1};if(t.status!=="hold"&&dd>=0&&dd<=rangeDays)return{t,kind:"soon",days:dd};return null}).filter(Boolean).sort((a,b)=>a.kind!==b.kind?a.kind==="over"?-1:1:a.kind==="over"?b.days-a.days:a.days-b.days).slice(0,5)};App.openLoadHistory=function(projId){const proj=this.getProj(projId);if(!proj)return;const daily=DATA.settings.dailyHours||6;const idToName={};(proj.depts||[]).forEach(d=>{idToName[d.id]=d.name});const hrsOf=t=>taskIntensity(t)*daily*(t.durationDays||0);const done=DATA.tasks.filter(t=>t.project===projId&&!t._deleted&&isTaskDone(t)&&t.measureType!=="hours");if(!done.length){this.openModal({title:"📜 完工負荷帳（只列已完工任務）— "+U.esc(proj.name),body:'<div class="proj-stages-empty" style="padding:16px 0;">尚無完工紀錄。任務結案（填實際完成日）後，其工時會沉澱到這本帳。</div>',footer:'<button class="tb-action ghost" onclick="App.closeModal()">關閉</button>'});return}const colOf=t=>isPmTask(t,proj)?"PM":t.dept?idToName[t.dept]||t.dept:"未指派";const acc={};done.forEach(t=>{const vid=t.variant||"";const st=typeof t.stage==="string"&&t.stage.trim()?t.stage.trim():"未分階段";const vv=acc[vid]=acc[vid]||{};const ss=vv[st]=vv[st]||{};const c=colOf(t);ss[c]=(ss[c]||0)+hrsOf(t)});const usedCols=new Set;Object.values(acc).forEach(vv=>Object.values(vv).forEach(ss=>Object.keys(ss).forEach(c=>usedCols.add(c))));const deptUniverse=new Set;DATA.tasks.filter(t=>t.project===projId&&!t._deleted&&t.measureType!=="hours").forEach(t=>{if(!isPmTask(t,proj))deptUniverse.add(t.dept?idToName[t.dept]||t.dept:"未指派")});const cols=(proj.depts||[]).map(d=>d.name).filter(n=>n!=="PM"&&deptUniverse.has(n));[...deptUniverse].forEach(n=>{if(n!=="未指派"&&n!=="PM"&&!cols.includes(n))cols.push(n)});if(deptUniverse.has("未指派"))cols.push("未指派");if(usedCols.has("PM"))cols.push("PM");const fmtH=h=>h>0?Math.round(h)+"h":"—";const varName=vid=>{const v=(proj.variants||[]).find(x=>x.id===vid);return v?v.name:""};const stages=this.getProjectStages(projId);const grand={};let grandAll=0;const sections=[];const vids=[...new Set(stages.map(s=>s.variantId||""))].filter(vid=>acc[vid]);const multi=vids.length>1;vids.forEach(vid=>{const vv=acc[vid];const rows=[];const vtotal={};stages.filter(s=>(s.variantId||"")===vid&&vv[s.name]).forEach(s=>{const ss=vv[s.name];const rowSum=cols.reduce((a,c)=>a+(ss[c]||0),0);rows.push('<tr><td class="lh-st">'+U.esc(s.name)+"</td>"+cols.map(c=>'<td class="lh-h">'+fmtH(ss[c]||0)+"</td>").join("")+'<td class="lh-h lh-sum">'+fmtH(rowSum)+"</td></tr>");cols.forEach(c=>{vtotal[c]=(vtotal[c]||0)+(ss[c]||0);grand[c]=(grand[c]||0)+(ss[c]||0)});grandAll+=rowSum});const vSum=cols.reduce((a,c)=>a+(vtotal[c]||0),0);rows.push('<tr class="lh-total"><td class="lh-st">小計</td>'+cols.map(c=>'<td class="lh-h">'+fmtH(vtotal[c]||0)+"</td>").join("")+'<td class="lh-h lh-sum">'+fmtH(vSum)+"</td></tr>");sections.push((multi&&varName(vid)?'<div class="pl-vhead">'+U.esc(varName(vid))+"</div>":"")+'<div class="lh-scroll"><table class="lh-tbl"><thead><tr><th class="lh-st">階段</th>'+cols.map(c=>'<th class="lh-h">'+U.esc(c)+"</th>").join("")+'<th class="lh-h lh-sum">合計</th></tr></thead><tbody>'+rows.join("")+"</tbody></table></div>")});const grandRow=multi?'<div class="lh-grand"><span>全案完工合計</span><span>'+fmtH(grandAll)+"</span></div>":"";this.openModal({wide:true,title:"📜 歷史負荷帳 — "+U.esc(proj.name),body:sections.join("")+grandRow+'<div class="field-hint">只列完工任務（工時＝結案時的投入% × 日工時 × 工期，§18.10c 同一把尺）；當前負荷請看內頁負荷卡。</div>',footer:'<button class="tb-action ghost" onclick="App.closeModal()">關閉</button>'})};App.setProjStageCoord=function(taskId,val){if(val==="")return;const t=(DATA.tasks||[]).find(x=>x.id===taskId);if(!t)return;const daily=DATA.settings.dailyHours||6;App._effortLogPush(t,t.effortRatio,parseInt(val,10)||0);t.effortRatio=parseInt(val,10)||0;t.estHours=Math.round(t.effortRatio/100*daily*(t.durationDays||0)*10)/10;Store.tasks.save();this.renderProject()};App.setProjStagePmEffort=function(projId,variantId,stage,val){if(val==="")return;if(App._roGuard&&App._roGuard())return;const proj=this.getProj(projId);if(!proj)return;const v=parseInt(val,10)||0;DATA.tasks.forEach(t=>{if(t.project!==projId||t._deleted)return;if((t.variant||"")!==(variantId||"")||(t.stage||"—")!==stage)return;if(t.status==="done"||t.measureType==="hours"||t.isPmCoord)return;if(!isPmTask(t,proj))return;App._effortLogPush(t,t.effortRatio,v);t.effortRatio=v});Store.tasks.save();this.renderProject()};App.seedProjStageCoord=function(projId){if(App._roGuard&&App._roGuard())return;const proj=this.getProj(projId);if(!proj)return;const daily=DATA.settings&&DATA.settings.dailyHours||6;const pmDept=(proj.depts||[]).find(d=>d.name==="PM");const tpl=tplNpi();const defByStage={};if(tpl&&tpl.stageDefaults)tpl.stageDefaults.forEach(s=>{if(s.pmCoordEffort!=null)defByStage[s.stage]=s.pmCoordEffort});const DEFAULT_EFF=25;const explKeys=new Set;DATA.tasks.forEach(t=>{if(t.project===projId&&!t._deleted&&!t.isPmCoord&&t.stage&&isPmTask(t,proj))explKeys.add((t.variant||"")+" "+t.stage)});const groups={};DATA.tasks.filter(t=>t.project===projId&&!t._deleted&&t.stage&&!t.isPmCoord&&!isPmTask(t,proj)).forEach(t=>{const key=(t.variant||"")+"\0"+t.stage;(groups[key]=groups[key]||{variant:t.variant||"",stage:t.stage,tasks:[]}).tasks.push(t)});const created=[],skipped=[];Object.values(groups).forEach(g=>{if(explKeys.has(g.variant+" "+g.stage))return;if(DATA.tasks.some(t=>t.project===projId&&t.isPmCoord&&(t.variant||"")===g.variant&&t.stage===g.stage))return;const ss=g.tasks.map(t=>t.plannedStart).filter(Boolean).sort();const ee=g.tasks.map(t=>t.plannedEnd).filter(Boolean).sort();if(!ss.length||!ee.length){skipped.push(g.stage);return}const s0=ss[0],e0=ee[ee.length-1];const span=Math.max(D.workdaysBetween(s0,e0),1);const eff=defByStage[g.stage]!=null?defByStage[g.stage]:DEFAULT_EFF;created.push({id:U.id(),project:projId,wbs:0,parentWbsId:"",name:"PM 階段協調（常駐）",desc:g.stage+" / PM 協調",category:"deep",taskType:"任務",predecessor:"",durationDays:span,owner:"",dept:pmDept?pmDept.id:"",role:"PM",variant:g.variant,start:"",end:"",plannedStart:s0,plannedEnd:e0,actualStart:"",actualEnd:"",progress:0,status:"pending",urgency:"med",estHours:Math.round(span*daily*(eff/100)*10)/10,method:"",canSplit:false,completedAt:null,createdAt:new Date().toISOString(),scheduledStart:"",scheduledEnd:"",synced:false,stage:g.stage,subgroup:"",mustDeliver:false,deliverableType:"",requiredTask:true,mustIssue:false,deliverable:"",riskIssue:"",delivered:"",deliverableLink:"",note:"",effortRatio:eff,taskAttr:"baseline",isPmCoord:true})});if(!created.length){U.toast("沒有可帶入的階段（任務缺計畫日期）"+(skipped.length?"："+skipped.join("、")+"——排好日期後再按一次即可補齊":""),"warning");return}DATA.tasks.push(...created);Store.tasks.save();this._pmCoordPrompted=this._pmCoordPrompted||{};this._pmCoordPrompted[projId]=true;if(skipped.length)U.toast("已帶入 PM "+created.length+" 個階段；「"+skipped.join("、")+"」因任務缺計畫日期未帶入，排好日期後可再按一次補齊","warning");else U.toast("已帶入 PM "+created.length+" 個階段的常駐協調，可逐階段調整投入%","success");this.renderProject()};App._maybePromptPmCoord=function(proj){if(!proj||proj.ecnType)return;if((proj.status||"active")!=="active")return;this._pmCoordPrompted=this._pmCoordPrompted||{};if(this._pmCoordPrompted[proj.id])return;const ts=DATA.tasks.filter(t=>t.project===proj.id&&!t._deleted);const seedable=ts.some(t=>t.stage&&!t.isPmCoord&&!isPmTask(t,proj));const hasCoord=ts.some(t=>t.isPmCoord);const hasExplicitPm=ts.some(t=>!t.isPmCoord&&t.stage&&isPmTask(t,proj));if(!seedable||hasCoord||hasExplicitPm)return;this._pmCoordPrompted[proj.id]=true;App.confirmModal({title:"尚未設定 PM 常駐協調負荷",msg:"本案還沒設定「PM 各階段常駐協調負荷」（背景盯場的投入%）。要現在一鍵帶入嗎？帶入後可在右欄「本案 PM 負荷」逐階段調整，也能之後再手動設定。",okText:"一鍵帶入",cancelText:"稍後再說",icon:"ti-user-cog",onConfirm:()=>App.seedProjStageCoord(proj.id)})};App.toggleBulkEdit=function(projId){this._bulkMode=this._bulkMode||{};this._bulkSel=this._bulkSel||{};this._bulkMode[projId]=!this._bulkMode[projId];if(!this._bulkMode[projId])delete this._bulkSel[projId];this.renderProject()};App._bulkSelOf=function(projId){this._bulkSel=this._bulkSel||{};return this._bulkSel[projId]=this._bulkSel[projId]||new Set};App._bulkToggle=function(projId,taskId,checked){const sel=this._bulkSelOf(projId);if(checked)sel.add(taskId);else sel.delete(taskId);const n=document.getElementById("bulk-count");if(n)n.textContent=sel.size};App._bulkSelectAll=function(projId,checked){const sel=this._bulkSelOf(projId);document.querySelectorAll("input.bulk-chk").forEach(cb=>{cb.checked=checked;const row=cb.closest("tr");const id=row?row.getAttribute("data-taskid"):null;if(!id)return;if(checked)sel.add(id);else sel.delete(id)});const n=document.getElementById("bulk-count");if(n)n.textContent=sel.size};App._bulkBarHtml=function(proj){const sel=this._bulkSelOf(proj.id);return`<div class="bulk-bar">
    <span class="bulk-info">已選 <b id="bulk-count">${sel.size}</b> 筆</span>
    <span class="bulk-hint">勾選下方任務列，再按「編輯已選」一次修改多筆</span>
    <button class="tb-action" onclick="App._bulkOpenModal('${proj.id}')" style="margin-left:auto;">✏ 編輯已選</button>
    ${proj.ecnType?"":`<button class="tb-action danger" onclick="App._bulkDelete('${proj.id}')">批量刪除</button>`}
  </div>`};App._bulkOpenModal=function(projId){const sel=this._bulkSelOf(projId);if(!sel.size){U.toast("⚠ 請先勾選任務","warning");return}const proj=this.getProj(projId);if(!proj)return;const ecn=!!proj.ecnType;const KEEP='<option value="__keep__" selected>— 不變更 —</option>';const owners=[...new Set(DATA.tasks.filter(t=>t.project===projId&&!t._deleted&&t.owner).map(t=>t.owner))].sort((a,b)=>a.localeCompare(b,"zh-Hant"));const tiers=App._EFFORT_TIERS.map(o=>`<option value="${o.v}">${o.v}% ${o.short}</option>`).join("");const ownerDeptRow=`
      <div class="form-row">
        <div class="form-field"><label>負責人 <span data-tip="負責人×部門|擇一批量：填了負責人會自動歸到名冊上他所屬的部門" style="cursor:help;">?</span></label>
          <input id="blk-owner" list="blk-own-dl" placeholder="不變更（留空）" oninput="App._bulkOwnerDeptExcl('owner')"><datalist id="blk-own-dl">${owners.map(o=>`<option value="${U.esc(o)}"></option>`).join("")}</datalist>
        </div>
        <div class="form-field"><label>部門 <span data-tip="負責人×部門|擇一批量：選了部門會自動帶該部門名冊負責人" style="cursor:help;">?</span></label>
          <select id="blk-dept" onchange="App._bulkOwnerDeptExcl('dept')">${KEEP}${ecn?"":'<option value="">未指派</option>'}${(proj.depts||[]).map(d=>`<option value="${d.id}">${U.esc(d.name)}</option>`).join("")}</select>
        </div>
      </div>`;const effortField=`
        <div class="form-field"><label>投入% <span data-tip="投入%|這件事吃掉某人一天的百分比；同一人同一天相加 >100% 就爆單" style="cursor:help;">?</span></label>
          <select id="blk-effort">${KEEP}${tiers}</select>
        </div>`;const npiRows=`
      <div class="form-row">
        <div class="form-field"><label>階段</label>
          <input id="blk-stage" list="blk-stage-dl" placeholder="不變更（留空）"><datalist id="blk-stage-dl">${this.stageDatalistOptions(projId)}</datalist>
        </div>
        <div class="form-field"><label>子群組</label>
          <input id="blk-subgroup" list="blk-sub-dl" placeholder="不變更（留空）"><datalist id="blk-sub-dl">${this.subgroupDatalistOptions(projId)}</datalist>
        </div>
      </div>
      ${ownerDeptRow}
      <div class="form-row">
        <div class="form-field"><label>緊急程度</label>
          <select id="blk-urgency">${KEEP}<option value="high">🔴 緊急</option><option value="medium">🟡 普通</option><option value="low">🟢 不急</option></select>
        </div>
        <div class="form-field"><label>狀態 <span data-tip="狀態|已完成走任務的「實際完成日」，不提供批量" style="cursor:help;">?</span></label>
          <select id="blk-status">${KEEP}<option value="pending">未開始</option><option value="wip">進行中</option><option value="hold">擱置</option></select>
        </div>
      </div>
      <div class="form-row">
        ${effortField}
        <div class="form-field"><label>需交付</label>
          <select id="blk-deliver">${KEEP}<option value="1">需交付 ✓</option><option value="0">不需交付</option></select>
        </div>
      </div>`;const ecnRows=`
      ${ownerDeptRow}
      <div class="form-row">
        ${effortField}
        <div class="form-field"></div>
      </div>`;this.openModal({title:`批量編輯（已選 ${sel.size} 筆）`,body:`<div class="task-form">
      ${ecn?ecnRows:npiRows}
      <div class="field-hint">留空或「不變更」的欄位不會動到任務。<b>負責人與部門擇一批量</b>（互相連動，避免指派衝突）。日期／工期／前置不支援批量（會連動整批重排），請逐筆調整。</div>
    </div>`,footer:`
      <button class="tb-action ghost" onclick="App.closeModal()">取消</button>
      <button class="tb-action" onclick="App._bulkApplyFromModal('${projId}')">套用到 ${sel.size} 筆</button>
    `})};App._bulkOwnerDeptExcl=function(which){const owner=document.getElementById("blk-owner");const dept=document.getElementById("blk-dept");if(!owner||!dept)return;if(which==="dept"){const on=dept.value!=="__keep__";if(on)owner.value="";owner.disabled=on}else{const on=!!owner.value.trim();if(on)dept.value="__keep__";dept.disabled=on}};App._bulkApplyFromModal=function(projId){if(App._roGuard())return;const proj=this.getProj(projId);if(!proj)return;const ecn=!!proj.ecnType;const sel=this._bulkSelOf(projId);const v=id=>{const e=document.getElementById(id);return e?e.value:"__keep__"};const txt=id=>{const e=document.getElementById(id);return e?e.value.trim():""};const ops=[];if(txt("blk-stage"))ops.push(["階段",t=>t.stage=txt("blk-stage")]);if(txt("blk-subgroup"))ops.push(["子群組",t=>t.subgroup=txt("blk-subgroup")]);if(txt("blk-owner"))ops.push(["負責人",t=>{const name=txt("blk-owner");t.owner=name;const d=(proj.depts||[]).find(dp=>(dp.members||[]).some(m=>m.name===name));if(d){t.dept=d.id;if(ecn)t.role=d.name}}]);if(v("blk-dept")!=="__keep__")ops.push(["部門",t=>{const dId=v("blk-dept");const d=(proj.depts||[]).find(dp=>dp.id===dId);t.dept=dId;if(ecn&&d)t.role=d.name;if(d&&!(d.members||[]).some(m=>m.name===t.owner)){const fm=(d.members||[]).find(m=>m.name);t.owner=fm?fm.name:""}}]);if(v("blk-urgency")!=="__keep__")ops.push(["緊急程度",t=>t.urgency=v("blk-urgency")]);if(v("blk-status")!=="__keep__")ops.push(["狀態",t=>t.status=v("blk-status")]);if(v("blk-effort")!=="__keep__")ops.push(["投入%",t=>{App._effortLogPush(t,t.effortRatio,parseInt(v("blk-effort"),10)||0);t.effortRatio=parseInt(v("blk-effort"),10)||0}]);if(v("blk-deliver")!=="__keep__")ops.push(["需交付",t=>t.mustDeliver=v("blk-deliver")==="1"]);if(!ops.length){U.toast("⚠ 沒有要變更的欄位（全部維持不變更）","warning");return}let n=0;sel.forEach(id=>{const t=DATA.tasks.find(x=>x.id===id);if(!t||t._deleted||t.status==="done")return;ops.forEach(([,set])=>set(t));n++});Store.tasks.save();this.closeModal();this.renderProject();U.toast(`已套用 ${ops.map(o=>o[0]).join("、")} 到 ${n} 筆`,"success")};App._bulkDelete=function(projId){if(App._roGuard())return;const sel=this._bulkSelOf(projId);if(!sel.size){U.toast("⚠ 請先勾選任務","warning");return}App.confirmModal({icon:"ti-trash",iconBg:"--rose-l",iconColor:"--rose-ink",title:`批量刪除 ${sel.size} 筆任務？`,msg:"刪除的任務會移到專案下方「🗑 已刪除」區塊保留 14 天，期間可隨時還原。",okText:"刪除",cancelText:"取消",okClass:"danger",onConfirm:()=>{let n=0;sel.forEach(id=>{const t=DATA.tasks.find(x=>x.id===id);if(!t||t._deleted)return;t._deleted=true;t._deletedAt=new Date().toISOString();if(DATA.schedule&&DATA.schedule.items)DATA.schedule.items=DATA.schedule.items.filter(it=>it.taskId!==id);n++});sel.clear();Store.tasks.save();Store.schedule.save();App.renderProject();U.toast(`已刪除 ${n} 筆（可在已刪除區還原）`,"success")}})};App.toggleProjectExpanded=function(projId){this._projectExpanded=this._projectExpanded||{};this._projectExpanded[projId]=!this._projectExpanded[projId];this.renderProject()};App.toggleDoneVisible=function(projId){this._doneVisible=this._doneVisible||{};this._doneVisible[projId]=!this._doneVisible[projId];this.renderProject()};App.toggleToScheduleVisible=function(projId){this._toScheduleVisible=this._toScheduleVisible||{};const cur=this._toScheduleVisible[projId]!==false;this._toScheduleVisible[projId]=!cur;this.renderProject()};App.restoreTask=function(id){const t=DATA.tasks.find(x=>x.id===id);if(!t)return;delete t._deleted;delete t._deletedAt;Store.tasks.save();this.refreshAll();U.toast("↺ 已還原")};App.permanentDeleteTask=function(id){App.confirmModal({icon:"ti-alert-triangle",iconBg:"--rose-l",iconColor:"--rose-ink",title:"永久刪除？",msg:"此操作無法復原。",okText:"永久刪除",cancelText:"取消",okClass:"danger",onConfirm:()=>{DATA.tasks=DATA.tasks.filter(t=>t.id!==id);if(DATA.schedule&&DATA.schedule.items){DATA.schedule.items=DATA.schedule.items.filter(it=>it.taskId!==id)}Store.tasks.save();Store.schedule.save();App.refreshAll();U.toast("🗑 已永久刪除")}})};App.cleanExpiredDeletedTasks=function(){const cutoff=D.addDays(D.today(),-14);const before=DATA.tasks.length;DATA.tasks=DATA.tasks.filter(t=>{if(!t._deleted)return true;if(t._delWithProj)return true;const delDate=new Date(t._deletedAt||0);return delDate>cutoff});if(before!==DATA.tasks.length){Store.tasks.save()}};App.buildTaskRowHtml=function(t,cls){const sch=getEffectiveSchedule(t);const cat=t.taskType==="milestone"?"milestone":t.category||"deep";const isPreview=!DATA.settings.previewWeeks?false:sch.end&&D.daysBetween(D.today(),new Date(sch.end))>7&&D.daysBetween(D.today(),new Date(sch.end))<=DATA.settings.previewWeeks*7;let dlText="—";let dlClass="";if(sch.end){const days=D.daysBetween(D.today(),new Date(sch.end));if(days<0){dlText=`逾${D.workdaysBetween(sch.end,D.today())-1}`;dlClass="overdue"}else if(days===0){dlText="今日";dlClass="near"}else if(days===1){dlText="明日";dlClass="near"}else if(days<=3){dlText=`${days}天`;dlClass="near"}else{dlText=D.fmt(new Date(sch.end),"md")}}const rangeText=sch.start&&sch.end?`${D.fmt(new Date(sch.start),"md")} → ${D.fmt(new Date(sch.end),"md")}`:"—";const _negDur=t.taskType!=="milestone"&&(sch.start&&sch.end&&new Date(sch.end)<new Date(sch.start)||parseFloat(t.durationDays)<=0);const SRC_LABELS={planned:"預計（未排程）",scheduled:"排程算出",override:"手釘錨點",actual:"實際",manual:"手填"};const srcLabel=SRC_LABELS[sch.startSource]||"";const pct=taskDisplayProgress(t);const barColor=pct>=100?"var(--sage-500)":"var(--ink4)";const isDelayed=dlClass==="overdue"&&t.status!=="done"&&t.status!=="hold";const statusCls=isDelayed?"late":t.status==="done"?"done":t.status==="wip"?"wip":"";const statusTxt=isDelayed?"逾期":STATUS_LABELS_ZH[t.status]||t.status||"";let slackTxt,slackTip="";if(t.status==="done"||!sch.end){slackTxt="—"}else{const today=D.today();today.setHours(0,0,0,0);if(new Date(sch.end)<today){slackTxt="超"+(D.workdaysBetween(sch.end,today)-1)+"天";slackTip="已超出 "+(D.workdaysBetween(sch.end,today)-1)+" 個工作天（不含假日）"}else{slackTxt="餘"+(D.workdaysBetween(today,sch.end)-1)+"天";slackTip="還有 "+(D.workdaysBetween(today,sch.end)-1)+" 個工作天餘裕（不含假日）"}}const _bk=(this._bulkMode||{})[t.project];const _bkSel=_bk&&this._bulkSel&&this._bulkSel[t.project];const chkTd=_bk?t.status==="done"?'<td class="col-chk"></td>':`<td class="col-chk" onclick="event.stopPropagation()"><input type="checkbox" class="bulk-chk" ${_bkSel&&_bkSel.has(t.id)?"checked":""} onchange="App._bulkToggle('${t.project}','${t.id}', this.checked)"></td>`:"";return`<tr class="task-row ${t.status==="done"?"done":""} ${_negDur?"neg-dur":""} ${cls||""}" data-taskid="${t.id}" onclick="App.openTaskModal('${t.id}')">
    ${chkTd}
    <td class="col-num">${_negDur?'<span class="neg-flag" data-tip="負工期|工期為負數，請確認是否調整">⚠</span>':""}<span style="font-family:var(--mono); font-size:11px; color:var(--ink4);">${App._seqOf(t.id)}</span></td>
    <td class="col-mid"><span style="font-size:12px; color:var(--ink2);">${U.esc(t.stage||"—")}</span></td>
    <td class="col-flex" title="${U.esc(t.name)}">
      <div class="task-info">
        <div class="task-name" style="overflow:hidden; text-overflow:ellipsis; white-space:nowrap;">
          ${U.esc(t.name)}
          ${isPreview?'<span class="preview-tag">📅 兩週預告</span>':""}
        </div>
      </div>
    </td>
    <td class="col-mid">
      <div style="display:flex; justify-content:flex-start; align-items:center; gap:6px;">
        <div class="stage-bar" style="border:1px solid var(--rule2);"><div class="stage-bar-fill" style="width:${pct}%; background:${barColor};"></div></div>
        <span style="font-family:var(--mono); font-size:10.5px; color:var(--ink3); min-width:30px; text-align:right;">${pct}%</span>
      </div>
    </td>
    <td class="col-mid"><span style="font-size:12px; color:var(--ink2);">${U.esc(t.owner||"—")}</span></td>
    <td class="col-mid task-pred" data-preds="${parsePredecessors(t.predecessor).map(p=>p.dep).join(",")}" onmouseenter="App._s2PredHlOn(this)" onmouseleave="App._s2PredHlOff()" title="${U.esc(predTitleOf(t.predecessor))}">${U.esc(prettyPredecessor(t.predecessor))}</td>
    <td class="col-num"><span class="rp-status ${statusCls}">${statusTxt}</span></td>
    <td class="col-mid">
      <div style="display:flex; flex-direction:column; align-items:flex-start; gap:2px;">
        <span class="task-range${_negDur?" neg":""}"${_negDur?' data-tip="負工期|工期為負數，請確認是否調整"':""}>${rangeText}</span>
        ${srcLabel?`<span class="task-tag tag-other">${srcLabel}</span>`:""}
      </div>
    </td>
    <td class="col-num"${slackTip?` title="${U.esc(slackTip)}"`:""}><span style="font-style:italic; color:var(--ink4); font-size:12px;">${slackTxt}</span></td>
    <td class="col-num"><span class="task-deadline ${dlClass}" style="font-size:12px;"${dlClass==="overdue"?` title="已逾期 ${D.workdaysBetween(sch.end,D.today())-1} 個工作天（不含假日）"`:""}>${dlText}</span></td>
  </tr>
  <tr class="dt-insert-row"><td colspan="10" class="dt-insert-cell"><div class="dt-insert"><button class="dt-insert-btn" title="在此列後插入" onclick="event.stopPropagation(); App._insertAfterId='${t.id}'; App.openNewTaskDialog('${t.project}');"><i class="ti ti-plus"></i></button></div></td></tr>`};App.openMeetingModal=function(){App.shotFiles=[];App.openModal({title:"📅 會議時程設定",body:App.buildMeetingModalBody(),footer:'<button class="tb-action" onclick="App.closeModal()" style="background:var(--surface2); color:var(--ink2);">關閉</button>',wide:true});if(!App._meetingPasteBound){App._meetingPasteBound=true;document.addEventListener("paste",App._meetingPasteHandler)}};App._meetingPasteHandler=function(e){if(!document.getElementById("meetingModalBody"))return;const items=e.clipboardData&&e.clipboardData.items||[];const files=[];for(const it of items){if(it.type&&it.type.indexOf("image/")===0){const f=it.getAsFile();if(f)files.push(f)}}if(!files.length)return;e.preventDefault();App.showMeetingAddView();const shot=document.getElementById("am-shot"),manual=document.getElementById("am-manual");if(shot&&manual){shot.style.display="";manual.style.display="none";document.querySelectorAll("#meetingModalBody .am-tab").forEach(b=>b.classList.toggle("active",/截圖/.test(b.textContent)))}App.handleShotUpload(files);U.toast("📋 已貼上截圖，按「🪄 一次解析全部」辨識","info")};App._meetingDeptOptions=function(sel){const pool=[...new Set((DATA.projects||[]).flatMap(p=>(p.depts||[]).map(d=>(d.name||"").trim())).filter(Boolean))];const cur=sel||"";let html=`<option value=""${cur===""?" selected":""}>未指派</option>`;html+=pool.map(n=>`<option value="${U.esc(n)}"${cur===n?" selected":""}>${U.esc(n)}</option>`).join("");html+=`<option value="__ALL__"${cur==="__ALL__"?" selected":""}>★ 全體均攤（跨部門）</option>`;return html};App.buildMeetingModalBody=function(){return`<div id="meetingModalBody">
    <!-- 管理主頁（開啟即見） -->
    <div id="am-home">
      <div style="display:flex; align-items:center; justify-content:space-between; margin:2px 0 8px;">
        <div style="font-size:13px; font-weight:600; color:var(--ink2);">⏰ 定期事件（會議 / 打掃）</div>
        <button class="am-add-btn" data-edit onclick="App.showMeetingAddView()" style="width:auto; padding:5px 14px; font-size:12px;">＋ 新增事件</button>
      </div>
      <div id="recurringMeetingList" style="border:1px solid var(--rule); border-radius:8px; overflow:hidden;">${App.buildRecurringMeetingsHtml()}</div>

      <div style="display:flex; align-items:center; justify-content:space-between; margin:16px 0 8px;">
        <div style="font-size:13px; font-weight:600; color:var(--ink2);">📌 特定日期事件</div>
        <button class="tb-action ghost" data-edit onclick="App.addSpecialMeeting()" style="font-size:11px; padding:3px 9px;">＋ 新增</button>
      </div>
      <div id="specialMeetingList" style="border:1px solid var(--rule); border-radius:8px; overflow:hidden; max-height:240px; overflow-y:auto;">${App.buildSpecialMeetingsHtml()}</div>
    </div>

    <!-- 新增（手動填入 / 上傳截圖） -->
    <div id="am-add" style="display:none">
      <div style="margin:0 0 10px;">
        <span onclick="App.showMeetingManageView()" style="display:inline-flex; align-items:center; gap:4px; font-size:12px; color:var(--ink3); cursor:pointer;">‹ 返回清單</span>
      </div>
      <div class="add-meeting-tabs">
        <button class="am-tab active" onclick="App.switchAmTab(this, 'manual')">⌨ 手動填入</button>
        <button class="am-tab" onclick="App.switchAmTab(this, 'shot')">📷 上傳截圖</button>
      </div>

      <div id="am-manual" class="am-form">
        <div class="form-row">
          <div class="form-field">
            <label>類型 *</label>
            <select id="mCat" onchange="App._toggleMcatLabel()">
              <option value="meeting">📅 會議</option>
              <option value="cleaning">🧹 雜項</option>
            </select>
          </div>
          <div class="form-field" style="flex:2;">
            <label>名稱 *</label>
            <input type="text" id="mTitle" placeholder="例：主管週會 / 輪值掃地">
          </div>
        </div>
        <div class="form-row" id="mCatLabelRow" style="display:none;">
          <div class="form-field">
            <label>分類名稱（雜項自訂）</label>
            <input id="mCatLabel" placeholder="如：打掃、外出、私人">
          </div>
        </div>
        <div class="form-row">
          <div class="form-field">
            <label>頻率 *</label>
            <select id="mFreq">
              <option value="once">單次（當週）</option>
              <option value="weekly">每週</option>
              <option value="biweekly">隔週</option>
              <option value="monthly">每月（第N個週幾）</option>
            </select>
          </div>
          <div class="form-field">
            <label>日期 *</label>
            <input type="date" id="mDate" value="${D.fmt(D.today(),"iso")}">
          </div>
          <div class="form-field">
            <label>開始時間 *</label>
            <input type="time" id="mStart" value="10:00">
          </div>
          <div class="form-field">
            <label>結束時間 *</label>
            <input type="time" id="mEnd" value="11:00">
          </div>
        </div>
        <div class="form-row">
          <div class="form-field">
            <label>負責人（預設＝我）</label>
            <input type="text" id="mOwner" value="${U.esc(DATA.settings.userName||"")}">
          </div>
          <div class="form-field">
            <label>部門（負載分流）</label>
            <select id="mDept">${App._meetingDeptOptions("")}</select>
          </div>
        </div>
        <button class="am-add-btn" data-edit onclick="App.addManualMeeting()">＋ 加入</button>
        <div class="ocr-tip">頻率選「每週/隔週/每月」存定期事件、自動重複；「單次」只放當週。星期由日期自動推算。</div>
      </div>

      <div id="am-shot" class="am-form" style="display:none">
        <div class="am-drop" id="shotDrop" onclick="document.getElementById('shotInput').click()">
          <div class="ic">🖼</div>
          <div class="tx">點擊、拖曳，或直接 Ctrl+V 貼上截圖</div>
          <div class="sub">免費 · 純本地辨識 · 截圖不會被儲存（可多張）</div>
        </div>
        <input type="file" id="shotInput" multiple accept="image/*" style="display:none"
               onchange="App.handleShotUpload(this.files)">
        <div id="shotList" class="shot-list" style="display:none;"></div>
        <div id="ocrResult"></div>
        <div class="ocr-tip">💡 週檢視日期抓不到時請在清單自己選；想最準用「單日檢視」截圖。多張自動去重。</div>
      </div>
    </div>
  </div>`};App._refreshMeetingUI=function(){if(typeof Workspace.render==="function")Workspace.render();const mb=document.querySelector("#modal .modal-body");if(mb&&document.getElementById("meetingModalBody"))mb.innerHTML=App.buildMeetingModalBody()};App.openGridSettingsModal=function(){const s=DATA.settings;App._gridSnap={s:s.gridStartHour,e:s.gridEndHour};const opt=(sel,lo,hi)=>{let o="";for(let h=lo;h<=hi;h++)o+=`<option value="${h}"${h===sel?" selected":""}>${String(h).padStart(2,"0")}:00</option>`;return o};App.openModal({title:"⚙ 時程表顯示設定",body:`<div class="form-field"><label>顯示時間範圍</label>
        <div style="display:flex; align-items:center; gap:8px;">
          <select onchange="App.setGridSetting('gridStartHour', this.value)" style="flex:1;">${opt(parseInt(s.gridStartHour,10)||8,0,22)}</select>
          <span style="color:var(--ink4);">→</span>
          <select onchange="App.setGridSetting('gridEndHour', this.value)" style="flex:1;">${opt(parseInt(s.gridEndHour,10)||18,1,24)}</select>
        </div></div>
      <div class="field-hint">☕ 午休 12:00–13:00 固定，不受此設定影響。</div>
      <div class="field-hint">☁ 此偏好存全域設定、自動同步雲端、跨機一致。</div>`,footer:'<button class="tb-action ghost" onclick="App._gridSettingsCancel()">取消</button><button class="tb-action" onclick="App.closeModal()">儲存</button>'})};App._gridSettingsCancel=function(){const g=App._gridSnap;if(g){DATA.settings.gridStartHour=g.s;DATA.settings.gridEndHour=g.e;Store.settings.save();Workspace.render()}App._gridSnap=null;App.closeModal()};App.setGridSetting=function(key,val){if(App._roGuard&&App._roGuard())return;let v=parseInt(val,10);if(isNaN(v))return;if(key==="gridStartHour")v=Math.max(0,Math.min(22,v));else if(key==="gridEndHour")v=Math.max(1,Math.min(24,v));DATA.settings[key]=v;if(DATA.settings.gridEndHour<=DATA.settings.gridStartHour){if(key==="gridStartHour")DATA.settings.gridEndHour=Math.min(24,DATA.settings.gridStartHour+1);else DATA.settings.gridStartHour=Math.max(0,DATA.settings.gridEndHour-1)}Store.settings.save();Workspace.render()};App.switchAmTab=function(btn,name){btn.parentElement.querySelectorAll(".am-tab").forEach(b=>b.classList.remove("active"));btn.classList.add("active");["shot","manual"].forEach(n=>{const el=document.getElementById("am-"+n);if(el)el.style.display=n===name?"":"none"})};App.showMeetingAddView=function(){const home=document.getElementById("am-home"),add=document.getElementById("am-add");if(home)home.style.display="none";if(add)add.style.display=""};App.showMeetingManageView=function(){const home=document.getElementById("am-home"),add=document.getElementById("am-add");if(add)add.style.display="none";if(home)home.style.display=""};App._toggleMcatLabel=function(){const row=document.getElementById("mCatLabelRow");if(row)row.style.display=(document.getElementById("mCat")||{}).value==="cleaning"?"":"none"};App.addManualMeeting=function(){if(App._roGuard())return;const freq=(document.getElementById("mFreq")||{}).value||"once";const cat=(document.getElementById("mCat")||{}).value||"meeting";const catLabelRaw=((document.getElementById("mCatLabel")||{}).value||"").trim();const catLabel=cat==="cleaning"?catLabelRaw||"雜項":"";const dateStr=(document.getElementById("mDate")||{}).value||D.fmt(D.today(),"iso");const start=document.getElementById("mStart").value;const end=document.getElementById("mEnd").value;const title=document.getElementById("mTitle").value.trim();if(!title){U.toast("⚠ 請填主題","warning");return}const owner=((document.getElementById("mOwner")||{}).value||"").trim();const dept=(document.getElementById("mDept")||{}).value||"";const dayNum=new Date(dateStr+"T00:00:00").getDay();if(freq==="once"){DATA.meetings.push({id:U.id(),date:dateStr,startTime:start,endTime:end,title,category:cat,categoryLabel:catLabel,owner,dept})}else{if(!DATA.settings.recurringMeetings)DATA.settings.recurringMeetings=[];DATA.settings.recurringMeetings.push({id:U.id(),category:cat,categoryLabel:catLabel,frequency:freq,day:dayNum,start,end,title,startDate:dateStr,endDate:"",enabled:true,owner,dept})}if(freq==="once")Store.meetings.save();else Store.settings.save();App._refreshMeetingUI();const fl={once:"單次",weekly:"每週",biweekly:"隔週",monthly:"每月"}[freq]||"";U.toast(`✓ 已加入${fl?"（"+fl+"）":""}${cat==="cleaning"?"雜項":"會議"}`,"success")};App.generateGlobalSchedule=function(){const activeTasks=DATA.tasks.filter(t=>t.status!=="done"&&t.status!=="hold");if(activeTasks.length===0){U.toast("⚠ 沒有任務可排程","warning");return}const result=generateSchedule();this.refreshAll();if(result.scheduledCount===0){U.toast("⚠ 本週沒有需要排程的任務（任務日期都在本週外）","warning",{soft:true});return}U.toast(`⚡ 本週智慧排程完成：${result.scheduledCount} 個時段`);if(this.currentPage!=="workspace"){this.showPage("workspace",document.querySelector("[data-page=workspace]"))}};App.quickAddTask=function(projId,input){if(App._roGuard())return;const name=input.value.trim();if(!name){this.openNewTaskDialog(projId);return}const task={id:U.id(),project:projId,name,desc:"",owner:DATA.settings.userName||"",urgency:"medium",category:"deep",estHours:1,canSplit:false,predecessor:"",wbs:"",durationDays:1,scheduledStart:"",scheduledEnd:"",parentWbsId:"",start:"",end:"",status:"pending",note:"",method:"",createdAt:new Date().toISOString()};DATA.tasks.push(task);Store.tasks.save();input.value="";this.renderProject();this.renderSidebar();U.toast(`✓ 已新增「${name}」`)};App.PRED_RELATIONS=[{code:"FS",label:"等它完成後，本任務才開始（最常用）"},{code:"SS",label:"跟它同一天開始"},{code:"FF",label:"跟它同一天完成"},{code:"SF",label:"它開始後，本任務才能完成"}];function orderTasksByDispStart(list){const dec=(list||[]).map((t,i)=>({t,i,ds:getEffectiveSchedule(t).start||""}));const dated=dec.filter(x=>x.ds!=="").sort((a,b)=>a.ds<b.ds?-1:a.ds>b.ds?1:a.i-b.i);const undated=dec.filter(x=>x.ds==="");return dated.map(x=>x.t).concat(undated.map(x=>x.t))}function applyTaskFilter(tasks,filter){const f=filter||{};const has=s=>s&&s.size>0;return(tasks||[]).filter(t=>{if(has(f.stages)&&!f.stages.has(t.stage))return false;if(has(f.owners)&&!f.owners.has(t.owner))return false;if(has(f.urg)&&!f.urg.has(t.urgency||"medium"))return false;if(has(f.status)&&!f.status.has(t.status))return false;return true})}App.orderedProjectTasks=function(projId){return orderTasksByDispStart((DATA.tasks||[]).filter(t=>t.project===projId&&!t._deleted&&!t.isPmCoord))};App._seqOf=function(taskId){const t=(DATA.tasks||[]).find(x=>x.id===taskId);if(!t)return"?";const i=this.orderedProjectTasks(t.project).findIndex(x=>x.id===taskId);return i<0?"?":i+1};App.predCandidates=function(projId,selfId,currentStage,selfVariant){const NO_STAGE="未分階段";const norm=s=>typeof s==="string"&&s.trim()?s.trim():NO_STAGE;const stages=App.getProjectStages(projId);const nameToIdx={};let k=0;stages.forEach(s=>{if(!(s.name in nameToIdx))nameToIdx[s.name]=k++});const idxOf=stage=>norm(stage)in nameToIdx?nameToIdx[norm(stage)]:stages.length;const S=idxOf(currentStage);const ordered=App.orderedProjectTasks(projId);const selfPos=selfId?ordered.findIndex(t=>t.id===selfId):-1;const selfBefore=selfPos<0?Infinity:selfPos;return ordered.map((t,pos)=>({t,pos})).filter(({t,pos})=>{if(t.id===selfId)return false;if(t.measureType==="hours")return false;if((t.variant||null)!==(selfVariant||null))return false;const d=S-idxOf(t.stage);if(d===0)return pos<selfBefore;if(d===1||d===2)return true;return false}).map(({t,pos})=>({id:t.id,seq:pos+1,name:t.name||"",stage:norm(t.stage),stageIdx:idxOf(t.stage)}))};App._predRowHtml=function(pred,candidates){const cands=candidates||App._predCands||[];const depId=pred?String(pred.dep):"";const type=pred?pred.type:"FS";const lag=pred?pred.lag:0;let optsHtml=`<option value="">（不設前置）</option>`;const byIdx={},order=[];cands.forEach(c=>{if(!(c.stageIdx in byIdx)){byIdx[c.stageIdx]={name:c.stage,items:[]};order.push(c.stageIdx)}byIdx[c.stageIdx].items.push(c)});order.sort((a,b)=>a-b);order.forEach(idx=>{const g=byIdx[idx];optsHtml+=`<optgroup label="${U.esc(g.name)}">`+g.items.map(c=>`<option value="${U.esc(c.id)}"${c.id===depId?" selected":""}>${U.esc(c.seq+" · "+c.name)}</option>`).join("")+`</optgroup>`});if(depId&&!cands.some(c=>c.id===depId)){const to=(DATA.tasks||[]).find(t=>t.id===depId);const label=to?App._seqOf(depId)+" · "+(to.name||""):depId;optsHtml+=`<optgroup label="（目前範圍外）"><option value="${U.esc(depId)}" selected>${U.esc(label)}（範圍外）</option></optgroup>`}const rels=App.PRED_RELATIONS.map(r=>`<option value="${r.code}" ${type===r.code?"selected":""}>${U.esc(r.label)}</option>`).join("");return`
      <div class="pred-row">
        <div class="pred-field">
          <label class="pred-field-label">🔗 要接在這個任務之後</label>
          <div class="pred-field-line">
            <select class="pred-search">${optsHtml}</select>
            <button type="button" class="pred-del" onclick="App.removePredRow(this)" title="刪除這條前置">✕</button>
          </div>
        </div>
        <div class="pred-field">
          <label class="pred-field-label">🔀 兩者的銜接方式</label>
          <select class="pred-rel">${rels}</select>
        </div>
        <div class="pred-field">
          <label class="pred-field-label">⏳ 中間留幾天緩衝</label>
          <input type="number" class="pred-lag" value="${lag}" step="1" min="0">
          <div class="field-hint">前置完成後想多等幾天再開始（等材料、簽核）才需要填，不需要則維持 0。</div>
        </div>
      </div>`};App.buildPredListHtml=function(t){const cands=App.predCandidates(t.project,t.id,t.stage,t.variant);App._predCands=cands;App._predProj=t.project;App._predSelfId=t.id;App._predVariant=t.variant;const preds=parsePredecessors(t.predecessor);const rows=preds.length?preds.map(p=>App._predRowHtml(p,cands)).join(""):App._predRowHtml(null,cands);return`
      <div class="field-hint pred-intro">設定這個任務接在哪個任務之後，系統會自動排好開始日期。</div>
      <div id="tf-pred-list">${rows}</div>
      <button type="button" class="pred-add" onclick="App.addPredRow()">＋ 加一條前置</button>
      <div class="tip pred-example">想成『這件事要排在誰後面』。例如本任務要等『#16 模具開發』做完、再隔 2 天材料到位才動工 → 選 #16、選『完成後才開始』、緩衝填 2。</div>`};App.serializePredecessors=function(){const rows=Array.from(document.querySelectorAll("#tf-pred-list .pred-row"));const parts=[];for(const row of rows){const sel=row.querySelector(".pred-search");const id=sel?(sel.value||"").trim():"";if(!id)continue;const type=(row.querySelector(".pred-rel")||{}).value||"FS";const lagEl=row.querySelector(".pred-lag");const lagVisible=lagEl&&lagEl.style.display!=="none";const lag=lagVisible?parseInt(lagEl.value,10)||0:0;let token=id+"#"+type;if(lag>0)token+="+"+lag;else if(lag<0)token+=lag;parts.push(token)}return parts.join(",")};App.addPredRow=function(){const list=document.getElementById("tf-pred-list");if(!list)return;list.insertAdjacentHTML("beforeend",App._predRowHtml(null,App._predCands||[]))};App.removePredRow=function(btn){const list=document.getElementById("tf-pred-list");const row=btn.closest(".pred-row");if(!list||!row)return;row.remove();if(!list.querySelector(".pred-row"))App.addPredRow()};App.onTaskStageChange=function(){const list=document.getElementById("tf-pred-list");if(!list)return;const newStage=(document.getElementById("tf-stage")||{}).value||"";const current=Array.from(list.querySelectorAll(".pred-row")).map(row=>{const sel=row.querySelector(".pred-search");const id=sel?(sel.value||"").trim():"";if(!id)return null;const type=(row.querySelector(".pred-rel")||{}).value||"FS";const lagEl=row.querySelector(".pred-lag");const lag=lagEl?parseInt(lagEl.value,10)||0:0;return{dep:id,type,lag}}).filter(Boolean);const cands=App.predCandidates(App._predProj,App._predSelfId,newStage,App._predVariant);App._predCands=cands;const inWindow=new Set(cands.map(c=>c.id));const kept=current.filter(p=>inWindow.has(p.dep));const dropped=current.length-kept.length;list.innerHTML=kept.length?kept.map(p=>App._predRowHtml(p,cands)).join(""):App._predRowHtml(null,cands);if(dropped>0){U.toast("⚠️"+dropped+" 筆前置因階段調整超出可選範圍，已移除","warning",{soft:true})}};App.startModeOf=function(t){if(t&&(t.startMode==="manual"||t.startMode==="auto"))return t.startMode;return t&&t.start&&String(t.start).trim()?"manual":"auto"};App.readStartField=function(){const el=document.getElementById("tf-start");if(!el)return{startMode:"auto",start:""};const form=document.querySelector(".task-form");if(form&&form.dataset.measure==="hours"){const v=el.value||"";return{startMode:"manual",start:v}}if(el.dataset.autostart==="1")return{startMode:"auto",start:""};const val=el.value||"";return{startMode:val?"manual":"auto",start:val}};App.readEffStart=function(){const manual=(document.getElementById("tf-start")||{}).value||"";if(manual)return manual;return(document.getElementById("tf-effstart")||{}).value||""};App.readDurationField=function(){const start=App.readEffStart();const endVal=(document.getElementById("tf-end")||{}).value||"";const durRaw=parseFloat((document.getElementById("tf-duration")||{}).value);const taskType=(document.getElementById("tf-taskType")||{}).value;if(taskType==="milestone")return 1;if(start&&endVal)return D.deriveDurationFromEnd(start,endVal);return isNaN(durRaw)?1:durRaw};App.recalcTaskTimeFields=function(){const startEl=document.getElementById("tf-start");const durEl=document.getElementById("tf-duration");const endEl=document.getElementById("tf-end");if(!durEl||!endEl)return;const start=App.readEffStart();if(!start)return;const dur=parseFloat(durEl.value);if(isNaN(dur))return;endEl.value=D.fmt(D.addWorkdays(start,dur-1),"iso")};App.bindTaskTimeListeners=function(){if(App._taskTimeDelegated)return;App._taskTimeDelegated=true;const f=e=>{const id=e.target&&e.target.id;if(e.target&&e.target.classList)e.target.classList.remove("tf-invalid");if(id==="tf-start"&&e.target.dataset)delete e.target.dataset.autostart;if(id==="tf-duration"||id==="tf-start")App.recalcTaskTimeFields();if(id==="tf-duration"||id==="tf-end"||id==="tf-start"){if(App._tfNetConv)App._tfNetConv()}};document.addEventListener("input",f);document.addEventListener("change",f)};App._markTaskRequired=function(reqs){const missing=[];reqs.forEach(r=>{const e=document.getElementById(r.id);if(!e)return;const empty=!(e.value||"").trim();e.classList.toggle("tf-invalid",empty);if(empty)missing.push(r.name)});return missing};App.buildHintBox=function(opts){const o=opts||{};const key=o.key||"";const _hbStored=(DATA.settings.hintBoxState||{})[key];const collapsed=_hbStored===void 0?!!o.collapsed:!!_hbStored;const icon=o.icon?`<i class="ti ${o.icon}"></i>`:"";const summary=o.summary?`<span class="hintbox-summary">${U.esc(o.summary)}</span>`:"";const tip=collapsed?` data-tip="${U.esc((o.title||"")+"|"+(o.summary||"")+" — 點擊展開看完整說明")}"`:"";return`<div class="hintbox${collapsed?" collapsed":""}" data-hintkey="${U.esc(key)}">
    <div class="hintbox-bar" onclick="App.toggleHintBox('${U.esc(key)}')"${tip}>
      <span class="hintbox-head">${icon}<b class="hintbox-title">${U.esc(o.title||"")}</b>${summary}</span>
      <span class="hintbox-toggle">${collapsed?"展開▾":"收起▴"}</span>
    </div>
    <div class="hintbox-body">${o.bodyHtml||""}</div>
  </div>`};App.toggleHintBox=function(key){if(!DATA.settings.hintBoxState)DATA.settings.hintBoxState={};DATA.settings.hintBoxState[key]=!DATA.settings.hintBoxState[key];Store.settings.save();const box=document.querySelector('.hintbox[data-hintkey="'+key+'"]');if(!box)return;const collapsed=!!DATA.settings.hintBoxState[key];box.classList.toggle("collapsed",collapsed);const tg=box.querySelector(".hintbox-toggle");if(tg)tg.textContent=collapsed?"展開▾":"收起▴";const bar=box.querySelector(".hintbox-bar");if(bar){if(collapsed){const title=(box.querySelector(".hintbox-title")||{}).textContent||"";const summary=(box.querySelector(".hintbox-summary")||{}).textContent||"";bar.setAttribute("data-tip",title+"|"+summary+" — 點擊展開看完整說明")}else{bar.removeAttribute("data-tip")}}};App.buildTaskFormHtml=function(task,mode,measure="duration"){const t=task||{};const v=x=>x==null?"":x;const startMode=mode==="new"?"auto":App.startModeOf(t);const effSch=getEffectiveSchedule(t);const deptNames=[...new Set((DATA.projects||[]).flatMap(p=>(p.depts||[]).map(d=>(d.name||"").trim())).filter(Boolean))];const isAutoStart=startMode==="auto";const startInputVal=isAutoStart?effSch.start||"":v(t.start);const startHint=isAutoStart?effSch.start?"預計開始目前依前置排到 "+D.fmt(effSch.start,"ymd")+"；直接改此日即固定為起點，下游接著排。改完成日會自動反推工期。":"預計開始留白＝依前置自動排；填入日期即固定為起點。改完成日會自動反推工期。":"預計開始已固定為起點，下游接著排；清空可改回依前置自動排。改完成日會自動反推工期。";const _held=t.status==="hold";const _dstat=App._deriveStatus(t.actualStart,t.actualEnd,_held,t.status);const _stChip=App._TF_STMAP[_dstat]||App._TF_STMAP.pending;const _spanWd=Number(t.durationDays)||0;const _netVal=t.netWorkDays!=null&&t.netWorkDays!==""?t.netWorkDays:"";const _netShown=_netVal!==""&&Number(_netVal)>0;const _advFilled=[t.predecessor&&String(t.predecessor).trim(),t.taskType&&t.taskType!=="task",t.riskHL,t.riskIssue&&String(t.riskIssue).trim(),t.deliverable&&String(t.deliverable).trim(),t.deliverableLink&&String(t.deliverableLink).trim(),t.note&&String(t.note).trim()].filter(Boolean).length;return`
    <div class="task-form tf-redesign" data-measure="${measure}">
    ${mode==="new"?`
    <div class="form-field">
      <label>專案 *</label>
      <select id="tf-project"><option value="" ${!t.project?"selected":""}>— 請選擇 —</option>${DATA.projects.map(p=>`<option value="${p.id}" ${t.project===p.id?"selected":""}>${U.esc(p.name)}</option>`).join("")}</select>
    </div>`:`
    <div class="form-field tf-proj-field">
      <label>專案</label>
      <div class="task-proj-readonly">${U.esc((DATA.projects.find(p=>p.id===t.project)||{}).name||"")}</div>
    </div>`}
    <div class="form-field tf-field-name">
      <label>任務名稱 *</label>
      <input type="text" id="tf-name" value="${U.esc(v(t.name))}" placeholder="例：完成 BOM 表 6 型壁掛機">
    </div>
    <div class="form-row">
      <div class="form-field"><label>階段 *</label>
        <input type="text" id="tf-stage" list="tf-stage-list" value="${U.esc(v(t.stage))}" placeholder="輸入或選擇階段" onchange="App.onTaskStageChange()">
        <datalist id="tf-stage-list">${this.stageDatalistOptions(t.project)}</datalist>
      </div>
      <div class="form-field"><label>負責人 *</label><input type="text" id="tf-owner" value="${U.esc(v(t.owner)||(mode==="new"?DATA.settings.userName||"":""))}"></div>
    </div>

    <div class="form-field mg-hours"><label>部門 <span data-tip="部門|這件雜事掛哪個部門：該部門的可用容量會扣掉它的預估工時（雜事＝容量流失·總覽部門水位即時反映）；選項為全專案出現過的部門" style="cursor:help;">?</span></label><!-- 稽核中11+高08B：tooltip 改寫為當前真行為（部門容量扣減） -->
      <select id="tf-dept">
        <option value="">未指派</option>
        ${deptNames.map(n=>`<option value="${U.esc(n)}" ${(t.dept||"")===n?"selected":""}>${U.esc(n)}</option>`).join("")}
      </select>
    </div>

    <div class="tf-sched-card">
      <div class="tf-sched-title"><i class="ti ti-clock-bolt" aria-hidden="true"></i>排程與進度</div>
      <div class="tf-chain">
        <div class="tf-chain-cell tf-start-cell">
          <div class="tf-cell-label">預計開始</div>
          <input type="date" id="tf-start" value="${startInputVal}"${isAutoStart?' data-autostart="1"':""}>
        </div>
        <div class="tf-chain-arrow dur-only"><i class="ti ti-arrow-right" aria-hidden="true"></i></div>
        <div class="tf-chain-cell tf-dur-cell mg-duration">
          <div class="tf-cell-label tf-cell-accent">工期（工作天）</div><!-- 稽核機66：天＝工作天標明（hint 有講欄名沒講） -->
          <input type="number" id="tf-duration" value="${v(t.durationDays)||1}" step="1">
        </div>
        <div class="tf-chain-cell tf-hours-cell mg-hours">
          <div class="tf-cell-label">預估工時 (h)</div>
          <input type="number" id="tf-hours" value="${v(t.estHours)||1}" min="0.5" step="0.5">
        </div>
        <div class="tf-chain-arrow dur-only"><i class="ti ti-arrow-right" aria-hidden="true"></i></div>
        <div class="tf-chain-cell tf-end-cell dur-only">
          <div class="tf-cell-label">預計完成 / Deadline</div>
          <input type="date" id="tf-end" value="${v(effSch.end)}">
        </div>
      </div>
      <input type="hidden" id="tf-effstart" value="${v(effSch.start)}">
      <div class="field-hint tf-chain-hint dur-only">${startHint}</div>

      <div class="tf-actual-row">
        <div class="tf-actual-cell"><div class="tf-cell-label tf-actual-lbl">實際開始</div><input type="date" id="tf-actualStart" value="${v(t.actualStart)}" onchange="App._tfSyncStatus()"></div>
        <div class="tf-actual-cell"><div class="tf-cell-label tf-actual-lbl">實際完成</div><input type="date" id="tf-actualEnd" value="${v(t.actualEnd)}" onchange="App._tfSyncStatus()"></div>
      </div>
      <div class="tf-statusbar">
        <span class="tf-stchip ${_stChip[1]}" id="tf-stchip" data-prev="${U.esc(v(t.status))}">● ${_stChip[0]}</span>
        <span class="tf-stlock"><i class="ti ti-lock" aria-hidden="true"></i> 狀態依實際日期自動判定；勾「擱置」則覆蓋</span>
        <label class="tf-hold"><input type="checkbox" id="tf-hold" ${_held?"checked":""} onchange="App._tfSyncStatus()"> 擱置</label>
      </div>
      <div class="tf-holdreason" id="tf-holdreason-wrap" style="${_held?"":"display:none"}">
        <input type="text" id="tf-holdReason" value="${U.esc(v(t.holdReason))}" placeholder="擱置原因（便於日後復盤）">
      </div>

      <div class="dur-only">${App.buildHintBox({key:"task-time",icon:"ti-clock-bolt",title:"時間怎麼連動",summary:"填兩個，第三個自動算",collapsed:true,bodyHtml:'<div class="ht-rule ht-start"><b>改開始日</b><span>工期不動，自動算出新的完成日。例：開始改 6/25、工期 5 天 → 完成自動變 7/1（跳週末與國定假日）。</span></div><div class="ht-rule ht-dur"><b>改工期</b><span>開始日當錨不動，自動算出新的完成日。例：工期改 7 天 → 完成日往後移到第 7 個工作天。</span></div><div class="ht-rule ht-end"><b>改完成日</b><span>開始日不動，回算工期（等於調整這任務要做多久）。例：完成改 7/3 → 工期自動變成 6/25 到 7/3 的工作天數。</span></div><div class="ht-rule ht-down"><b>下游連動</b><span>這任務時間一改，有設前置的下游任務跟著自動重排；你手動指定過日期的任務不會被動到。</span></div>'})}</div>
    </div>

    <div class="form-row">
      <div class="form-field dur-only"><label>每日投入程度</label><!-- 稽核中11：時段制隱藏——hours 任務的 effortRatio 全站零消費（負荷走 estHours 容量扣減·高08B），顯示會讓人以為有作用＝UI 說謊 -->
        <select id="tf-effort"${t.netWorkDays>0?' title="此任務已填精算淨工作天——負荷以淨工作天為準，投入% 不參與計算（§18.10d 第一優先）"':""}>${App._effortOptionsFull(t.effortRatio,mode==="edit")}</select><!-- 稽核中05：編輯態空值顯「未設定」不假裝 100；netWorkDays 優先時 title 說明 % 不生效 -->
        ${App._effortLogHtml(t)}
      </div>
      <div class="form-field"><label>緊急程度 <span data-tip="緊急程度|系統自動推算，可手動覆蓋" style="cursor:help;">?</span></label>
        <select id="tf-urgency">
          <option value="high" ${t.urgency==="high"?"selected":""}>🔴 緊急</option>
          <option value="medium" ${t.urgency==="medium"||!t.urgency?"selected":""}>🔵 普通</option>
          <option value="low" ${t.urgency==="low"?"selected":""}>⚪ 不急</option>
        </select>
      </div>
    </div>
    <div class="form-field">
      <div class="field-hint dur-only">資源負荷計算基準：單日累計投入超過 100% 時，系統將警示資源過載。</div><!-- 稽核中11：此承諾只在工期制成立（時段制不進投入%告警）——時段制隱藏 -->
      <button type="button" class="tf-netdays-btn dur-only" id="tf-netdays-btn" onclick="App.toggleNetDays()" style="${_netShown?"display:none":""}">＋ 精算淨工作天</button>
      <div class="tf-netdays dur-only" id="tf-netdays-block" style="${_netShown?"":"display:none"}">
        <div class="tf-netdays-head">🎯 淨工作天（資源精算）<span class="tf-netdays-x" onclick="App.toggleNetDays(true)">✕ 收合</span></div>
        <div class="tf-netdays-derived">系統依預計工期初估為 <b id="tf-netdays-span">${_spanWd||"—"}</b> 個工作天。</div>
        <div class="tf-netdays-row">
          <input type="number" id="tf-netdays" min="0" step="0.5" value="${_netVal}" oninput="App._tfNetConv()" placeholder="—"> 個工作天
          <span class="tf-netdays-conv" id="tf-netdays-conv"></span>
        </div>
        <div class="tf-netdays-warn" id="tf-netdays-warn" style="display:none"></div>
        <div class="field-hint">請填寫扣除等待與閒置時間後的實際作業天數。此數值僅用於精算資源負荷，不影響排程區間；若留空，將依每日投入程度自動計算。</div>
      </div>
    </div>

    <div class="form-collapse" id="tf-advSection">
      <div class="form-collapse-head" onclick="document.getElementById('tf-advSection').classList.toggle('open')">
        <span class="form-collapse-chevron">▸</span> 進階設定${_advFilled>0?'<span class="tf-adv-badge">'+_advFilled+" 項已填</span>":""}
        <span class="tf-adv-cap">前置任務 · 類型 · 交付物 · 風險 · 備註</span>
      </div>
      <div class="collapse-body">
        <div class="form-field dur-only tf-pred-field">
          <label>前置任務 <span class="tf-sublabel">· 若留空則依預設排序；設定後將自動連動並推算下游任務排程</span></label>
          ${App.buildPredListHtml(t)}
        </div>
        <div class="form-row">
          <div class="form-field"><label>類型 <span data-tip="類型|任務=要排程的工作；里程碑=時間點標記（工期0）；群組=純分類母項，不排程" style="cursor:help;">?</span></label>
            <select id="tf-taskType">
              <option value="task" ${t.taskType==="task"||!t.taskType?"selected":""}>📋 任務</option>
              <option value="milestone" ${t.taskType==="milestone"?"selected":""}>◆ 里程碑</option>
              <option value="group" ${t.taskType==="group"?"selected":""}>▦ 群組</option>
            </select>
          </div>
          <div class="form-field"><label>風險標記</label>
            <label class="tf-chk"><input type="checkbox" id="tf-riskHL" ${t.riskHL?"checked":""} onchange="App._tfToggleRisk()"> 標示為高風險任務 (High Risk)</label>
          </div>
        </div>
        <div class="form-field" id="tf-riskissue-wrap" style="${t.riskHL?"":"display:none"}">
          <label>風險內容</label>
          <textarea id="tf-riskIssue" placeholder="請描述風險與因應方式">${U.esc(v(t.riskIssue))}</textarea>
        </div>
        <div class="form-field">
          <label>交付物</label>
          <textarea id="tf-deliverable" placeholder="請簡述本任務的最終產出或驗收標準（選填）">${U.esc(v(t.deliverable))}</textarea>
        </div>
        <div class="form-field">
          <label>交付物連結</label>
          <div class="tf-link-wrap"><i class="ti ti-link tf-link-ico" aria-hidden="true"></i><input type="text" id="tf-deliverableLink" class="tf-has-ico" value="${U.esc(v(t.deliverableLink))}" placeholder="請貼上檔案或雲端資料夾連結（選填）"></div>
        </div>
        <div class="form-field">
          <label>備註</label>
          <input type="text" id="tf-note" value="${U.esc(v(t.note))}">
        </div>
      </div>
    </div>
    </div>
  `};App._TF_STMAP={pending:["未開始","st-pending"],wip:["進行中","st-wip"],done:["已完成","st-done"],hold:["擱置中","st-hold"]};App._deriveStatus=function(actualStart,actualEnd,held,prevStatus){if(actualEnd)return"done";if(held)return"hold";if(actualStart)return"wip";if(prevStatus==="done")return"done";if(prevStatus==="wip")return"wip";return"pending"};App._tfSyncStatus=function(){const chip=document.getElementById("tf-stchip");if(!chip)return;const held=!!(document.getElementById("tf-hold")||{}).checked;const aS=(document.getElementById("tf-actualStart")||{}).value||"";const aE=(document.getElementById("tf-actualEnd")||{}).value||"";const st=App._deriveStatus(aS,aE,held,chip.dataset.prev||"");const m=App._TF_STMAP[st]||App._TF_STMAP.pending;chip.className="tf-stchip "+m[1];chip.textContent="● "+m[0];const hr=document.getElementById("tf-holdreason-wrap");if(hr)hr.style.display=held?"":"none"};App.toggleNetDays=function(close){const blk=document.getElementById("tf-netdays-block"),btn=document.getElementById("tf-netdays-btn");if(!blk)return;if(close){blk.style.display="none";if(btn)btn.style.display="";const inp=document.getElementById("tf-netdays");if(inp)inp.value="";App._tfNetConv()}else{blk.style.display="";if(btn)btn.style.display="none";const inp=document.getElementById("tf-netdays");if(inp)inp.focus();App._tfNetConv()}};App._tfNetConv=function(){const inp=document.getElementById("tf-netdays");if(!inp)return;const conv=document.getElementById("tf-netdays-conv"),warn=document.getElementById("tf-netdays-warn"),spanEl=document.getElementById("tf-netdays-span");const durEl=document.getElementById("tf-duration");const span=durEl?parseFloat(durEl.value):0;if(spanEl&&span>0)spanEl.textContent=span;const nd=parseFloat(inp.value);if(!(nd>0)||!(span>0)){if(conv)conv.textContent="";if(warn)warn.style.display="none";return}const pct=Math.round(nd/span*100);if(conv)conv.textContent="≈ "+pct+"%／天";if(warn){const effSel=document.getElementById("tf-effort");const effVal=effSel?parseInt(effSel.value,10):100;warn.style.display="";warn.innerHTML="負荷以淨工作天精算為準（<b>≈"+pct+"%／天</b>），已覆蓋每日投入程度（"+effVal+"%）。落差大屬正常——反映此任務跨度長、真正動工天數少。"}};App._tfToggleRisk=function(){const cb=document.getElementById("tf-riskHL"),wrap=document.getElementById("tf-riskissue-wrap");if(wrap)wrap.style.display=cb&&cb.checked?"":"none"};App._NETDAYS_MIN_SPAN=()=>DATA.settings&&DATA.settings.longTaskDays>0?DATA.settings.longTaskDays:5;App._promptNetDaysCalib=function(taskId){const t=(DATA.tasks||[]).find(x=>x.id===taskId);if(!t)return;const span=Number(t.durationDays)||0;const sch=getEffectiveSchedule(t)||{};const hasNet=t.netWorkDays>0;App.openModal({title:"實際工作天數回報",body:'<div class="netcalib"><ul class="netcalib-info"><li><b>任務：</b>'+U.esc(t.name)+"</li><li><b>預期進度：</b>"+span+" 天（"+D.fmt(sch.start,"md")+" → "+D.fmt(sch.end,"md")+'）</li></ul><div class="netcalib-opts"><label class="netcalib-opt"><input type="radio" name="netcalib-mode" value="est"'+(hasNet?"":" checked")+' onchange="App._netCalibMode()"><span>採用預估工期（'+span+' 天）</span></label><label class="netcalib-opt"><input type="radio" name="netcalib-mode" value="actual"'+(hasNet?" checked":"")+' onchange="App._netCalibMode()"><span>填寫實際工期：</span><input type="number" id="netcalib-input" min="0" step="0.5" value="'+(hasNet?t.netWorkDays:"")+'" placeholder="填寫數字"'+(hasNet?"":" disabled")+'><span class="netcalib-unit">天</span></label></div><div class="netcalib-feel"><span class="netcalib-feel-lbl">主觀感受：</span><label class="netcalib-feel-opt"><input type="radio" name="netcalib-feel" value="lighter"'+(t.calibFeel==="lighter"?" checked":"")+'><span>比預估輕</span></label><label class="netcalib-feel-opt"><input type="radio" name="netcalib-feel" value="asExpected"'+(t.calibFeel==="asExpected"?" checked":"")+'><span>差不多</span></label><label class="netcalib-feel-opt"><input type="radio" name="netcalib-feel" value="heavier"'+(t.calibFeel==="heavier"?" checked":"")+'><span>比預估重</span></label></div><div class="netcalib-desc">💡 此數據將用於計算您的工時負荷，並讓系統分析預期進度與實際狀況的差異，以利抓出延遲風險與優化未來排程。（若目前無法評估可先取消）</div></div>',footer:`<button class="tb-action ghost" onclick="App.closeModal()">取消</button><button class="tb-action" onclick="App._netDaysCalibOk('`+t.id+`')">確認更新</button>`});setTimeout(()=>{App._netCalibMode()},50)};App._netCalibMode=function(){const actual=document.querySelector('input[name="netcalib-mode"][value="actual"]');const inp=document.getElementById("netcalib-input");if(!inp)return;const on=!!(actual&&actual.checked);inp.disabled=!on;if(on){inp.focus();if(inp.select)inp.select()}};App._netDaysCalibOk=function(taskId){const t=(DATA.tasks||[]).find(x=>x.id===taskId);const mode=(document.querySelector('input[name="netcalib-mode"]:checked')||{}).value;let n;if(mode==="actual"){const i=document.getElementById("netcalib-input");n=i?parseFloat(i.value):NaN}else{n=Number(t&&t.durationDays)||0}App.closeModal();const feel=(document.querySelector('input[name="netcalib-feel"]:checked')||{}).value||"";if(t&&n>0){t.netWorkDays=n;if(feel)t.calibFeel=feel;Store.tasks.save();App.refreshAll();U.toast("已記錄實際淨工作天："+n+" 天","success")}};App.openNewTaskDialog=function(projId){this.openModal({title:"新增任務",body:App.buildTaskFormHtml({project:projId,start:D.fmt(D.today(),"iso")},"new"),footer:`
      <button class="tb-action ghost" onclick="App.closeModal()">取消</button>
      <button class="tb-action" data-edit-hide onclick="App.saveNewTask('${projId}')">建立任務</button>
    `});App.bindTaskTimeListeners();setTimeout(()=>{const nameField=document.getElementById("tf-name");if(nameField)nameField.focus()},50)};App.openHoursTaskDialog=function(){this.openModal({title:"新增小時 Task",body:App.buildTaskFormHtml({start:D.fmt(D.today(),"iso")},"new","hours"),footer:`<button class="tb-action ghost" onclick="App.closeModal()">取消</button>
             <button class="tb-action" data-edit-hide onclick="App.saveNewTask()">建立任務</button>`});App.bindTaskTimeListeners();setTimeout(()=>{const n=document.getElementById("tf-name");if(n)n.focus()},50)};App.saveNewTask=function(projId,_skipNegCheck){if(App._roGuard())return;const _miss=App._markTaskRequired([{id:"tf-project",name:"專案"},{id:"tf-name",name:"任務名稱"},{id:"tf-owner",name:"負責人"},{id:"tf-stage",name:"階段"}]);if(_miss.length){U.toast("⚠ 請填必填欄位："+_miss.join("、"),"warning");return}const name=document.getElementById("tf-name").value.trim();const _aS=(document.getElementById("tf-actualStart")||{}).value||"";const _aE=(document.getElementById("tf-actualEnd")||{}).value||"";const _held=!!(document.getElementById("tf-hold")||{}).checked;const _nd=parseFloat((document.getElementById("tf-netdays")||{}).value);const status=App._deriveStatus(_aS,_aE,_held,"");const startField=App.readStartField();const _negStart=App.readEffStart();const _pEnd=document.getElementById("tf-end").value;const _taskTypeV=document.getElementById("tf-taskType").value;if(!_skipNegCheck&&_taskTypeV!=="milestone"&&_negStart&&_pEnd&&_pEnd<_negStart){App.confirmModal({title:"工期為負數",msg:"預計完成日早於開始日（工期為負數），確認要這樣修改嗎？系統會照您輸入儲存。",okText:"確認儲存",cancelText:"取消",onConfirm:()=>App.saveNewTask(projId,true)});return}const task={id:U.id(),project:document.getElementById("tf-project").value||projId,name,desc:"",owner:document.getElementById("tf-owner").value.trim(),dept:document.querySelector(".task-form").dataset.measure==="hours"?(document.getElementById("tf-dept")||{}).value||"":"",category:"deep",taskType:document.getElementById("tf-taskType").value,stage:document.getElementById("tf-stage").value.trim(),subgroup:"",urgency:document.getElementById("tf-urgency").value,status,actualStart:_aS,actualEnd:_aE,holdReason:_held?((document.getElementById("tf-holdReason")||{}).value||"").trim():"",netWorkDays:_nd>0?_nd:void 0,start:startField.start,startMode:startField.startMode,estHours:parseFloat(document.getElementById("tf-hours").value)||1,effortRatio:(()=>{const _v=parseInt((document.getElementById("tf-effort")||{}).value,10);return isNaN(_v)?100:_v})(),predecessor:App.serializePredecessors(),wbs:"",durationDays:App.readDurationField(),measureType:document.querySelector(".task-form").dataset.measure||"duration",scheduledStart:"",scheduledEnd:"",parentWbsId:"",method:"",riskHL:document.getElementById("tf-riskHL").checked,riskIssue:document.getElementById("tf-riskIssue").value.trim(),deliverable:document.getElementById("tf-deliverable").value.trim(),deliverableLink:document.getElementById("tf-deliverableLink").value.trim(),deliverableType:"",requiredTask:true,mustIssue:false,note:document.getElementById("tf-note").value.trim(),canSplit:true,scheduleToCalendar:false,completedAt:status==="done"?new Date().toISOString():null,createdAt:new Date().toISOString()};if(App._insertAfterId){if(!task.predecessor)task.predecessor=App._insertAfterId+"#FS";const _i=DATA.tasks.findIndex(x=>x.id===App._insertAfterId);if(_i>=0){DATA.tasks.splice(_i+1,0,task)}else{DATA.tasks.push(task)}App._insertAfterId=null}else{DATA.tasks.push(task)}if(task.measureType==="hours"){const _ps=document.getElementById("tf-start")&&document.getElementById("tf-start").value||D.fmt(D.today(),"iso");if(!DATA.schedule||!Array.isArray(DATA.schedule.items))DATA.schedule={week:null,items:[]};DATA.schedule.items.push({taskId:task.id,date:_ps,start:DATA.settings.workStart1||"09:00",duration:Math.max(30,Math.round((parseFloat(task.estHours)||1)*60)),chunk:null,totalHours:parseFloat(task.estHours)||1,week:D.weekKey(new Date(_ps)),locked:false,provisional:true})}const _sch=applySchedule(DATA.tasks,"full");const _blocked=_sch.skipped.filter(s=>!String(s.reason||"").startsWith("anchor"));const _pid=this.currentProjectId;if(_pid){const _proj=(DATA.projects||[]).find(p=>p.id===_pid);const _projBlocked=_blocked.filter(b=>(DATA.tasks.find(t=>t.id===b.id)||{}).project===_pid);if(_projBlocked.length){U.toast("⚠️【"+(_proj&&_proj.name||"本專案")+"】"+_projBlocked.length+" 筆任務無法排程（循環或缺前置）","warning")}}Store.tasks.save();Store.schedule.save();this.closeModal();this.refreshAll();U.toast(`✓ 已新增「${name}」`)};App.openTaskInProject=function(id){const task=DATA.tasks.find(t=>t.id===id);if(!task){U.toast("⚠ 找不到任務","warning");return}this.currentProjectId=task.project;const btn=document.querySelector(`.sb-proj[onclick*="${task.project}"]`);this.showPage("project",btn);setTimeout(()=>{this.openTaskModal(id)},100)};App.openTaskModal=function(id){const t=DATA.tasks.find(x=>x.id===id);if(!t)return;const sch=getEffectiveSchedule(t);const proj=this.getProj(t.project);const currentWeekBadge=t.currentWeek&&t.status!=="done"?`<span style="display:inline-block; margin-left:8px; padding:2px 8px; background:var(--terracotta-l); color:var(--terracotta); border-radius:10px; font-size:11px; font-weight:600;">${U.esc(t.currentWeek)} <span style="color:var(--terracotta);">⁂</span></span>`:t.currentWeek?`<span style="display:inline-block; margin-left:8px; padding:2px 8px; background:var(--sage-50); color:var(--sage-700); border-radius:10px; font-size:11px; font-weight:600;">${U.esc(t.currentWeek)} ✓</span>`:"";const history=t.history||[];let historyHtml="";if(history.length>0){const rows=history.map(h=>{const statusColor=h.status?.includes("完成")?"var(--sage-700)":h.status?.includes("延遲")?"var(--terracotta)":"var(--ink2)";return`<tr>
        <td class="col-num" style="font-family:var(--mono); font-size:10.5px; color:var(--ink3);">${U.esc(h.week||"")}</td>
        <td class="col-num" style="color:${statusColor};">${U.esc(h.status||"")}</td>
        <td class="col-flex col-wrap" style="line-height:1.4;">${U.esc(h.work||"—")}</td>
        <td class="col-mid col-wrap" style="font-family:var(--mono); font-size:10.5px; color:var(--ink3);">${h.planEnd||"—"}${h.planEndOriginal&&h.planEndOriginal!==h.planEnd?'<br><span style="color:var(--ink4); font-size:10px;">原:'+h.planEndOriginal+"</span>":""}</td>
        <td class="col-mid" style="font-family:var(--mono); font-size:10.5px; color:${h.actualEnd?"var(--sage-700)":"var(--ink3)"};">${h.actualEnd||"—"}</td>
        <td class="col-mid" style="color:var(--terracotta); font-size:11px;" title="${U.esc(h.delayReason||"")}">${U.esc(h.delayReason||"")}</td>
      </tr>`}).join("");historyHtml=`
      <div class="form-field" style="margin-top:18px;">
        <label style="display:flex; align-items:center; gap:8px;">
          📋 歷史紀錄
          <span style="font-size:10.5px; color:var(--ink3); font-weight:400;">（共 ${history.length} 週的執行紀錄）</span>
        </label>
        <div style="border:1px solid var(--rule); border-radius:8px; overflow:hidden; max-height:220px; overflow-y:auto;">
          <table class="data-table" style="font-size:11.5px;">
            <thead>
              <tr>
                <th class="col-num">週次</th>
                <th class="col-num">狀態</th>
                <th class="col-flex">本週工作</th>
                <th class="col-mid">預計完成</th>
                <th class="col-mid">實際完成</th>
                <th class="col-mid">延誤理由</th>
              </tr>
            </thead>
            <tbody>${rows}</tbody>
          </table>
        </div>
      </div>
    `}this.openModal({title:`編輯任務 ${currentWeekBadge}`,body:App.buildTaskFormHtml({...t,start:sch.start,end:sch.end},"edit",t.measureType==="hours"?"hours":"duration")+`${historyHtml}`,footer:`
      <button class="tb-action danger" data-edit-hide onclick="App.deleteTask('${t.id}')" style="margin-right:auto;">刪除任務</button>
      <button class="tb-action ghost" onclick="App.closeModal()">取消</button>
      <button class="tb-action" data-edit-hide onclick="App.saveTask('${t.id}')">儲存</button>
    `});App.bindTaskTimeListeners()};App.saveTask=function(id,_skipNegCheck){if(App._roGuard())return;const t=DATA.tasks.find(x=>x.id===id);if(!t)return;const _miss=App._markTaskRequired([{id:"tf-name",name:"任務名稱"},{id:"tf-owner",name:"負責人"},{id:"tf-stage",name:"階段"}]);if(_miss.length){U.toast("⚠ 請填必填欄位："+_miss.join("、"),"warning");return}const name=document.getElementById("tf-name").value.trim();const _negStart=App.readEffStart();const _pEnd=document.getElementById("tf-end").value;const _taskTypeV=document.getElementById("tf-taskType").value;if(!_skipNegCheck&&_taskTypeV!=="milestone"&&_negStart&&_pEnd&&_pEnd<_negStart){App.confirmModal({title:"工期為負數",msg:"預計完成日早於開始日（工期為負數），確認要這樣修改嗎？系統會照您輸入儲存。",okText:"確認儲存",cancelText:"取消",onConfirm:()=>App.saveTask(id,true)});return}const _aS=document.getElementById("tf-actualStart").value;const _aE=document.getElementById("tf-actualEnd").value;if(_aS&&_aE&&_aE<_aS){U.toast("⚠ 實際完成日不能早於實際開始日","warning");return}t.name=name;t.owner=document.getElementById("tf-owner").value.trim();t.taskType=document.getElementById("tf-taskType").value;t.stage=document.getElementById("tf-stage").value.trim();t.predecessor=App.serializePredecessors();t.durationDays=App.readDurationField();t.measureType=t.measureType||"duration";if(t.measureType==="hours"){const _de=document.getElementById("tf-dept");if(_de)t.dept=_de.value}t.urgency=document.getElementById("tf-urgency").value;{const _ef=document.getElementById("tf-effort");if(_ef&&_ef.value!==""){const _v=parseInt(_ef.value,10);const _nv=isNaN(_v)?100:_v;App._effortLogPush(t,t.effortRatio,_nv);t.effortRatio=_nv}}const startField=App.readStartField();t.start=startField.start;t.startMode=startField.startMode;const _prevAE=t.actualEnd||"";t.actualStart=document.getElementById("tf-actualStart").value;t.actualEnd=document.getElementById("tf-actualEnd").value;t.estHours=parseFloat(document.getElementById("tf-hours").value)||1;t.riskHL=document.getElementById("tf-riskHL").checked;t.riskIssue=document.getElementById("tf-riskIssue").value.trim();t.deliverable=document.getElementById("tf-deliverable").value.trim();t.deliverableLink=document.getElementById("tf-deliverableLink").value.trim();t.note=document.getElementById("tf-note").value.trim();ensureDeliverFields(t);const _held=!!(document.getElementById("tf-hold")||{}).checked;t.holdReason=_held?((document.getElementById("tf-holdReason")||{}).value||"").trim():"";{const _nEl=document.getElementById("tf-netdays");const _nd=_nEl?parseFloat(_nEl.value):NaN;if(_nd>0)t.netWorkDays=_nd;else delete t.netWorkDays}let newStatus=App._deriveStatus(t.actualStart,t.actualEnd,_held,t.status);if(newStatus==="done"){if(t.status!=="done")t.completedAt=t.actualEnd||new Date().toISOString();t.progress=100}else{t.completedAt=null;if(t.progress===100)t.progress=0}t.status=newStatus;const _sch=applySchedule(DATA.tasks,"full");const _blocked=_sch.skipped.filter(s=>!String(s.reason||"").startsWith("anchor"));const _pid=this.currentProjectId;if(_pid){const _proj=(DATA.projects||[]).find(p=>p.id===_pid);const _projBlocked=_blocked.filter(b=>(DATA.tasks.find(t2=>t2.id===b.id)||{}).project===_pid);if(_projBlocked.length){U.toast("⚠️【"+(_proj&&_proj.name||"本專案")+"】"+_projBlocked.length+" 筆任務無法排程（循環或缺前置）","warning")}}Store.tasks.save();this.closeModal();this.refreshAll();U.toast("✓ 任務已儲存");if(t.measureType!=="hours"&&t.status==="done"&&t.actualEnd&&t.actualEnd!==_prevAE&&(Number(t.durationDays)||0)>App._NETDAYS_MIN_SPAN()){App._promptNetDaysCalib(t.id)}};App.deleteTask=function(id){if(App._roGuard())return;App.confirmModal({icon:"ti-trash",iconBg:"--rose-l",iconColor:"--rose-ink",title:"刪除任務？",msg:"刪除的任務會移到專案下方「🗑 已刪除」區塊保留 14 天，期間可隨時還原。",okText:"刪除",cancelText:"取消",okClass:"danger",onConfirm:()=>{const t=DATA.tasks.find(x=>x.id===id);if(!t)return;t._deleted=true;t._deletedAt=new Date().toISOString();if(DATA.schedule&&DATA.schedule.items){DATA.schedule.items=DATA.schedule.items.filter(it=>it.taskId!==id)}Store.tasks.save();Store.schedule.save();App.closeModal();App.refreshAll();U.toast("✓ 已移到「已刪除」區塊（14 天內可還原）")}})};App._ecnForwardVariants=function(variants,tasks){let migrated=false;(variants||[]).forEach(v=>{if(!v.schedule)return;if(!v.schedule.startDate&&tasks){const ss=tasks.filter(t=>t.variant===v.id).map(t=>t.plannedStart).filter(Boolean).sort();if(ss.length)v.schedule.startDate=ss[0]}if(v.schedule.endDate){v.schedule.targetEndDate=v.schedule.endDate;v.schedule.endDate="";migrated=true}v.schedule.direction="forward"});return migrated};App._ecnPts=function(t){const dh=DATA.settings&&DATA.settings.dailyHours||6;return taskIntensity(t)*dh*(t.durationDays||0)};App._EFFORT_TIERS=[{v:0,short:"暫無投入",def:""},{v:10,short:"微幅處理",def:"如：簽核／回信"},{v:25,short:"零星投入",def:"約 ¼ 天"},{v:50,short:"半日投入",def:"約 ½ 天"},{v:75,short:"大半日投入",def:""},{v:100,short:"全日投入",def:"單一專注"}];App._legacyPct=function(){return DATA.settings&&DATA.settings.legacyLoadPct>0?DATA.settings.legacyLoadPct:50};App._ecnEffortOptions=function(v){const unset=v==null?'<option value="" selected>未設定·以 '+App._legacyPct()+"% 估</option>":"";const cur=v!=null?v:-1;return unset+App._EFFORT_TIERS.map(o=>'<option value="'+o.v+'"'+(o.v===cur?" selected":"")+">"+o.v+"% "+o.short+"</option>").join("")};App._effortOptionsFull=function(v,allowUnset){const unset=allowUnset&&v==null?'<option value="" selected>未設定（以 '+App._legacyPct()+"% 保守估）</option>":"";const cur=v!=null?v:allowUnset?-1:100;return unset+App._EFFORT_TIERS.map(o=>'<option value="'+o.v+'"'+(o.v===cur?" selected":"")+">"+o.v+"% · "+o.short+(o.def?"（"+o.def+"）":"")+"</option>").join("")};App._ecnPmEffortOptions=function(cur,floor){const six=[0,10,25,50,75,100];let vals=[floor].concat(six.filter(v=>v>floor));if(cur!=null&&vals.indexOf(cur)<0)vals.push(cur);vals=Array.from(new Set(vals)).sort((a,b)=>a-b);const c=cur!=null?cur:floor;return vals.map(v=>'<option value="'+v+'"'+(v===c?" selected":"")+">"+v+"%</option>").join("")};App._ecnSeedCoord=function(projId){if(App._roGuard&&App._roGuard())return;const proj=this.getProj(projId);if(!proj||!proj.ecnType)return;const cur=(DATA.tasks||[]).filter(t=>t.project===projId&&!t._deleted&&(t.epoch==null||t.epoch===(proj.version||1)));if(cur.some(t=>t.isPmCoord)){U.toast("本案已有 PM 常駐列","info");return}const ds=cur.map(t=>t.plannedStart).filter(Boolean).sort();const de=cur.map(t=>t.plannedEnd).filter(Boolean).sort();if(!ds.length||!de.length){U.toast("任務缺計畫日期——排好日期後再按一次即可帶入 PM 常駐","warning");return}const s0=ds[0],e0=de[de.length-1];const span=Math.max(D.workdaysBetween(s0,e0),1);const daily=DATA.settings&&DATA.settings.dailyHours||6;const _et=tplEcn();const eff=_et&&_et.sizeMeta&&_et.sizeMeta[proj.size]?_et.sizeMeta[proj.size].pmEffort:20;const pmDept=(proj.depts||[]).find(d=>d.name==="PM");const row={id:U.id(),project:projId,wbs:99,parentWbsId:"",name:"PM 設變協調／文件彙整（常駐）",desc:"全程 / PM 協調",category:"deep",taskType:"任務",predecessor:"",durationDays:span,owner:"",dept:pmDept?pmDept.id:"",role:"PM",variant:((proj.variants||[])[0]||{}).id||null,start:"",end:"",plannedStart:s0,plannedEnd:e0,actualStart:"",actualEnd:"",progress:0,status:"pending",urgency:"med",estHours:Math.round(span*daily*(eff/100)*10)/10,method:"",canSplit:false,completedAt:null,createdAt:new Date().toISOString(),scheduledStart:"",scheduledEnd:"",synced:false,stage:"",subgroup:"",mustDeliver:false,deliverableType:"",requiredTask:true,mustIssue:false,deliverable:"",riskIssue:"",delivered:"",deliverableLink:"",note:"",effortRatio:eff,taskAttr:"baseline",isPmCoord:true};DATA.tasks.push(row);if(App._tplPreview&&App._s2EcnMode===projId&&Array.isArray(App._tplPreview.tasks))App._tplPreview.tasks.push(row);Store.tasks.save();U.toast("已帶入 PM 常駐協調（"+eff+"%·全程 "+span+" 工作天），可於常駐列上調","success");this.renderProject()};App._ecnSetEffort=function(taskId,val){if(val==="")return;const res=App._tplPreview;if(!res)return;const t=(res.tasks||[]).find(x=>x.id===taskId);if(!t)return;App._effortLogPush(t,t.effortRatio,parseInt(val,10));t.effortRatio=parseInt(val,10);App._s2EcnPersist()};App._ecnDeptOptions=function(cur){const res=App._tplPreview;const depts=res&&res.depts||[];let html='<option value=""'+(cur?"":" selected")+">—</option>";depts.forEach(d=>{html+='<option value="'+U.esc(d.name)+'"'+(d.name===cur?" selected":"")+">"+U.esc(d.name)+"</option>"});return html};App._ecnSetDept=function(taskId,deptName){const res=App._tplPreview;if(!res)return;const t=(res.tasks||[]).find(x=>x.id===taskId);if(!t)return;const d=(res.depts||[]).find(x=>x.name===deptName);t.role=deptName;t.dept=d?d.id:"";if(!(d&&(d.members||[]).some(m=>m.name===t.owner))){const fm=d?(d.members||[]).find(m=>m.name):null;t.owner=fm?fm.name:""}App._s2EcnPersist()};App._ecnSetOwner=function(taskId,value){const res=App._tplPreview;if(!res)return;const t=(res.tasks||[]).find(x=>x.id===taskId);if(!t)return;t.owner=value;if(value){const d=(res.depts||[]).find(dp=>(dp.members||[]).some(m=>m.name===value));if(d){t.role=d.name;t.dept=d.id}}App._s2EcnPersist()};App._ecnSetStart=function(taskId,val){const res=App._tplPreview;if(!res)return;const t=(res.tasks||[]).find(x=>x.id===taskId);if(!t)return;t.start=val||"";App._reschedulePreview(res.tasks,res.variants,[]);App._s2EcnPersist()};App._ecnSetEnd=function(taskId,val){const res=App._tplPreview;if(!res)return;const t=(res.tasks||[]).find(x=>x.id===taskId);if(!t)return;const eff=getEffectiveSchedule(t);const s=t.start||t.plannedStart||eff.start;if(val&&s)t.durationDays=Math.max(1,D.workdaysBetween(s,val));App._reschedulePreview(res.tasks,res.variants,[]);App._s2EcnPersist()};App._ecnStatusDerive=function(t){const todayIso=D.fmt(D.today(),"iso");if(t.actualEnd||t.status==="done")return{label:"已完成",cls:"done"};if(t.status==="hold")return{label:"擱置",cls:"hold"};if(t.plannedEnd&&t.plannedEnd<todayIso)return{label:"逾期",cls:"delayed"};if(t.actualStart)return{label:"進行中",cls:"wip"};if(t.plannedStart&&t.plannedStart<=todayIso)return{label:"進行中",cls:"wip"};return{label:"未開始",cls:"todo"}};App._ecnEditName=function(span,taskId){const res=App._tplPreview;if(!res)return;const t=(res.tasks||[]).find(x=>x.id===taskId);if(!t)return;const inp=document.createElement("input");inp.className="s2-name-inp";inp.value=t.name;inp.onblur=function(){App._s2SetName(taskId,inp.value)};inp.onkeydown=function(e){if(e.key==="Enter")inp.blur()};span.replaceWith(inp);inp.focus();if(inp.select)inp.select()};App._ecnDelTask=function(taskId){const res=App._tplPreview;if(!res)return;const dependents=(res.tasks||[]).filter(t=>String(t.predecessor||"").split(/[,，;；]/).some(p=>p.split("#")[0].trim()===taskId)).length;App.confirmModal({icon:"ti-trash",iconBg:"--rose-l",iconColor:"--rose-ink",okClass:"danger",title:"刪除任務",msg:"此舉將連帶影響 <b>"+dependents+"</b> 個後置任務的排程，確定刪除？",okText:"刪除",cancelText:"取消",onConfirm:function(){App._s2DelRow(taskId)}})};App._ecnEditTask=function(taskId){const res=App._tplPreview;if(!res)return;const t=(res.tasks||[]).find(x=>x.id===taskId);if(!t)return;const isDone=t.status==="done"||!!t.actualEnd;const isHold=t.status==="hold";const anomaly=isDone?`<div class="form-field"><label>異常變更</label><button class="tb-action ghost" style="color:var(--rose-ink);border-color:var(--rose-l)" onclick="App._ecnLoopTask('`+taskId+`')">↩ 打回重測</button><div class="ecn-edit-hint">👉 點擊後直接連動成因選單（＝完工後被退重做）。</div></div>`:'<div class="form-field"><label>異常變更</label><div class="ecn-edit-hint">※ 任務有「實際完工日」後，方可解鎖「打回重測」。<br>中途追加請用大表列間的「＋」；結案後翻案重啟屬整案結案流程（下一波）。</div></div>';App.openModal({title:"⚙ 編輯任務 · "+U.esc(t.name),body:'<div class="ecn-edit-hint" style="margin-bottom:12px">💡 進度狀態由<b>實際日期</b>判定。（外層大表的「計畫日期」僅供排程參考，不影響狀態）</div><div class="form-row"><div class="form-field"><label>實際開工日</label><input type="date" id="ecn-edit-astart" value="'+(t.actualStart||"")+'"><div class="ecn-edit-hint">填寫即轉為「進行中」</div></div><div class="form-field"><label>實際完工日</label><input type="date" id="ecn-edit-aend" value="'+(t.actualEnd||"")+'"><div class="ecn-edit-hint">填寫即轉為「已完成」，自動退出待消化工時</div></div></div><div class="form-field" style="margin-top:8px"><label class="ecn-edit-chk"><input type="checkbox" id="ecn-edit-hold"'+(isHold?" checked":"")+' onchange="App._ecnEditToggleHold()"> 擱置（暫停這項）</label></div><div class="form-field" id="ecn-edit-holdwrap" style="'+(isHold?"":"display:none")+'"><label>擱置原因（為何暫停這項？結案檢討會用到）</label><textarea id="ecn-edit-holdreason" rows="2" placeholder="例：等客戶確認規格／供應商缺料／上級喊停">'+U.esc(t.holdReason||"")+"</textarea></div>"+App._effortLogHtml(t)+anomaly,footer:`<button class="tb-action ghost" style="color:var(--rose-ink);margin-right:auto" onclick="App._ecnDelTask('`+taskId+`')">🗑 刪除任務</button><button class="tb-action ghost" onclick="App.closeModal()">取消</button><button class="tb-action" onclick="App._ecnEditTaskSave('`+taskId+`')">儲存</button>`})};App._ecnEditToggleHold=function(){const c=document.getElementById("ecn-edit-hold");const w=document.getElementById("ecn-edit-holdwrap");if(w)w.style.display=c&&c.checked?"":"none"};App._ecnEditTaskSave=function(taskId){const res=App._tplPreview;if(!res)return;const t=(res.tasks||[]).find(x=>x.id===taskId);if(!t)return;const hold=!!(document.getElementById("ecn-edit-hold")||{}).checked;const reason=(document.getElementById("ecn-edit-holdreason")||{}).value||"";const astart=(document.getElementById("ecn-edit-astart")||{}).value||"";const aend=(document.getElementById("ecn-edit-aend")||{}).value||"";const _prevAE=t.actualEnd||"";t.actualStart=astart;t.actualEnd=aend;if(aend){t.status="done";t.progress=100;t.holdReason=""}else if(hold){t.status="hold";t.holdReason=reason}else{t.holdReason="";t.status=astart?"wip":"pending";if(typeof t.progress==="number"&&t.progress>=100)t.progress=0}App.closeModal();App._s2EcnPersist();if(t.measureType!=="hours"&&t.status==="done"&&t.actualEnd&&t.actualEnd!==_prevAE&&(Number(t.durationDays)||0)>App._NETDAYS_MIN_SPAN()){App._promptNetDaysCalib(t.id)}};App._ecnLoopTask=function(taskId){App.closeModal();App._ecnCauseModal("loop",function(cause){const res=App._tplPreview;if(!res)return;const orig=(res.tasks||[]).find(x=>x.id===taskId);if(!orig)return;const proj=App.getProj(App._s2EcnMode);if(proj){proj.loopCount=(proj.loopCount||0)+1;(proj.ecnEvents=proj.ecnEvents||[]).push({type:"loop",date:D.fmt(new Date,"iso"),label:"打回重測："+orig.name,cause})}const idx=res.tasks.findIndex(x=>x.id===taskId);const dh=DATA.settings&&DATA.settings.dailyHours||6;const nt={id:U.id(),project:res.project.id,wbs:"",parentWbsId:"",name:"重工："+orig.name,desc:orig.stage||"",category:"deep",taskType:"任務",predecessor:orig.id+"#FS",durationDays:orig.durationDays||1,owner:orig.owner||"",dept:orig.dept||"",role:orig.role||"",variant:orig.variant,start:"",end:"",plannedStart:"",plannedEnd:"",actualStart:"",actualEnd:"",progress:0,status:"pending",urgency:"med",estHours:dh,method:"",canSplit:false,completedAt:null,createdAt:new Date().toISOString(),scheduledStart:"",scheduledEnd:"",synced:false,stage:orig.stage||"",subgroup:"",mustDeliver:false,deliverableType:"",requiredTask:true,mustIssue:false,deliverable:"",riskIssue:"",delivered:"",deliverableLink:"",note:"",effortRatio:orig.effortRatio!=null?orig.effortRatio:50,taskAttr:"baseline",isLoopTask:true,loopFromId:orig.id,causeTag:cause};res.tasks.splice(idx+1,0,nt);App._reschedulePreview(res.tasks,res.variants,[]);App._s2EcnPersist()})};App._ecnSetTab=function(tab){App._ecnTab=tab;const p=App.getProj(App.currentProjectId);if(p)App._bomRerender(p)};App._bomSetSubTab=function(t){App._bomSubTab=t;const p=App.getProj(App.currentProjectId);if(p)App._bomRerender(p)};App._ecnEnsureState=function(proj){if(proj.version==null)proj.version=1;if(!Array.isArray(proj.epochs))proj.epochs=[];if(proj.status==null)proj.status="active"};App._ecnLocked=function(proj){return(proj.status||"active")==="closed"};App._ecnIsCurrentEpoch=function(t,proj){return t.epoch==null||t.epoch===(proj.version||1)};App._ecnEpochPts=function(proj,tasks){return tasks.filter(t=>App._ecnIsCurrentEpoch(t,proj)).reduce((s,t)=>s+App._ecnPts(t),0)};App._ecnStockGate=function(qty,rate,unit,price,effDate){const q=parseFloat(qty)||0,r=parseFloat(rate)||0,pr=parseFloat(price)||0;if(q<=0)return null;if(r<=0){return{runoutDate:null,infinite:true,scrapQty:Math.round(q*100)/100,scrapCost:Math.round(q*pr),red:!!effDate}}const dailyR=unit==="week"?r/7:r;const todayIso=D.fmt(D.today(),"iso");const runoutDate=D.fmt(D.addDays(D.today(),Math.ceil(q/dailyR)),"iso");const daysToEff=effDate?Math.round((new Date(effDate)-new Date(todayIso))/864e5):0;const scrapQty=Math.max(0,q-dailyR*Math.max(0,daysToEff));return{runoutDate,scrapQty:Math.round(scrapQty*100)/100,scrapCost:Math.round(scrapQty*pr),red:!!(effDate&&scrapQty>0)}};App._ecnStockCalc=function(projId){const el=document.getElementById("ecn-stk-result");if(!el)return;const gv=id=>(document.getElementById(id)||{}).value;const res=App._ecnStockGate(gv("ecn-stk-qty"),gv("ecn-stk-rate"),gv("ecn-stk-unit"),gv("ecn-stk-price"),gv("ecn-close-eff"));const p=App.getProj(projId);const base=p&&p.bomBaseCurrency||"NTD";const _ovrEl=document.getElementById("ecn-override");if(!res){el.className="ecn-stk-result na";el.innerHTML="填「舊料剩餘量」與「消耗率」即自動算耗盡日並比對生效日。";if(_ovrEl)_ovrEl.style.display="none";return}el.className="ecn-stk-result "+(res.red?"red":"green");el.innerHTML=res.infinite?"🔴 舊料無固定消耗率（耗盡日視為無限遠）→ 生效日切換將<b>全數報廢 "+res.scrapQty+"</b> 顆＝<b>"+res.scrapCost.toLocaleString()+" "+base+"</b>。請確認是否為專案專用料。":res.red?"🔴 舊料耗盡日 <b>"+res.runoutDate+"</b>，晚於生效日 → 切換時報廢 <b>"+res.scrapQty+"</b> 顆＝<b>"+res.scrapCost.toLocaleString()+" "+base+"</b>。改「漸進切換」或延後生效日可消呆滯。":"🟢 舊料耗盡日 <b>"+res.runoutDate+"</b>，不晚於生效日 → 舊料用完再切、無報廢，放行。";const gateOn=DATA.settings.ecnScrapGateOn??true;const thr=p&&p.scrapThresholdOverride!=null?p.scrapThresholdOverride:DATA.settings.ecnScrapThreshold??3e4;const over=!!(res.red&&gateOn&&res.scrapCost>thr);if(_ovrEl)_ovrEl.style.display=over?"block":"none";if(over)el.innerHTML+=" <b>· 超過特批門檻 "+thr.toLocaleString()+" "+base+"，需下方特批。</b>"};App.closeEcn=function(projId){if(App._roGuard())return;const p=App.getProj(projId);if(!p)return;App._ecnEnsureState(p);const ss=p.stockSnapshot||{};const approvers=DATA.settings.ecnApprovers||[];const apprOpts=approvers.length?'<option value="">— 請選擇核准人 —</option>'+approvers.map(a=>'<option value="'+U.esc(a)+'">'+U.esc(a)+"</option>").join(""):'<option value="">（名冊為空 · 請先至 設定 → ECN 流程參數配置 建立）</option>';App.openModal({title:"結案設變 — 打正式 ECN 單號＋生效日",body:'<div class="ecn-close-form"><div class="field-hint">結案後本版（v'+p.version+'）任務凍結為唯讀歷史；正式 ECN 單號與生效日為必填（打進 PLM/ERP 的追溯依據）。若後續再有變更，可「翻案重啟」開新版。</div><div class="form-field"><label>正式 ECN 單號 <b class="imap-req">*</b></label><input id="ecn-close-no" type="text" value="'+U.esc(p.ecnNo||"")+'" placeholder="如 ECN-2026-0042"></div><div class="form-field"><label>簽核生效日 <b class="imap-req">*</b></label><input id="ecn-close-eff" type="date" value="'+U.esc(p.effectiveDate||"")+`" oninput="App._ecnStockCalc('`+projId+`')"></div><div class="ecn-stk-sect">舊料切換卡控（選填 · 生效日雙卡）</div><div class="field-hint">切 BOM 前填舊料剩餘量與消耗率，系統算耗盡日、比對生效日：舊料沒用完就切＝報廢，會提醒。漸進切換或無舊料可留空。</div><div class="ecn-stk-row"><div class="form-field"><label>舊料剩餘量</label><input id="ecn-stk-qty" type="number" min="0" value="`+(ss.oldQtyLeft!=null?ss.oldQtyLeft:"")+`" oninput="App._ecnStockCalc('`+projId+`')"></div><div class="form-field"><label>消耗率</label><div class="ecn-stk-rate-wrap"><input id="ecn-stk-rate" type="number" min="0" value="`+(ss.consumeRate!=null?ss.consumeRate:"")+`" oninput="App._ecnStockCalc('`+projId+`')"><select id="ecn-stk-unit" onchange="App._ecnStockCalc('`+projId+`')"><option value="day"`+(ss.consumeUnit!=="week"?" selected":"")+'>顆/日</option><option value="week"'+(ss.consumeUnit==="week"?" selected":"")+'>顆/週</option></select></div></div><div class="form-field"><label>舊單價（算報廢金額）</label><input id="ecn-stk-price" type="number" min="0" value="'+(ss.oldPrice!=null?ss.oldPrice:"")+`" oninput="App._ecnStockCalc('`+projId+`')"></div></div><div id="ecn-stk-result" class="ecn-stk-result na">填「舊料剩餘量」與「消耗率」即自動算耗盡日並比對生效日。</div><div id="ecn-override" class="ecn-override" style="display:none"><div class="form-field"><label>特批理由 <b class="imap-req">*</b>（至少 10 字，避免敷衍留痕）</label><textarea id="ecn-ovr-reason" rows="2" placeholder="請輸入具體特批原因（例如：客戶要求提前導入，料件由專案全額吸收）"></textarea></div><div class="form-field"><label>核准人 <b class="imap-req">*</b>（從「ECN 財務特批權責名冊」選、不可手打）</label><select id="ecn-ovr-approver">`+apprOpts+"</select></div></div></div>",footer:`<button class="tb-action ghost" onclick="App.closeModal()">取消</button><button class="tb-action" onclick="App._ecnCloseConfirm('`+projId+`')">確認結案</button>`});setTimeout(()=>App._ecnStockCalc(projId),0)};App._ecnCloseConfirm=function(projId){const p=App.getProj(projId);if(!p)return;const gv=id=>(document.getElementById(id)||{}).value||"";const no=gv("ecn-close-no").trim();const eff=gv("ecn-close-eff");if(!no){U.toast("⚠ 正式 ECN 單號必填","warning");return}if(!eff){U.toast("⚠ 簽核生效日必填","warning");return}App._ecnEnsureState(p);const stkQty=gv("ecn-stk-qty"),stkRate=gv("ecn-stk-rate"),stkUnit=gv("ecn-stk-unit"),stkPrice=gv("ecn-stk-price");const gate=App._ecnStockGate(stkQty,stkRate,stkUnit,stkPrice,eff);if(gate){p.stockSnapshot={oldQtyLeft:parseFloat(stkQty)||0,consumeRate:parseFloat(stkRate)||0,consumeUnit:stkUnit||"day",oldPrice:parseFloat(stkPrice)||0,runoutDate:gate.runoutDate,scrapQty:gate.scrapQty,scrapCost:gate.scrapCost}}const gateOn=DATA.settings.ecnScrapGateOn??true;const thr=p.scrapThresholdOverride!=null?p.scrapThresholdOverride:DATA.settings.ecnScrapThreshold??3e4;const over=!!(gate&&gate.red&&gateOn&&gate.scrapCost>thr);let override=null;if(over){if(!(DATA.settings.ecnApprovers||[]).length){U.toast("⚠ 呆滯超門檻需特批，請先至 設定 → ECN 流程參數配置 建立財務特批權責名冊","warning");return}const reason=gv("ecn-ovr-reason").trim(),approver=gv("ecn-ovr-approver");if(reason.length<10){U.toast("⚠ 呆滯超門檻：特批理由需至少 10 字","warning");return}if(!approver){U.toast("⚠ 呆滯超門檻：請從名冊選核准人","warning");return}override={reason,approver,at:D.fmt(new Date,"iso")}}const tasks=DATA.tasks.filter(t=>t.project===p.id&&!t._deleted);const base=p.bomBaseCurrency||"NTD";p.ecnNo=no;p.effectiveDate=eff;p.status="closed";p.epochs.push({v:p.version,frozenAt:D.fmt(new Date,"iso"),effectiveDate:eff,ecnNo:no,snapshotHours:Math.round(App._ecnEpochPts(p,tasks)),loopCount:p.loopCount||0,scopeGrowthCount:p.scopeGrowthCount||0,scrapCost:gate?gate.scrapCost:0,override});(p.ecnEvents=p.ecnEvents||[]).push({type:"close",date:D.fmt(new Date,"iso"),label:"結案 v"+p.version+"（"+no+"·生效 "+eff+"）"+(gate&&gate.red?"·切換報廢 "+gate.scrapCost.toLocaleString()+" "+base:"")+(override?"·特批："+U.esc(override.approver):"")});Store.projects.save();App.closeModal();App._bomRerender(p);U.toast("✓ 已結案 v"+p.version+"："+no+(gate&&gate.red?"（⚠ 切換報廢 "+gate.scrapCost.toLocaleString()+" "+base+"）":""),gate&&gate.red?"warning":"success")};App.reopenEcn=function(projId){if(App._roGuard())return;const p=App.getProj(projId);if(!p)return;App._ecnEnsureState(p);App.confirmModal({title:"翻案重啟？",okText:"開新版重啟",cancelText:"取消",msg:"本版（v"+p.version+"·"+(p.ecnNo||"無單號")+"）將凍結為唯讀歷史，開新版 v"+((p.version||1)+1)+" 繼續。翻案次數計入紅旗（血淚證據）。確定？",onConfirm:()=>{const tasks=DATA.tasks.filter(t=>t.project===p.id&&!t._deleted);const oldV=p.version||1;tasks.forEach(t=>{if(t.epoch==null)t.epoch=oldV});p.version=oldV+1;p.status="active";p.reopenCount=(p.reopenCount||0)+1;p.ecnNo="";(p.ecnEvents=p.ecnEvents||[]).push({type:"reopen",date:D.fmt(new Date,"iso"),label:"翻案重啟 → v"+p.version+"（前版凍結）"});Store.tasks.save();Store.projects.save();App._bomRerender(p);U.toast("↻ 已開新版 v"+p.version+"，前版凍結為唯讀歷史","success")}})};App._ecnCauseModal=function(kind,onConfirm){const sub=kind==="loop"?"這筆退回重做是誰造成的？":"這筆中途追加是誰造成的？";App.openModal({title:"記錄異常成因（必填，不可跳過）",body:'<div class="form-field"><label>'+sub+'</label><select id="ecn-cause-sel">'+["客戶方","供應商","內部設計","物理干涉","法規追加","測試不確定"].map(c=>"<option>"+c+"</option>").join("")+'</select></div><div class="form-field"><label>備註（選填，一句話）</label><input id="ecn-cause-note" type="text" placeholder="例：客戶反映外殼異音，需追加緩衝墊"></div>',footer:'<button class="tb-action ghost" onclick="App.closeModal()">取消</button><button class="tb-action" onclick="App._ecnCauseOk()">確認記錄</button>'});App._ecnCauseCb=onConfirm};App._ecnCauseOk=function(){const cause=(document.getElementById("ecn-cause-sel")||{}).value||"";const cb=App._ecnCauseCb;App._ecnCauseCb=null;App.closeModal();if(cb)cb(cause)};App._ecnInsertTask=function(afterId,variantId){App._ecnCauseModal("scope",function(cause){const proj=App.getProj(App._s2EcnMode);if(proj){proj.scopeGrowthCount=(proj.scopeGrowthCount||0)+1;(proj.ecnEvents=proj.ecnEvents||[]).push({type:"scope",date:D.fmt(new Date,"iso"),label:"中途追加任務",cause})}App._s2InsertRow(afterId,variantId)})};App._ecnNewTask=function(projId){const proj=App.getProj(projId);if(!proj)return;if(App._ecnLocked(proj)){U.toast("已結案專案為唯讀，如需變更請先翻案重啟","warning",{soft:true});return}const res=App._tplPreview;if(!res)return;const variants=proj.variants&&proj.variants.length?proj.variants:[{id:(res.tasks[0]||{}).variant||null,name:""}];const stageSeen=[];variants.forEach(v=>{App._s2GroupByStage(v.id).order.forEach(st=>{if(st&&stageSeen.indexOf(st)<0)stageSeen.push(st)})});const stageOpts=stageSeen.length?stageSeen.map(s=>'<option value="'+U.esc(s)+'">'+U.esc(s)+"</option>").join(""):'<option value="">（無階段）</option>';const activeVid=App._ecnActiveVariant(proj);const varRow=variants.length>1?'<div class="form-field"><label>案別</label><select id="ecn-new-variant">'+variants.map(v=>'<option value="'+v.id+'"'+(v.id===activeVid?" selected":"")+">"+U.esc(v.name||v.id)+"</option>").join("")+"</select></div>":"";App.openModal({title:"＋ 新增任務",body:'<div class="ecn-edit-hint" style="margin-bottom:12px">💡 臨時追加的全新工作項目（非重工、非中途追加，不記異常成因）。建立後可於大表行內續填負責人、工期、前置。</div><div class="form-field"><label>任務名稱</label><input type="text" id="ecn-new-name" placeholder="例：追加防水測試"></div><div class="form-field"><label>歸屬階段</label><select id="ecn-new-stage">'+stageOpts+"</select></div>"+varRow,footer:`<button class="tb-action ghost" onclick="App.closeModal()">取消</button><button class="tb-action" onclick="App._ecnNewTaskOk('`+projId+`')">建立任務</button>`});setTimeout(()=>{const n=document.getElementById("ecn-new-name");if(n){n.focus()}},50)};App._ecnNewTaskOk=function(projId){const res=App._tplPreview;if(!res)return;const name=((document.getElementById("ecn-new-name")||{}).value||"").trim();if(!name){U.toast("⚠ 請填任務名稱","warning");return}const stage=(document.getElementById("ecn-new-stage")||{}).value||"";const variantSel=(document.getElementById("ecn-new-variant")||{}).value;const variantId=variantSel||(res.tasks[0]||{}).variant||(res.variants[0]||{}).id||null;const dailyHours=DATA.settings&&DATA.settings.dailyHours||6;const newTask={id:U.id(),project:res.project.id,wbs:"",parentWbsId:"",name,desc:stage,category:"deep",taskType:"任務",predecessor:"",durationDays:1,owner:"",dept:"",role:"",variant:variantId,start:"",end:"",plannedStart:"",plannedEnd:"",actualStart:"",actualEnd:"",progress:0,status:"pending",urgency:"med",estHours:dailyHours,method:"",canSplit:false,completedAt:null,createdAt:new Date().toISOString(),scheduledStart:"",scheduledEnd:"",synced:false,stage,subgroup:"",mustDeliver:false,deliverableType:"",requiredTask:true,mustIssue:false,deliverable:"",riskIssue:"",delivered:"",deliverableLink:"",note:"",taskAttr:"baseline"};let insertAt=-1;for(let i=0;i<res.tasks.length;i++){if(res.tasks[i].variant===variantId&&res.tasks[i].stage===stage)insertAt=i}if(insertAt>=0)res.tasks.splice(insertAt+1,0,newTask);else res.tasks.push(newTask);App.closeModal();App._s2EcnPersist();U.toast("已新增任務："+name+"（待排，請於大表填日期）","success")};App.renderEcnDashboard=function(proj){App._ecnEnsureState(proj);const tasks=DATA.tasks.filter(t=>t.project===proj.id&&!t._deleted);App._tplPreview={project:proj,tasks,variants:proj.variants||[],depts:proj.depts||[],warnings:[]};App._s2EcnMode=proj.id;if(App._ecnForwardVariants(proj.variants,tasks)){App._reschedulePreview(tasks,proj.variants||[],[]);Store.projects.save();Store.tasks.save()}const tab=App._ecnTab||"overview";const sizeLbl={S:"S 級 · 輕量換料",M:"M 級 · 結構認定",L:"L 級 · 重大改模"}[proj.size]||proj.size+" 級";const stLbl={active:"進行中",closed:"已結案",reopened:"已重啟"}[proj.status]||"進行中";const roiLbl=proj.roiType==="forced"?"被迫型 · 合規/停線":"效益型 · Cost Down";const on=t=>tab===t?" on":"";const locked=App._ecnLocked(proj);const headAction=locked?`<button class="ecn-hd-act reopen" onclick="App.reopenEcn('`+proj.id+`')">↻ 翻案重啟</button>`:`<button class="ecn-hd-act close" onclick="App.closeEcn('`+proj.id+`')">✔ 結案</button>`;const lastEp=proj.epochs&&proj.epochs.length?proj.epochs[proj.epochs.length-1]:null;const closedBanner=locked&&lastEp?'<div class="ecn-closed-banner">🔒 已結案（唯讀）· 單號 <b>'+U.esc(proj.ecnNo||lastEp.ecnNo||"")+"</b> · 生效日 "+U.esc(proj.effectiveDate||lastEp.effectiveDate||"")+" · 凍結工時 <b>"+(lastEp.snapshotHours||0)+"h</b> · v"+lastEp.v+(lastEp.scrapCost?" · ⚠ 切換報廢 <b>"+lastEp.scrapCost.toLocaleString()+" "+U.esc(proj.bomBaseCurrency||"NTD")+"</b>":"")+"。如需再變更，點右上「翻案重啟」開新版。</div>":"";const panel=tab==="overview"?App._ecnOverviewHtml(proj,tasks):tab==="b"?App._ecnTabBHtml(proj):tab==="c"?App._ecnTabCHtml(proj):App._ecnTabAHtml(proj);document.getElementById("page-project").innerHTML='<div class="ecn-dash"><div class="ecn-hd"><span class="ecn-hd-name">'+U.esc(proj.name)+'</span><span class="ecn-tag ecn-tag-size">'+sizeLbl+'</span><span class="ecn-tag ecn-tag-st'+(locked?" closed":"")+'">'+stLbl+'</span><span class="ecn-tag ecn-tag-roi">'+roiLbl+'</span><span class="ecn-hd-ver">目前版本 · v'+(proj.version||1)+(proj.reopenCount?"（翻案 "+proj.reopenCount+" 次）":"")+`</span><button class="ecn-hd-act edit" data-edit onclick="App.editProject('`+proj.id+`')">編輯專案</button>`+headAction+"</div>"+closedBanner+'<div class="ecn-layout ecn-focus"><div class="ecn-main"><div class="ecn-tabs"><span class="ecn-tab'+on("overview")+`" onclick="App._ecnSetTab('overview')">總覽</span><span class="ecn-tab`+on("a")+`" onclick="App._ecnSetTab('a')">任務與排程</span><span class="ecn-tab`+on("b")+`" onclick="App._ecnSetTab('b')">BOM 成本差異分析</span><span class="ecn-tab`+on("c")+`" onclick="App._ecnSetTab('c')">版本歷史</span></div><div class="ecn-panel">`+panel+"</div></div></div></div>";window.scrollTo({top:0,behavior:"smooth"})};App.renderProjectBom=function(targetId,pid){const proj=App.getProj(pid||App.currentProjectId);if(!proj)return;const el=document.getElementById(targetId||"proj-view-body");if(!el)return;App._bomEnsure(proj);el.innerHTML='<div class="bom-npi">'+App._ecnTabBHtml(proj)+"</div>"};App._bomRerender=function(p){if(!p)return;if(p.ecnType)App["renderEcnDashboard"](p);else App.renderProjectBom("proj-view-body",p.id);const _es=document.getElementById("projExportSlot");if(_es)_es.innerHTML=App._projExportBtnHtml(p);App._bomCountUp()};App._bomCV={pid:null,vals:{}};App._bomCountUp=function(){if(typeof requestAnimationFrame!=="function")return;const pid=App.currentProjectId;const fresh=App._bomCV.pid!==pid;if(fresh)App._bomCV={pid,vals:{}};const els=document.querySelectorAll(".bxb-bigcard .bv, .bom-stick .v");els.forEach((el,i)=>{const node=Array.prototype.find.call(el.childNodes,n=>n.nodeType===3&&/-?\d/.test(n.nodeValue));if(!node)return;const raw=node.nodeValue;const m=raw.match(/-?[\d,]+(\.\d+)?/);if(!m)return;const target=parseFloat(m[0].replace(/,/g,""));if(isNaN(target))return;const key="k"+i,prev=App._bomCV.vals[key];App._bomCV.vals[key]=target;if(fresh||prev==null||prev===target)return;const hasComma=m[0].indexOf(",")>=0,dec=m[1]?m[1].length-1:0;const before=raw.slice(0,m.index),after=raw.slice(m.index+m[0].length);const fmt=v=>{const n=dec?Number(v.toFixed(dec)):Math.round(v);return hasComma?n.toLocaleString(void 0,{minimumFractionDigits:dec,maximumFractionDigits:dec}):dec?n.toFixed(dec):String(n)};const t0=performance.now(),dur=480,ease=t=>1-Math.pow(1-t,3);(function step(now){const t=Math.min(1,(now-t0)/dur);node.nodeValue=before+fmt(prev+(target-prev)*ease(t))+after;if(t<1)requestAnimationFrame(step);else node.nodeValue=raw})(t0)})};App._ecnOverviewHtml=function(proj,tasks){const todayIso=D.fmt(D.today(),"iso"),today=D.today();const pending=tasks.filter(t=>t.status!=="done"&&App._ecnIsCurrentEpoch(t,proj)).reduce((s,t)=>s+App._ecnPts(t),0);const total=tasks.reduce((s,t)=>s+App._ecnPts(t),0);let wsum=0,actW=0,expW=0;tasks.filter(t=>App._ecnIsCurrentEpoch(t,proj)).forEach(t=>{const w=App._ecnPts(t);if(w<=0)return;wsum+=w;actW+=w*(taskDisplayProgress(t)||0);const eff=getEffectiveSchedule(t);let exp=0;if(eff.start&&eff.end){if(todayIso<=eff.start)exp=0;else if(todayIso>=eff.end)exp=100;else exp=Math.max(0,Math.min(100,(D.workdaysBetween(eff.start,todayIso)-1)/Math.max(1,D.workdaysBetween(eff.start,eff.end)-1)*100))}expW+=w*exp});const actual=wsum?Math.round(actW/wsum):0;const expected=wsum?Math.round(expW/wsum):0;const behind=actual-expected;const scopeGrowth=proj.baselineHours?Math.round((total-proj.baselineHours)/proj.baselineHours*100):0;const stageLoad={};tasks.filter(t=>App._ecnIsCurrentEpoch(t,proj)&&!t.isPmCoord).forEach(t=>{const st=typeof t.stage==="string"&&t.stage.trim()?t.stage.trim():"未分階段";stageLoad[st]=(stageLoad[st]||0)+App._ecnPts(t)});const loadArr=Object.keys(stageLoad).map(k=>({st:k,h:Math.round(stageLoad[k])})).filter(x=>x.h>0).sort((a,b)=>b.h-a.h);const loadTot=loadArr.reduce((s,x)=>s+x.h,0),loadMax=loadArr.length?loadArr[0].h:1;const alertRange=App._ecnAlertRange||"soon";const rangeDays=alertRange==="today"?0:14;const loopItems=tasks.filter(t=>t.isLoopTask&&App._ecnIsCurrentEpoch(t,proj)).map(t=>{const anchor=t.actualStart||getEffectiveSchedule(t).start;return{t,dwell:anchor?Math.max(0,D.daysBetween(anchor,todayIso)):0}}).sort((a,b)=>b.dwell-a.dwell).slice(0,4);const dueItems=App._classifyDueRisk(tasks,t=>!t.isPmCoord&&!t.isLoopTask&&t.status!=="done"&&App._ecnIsCurrentEpoch(t,proj),rangeDays,today);const variants=proj.variants&&proj.variants.length?proj.variants:[{id:(tasks[0]||{}).variant||"",name:""}];const variantId=variants[0].id;const helpBox=App.buildHintBox({key:"ecn-hud-help",icon:"ti-bulb",title:"戰情指標面板說明",summary:"進度落差／異常變更／負荷分佈怎麼看",collapsed:true,bodyHtml:"<ol><li><b>進度落差</b>：豎線＝預期、實心＝實際，落差自動標紅；再拆「真的做慢」vs「範圍變大稀釋」。</li><li><b>卡關警報</b>：異常變更（退回重做/中途追加）＋停留天數＋近兩週到期，點列開任務。</li><li><b>負荷分佈</b>：各階段工時佔比，抓資源瓶頸。</li></ol>"});const ganttRows=variants.map(v=>{const g=App._s2GanttHtml(v.id);return g?(variants.length>1?'<div class="ecn-ov-gvar">'+U.esc(v.name||"")+"</div>":"")+'<div class="s2-gantt" data-variant="'+v.id+'">'+g+"</div>":""}).join("");const ganttCard='<div class="ecn-hudcard"><div class="ecn-hud-t">各階段時程甘特</div>'+(ganttRows||'<div class="ecn-empty-edu">尚無階段任務。</div>')+"</div>";const gapPct=actual<expected?expected-actual:0;const dualCard='<div class="ecn-hudcard"><div class="ecn-hud-t">進度落差分析（預期 vs 實際）</div><div class="ecn-ov-dual"><div class="ecn-ovd-act" style="width:'+actual+'%"></div>'+(gapPct?'<div class="ecn-ovd-gap" style="left:'+actual+"%;width:"+gapPct+'%"></div>':"")+'<div class="ecn-ovd-mark" style="left:'+expected+'%"></div></div><div class="ecn-ovd-pcts"><div class="ecn-ovd-p"><span>預期</span><b>'+expected+'%</b></div><div class="ecn-ovd-p'+(behind<0?" bad":"")+'"><span>實際</span><b>'+actual+'%</b></div><div class="ecn-ovd-p'+(behind<0?" bad":"")+'"><span>落差</span><b>'+(behind>=0?"+":"")+behind+'%</b></div></div><div class="ecn-ovd-split"><div class="ecn-ovd-sp amber"><b>'+(behind>=0?"+":"")+behind+'%</b>執行落後（真的做慢）</div><div class="ecn-ovd-sp rose"><b>+'+scopeGrowth+"%</b>範圍蔓延（範圍變大稀釋）</div></div></div>";const _base3=proj.bomBaseCurrency||"NTD";const tv=parseFloat(proj.targetSavePerUnit)||0;const fmtC=x=>Math.round(x).toLocaleString()+" "+_base3;const wt=App._bomWholeTotals(proj);const cCell=(l,v,hi)=>'<div class="ecn-ov-costcell'+(hi?" hi":"")+'"><div class="l">'+l+'</div><div class="v">'+v+"</div></div>";let costCard;if(!wt.ok){costCard='<div class="ecn-hudcard"><div class="ecn-hud-t">成本效益摘要</div><div class="ecn-ov-costempty">'+(wt.noData?"尚未建立成本基準。":"成本結構待判定，請到 BOM 頁完成設定。")+`<button class="ecn-ov-costbtn" onclick="App._ecnSetTab('b')">前往 BOM 頁面執行比對 →</button></div></div>`}else if(proj.roiType==="forced"){costCard='<div class="ecn-hudcard"><div class="ecn-hud-t">變更成本評估<span class="ecn-ov-sub">被迫型 · 合規總代價</span></div><div class="ecn-ov-costgrid">'+cCell("現行成本 / 台",fmtC(wt.oldTotal))+cCell("變更後 / 台",fmtC(wt.newTotal))+cCell("每台變更代價",(wt.delta>=0?"+":"")+fmtC(wt.delta),true)+"</div></div>"}else{const saved=-wt.delta;const goalPct=tv>0?Math.max(0,Math.min(100,Math.round(saved/tv*100))):null;costCard='<div class="ecn-hudcard"><div class="ecn-hud-t">成本效益摘要<span class="ecn-ov-sub">效益型 · Cost Down</span></div><div class="ecn-ov-costgrid">'+cCell("現行成本 / 台",fmtC(wt.oldTotal))+cCell("目標成本 / 台",tv>0?fmtC(wt.oldTotal-tv):"—")+cCell("成本調降貢獻 / 台",fmtC(saved),true)+cCell("達成率",goalPct!=null?goalPct+"%":"—")+"</div>"+(goalPct!=null?'<div class="ecn-ov-costprog"><div class="cpbar"><div class="cpfill" style="width:'+goalPct+'%"></div></div><div class="cptxt">目標成本調降 '+tv.toLocaleString()+" "+_base3+"/台，已達 <b>"+Math.round(saved).toLocaleString()+"（"+goalPct+"%）</b>。</div></div>":"")+"</div>"}const loadRows=loadArr.slice(0,6).map(x=>{const pct=loadTot?Math.round(x.h/loadTot*100):0;return'<div class="ecn-ov-lrow"><span class="ecn-ov-lname">'+U.esc(x.st)+'</span><span class="ecn-ov-lbar-w"><span class="ecn-ov-lbar'+(pct>=35?" hot":"")+'" style="width:'+Math.round(x.h/loadMax*100)+'%"></span></span><span class="ecn-ov-lval">'+x.h+"h · "+pct+"%</span></div>"}).join("");const loadCard='<div class="ecn-hudcard"><div class="ecn-hud-t">工作負荷分佈<span class="ecn-ov-sub">按階段 · 工時佔比</span></div>'+(loadRows||'<div class="ecn-empty-edu">尚無工時。</div>')+"</div>";const hoursCard=`<div class="ecn-hudcard"><div class="ecn-hud-t dept-head"><span>工時</span><button class="pl-hist-btn" onclick="App.openLoadHistory('`+proj.id+`')" title="只列已完工任務的階段×部門工時">📜 完工帳</button></div><div class="ecn-hud-nums"><div class="ecn-hud-num"><b>`+Math.round(pending)+'<span>h</span></b><span>待消化工時</span></div><div class="ecn-hud-num"><b>'+Math.round(total)+"<span>h</span></b><span>歷史總投入</span></div></div></div>";const seg='<span class="ecn-ov-seg"><span class="'+(alertRange==="today"?"on":"")+`" onclick="App._ecnSetAlertRange('today')">今日</span><span class="`+(alertRange==="soon"?"on":"")+`" onclick="App._ecnSetAlertRange('soon')">近兩週</span></span>`;const loopRows=loopItems.map(({t,dwell})=>`<div class="ecn-ov-arow" onclick="App.openTaskModal('`+t.id+`')"><span class="ecn-ov-aic">↩</span><div class="ecn-ov-abody"><div class="ecn-ov-aname">`+U.esc(t.name)+'</div><div class="ecn-ov-asub">'+(t.causeTag?"成因："+U.esc(t.causeTag)+" · ":"")+U.esc(t.stage||"")+'</div></div><span class="ecn-ov-apill rose">停留 '+dwell+" 天</span></div>").join("");const dueRows=dueItems.map(({t,kind,days})=>`<div class="ecn-ov-arow" onclick="App.openTaskModal('`+t.id+`')"><span class="ecn-ov-aic">◆</span><div class="ecn-ov-abody"><div class="ecn-ov-aname">`+U.esc(t.name)+'</div><div class="ecn-ov-asub">'+U.esc(t.stage||"")+(t.owner?" · "+U.esc(t.owner):"")+'</div></div><span class="ecn-ov-apill '+(kind==="over"?"rose":"amber")+'">'+(kind==="over"?"逾期 "+days+" 工作天":days===0?"今日到期":days+" 天內")+"</span></div>").join("");const alertBody=loopRows||dueRows?(loopRows?'<div class="ecn-ov-asec">🚩 異常變更（卡關）</div>'+loopRows:"")+(dueRows?'<div class="ecn-ov-asec">⏰ '+(alertRange==="today"?"今日":"近兩週")+"到期</div>"+dueRows:""):'<div class="ecn-empty-edu">目前無異常變更或'+(alertRange==="today"?"今日":"近兩週")+"到期任務。</div>";const alertCard='<div class="ecn-hudcard"><div class="ecn-hud-t risk-head">里程碑與卡關警報'+seg+'</div><div class="ecn-ov-acounts"><span>📌 中途追加 <b>'+(proj.scopeGrowthCount||0)+"</b></span><span>↩ 退回重做 <b>"+(proj.loopCount||0)+"</b></span><span>↻ 翻案 <b>"+(proj.reopenCount||0)+"</b></span></div>"+alertBody+"</div>";const deptCard='<div class="ecn-hudcard ecn-hud-dept">'+App._s2DeptPanelHtml(variantId)+"</div>";const tlEvents=(proj.ecnEvents||[]).map(e=>({date:e.date,label:e.label||e.type,cause:e.cause,kind:e.type}));tasks.filter(t=>t.taskType==="milestone"&&t.actualEnd&&App._ecnIsCurrentEpoch(t,proj)).forEach(t=>tlEvents.push({date:t.actualEnd,label:"完成里程碑："+t.name,kind:"milestone"}));tlEvents.sort((a,b)=>String(b.date||"").localeCompare(String(a.date||"")));const tlRows=tlEvents.slice(0,6).map(e=>'<div class="ecn-ov-tlitem'+(e.kind==="milestone"?" done":"")+'"><div class="ecn-ov-tldate">'+U.esc(e.date||"")+'</div><div class="ecn-ov-tltxt">'+U.esc(e.label||"")+(e.cause?" · "+U.esc(e.cause):"")+"</div></div>").join("");const timelineCard='<div class="ecn-hudcard"><div class="ecn-hud-t risk-head">專案動態時間軸'+(tlEvents.length?`<button class="ecn-ov-tlall" onclick="App._ecnSetTab('c')">查看全部 →</button>`:"")+"</div>"+(tlRows?'<div class="ecn-ov-tl">'+tlRows+"</div>":'<div class="ecn-empty-edu">尚無事件（開案／追加／退回／結案／完工里程碑會自動記錄）。</div>')+"</div>";const variantNames=(proj.variants||[]).map(v=>v.name).filter(Boolean).join("、")||"（單一 · 未分機種）";const goalTxt=proj.roiType==="forced"?"被迫型 · 合規/停線（不計回本）":tv>0?"目標成本調降 "+tv.toLocaleString()+" "+_base3+" / 台":"尚未設定目標成本調降（開案／BOM 頁可設）";const bgBar='<div class="ecn-ov-ctx"><div class="ecn-ov-ctxcell"><span>受影響機種</span><b>'+U.esc(variantNames)+'</b></div><div class="ecn-ov-ctxcell goal"><span>核心目標</span><b>'+U.esc(goalTxt)+'</b></div><div class="ecn-ov-ctxcell"><span>變更背景</span><b>'+(proj.changeReason?U.esc(proj.changeReason):"—")+"</b></div></div>";const se=proj.stockEstimate||{};const estR=App._ecnEstResultHtml(App._ecnStockGate(se.oldQtyLeft,se.consumeRate,se.consumeUnit,se.oldPrice,se.effectiveDate),_base3);const _estIn=(id,val,ty)=>'<input id="'+id+'" type="'+(ty||"number")+'"'+(ty?"":' min="0"')+' value="'+val+`" oninput="App._ecnEstCalc('`+proj.id+`')" onchange="App._ecnEstSave('`+proj.id+`')">`;const estCard='<div class="ecn-hudcard"><div class="ecn-hud-t">生效日與舊料呆滯<span class="ecn-ov-sub">早期預估</span></div><div class="ecn-est-sub">立案評估就先估：切換會不會撞到還沒用完的舊料（同結案卡控算法）</div><div class="ecn-est-grid"><div class="ecn-est-f"><label>舊料剩餘量</label>'+_estIn("ecn-est-qty",se.oldQtyLeft!=null?se.oldQtyLeft:"")+'</div><div class="ecn-est-f"><label>消耗率</label><div class="ecn-stk-rate-wrap"><input id="ecn-est-rate" type="number" min="0" value="'+(se.consumeRate!=null?se.consumeRate:"")+`" oninput="App._ecnEstCalc('`+proj.id+`')" onchange="App._ecnEstSave('`+proj.id+`')"><select id="ecn-est-unit" onchange="App._ecnEstSave('`+proj.id+`')"><option value="day"`+(se.consumeUnit!=="week"?" selected":"")+'>顆/日</option><option value="week"'+(se.consumeUnit==="week"?" selected":"")+'>顆/週</option></select></div></div><div class="ecn-est-f"><label>舊單價</label>'+_estIn("ecn-est-price",se.oldPrice!=null?se.oldPrice:"")+'</div><div class="ecn-est-f"><label>預計生效日</label>'+_estIn("ecn-est-eff",U.esc(se.effectiveDate||""),"date")+'</div></div><div id="ecn-est-result" class="ecn-stk-result '+estR.cls+'">'+estR.html+"</div></div>";return'<div class="ecn-ov">'+helpBox+bgBar+ganttCard+'<div class="ecn-ov-cols"><div class="ecn-ov-col">'+dualCard+costCard+estCard+loadCard+hoursCard+'</div><div class="ecn-ov-col">'+alertCard+timelineCard+deptCard+"</div></div></div>"};App._ecnSetAlertRange=function(r){App._ecnAlertRange=r;const p=App.getProj(App.currentProjectId);if(p)App.renderEcnDashboard(p)};App._ecnEstResultHtml=function(res,base){if(!res)return{cls:"na",html:"填「舊料剩餘量」與「消耗率」即早估耗盡日與潛在呆滯。"};base=base||"NTD";const cls=res.red?"red":"green";const html=res.infinite?"🔴 舊料無固定消耗率（耗盡日視為無限遠）→ 生效日切換將<b>全數報廢 "+res.scrapQty+"</b> 顆＝<b>"+res.scrapCost.toLocaleString()+" "+base+"</b>。請確認是否為專案專用料。":res.red?"🔴 預估耗盡日 <b>"+res.runoutDate+"</b>，晚於預計生效日 → 預估提前切換報廢 <b>"+res.scrapQty+"</b> 顆＝<b>"+res.scrapCost.toLocaleString()+" "+base+"</b>。建議延生效日或改漸進切換。":"🟢 預估耗盡日 <b>"+res.runoutDate+"</b>，不晚於預計生效日 → 可如期切換、無呆滯。";return{cls,html}};App._ecnEstCalc=function(projId){const el=document.getElementById("ecn-est-result");if(!el)return;const gv=id=>(document.getElementById(id)||{}).value;const res=App._ecnStockGate(gv("ecn-est-qty"),gv("ecn-est-rate"),gv("ecn-est-unit"),gv("ecn-est-price"),gv("ecn-est-eff"));const p=App.getProj(projId);const r=App._ecnEstResultHtml(res,p&&p.bomBaseCurrency||"NTD");el.className="ecn-stk-result "+r.cls;el.innerHTML=r.html};App._ecnEstSave=function(projId){const p=App.getProj(projId);if(!p)return;if(App._roGuard&&App._roGuard())return;const gv=id=>(document.getElementById(id)||{}).value;const qty=parseFloat(gv("ecn-est-qty"))||0;if(qty<=0){delete p.stockEstimate}else{p.stockEstimate={oldQtyLeft:qty,consumeRate:parseFloat(gv("ecn-est-rate"))||0,consumeUnit:gv("ecn-est-unit")||"day",oldPrice:parseFloat(gv("ecn-est-price"))||0,effectiveDate:gv("ecn-est-eff")||""}}Store.projects.save();App._ecnEstCalc(projId)};App._ecnActiveVariant=function(proj){const vs=proj.variants&&proj.variants.length?proj.variants:[];if(!vs.length)return null;App._ecnVariantTab=App._ecnVariantTab||{};const cur=App._ecnVariantTab[proj.id];return cur&&vs.some(v=>v.id===cur)?cur:vs[0].id};App._ecnVariantTabsHtml=function(proj){const vs=proj.variants&&proj.variants.length?proj.variants:[];if(vs.length<2)return"";const active=App._ecnActiveVariant(proj);return'<div class="ecn-vtabs">'+vs.map((v,i)=>'<button class="ecn-vtab'+(v.id===active?" on":"")+`" onclick="App._ecnSetVariantTab('`+proj.id+"', '"+v.id+`')">`+(i===0?'<i class="ti ti-star" aria-hidden="true"></i> 主案 · ':"")+U.esc(v.name||"案別"+(i+1))+"</button>").join("")+"</div>"};App._ecnSetVariantTab=function(projId,vid){App._ecnVariantTab=App._ecnVariantTab||{};App._ecnVariantTab[projId]=vid;const p=App.getProj(projId);if(p)App.renderEcnDashboard(p)};App._ecnTabAHtml=function(proj){const guide=App.buildHintBox({key:"ecn-tbl-guide",icon:"ti-bulb",title:"任務大表操作指南",summary:"負荷警示／行內速改／智慧排程連動",collapsed:true,bodyHtml:'<ol><li><b>負荷警示燈與投入級別</b>：投入%改採「行為錨點」下拉評估，無須硬湊 100%。系統會自動跨案疊加，同人單日合計 >100% 會在「全專案總覽 → 嚴重過載警報」亮紅旗。<ul class="ecn-hint-lv"><li><b>100% 獨佔</b>：這天只有這件。</li><li><b>75% 大半天</b>：這天主要在做它。</li><li><b>50% 半天</b>：佔掉半個工作天。</li><li><b>25% 零星盯</b>：發出去等回來，一天巡幾趟。</li><li><b>10% 點一下</b>：幾分鐘、一兩個觸點。</li><li><b>0% 舉證專用（掛名未出力）</b>：列在他名下但實際由別人代打或擺爛。<b>請勿刪除任務</b>，保留此 0% 紀錄作為結案檢討的客觀鐵證。</li></ul></li><li><b>行內編輯與權限</b>：點擊儲存格直接修改任務、負責人、工期與投入級別。PM 具全局權限，各負責人僅限更新自身任務。</li><li><b>智慧排程連動</b>：前置任務採全白話設定。修改前置或工期後，系統會自動重算整體時程（手動鎖定之日期除外）。</li><li><b>異常與進度回報</b>：點列表右側「⚙ 編輯」回報實際開／完工日。中途追加請點列與列之間的「＋」；「打回重測」或「刪除」請進編輯彈窗操作，系統會強制記錄成因以保全客觀證據。</li></ol>'});const _bk=!!(App._bulkMode||{})[proj.id];return guide+'<div class="ecn-panel-toolrow"><span class="ecn-pt-title">任務大表</span><div class="ecn-pt-actions">'+(App._ecnLocked(proj)?"":`<button class="tb-action ecn-newtask-btn" onclick="App._ecnNewTask('`+proj.id+`')">＋ 新增任務</button>`)+`<button class="tb-action ecn-bulk-btn" onclick="App.toggleBulkEdit('`+proj.id+`')">`+(_bk?"完成批量編輯":"☑ 批量編輯")+"</button></div></div>"+App._ecnVariantTabsHtml(proj)+(_bk?App._bulkBarHtml(proj):"")+'<div class="ecn-tbl-scroll">'+App._ecnTableHtml(proj)+"</div>"};App._ecnTableHtml=function(proj){const res=App._tplPreview;const allVariants=proj.variants&&proj.variants.length?proj.variants:[{id:(res.tasks[0]||{}).variant||null}];const multi=allVariants.length>1;const activeVid=multi?App._ecnActiveVariant(proj):allVariants[0].id;const variants=multi?allVariants.filter(v=>v.id===activeVid):allVariants;let rows="",seq=0;variants.forEach(v=>{const g=App._s2GroupByStage(v.id);g.order.forEach(st=>{(g.byStage[st]||[]).forEach(t=>{seq++;rows+=App._ecnRowHtml(t,v.id,seq)})})});const _bk=!!(App._bulkMode||{})[proj.id];if(!rows)rows='<tr><td colspan="'+(_bk?13:12)+'" class="ecn-empty">尚無任務</td></tr>';else if(!App._ecnLocked(proj)&&!(res.tasks||[]).some(t=>t.isPmCoord&&(!proj.version||t.epoch==null||t.epoch===proj.version))){rows+='<tr class="ecn-pmrow" style="opacity:.72"><td class="col-num">—</td><td colspan="'+(_bk?10:9)+`"><span class="ecn-pm-nm">尚未設 PM 常駐協調</span><span style="font-size:11.5px;color:var(--ink3);margin-left:8px;">設變案建議至少帶 PM 常駐（Model Y）——追蹤/文件/協調工時不落帳＝PM 負荷被低估</span></td><td class="col-action" colspan="2"><button class="pl-cta-btn pl-cta-sm" onclick="App._ecnSeedCoord('`+proj.id+`')">＋ 一鍵帶入</button></td></tr>`}return'<table class="data-table s2-tbl ecn-tbl'+(App._ecnLocked(proj)?" ecn-tbl-locked":"")+'"><thead><tr>'+(_bk?`<th class="col-chk" rowspan="2"><input type="checkbox" onchange="App._bulkSelectAll('`+proj.id+`', this.checked)" data-tip="全選|勾選目前顯示的全部未完工任務"></th>`:"")+'<th class="col-num" rowspan="2">序</th><th class="col-flex" rowspan="2">任務名</th><th class="col-mid" rowspan="2">部門</th><th class="col-mid" rowspan="2">負責人</th><th class="col-pred-group" colspan="3">前置任務</th><th class="col-mid s2-dur-th" rowspan="2">工期</th><th class="col-mid" rowspan="2">投入%</th><th class="col-mid" rowspan="2">計畫日期（起訖）</th><th class="col-mid" rowspan="2">狀態 · 成因</th><th class="col-action" rowspan="2">操作</th></tr><tr><th class="col-pred-sub">序號</th><th class="col-pred-sub">銜接方式</th><th class="col-pred-sub">緩衝</th></tr></thead><tbody>'+rows+"</tbody></table>"};App._ecnRowHtml=function(t,variantId,seq){const _projFr=App.getProj(App._s2EcnMode);const frCls=_projFr&&!App._ecnIsCurrentEpoch(t,_projFr)?" ecn-row-frozen":"";const _bk=!!(App._bulkMode||{})[App._s2EcnMode];const _bkSel=_bk&&App._bulkSel&&App._bulkSel[App._s2EcnMode];const _chk=_bk?t.isPmCoord||t.actualEnd?'<td class="col-chk"></td>':'<td class="col-chk" onclick="event.stopPropagation()"><input type="checkbox" class="bulk-chk" '+(_bkSel&&_bkSel.has(t.id)?"checked ":"")+`onchange="App._bulkToggle('`+App._s2EcnMode+"','"+t.id+`', this.checked)"></td>`:"";if(t.isPmCoord){const _proj=App.getProj(App._s2EcnMode);const _et=tplEcn();const _floor=_proj&&_et&&_et.sizeMeta&&_et.sizeMeta[_proj.size]?_et.sizeMeta[_proj.size].pmEffort:15;return'<tr class="ecn-pmrow" data-taskid="'+t.id+'">'+_chk+'<td class="col-num">—</td><td class="col-flex"><span class="ecn-pm-nm">'+U.esc(t.name)+'</span></td><td class="col-mid">PM</td><td class="col-mid">PM</td><td class="col-pred">—</td><td class="col-pred">—</td><td class="col-pred"></td><td class="col-mid">全程</td><td class="col-mid s2-pct-cell"><select class="s2-pct-sel ecn-pm-pct" title="PM 常駐可上調、不可低於該級預設 '+_floor+`%" onchange="App._ecnSetEffort('`+t.id+`', this.value)">`+App._ecnPmEffortOptions(t.effortRatio,_floor)+'</select></td><td class="col-mid s2-date">全程</td><td class="col-mid"><span class="ecn-lock">🔒 常駐 · 不可降級</span></td><td class="col-action"></td></tr>'}const isLoop=!!t.isLoopTask;const causeTag=isLoop&&t.causeTag?' <span class="ecn-ct">'+U.esc(t.causeTag)+" ▾</span>":"";const _st=App._ecnStatusDerive(t);return'<tr class="'+(seq%2===0?"s2-rz ":"")+(isLoop?"ecn-loop ":"")+frCls+'" data-taskid="'+t.id+'">'+_chk+'<td class="col-num">'+(isLoop?"↳":seq)+'</td><td class="col-flex s2-namecell" title="'+U.esc(t.name)+'"><div class="s2-nameflex"><span class="ecn-name-txt" title="'+U.esc(t.name)+`（點擊編輯）" onclick="App._ecnEditName(this, '`+t.id+`')">`+U.esc(t.name)+"</span>"+(isLoop?'<span class="ecn-loop-tag">重做 +1</span>':"")+`</div></td><td class="col-mid s2-deptcell"><select class="ecn-dept-sel" title="可換部門（設變思維：任務由哪部門做不固定）" onchange="App._ecnSetDept('`+t.id+`', this.value)">`+App._ecnDeptOptions(t.role)+'</select></td><td class="col-mid s2-ownercell"><select class="s2-owner-sel'+(t.owner?"":" s2-owner-unassigned")+`" onchange="App._ecnSetOwner('`+t.id+`', this.value)">`+App._s2OwnerOptions(t)+"</select></td>"+App._s2PredCells(t,variantId)+'<td class="col-mid s2-dur"><input class="s2-dur-inp" type="number" min="0" value="'+(t.durationDays!=null?t.durationDays:"")+`" onchange="App._s2SetDuration('`+t.id+`', this.value)"></td><td class="col-mid s2-pct-cell"><select class="s2-pct-sel" onchange="App._ecnSetEffort('`+t.id+`', this.value)">`+App._ecnEffortOptions(t.effortRatio)+'</select></td><td class="col-mid s2-datecell"><div class="ecn-dates"><input type="date" class="ecn-date-in" value="'+(t.plannedStart||"")+`" title="開始日（手動改＝釘錨點；清空＝回歸前置驅動）" onchange="App._ecnSetStart('`+t.id+`', this.value)"><input type="date" class="ecn-date-in" value="`+(t.plannedEnd||"")+`" title="結束日（改＝回算工期，開始為錨）" onchange="App._ecnSetEnd('`+t.id+`', this.value)"></div></td><td class="col-mid s2-stcell"><span class="ecn-st ecn-st-`+_st.cls+'" title="狀態依時間自動判定；完工/擱置在「⚙ 編輯」設定">'+_st.label+"</span>"+causeTag+`</td><td class="col-action"><button class="ecn-op-btn ecn-op-edit" title="編輯／特殊操作（完工·擱置·打回重測·刪除）" onclick="App._ecnEditTask('`+t.id+`')">⚙ 編輯</button></td></tr><tr class="dt-insert-row"><td colspan="`+(_bk?13:12)+`" class="dt-insert-cell"><div class="dt-insert"><button class="dt-insert-btn" title="在此列後插入（記為中途追加）" onclick="App._ecnInsertTask('`+t.id+"', '"+variantId+`')">＋</button></div></td></tr>`};App._BOM_ZONES=[{kind:"add",name:"新增",sub:""},{kind:"del",name:"刪除",sub:"呆滯大來源"},{kind:"rev",name:"進版 · 改量/換料",sub:""},{kind:"priceOnly",name:"同料號價差 · 採購降價",sub:"非設變本體"}];App._BOM_CURR=["NTD","USD","EUR","JPY","RMB"];App._bomEnsure=function(proj){if(proj.bomQuoteCurrency==null)proj.bomQuoteCurrency="NTD";if(proj.bomBaseCurrency==null)proj.bomBaseCurrency="NTD";if(proj.bomRate==null)proj.bomRate=1;if(proj.annualVolume==null)proj.annualVolume=0;if(proj.evalYears==null)proj.evalYears=3;if(proj.targetSavePerUnit==null)proj.targetSavePerUnit=0;if(proj.targetUnitCost==null)proj.targetUnitCost=0;if(proj.unitSellPrice==null)proj.unitSellPrice=0;if(proj.targetUnitCostBase==null)proj.targetUnitCostBase=proj.targetUnitCost||0;if(proj.unitSellPriceBase==null)proj.unitSellPriceBase=proj.unitSellPrice||0;proj.oneTimeCost=proj.oneTimeCost||{mold:0,cert:0,deadStock:0};proj.bomRows=proj.bomRows||[]};App._bomDiff=r=>(parseFloat(r.newPrice)||0)*(parseFloat(r.newQty)||0)-(parseFloat(r.oldPrice)||0)*(parseFloat(r.oldQty)||0);App._matIsAssembly=function(v){const s=String(v==null?"":v).trim().toUpperCase();return/^A/.test(s)||s.indexOf("組")>=0||s.indexOf("加工")>=0||s.indexOf("自製")>=0||s.indexOf("ASSEM")>=0||s.indexOf("MAKE")>=0};App._bomPartMatClass=function(proj,partNo){const pn=String(partNo==null?"":partNo).trim();if(!pn)return"";const scan=sheet=>{if(!sheet||!sheet.grid||sheet.matCatIdx==null||sheet.partNoIdx==null)return"";for(let i=1;i<sheet.grid.length;i++){const row=sheet.grid[i];if(row&&String(row[sheet.partNoIdx]==null?"":row[sheet.partNoIdx]).trim()===pn)return App._matIsAssembly(row[sheet.matCatIdx])?"自製":"外購"}return""};const sh=proj.bomSheets||{};return scan(sh.new)||scan(sh.old)||""};App._bomZoneBreakdown=function(proj){const parts=typeof DATA!=="undefined"&&DATA.parts||[];const catOf=pn=>{const p=parts.find(x=>x.partNo===pn);return p&&p.category||"未分類"};const cats={},io={"外購":0,"自製":0,"未標示":0};let anyUnreg=false;(proj.bomRows||[]).forEach(r=>{if(r.includeInTarget===false)return;const subj=String(r.replacePartNoB||r.partNoA||"").trim();if(!subj)return;const d=App._bomDiff(r),cat=catOf(subj);if(cat==="未分類")anyUnreg=true;const cls=App._bomPartMatClass(proj,subj)||"未標示";const g=cats[cat]||(cats[cat]={cat,delta:0,count:0,cls:{}});g.delta+=d;g.count++;g.cls[cls]=(g.cls[cls]||0)+1;io[cls]+=d});const rows=Object.keys(cats).map(k=>{const g=cats[k],keys=Object.keys(g.cls);const dom=keys.slice().sort((a,b)=>g.cls[b]-g.cls[a])[0]||"未標示";const mixed=keys.filter(x=>x!=="未標示").length>1;return{cat:g.cat,delta:g.delta,count:g.count,cls:mixed?"混合":dom}}).sort((a,b)=>Math.abs(b.delta)-Math.abs(a.delta));return{rows,total:rows.reduce((s,r)=>s+r.delta,0),io,anyUnreg}};App._bomBreakdownHtml=function(proj){const bd=App._bomZoneBreakdown(proj);if(!bd.rows.length)return"";const base=proj.bomBaseCurrency||"";const maxAbs=bd.rows.reduce((m,r)=>Math.max(m,Math.abs(r.delta)),0)||1;const ioChip=c=>c==="自製"?'<span class="bbd-io in">自製</span>':c==="外購"?'<span class="bbd-io out">外購</span>':c==="混合"?'<span class="bbd-io mix">混合</span>':'<span class="bbd-io na">未標示</span>';const catRow=r=>{const pct=Math.round(Math.abs(r.delta)/maxAbs*100),cls=r.delta>0?"up":r.delta<0?"dn":"";const fill=r.delta>=0?"var(--rose)":"var(--sage-500)";const hint=r.cat==="未分類"?` <span class="bbd-hint" onclick="App.showPage('materials')">帶入主檔→</span>`:"";return'<tr><td class="bbd-cat">'+U.esc(r.cat)+hint+"</td><td>"+ioChip(r.cls)+'</td><td class="r bbd-cnt">'+r.count+'</td><td class="bbd-barc"><span class="bbd-bar"><span class="bbd-fill" style="width:'+pct+"%;background:"+fill+'"></span></span></td><td class="r bbd-d '+cls+'">'+App._bomFmt(r.delta)+"</td></tr>"};const ioCell=(lbl,val)=>'<div class="bbd-iocell"><span class="k">'+lbl+'</span><span class="v '+(val>0?"up":val<0?"dn":"")+'">'+App._bomFmt(val)+"</span></div>";return'<div class="bbd"><div class="bbd-h">成本差額 · 依部位彙總 <span class="bbd-sub">每列差異料號→料號主檔部位（類別）加權；內／外＝BOM 材料屬性（外購 M／自製 A）</span></div><div class="bbd-scroll"><table class="bbd-tbl"><thead><tr><th>部位</th><th>內／外</th><th class="r">品項</th><th>差額貢獻</th><th class="r">每台差額（'+U.esc(base)+"）</th></tr></thead><tbody>"+bd.rows.map(catRow).join("")+'</tbody><tfoot><tr><td>合計</td><td></td><td class="r">'+bd.rows.reduce((s,r)=>s+r.count,0)+'</td><td></td><td class="r bbd-d '+(bd.total>0?"up":bd.total<0?"dn":"")+'">'+App._bomFmt(bd.total)+'</td></tr></tfoot></table></div><div class="bbd-io-split">'+ioCell("外購件（M）",bd.io["外購"])+ioCell("自製件（A）",bd.io["自製"])+(bd.io["未標示"]?ioCell("未標示",bd.io["未標示"]):"")+"</div>"+(bd.anyUnreg?'<div class="bbd-note">「未分類」＝該料號尚未帶入料號主檔（無部位資訊）→ 到「物料 → 從專案帶入」補齊後自動歸位。</div>':"")+"</div>"};App._bomAltPairs=function(proj){const parts=typeof DATA!=="undefined"&&DATA.parts||[];const findP=pn=>parts.find(x=>x.partNo===pn);const num=v=>{const n=parseFloat(v);return isNaN(n)?null:n};const seen={},out=[];(proj.bomRows||[]).forEach(r=>{const a=String(r.partNoA==null?"":r.partNoA).trim();const b=String(r.replacePartNoB==null?"":r.replacePartNoB).trim();if(!a||!b||a===b)return;const key=a+""+b;if(seen[key])return;seen[key]=1;const pa=findP(a),pb=findP(b);const aPrice=pa?num(pa.unitPrice):null,bPrice=pb?num(pb.unitPrice):null;const alts=pa&&pa.alternates?String(pa.alternates).split(/[,、;；\s]+/).map(s=>s.trim()).filter(Boolean):[];out.push({a,b,aName:pa?pa.name||"":"",bName:pb?pb.name||"":"",aInMaster:!!pa,bInMaster:!!pb,aPrice,bPrice,diff:aPrice!=null&&bPrice!=null?bPrice-aPrice:null,alreadyAlt:alts.indexOf(b)>=0})});return out};App._bomAltCrossPairs=function(proj){const parts=typeof DATA!=="undefined"&&DATA.parts||[];const findP=pn=>parts.find(x=>x.partNo===pn);const num=v=>{const n=parseFloat(v);return isNaN(n)?null:n};const altsOf=pn=>{const p=findP(pn);return p&&p.alternates?String(p.alternates).split(/[,、;；\s]+/).map(s=>s.trim()).filter(Boolean):[]};const rows=proj.bomRows||[];const dels=rows.filter(r=>r.changeKind==="del"&&String(r.partNoA==null?"":r.partNoA).trim());const adds=rows.filter(r=>r.changeKind==="add"&&String(r.partNoA==null?"":r.partNoA).trim());const addList=[],addSeen={};adds.forEach(r=>{const b=String(r.partNoA).trim();if(addSeen[b])return;addSeen[b]=1;const pb=findP(b);addList.push({partNo:b,name:pb?pb.name||"":"",inMaster:!!pb})});const mk=(aNo,bNo)=>{aNo=String(aNo||"").trim();bNo=String(bNo||"").trim();const pa=findP(aNo),pb=bNo?findP(bNo):null;const aPrice=pa?num(pa.unitPrice):null,bPrice=pb?num(pb.unitPrice):null;return{a:aNo,b:bNo,aName:pa?pa.name||"":"",bName:pb?pb.name||"":"",aInMaster:!!pa,bInMaster:!!pb,aPrice,bPrice,diff:aPrice!=null&&bPrice!=null?bPrice-aPrice:null,alreadyAlt:bNo?altsOf(aNo).indexOf(bNo)>=0:false,options:null}};const auto=dels.length===1&&adds.length===1&&String(dels[0].partNoA).trim()!==String(adds[0].partNoA).trim();const out={auto,manual:!auto&&dels.length>0,delCount:dels.length,addCount:adds.length,addList,rows:[],hasAny:dels.length>0||adds.length>0};if(auto){out.rows.push(mk(dels[0].partNoA,adds[0].partNoA));return out}if(!dels.length)return out;const registered={};dels.forEach(r=>{const alts=altsOf(String(r.partNoA).trim());addList.forEach(x=>{if(alts.indexOf(x.partNo)>=0)registered[x.partNo]=1})});dels.forEach(r=>{const aNo=String(r.partNoA).trim(),alts=altsOf(aNo);const myB=addList.find(x=>alts.indexOf(x.partNo)>=0);if(myB){out.rows.push(mk(aNo,myB.partNo));return}const row=mk(aNo,"");row.options=addList.filter(x=>!registered[x.partNo]);out.rows.push(row)});return out};App._bomAltPairHtml=function(proj){const pid=proj.id,pairs=App._bomAltPairs(proj),cross=App._bomAltCrossPairs(proj);const head='<div class="bap-h">🔀 替代料配對備查 <span class="bap-badge">同列換料 '+pairs.length+" 筆 · 跨列候選 "+cross.rows.length+' 組</span></div><div class="bap-sub">把「舊料 A → 新料 B」回登為 A 的主檔替代料（下個設變案自動繼承·出現在缺口對帳單替代料建議）。價差取兩料的料號主檔單價。</div>';const rowHtml=p=>{let diffCell;if(p.aInMaster&&p.bInMaster&&p.diff!=null){const cls=p.diff<0?"dn":p.diff>0?"up":"eq";const tag=p.diff<0?"省 "+Math.abs(p.diff):p.diff>0?"貴 "+p.diff:"同價";diffCell='<div class="bap-diffline">$'+p.aPrice+" → $"+p.bPrice+'</div><span class="bap-tag '+cls+'">'+tag+"</span>"}else diffCell='<span class="bap-tag na">'+(!p.aInMaster?"A 未帶入主檔":"B 未帶入主檔")+" · 價差不明</span>";let statusCell;if(!p.aInMaster||!p.bInMaster)statusCell=`<button class="bap-btn ghost" onclick="App.showPage('materials')">帶入主檔 →</button><div class="bap-subact">帶入後才可登記為替代料</div>`;else if(p.alreadyAlt)statusCell='<span class="bap-reg">✓ 已是 '+U.esc(p.a)+" 的替代料</span>";else statusCell=`<button class="bap-btn" onclick="App.registerAlt('`+pid+"','"+Materials._jsq(p.a)+"','"+Materials._jsq(p.b)+`')">登記為替代料</button><div class="bap-subact">寫入 `+U.esc(p.a)+" 主檔 · 記履歷</div>";return'<tr><td class="bap-old"><span class="bap-pno">'+U.esc(p.a)+"</span>"+(p.aName?'<br><span class="bap-nm">'+U.esc(p.aName)+"</span>":"")+'</td><td><span class="bap-arrow">→</span> <span class="bap-pno">'+U.esc(p.b)+"</span>"+(p.bName?'<br><span class="bap-nm">'+U.esc(p.bName)+"</span>":"")+'</td><td class="r">'+diffCell+"</td><td>"+statusCell+"</td></tr>"};const sameBlock=!pairs.length?'<div class="bap-empty">本案 BOM 尚無同列替代／換料（A→B）。當設變在同一列把舊料換成新料時，配對會自動出現在此。</div>':'<div class="bap-scroll"><table class="bap-tbl"><thead><tr><th>舊料（A）</th><th>新料（B）</th><th class="r">單價價差（B−A）</th><th>主檔備查狀態</th></tr></thead><tbody>'+pairs.map(rowHtml).join("")+"</tbody></table></div>";return'<div class="bap">'+head+sameBlock+App._bomAltCrossHtml(pid,cross)+"</div>"};App._bomAltCrossHtml=function(pid,cross){const diffCell=p=>{if(p.aInMaster&&p.bInMaster&&p.diff!=null){const cls=p.diff<0?"dn":p.diff>0?"up":"eq";const tag=p.diff<0?"省 "+Math.abs(p.diff):p.diff>0?"貴 "+p.diff:"同價";return'<div class="bap-diffline">$'+p.aPrice+" → $"+p.bPrice+'</div><span class="bap-tag '+cls+'">'+tag+"</span>"}if(!p.b)return'<span class="bap-tag na">未選 · 價差待定</span>';return'<span class="bap-tag na">'+(!p.aInMaster?"A 未帶入主檔":"B 未帶入主檔")+" · 價差不明</span>"};const mini="del 列 "+cross.delCount+" · add 列 "+cross.addCount+(cross.auto?" · 已自動配 "+cross.rows.length+" 組":cross.manual?" · 多刪多增·請手動牽線":"");const sech='<div class="bap-sech">↔ 跨列刪＋增 配對 <span class="mini">'+mini+"</span></div>";if(!cross.rows.length)return'<div class="bap-sec">'+sech+'<div class="bap-empty2">本案 BOM 無跨列的刪除＋新增。當設變在不同列刪掉舊料、另列新增新料時，配對候選會自動出現在此。</div></div>';const subNote=cross.manual?'<div class="bap-sub" style="color:var(--amber-ink)">本案有多列刪除與多列新增，系統不自動猜哪刪對哪增（避免配錯）。請在「新料 B」欄自行選出對應的新增料號，再登記。已被別列登記的新料會自動從其他列下拉濾掉。</div>':"";const rowHtml=(p,i)=>{const selId="bapx-"+pid+"-"+i,btnId=selId+"-b";let bCell,statusCell;if(cross.auto||p.alreadyAlt){bCell='<span class="bap-arrow">→</span> <span class="bap-pno">'+U.esc(p.b)+"</span>"+(cross.auto&&!p.alreadyAlt?'<span class="bap-autoflag">自動配對</span>':"")+(p.bName?'<br><span class="bap-nm">'+U.esc(p.bName)+"</span>":"")}else{const opts='<option value="">（尚未選 · 不登記）</option>'+(p.options||[]).map(x=>'<option value="'+Materials._jsq(x.partNo)+'">'+U.esc(x.partNo)+(x.name?" · "+U.esc(x.name):"")+(x.inMaster?"":" · 未帶入主檔")+"</option>").join("");bCell='<select id="'+selId+'" class="bap-pick" data-btn="'+btnId+'" onchange="App._bomAltPickSel(this)">'+opts+"</select>"}if(p.alreadyAlt)statusCell='<span class="bap-reg">✓ 已是 '+U.esc(p.a)+" 的替代料</span>";else if(!p.b)statusCell='<button id="'+btnId+`" class="bap-btn" disabled onclick="App.registerAltPick('`+pid+"','"+Materials._jsq(p.a)+"','"+selId+`')">登記為替代料</button><div class="bap-subact">先在左方選出對應新料</div>`;else if(!p.aInMaster||!p.bInMaster)statusCell=`<button class="bap-btn ghost" onclick="App.showPage('materials')">帶入主檔 →</button><div class="bap-subact">帶入後才可登記為替代料</div>`;else statusCell=`<button class="bap-btn" onclick="App.registerAlt('`+pid+"','"+Materials._jsq(p.a)+"','"+Materials._jsq(p.b)+`')">登記為替代料</button><div class="bap-subact">寫入 `+U.esc(p.a)+" 主檔 · 記履歷</div>";return'<tr><td class="bap-old"><span class="bap-pno">'+U.esc(p.a)+"</span>"+(p.aName?'<br><span class="bap-nm">'+U.esc(p.aName)+"</span>":"")+"</td><td>"+bCell+'</td><td class="r">'+diffCell(p)+"</td><td>"+statusCell+"</td></tr>"};const th=cross.manual?"選新料 B（本案新增列）":"新增列（新料 B）";return'<div class="bap-sec">'+sech+subNote+'<div class="bap-scroll"><table class="bap-tbl"><thead><tr><th>刪除列（舊料 A）</th><th>'+th+'</th><th class="r">單價價差（B−A）</th><th>登記</th></tr></thead><tbody>'+cross.rows.map(rowHtml).join("")+"</tbody></table></div></div>"};App._bomAltPickSel=function(sel){const btn=document.getElementById(sel.getAttribute("data-btn"));if(btn)btn.disabled=!sel.value};App.registerAltPick=function(pid,aNo,selId){const sel=document.getElementById(selId),bNo=sel?sel.value:"";if(!bNo){U.toast("請先選出對應的新料");return}App.registerAlt(pid,aNo,bNo)};App.registerAlt=function(pid,aNo,bNo){const p0=App.getProj(pid);const projName=p0?p0.name:"";const pa=(DATA.parts||[]).find(x=>x.partNo===aNo);if(!pa){U.toast("找不到主檔料號 "+aNo);return}if(!(DATA.parts||[]).find(x=>x.partNo===bNo)){U.toast("新料 "+bNo+" 未帶入主檔，請先帶入才可登記為替代料");return}App.confirmModal({title:"登記為替代料",icon:"ti-arrows-exchange",iconBg:"--mat-l",iconColor:"--mat-ink",msg:'<div style="text-align:left;font-size:12.5px;line-height:1.6;color:var(--ink2)">將 <b>'+U.esc(bNo)+"</b> 登記為 <b>"+U.esc(aNo)+"</b> 的替代料：<br>· 寫入 "+U.esc(aNo)+" 主檔替代料欄（已存在則不重複）<br>· 記一筆履歷（來源案「"+U.esc(projName)+"」）<br>· 下個設變案換此料自動出現在替代料建議</div>",okText:"確認登記",cancelText:"取消",onConfirm:()=>{const cur=pa.alternates?String(pa.alternates).split(/[,、;；\s]+/).map(s=>s.trim()).filter(Boolean):[];if(cur.indexOf(bNo)<0){cur.push(bNo);pa.alternates=cur.join("、");(pa.history=pa.history||[]).push({field:"替代料",from:"",to:bNo,at:D.fmt(new Date,"iso"),note:"設變登記·"+projName,by:DATA.settings&&DATA.settings._role||""});Store.parts.save();U.toast("✓ 已登記 "+bNo+" 為 "+aNo+" 的替代料")}else U.toast(bNo+" 已是 "+aNo+" 的替代料");const p=App.getProj(pid);if(p)App._bomRerender(p)}})};App._bomDead=r=>r.switchMode==="immediately"?(parseFloat(r.stockQty)||0)*(parseFloat(r.oldPrice)||0):0;App._bomCalc=function(proj){const rows=proj.bomRows||[];const sum=ks=>rows.filter(r=>ks.includes(r.changeKind)&&r.includeInTarget!==false).reduce((a,r)=>a+App._bomDiff(r),0);const chg=sum(["add","del","rev"]);const buy=sum(["priceOnly"]);const dead=rows.reduce((a,r)=>a+App._bomDead(r),0);const ot=proj.oneTimeCost||{};const autoMold=App._moldCostForProject(proj.id);const manualMold=parseFloat(ot.mold)||0;const moldTotal=autoMold+manualMold;const oneTime=moldTotal+(parseFloat(ot.cert)||0)+dead;const target=chg+buy;const annual=-target*(proj.annualVolume||0);const payback=annual>0?oneTime/(annual/12):null;const netN=annual*(proj.evalYears||0)-oneTime;const forcedCost=target*(proj.annualVolume||0)*(proj.evalYears||0)+oneTime;return{chg,buy,target,dead,oneTime,annual,payback,netN,forcedCost,autoMold,manualMold,moldTotal}};App._moldTwd=function(m){const base=App._normCurA(m.baseCurrency||"TWD");const amtBase=parseFloat(m.priceTwd)||(parseFloat(m.price)||0)*(parseFloat(m.rate)||1);const r=base==="TWD"?1:App._ratePref.get(base)||1;return amtBase*r};App._moldCostForProject=function(projId){const proj=(DATA.projects||[]).find(p=>p.id===projId);const projBase=App._normCurA(proj&&proj.bomBaseCurrency||"TWD");const projRate=projBase==="TWD"?1:App._ratePref.get(projBase)||1;const twdSum=(DATA.molds||[]).reduce((sum,m)=>{const twd=App._moldTwd(m);(m.allocations||[]).forEach(a=>{if(a&&a.projId===projId)sum+=twd*((parseFloat(a.sharePct)||0)/100)});return sum},0);return twdSum/projRate};App._moldDetailPopup=function(pid){const proj=(DATA.projects||[]).find(p=>p.id===pid);if(!proj)return;const projBase=App._normCurA(proj.bomBaseCurrency||"TWD");const projRate=projBase==="TWD"?1:App._ratePref.get(projBase)||1;const rows=[];(DATA.molds||[]).forEach(m=>{(m.allocations||[]).forEach(a=>{if(a&&a.projId===pid){const pct=parseFloat(a.sharePct)||0;rows.push({name:m.moldName||"(未命名模具)",vendor:m.vendor||"",pct,amt:App._moldTwd(m)*pct/100/projRate})}})});const total=rows.reduce((s,r)=>s+r.amt,0);const cell="padding:6px 10px;border-bottom:1px solid var(--rule)";const body=rows.length?'<table style="width:100%;border-collapse:collapse;font-size:13px"><thead><tr><th style="'+cell+';text-align:left;color:var(--ink3)">模具</th><th style="'+cell+';text-align:left;color:var(--ink3)">廠商</th><th style="'+cell+';text-align:right;color:var(--ink3)">分攤</th><th style="'+cell+';text-align:right;color:var(--ink3)">金額('+U.esc(projBase)+")</th></tr></thead><tbody>"+rows.map(r=>'<tr><td style="'+cell+'">'+U.esc(r.name)+'</td><td style="'+cell+'">'+U.esc(r.vendor)+'</td><td style="'+cell+';text-align:right">'+r.pct+'%</td><td style="'+cell+';text-align:right">'+App._bomMoney(r.amt)+"</td></tr>").join("")+'</tbody><tfoot><tr><td colspan="3" style="'+cell+';text-align:right"><b>本案模具費合計</b></td><td style="'+cell+';text-align:right"><b>'+App._bomMoney(total)+'</b></td></tr></tfoot></table><div style="margin-top:10px;color:var(--ink3);font-size:12px">金額以本案 BOM 基準幣（'+U.esc(projBase)+"）顯示·跨基準幣自動換算。改分攤比例／新增模具請到「物料 → 模具費用」。</div>":'<div style="padding:16px;color:var(--ink3)">本案尚未分攤任何模具費用。到「物料 → 模具費用」匯入或新增模具並分攤到本案。</div>';App.openModal({title:"本案模具費明細 · "+U.esc(proj.name),body})};App._bomFmt=n=>{const v=Math.round(n*10)/10;return(v>0?"+":"")+v.toLocaleString()};App._bomMoney=n=>Math.round(n).toLocaleString();App._bomWholeMachine=function(sheet,method,cols,outLeaf){if(!sheet||!sheet.grid||sheet.grid.length<2)return{ok:false,needWizard:true,reason:"nodata"};const grid=sheet.grid,header=grid[0]||[];const norm=v=>String(v==null?"":v).trim().toUpperCase();const num=v=>{const n=parseFloat(v);return isNaN(n)?0:n};const colByHead=h=>{if(h==null)return null;const k=norm(h);for(let i=0;i<header.length;i++)if(norm(header[i])===k)return i;return null};let lvlIdx=sheet.levelIdx,matIdx=sheet.matCatIdx,amtIdx=null;if(cols){if(cols.level!=null)lvlIdx=colByHead(cols.level);if(cols.matCat!=null)matIdx=colByHead(cols.matCat);if(cols.amount!=null)amtIdx=colByHead(cols.amount)}let m=method||null;if(!m){if(lvlIdx!=null)m="level";else if(matIdx!=null)m="matCat";else return{ok:false,needWizard:true,reason:"nostruct"}}const pIdx=sheet.priceIdx,qIdx=sheet.qtyIdx;const noPrice=m!=="topRow"&&pIdx==null;const hasPart=r=>grid[r]&&grid[r][sheet.partNoIdx]!=null&&String(grid[r][sheet.partNoIdx]).trim()!=="";const rowVal=r=>num(grid[r][pIdx])*(qIdx!=null?grid[r][qIdx]==null||grid[r][qIdx]===""?1:num(grid[r][qIdx]):1);let total=0,leafCount=0;if(m==="level"){if(lvlIdx==null)return{ok:false,needWizard:true,reason:"nolevel"};const lv=r=>{if(!grid[r])return null;const n=parseFloat(grid[r][lvlIdx]);return isNaN(n)?null:n};const nextLv=from=>{for(let j=from;j<grid.length;j++){const v=lv(j);if(v!=null)return v}return null};for(let i=1;i<grid.length;i++){if(!hasPart(i))continue;const cur=lv(i),nxt=nextLv(i+1);if(!(nxt!=null&&cur!=null&&nxt>cur)){total+=rowVal(i);leafCount++;if(outLeaf)outLeaf.push(i)}}}else if(m==="matCat"){if(matIdx==null)return{ok:false,needWizard:true,reason:"nomatcat"};const isAssembly=App._matIsAssembly;for(let i=1;i<grid.length;i++){if(!hasPart(i))continue;if(!isAssembly(grid[i][matIdx])){total+=rowVal(i);leafCount++;if(outLeaf)outLeaf.push(i)}}}else if(m==="sumAll"){for(let i=1;i<grid.length;i++){if(!hasPart(i))continue;total+=rowVal(i);leafCount++;if(outLeaf)outLeaf.push(i)}}else if(m==="topRow"){for(let i=1;i<grid.length;i++){if(!hasPart(i))continue;if(amtIdx!=null)total=num(grid[i][amtIdx]);else if(pIdx!=null)total=rowVal(i);else return{ok:false,needWizard:true,reason:"noamount"};leafCount=1;if(outLeaf)outLeaf.push(i);break}}else return{ok:false,needWizard:true,reason:"unknown"};return{ok:!noPrice,needWizard:noPrice,reason:noPrice?"noprice":void 0,total,leafCount,method:m}};App._bomWholeTotals=function(proj){const bs=proj.bomSheets;if(!bs||!bs.old||!bs.new)return{ok:false,noData:true};const method=proj.bomWholeMethod||null,cols=proj.bomWholeCols||null;const oldR=App._bomWholeMachine(bs.old,method,cols);const newR=App._bomWholeMachine(bs.new,method,cols);if(!oldR.ok||!newR.ok)return{ok:false,needWizard:true,reason:oldR.reason||newR.reason};const f=proj.bomQuoteCurrency===proj.bomBaseCurrency?1:parseFloat(proj.bomRate)||1;const oldTotal=oldR.total*f,newTotal=newR.total*f;return{ok:true,oldTotal,newTotal,delta:newTotal-oldTotal,method:newR.method,oldLeaf:oldR.leafCount,newLeaf:newR.leafCount}};App._bomBuildModels=function(oldCands,newCands,factor,priceIdx,mapping){const f=factor||1;const exOpts={};if(priceIdx!=null&&priceIdx>=0)exOpts.priceIdx=priceIdx;if(mapping)exOpts.mapping=mapping;const norm=s=>String(s==null?"":s).trim().toUpperCase();const oldBy={},newBy={};(oldCands||[]).forEach(c=>{if(oldBy[norm(c.sheetName)]==null)oldBy[norm(c.sheetName)]=c});(newCands||[]).forEach(c=>{if(newBy[norm(c.sheetName)]==null)newBy[norm(c.sheetName)]=c});const order=[];(newCands||[]).forEach(c=>{if(order.indexOf(c.sheetName)<0)order.push(c.sheetName)});(oldCands||[]).forEach(c=>{if(order.indexOf(c.sheetName)<0)order.push(c.sheetName)});const packEx=(cand,ex)=>cand&&ex&&ex.ok?{sheetName:cand.sheetName,partNoIdx:ex.partNoIdx,priceIdx:ex.priceIdxUsed,qtyIdx:ex.qtyIdx,levelIdx:ex.levelIdx,matCatIdx:ex.matCatIdx,grid:cand.aoa.slice(ex.headerIdx)}:null;return order.map(nm=>{const oc=oldBy[norm(nm)],nc=newBy[norm(nm)];const oldEx=oc?extractBomRows(oc.aoa,exOpts):null;const newEx=nc?extractBomRows(nc.aoa,exOpts):null;const oldRows=oldEx&&oldEx.ok?oldEx.rows:[];const newRows=newEx&&newEx.ok?newEx.rows:[];const diff=bomDiffRows(oldRows,newRows,f);const sheets={old:packEx(oc,oldEx),new:packEx(nc,newEx)};const ow=sheets.old?App._bomWholeMachine(sheets.old,null,null):null;const nw=sheets.new?App._bomWholeMachine(sheets.new,null,null):null;return{id:U.id(),name:nm,bomRows:diff.rows,bomOldCount:diff.oldCount,bomNewCount:diff.newCount,bomSheets:sheets,oldTotal:ow&&ow.ok?Math.round(ow.total*f):null,newTotal:nw&&nw.ok?Math.round(nw.total*f):null,matched:!!(oc&&nc)}})};App._bomActivateModel=function(proj,idx){const m=(proj.bomModels||[])[idx];if(!m)return;proj.bomModelIdx=idx;proj.bomRows=m.bomRows;proj.bomSheets=m.bomSheets;proj.bomOldCount=m.bomOldCount;proj.bomNewCount=m.bomNewCount;proj.targetUnitCost=m.targetUnitCost||proj.targetUnitCostBase||0;proj.unitSellPrice=m.unitSellPrice||proj.unitSellPriceBase||0;proj.annualVolume=m.annualVolume||0;proj.targetSavePerUnit=m.targetSavePerUnit!=null?m.targetSavePerUnit:proj.targetSavePerUnit||0};App._bomWrapSingleModel=function(proj){if(proj.bomModels&&proj.bomModels.length)return false;if(!(proj.bomRows&&proj.bomRows.length)&&!proj.bomSheets)return false;const sh=proj.bomSheets||{};const ow=sh.old?App._bomWholeMachine(sh.old,proj.bomWholeMethod||null,proj.bomWholeCols||null):null;const nw=sh.new?App._bomWholeMachine(sh.new,proj.bomWholeMethod||null,proj.bomWholeCols||null):null;proj.bomModels=[{id:U.id(),name:proj.name||"單一機種",bomRows:proj.bomRows||[],bomSheets:proj.bomSheets||null,bomOldCount:proj.bomOldCount||0,bomNewCount:proj.bomNewCount||0,oldTotal:ow&&ow.ok?Math.round(ow.total):null,newTotal:nw&&nw.ok?Math.round(nw.total):null,matched:true,targetSavePerUnit:proj.targetSavePerUnit||0,annualVolume:proj.annualVolume||0}];proj.bomModelIdx=0;return true};App._bomModelsSummary=function(proj){return(proj.bomModels||[]).map(m=>{const diffSum=(m.bomRows||[]).reduce((a,r)=>a+App._bomDiff(r),0);const delta=m.oldTotal!=null&&m.newTotal!=null?m.newTotal-m.oldTotal:null;const tuc=parseFloat(m.targetUnitCost)||parseFloat(proj.targetUnitCostBase)||0;const achv=tuc>0&&m.newTotal?Math.round(tuc/m.newTotal*100):null;const vol=parseFloat(m.annualVolume)||0;const annualBenefit=delta!=null&&vol>0?-delta*vol:null;return{name:m.name,oldTotal:m.oldTotal,newTotal:m.newTotal,delta,dropPct:m.oldTotal&&delta!=null?-delta/m.oldTotal:null,diffSum:Math.round(diffSum*10)/10,matched:m.matched,targetUnitCost:tuc,achv,annualBenefit}})};App._bomAllSeriesStats=function(proj){const ms=proj.bomModels||[];let volSum=0,tgtW=0,actW=0,oldW=0,sellW=0,profitYr=0,saveYr=0,anyTarget=false,anySell=false;ms.forEach(m=>{const vol=parseFloat(m.annualVolume)||0;const tgt=parseFloat(m.targetUnitCost)||parseFloat(proj.targetUnitCostBase)||0;const sell=parseFloat(m.unitSellPrice)||parseFloat(proj.unitSellPriceBase)||0;const act=m.newTotal!=null?m.newTotal:null;const old=m.oldTotal!=null?m.oldTotal:null;if(vol>0){volSum+=vol;if(act!=null)actW+=act*vol;if(old!=null)oldW+=old*vol;if(tgt>0){tgtW+=tgt*vol;anyTarget=true}if(sell>0){sellW+=sell*vol;anySell=true}if(act!=null&&sell>0)profitYr+=(sell-act)*vol;if(act!=null&&tgt>0)saveYr+=(tgt-act)*vol}});return{volSum,anyTarget,anySell,modelCount:ms.length,achv:tgtW>0&&actW>0?Math.round(tgtW/actW*100):null,marginPct:sellW>0&&actW>0?(sellW-actW)/sellW*100:null,profitYr,saveYr,tgtUnit:volSum?tgtW/volSum:0,actUnit:volSum?actW/volSum:0,oldUnit:volSum?oldW/volSum:0,sellUnit:volSum?sellW/volSum:0}};App._bomModelsPick=async function(projId,which,inputEl){const file=inputEl.files&&inputEl.files[0];inputEl.value="";if(!file)return;App.openLoadingModal("BOM 匯入","解析"+(which==="old"?"舊版":"新版")+" Excel …請稍候");let parsed;try{parsed=await parseBomAoa(file)}finally{App.closeModal()}if(!parsed.ok){U.toast("⚠ "+parsed.errors.join("；"),"warning");return}App._bomMStage=App._bomMStage||{};App._bomMStage[projId]=App._bomMStage[projId]||{};App._bomMStage[projId][which]=parsed.candidates;U.toast("已讀取"+(which==="old"?"舊版":"新版")+" "+parsed.candidates.length+" 個分頁","success");const p=App.getProj(projId);if(p){p.bomModelMapping=null;p.bomModelPriceIdx=null;App._bomRerender(p)}};App._bomSetScope=function(projId,val){App._bomMScope=App._bomMScope||{};App._bomMScope[projId]=val;const p=App.getProj(projId);if(p)App._bomRerender(p)};App._bomModelsCompare=function(projId,priceIdxArg){if(App._roGuard())return;const p=App.getProj(projId);if(!p)return;App._bomEnsure(p);const st=(App._bomMStage||{})[projId]||{};if(!st.old||!st.new){U.toast("⚠ 請先上傳「舊版」與「新版」兩份 Excel（單一機種＝單一分頁；多機種＝各機種一分頁）","warning");return}const scope=(App._bomMScope||{})[projId]||"all";let oldC=st.old,newC=st.new;if(scope!=="all"){oldC=(st.old||[]).filter(c=>c.sheetName===scope);newC=(st.new||[]).filter(c=>c.sheetName===scope)}if(priceIdxArg==null&&p.bomModelPriceIdx!=null)priceIdxArg=p.bomModelPriceIdx;const mapping=p.bomModelMapping||null;const firstNew=(newC[0]||{}).aoa||[];const probe=extractBomRows(firstNew,mapping?{mapping}:{});if(probe&&probe.needMapping){App.renderImportMapping({title:"BOM 欄位對應",specs:BOM_COLUMNS,headerCells:probe.headerCells,resolved:probe.resolved,requiredKeys:BOM_REQUIRED_KEYS,domain:"bom",onConfirm:m=>{p.bomModelMapping=m;App._bomModelsCompare(projId,priceIdxArg)}});return}if(priceIdxArg==null&&probe&&probe.needPriceChoice&&probe.priceHeaders&&probe.priceHeaders.length>1){App._bomModelsPriceWizard(projId,probe.priceHeaders);return}if(probe&&!probe.ok&&!probe.needMapping){U.toast("⚠ 無法解析 BOM："+(probe.errors&&probe.errors.length?probe.errors.join("；"):"找不到表頭列")+"（請確認新版第一個分頁有「料號」等 BOM 表頭）","warning");return}const f=p.bomQuoteCurrency===p.bomBaseCurrency?1:parseFloat(p.bomRate)||1;const models=App._bomBuildModels(oldC,newC,f,priceIdxArg,mapping);p.bomModels=models;p.bomModelIdx=0;p.bomUpdatedAt=new Date().toISOString();if(priceIdxArg!=null)p.bomModelPriceIdx=priceIdxArg;App._bomActivateModel(p,0);try{Store.projects.save()}catch(e){(p.bomModels||[]).forEach(m=>delete m.bomSheets);delete p.bomSheets;Store.projects.save();U.toast("⚠ 多機種原始表過大存不進本機——差異行與整機總額已保留（完整匯出請趁本次比對）","warning")}App._bomRerender(p);U.toast("比對完成："+models.length+" 個機種","success")};App._bomModelsPriceWizard=function(projId,priceHeaders){const opts=priceHeaders.map((ph,i)=>'<label style="display:flex;align-items:center;gap:9px;padding:9px 13px;border:1px solid var(--rule);border-radius:var(--r8);cursor:pointer;font-size:14px"><input type="radio" name="bomm-price" value="'+ph.idx+'"'+(i===0?" checked":"")+"><b>"+U.esc(toTrad(ph.header))+"</b></label>").join("");App.openModal({title:"選擇計算價差的金額欄位",body:'<div class="field-hint">這份 Excel 偵測到<b>多個金額欄</b>（單價／成本欄名與系統定義不同時常見）。請選要用哪一欄計算成本差異，選定會套用到<b>全部機種分頁</b>。</div><div style="display:flex;flex-direction:column;gap:8px;margin-top:12px">'+opts+"</div>",footer:`<button class="tb-action ghost" onclick="App.closeModal()">取消</button><button class="tb-action" onclick="App._bomModelsPriceApply('`+projId+`')">套用並比對</button>`})};App._bomModelsPriceApply=function(projId){const sel=document.querySelector('input[name="bomm-price"]:checked');const idx=sel?parseInt(sel.value,10):null;App.closeModal();if(idx!=null&&!isNaN(idx))App._bomModelsCompare(projId,idx)};App._bomSelectModel=function(projId,idx){const p=App.getProj(projId);if(!p)return;idx=parseInt(idx,10);if(idx===-1)p.bomModelIdx=-1;else App._bomActivateModel(p,idx);Store.projects.save();App._bomRerender(p)};App._bomCurFieldsHtml=function(proj){const pid=proj.id,base=proj.bomBaseCurrency||"NTD";const currOpt=cur=>App._BOM_CURR.map(c=>'<option value="'+c+'"'+(c===cur?" selected":"")+">"+c+"</option>").join("");return`<div class="bom-cfield"><label>報價來源幣別</label><select onchange="App._bomSet('`+pid+`','bomQuoteCurrency',this.value)">`+currOpt(proj.bomQuoteCurrency)+`</select></div><div class="bom-cfield"><label>換算基準幣別</label><select onchange="App._bomSet('`+pid+`','bomBaseCurrency',this.value)">`+currOpt(proj.bomBaseCurrency)+'</select></div><div class="bom-cfield bom-rate"><label>匯率</label><span>1 '+U.esc(proj.bomQuoteCurrency)+' =</span><input type="number" step="0.001" value="'+proj.bomRate+`" onchange="App._bomSet('`+pid+`','bomRate',this.value)"><span>`+U.esc(base)+"</span></div>"};App._bomNpiBar=function(proj){const pid=proj.id,base=proj.bomBaseCurrency||"NTD";const st=(App._bomMStage||{})[pid]||{};const models=proj.bomModels||[];const fileCard=(which,lbl)=>{const cands=st[which],n=cands?cands.length:0,inputId="bomm-"+which+"-"+pid;return'<div class="bomm-drop'+(n?" done":"")+'"><input type="file" id="'+inputId+`" accept=".xlsx,.xls" style="display:none" onchange="App._bomModelsPick('`+pid+"','"+which+`',this)"><div class="bomm-drop-tag">`+lbl+"</div>"+(n?'<div class="bomm-drop-done">✓ 已讀取 <b>'+n+`</b> 個機種分頁 <button class="bxb-relink" onclick="document.getElementById('`+inputId+`').click()">重選</button></div>`:`<div class="bomm-drop-ph" onclick="document.getElementById('`+inputId+`').click()"><span class="big">📄</span>上傳（各機種一分頁）</div>`)+"</div>"};const scope=(App._bomMScope||{})[pid]||"all";const bothUp=st.old&&st.new;const sheetNames=bothUp?Array.from(new Set([].concat((st.new||[]).map(c=>c.sheetName),(st.old||[]).map(c=>c.sheetName)))):[];const scopeRow=bothUp?`<div style="display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin-bottom:2px"><label style="font-size:12px;color:var(--ink3);font-weight:600">比對範圍</label><select onchange="App._bomSetScope('`+pid+`',this.value)" style="font-size:13px;padding:6px 9px;border:1px solid var(--rule);border-radius:6px;background:var(--surface)"><option value="all"`+(scope==="all"?" selected":"")+">全部機種（"+sheetNames.length+" 分頁各當一機種）</option>"+sheetNames.map(nm=>'<option value="'+U.esc(nm)+'"'+(scope===nm?" selected":"")+">單一機種："+U.esc(nm)+"</option>").join("")+'</select><span style="font-size:11px;color:var(--ink4)">Excel 含多分頁時，可只比對單一機種</span></div>':"";const cmpLabel=scope!=="all"?"⚡ 比對此機種":"⚡ 全機種自動比對";const importCard='<div class="bomm-import"><div class="bxb-ey"><span class="bxb-no">1</span><span class="bxb-tt">多機種比對匯入</span><span class="bxb-note">單一機種＝單一分頁；多機種＝每機種一分頁（分頁名＝機種）</span></div><div class="bomm-row">'+fileCard("old","◄ 舊版／上一代")+fileCard("new","► 新版／這一代")+"</div>"+scopeRow+'<div class="bxb-cur">'+App._bomCurFieldsHtml(proj)+`<div class="bxb-cta-row"><button class="bxb-cta ghost" onclick="App._bomAddManual('`+pid+`')">＋ 手動輸入</button><button class="bxb-cta" onclick="App._bomModelsCompare('`+pid+`')">`+cmpLabel+"</button></div></div></div>";if(!models.length)return'<div class="bomm-wrap">'+importCard+"</div>";const sum=App._bomModelsSummary(proj);const allS=App._bomAllSeriesStats(proj);const allSel=proj.bomModelIdx===-1;const money=n=>n==null?"—":Math.round(n).toLocaleString();const fmt=n=>n==null?"—":(n>0?"+":"")+Math.round(n).toLocaleString();const pctS=n=>n==null?"—":Math.round(n*1e3)/10+"%";const dir=n=>n==null?"":n<0?"neg":"";const isEcn=!!proj.ecnType;const mfmtM=n=>n==null?"—":Math.abs(n)>=1e6?(n>0?"+":"")+Math.round(n/1e5)/10+"M":(n>0?"+":"")+Math.round(n).toLocaleString();const lastHead=isEcn?"預估年效益":"目標達成";const lastCell=s=>isEcn?'<b style="color:var(--'+(s.annualBenefit!=null&&s.annualBenefit>=0?"sage-700":"rose-ink")+')">'+mfmtM(s.annualBenefit)+"</b>":s.achv!=null?'<b style="color:var(--'+(s.achv>=100?"sage-700":"rose-ink")+')">'+s.achv+"%</b>":'<span style="color:var(--ink4);font-size:11px">未填目標</span>';const rows=sum.map((s,i)=>'<tr class="'+(i===proj.bomModelIdx?"bomm-sel":"")+`" onclick="App._bomSelectModel('`+pid+"',"+i+')"><td>'+U.esc(s.name)+(s.matched?"":' <span class="bomm-unmatched">未配對</span>')+'</td><td class="num">'+money(s.oldTotal)+'</td><td class="num">'+money(s.newTotal)+'</td><td class="num '+dir(s.delta)+'">'+fmt(s.delta)+'</td><td class="num">'+pctS(s.dropPct)+'</td><td class="num">'+lastCell(s)+"</td></tr>").join("");const tot=sum.reduce((a,s)=>{a.o+=s.oldTotal||0;a.n+=s.newTotal||0;return a},{o:0,n:0});const totDelta=tot.n-tot.o;const totBenefit=sum.reduce((a,s)=>a+(s.annualBenefit||0),0);const lastTot=isEcn?sum.some(s=>s.annualBenefit!=null)?"<b>"+mfmtM(totBenefit)+"</b>":"—":allS.achv!=null?"<b>"+allS.achv+"%</b>":"—";const totRowOpen=isEcn?'<tr class="bomm-tot"><td>合計</td>':'<tr class="bomm-tot'+(allSel?" bomm-sel":"")+`" onclick="App._bomSelectModel('`+pid+`',-1)" style="cursor:pointer" title="點此看全系列加權綜合結算"><td>全系列加權平均 ◂</td>`;const summary='<div class="bomm-sumcard"><div class="bomm-hd">型號比較列表（'+models.length+" 機種 · 幣別 "+U.esc(base)+'）· 點擊該列可切換下方機種數據</div><div class="bomm-tbl-scroll"><table class="bomm-tbl"><thead><tr><th>機種</th><th>舊整機總額</th><th>新整機總額</th><th>整機價差</th><th>降幅</th><th>'+lastHead+"</th></tr></thead><tbody>"+rows+totRowOpen+'<td class="num">'+money(tot.o)+'</td><td class="num">'+money(tot.n)+'</td><td class="num '+dir(totDelta)+'">'+fmt(totDelta)+'</td><td class="num">'+(tot.o?pctS(-totDelta/tot.o):"—")+'</td><td class="num">'+lastTot+"</td></tr></tbody></table></div></div>";const sel=`<div class="bomm-picker"><label>檢視機種明細</label><select onchange="App._bomSelectModel('`+pid+`',this.value)">`+(isEcn?"":'<option value="-1"'+(allSel?" selected":"")+">全系列加權平均</option>")+models.map((m,i)=>'<option value="'+i+'"'+(i===proj.bomModelIdx?" selected":"")+">"+U.esc(m.name)+"</option>").join("")+'</select><span class="bomm-picker-note">下方差異四區／整機成本＝目前選的機種；在區塊3填'+(isEcn?"目標調降／年產／一次性":"目標成本／售價／年產")+"＝記到「這個機種」，各機種各自算損益（切機種各自保留）。匯出在右上「匯出成本差異報告」</span></div>";return'<div class="bomm-wrap"><div class="bomm-toprow">'+importCard+summary+"</div>"+sel+"</div>"};App._ecnTabBHtml=function(proj){App._bomEnsure(proj);if(proj.ecnType&&App._bomWrapSingleModel(proj)){try{Store.projects.save()}catch(e){}}const C=App._bomCalc(proj);const base=proj.bomBaseCurrency;const pid=proj.id;const dir=n=>n>0?"up":"dn";const forced=proj.roiType==="forced";const roiModeBar='<div class="bxb-roimode"><div class="bxb-roimode-hd"><b>設變效益評估類型</b><span>決定下方結算方式——可在此修改（預設沿用開案選擇）</span></div><div class="bxb-rmseg"><button class="bxb-rmbtn'+(!forced?" on":"")+`" onclick="App._bomSet('`+pid+`','roiType','benefit')"><b>效益型 · 主動成本調降</b><span>算每台省多少 · 多久回本 · N 年賺多少</span></button><button class="bxb-rmbtn`+(forced?" on":"")+`" onclick="App._bomSet('`+pid+`','roiType','forced')"><b>被迫型 · 不可抗力</b><span>不算回本 · 算變更總代價 · 壓到最小</span></button></div></div>`;const flowSteps=proj.ecnType?'<li><b>上傳並比對各機種</b>：「多機種比對匯入」放設變前/後各一份 Excel（每受影響機種一分頁）、確認幣別/匯率→按「⚡全機種自動比對」→點型號列表選要看的機種（單機種＝上傳單一分頁即可）。</li><li><b>執行差異比對</b>：自動分四區（新增/刪除/改量·換料/只有價差），每行可決定要不要納入成本，預設全納，未變更料不佔畫面。</li><li><b>標切換方式與庫存</b>：每行選即刻（舊料立停→呆滯）或漸進（用完再換→無呆滯）；刪除料/換掉舊料填庫存，自動算呆滯。</li><li><b>看每台目標成本差額</b>：中間大數字＝每台省（或多花）多少；設「目標成本調降/台」顯達成進度。</li><li><b>設效益參數出回本期</b>：填年產/年限/一次性（模具＋認證，呆滯自動）→年效益/回本期/N年淨效益；被迫型改看合規總代價壓最小。</li><li><b>匯出報告</b>：區塊③「匯出 Excel 報告」→選對照/替換與欄位→三頁報表。系統不存 Excel、現場重生。<span class="bom-wave-tag">不保留上傳檔</span></li>':'<li><b>上傳並比對各機種</b>：「多機種比對匯入」放新舊各一份 Excel（每機種一分頁）、確認幣別/匯率→按「⚡全機種自動比對」→點型號總表選要看的機種。</li><li><b>標切換方式與庫存</b>：每行選即刻（舊料立停→呆滯）或漸進（用完再換→無呆滯）；刪除料/換掉舊料填庫存，自動算呆滯。</li><li><b>看每台目標成本差額</b>：中間大數字＝每台省（或多花）多少；設「目標成本調降/台」顯達成進度。</li><li><b>設效益參數出回本期</b>：填年產/年限/一次性（模具＋認證，呆滯自動）→年效益/回本期/N年淨效益；被迫型改看合規總代價壓最小。</li><li><b>匯出報告</b>：右上「匯出成本差異報告」→多機種比較（全機種）或本機種設變分析（3頁）。系統不存 Excel、現場重生。<span class="bom-wave-tag">不保留上傳檔</span></li>';const flow=App.buildHintBox({key:"ecn-bom-flow",icon:"ti-settings",title:"BOM 成本差異分析 怎麼用",summary:"成本與效益試算流程",collapsed:true,bodyHtml:"<ol>"+flowSteps+"</ol>"});const bno={board:"2",roi:"3"};const cnt=proj.bomOldCount?"舊 "+proj.bomOldCount+"／新 "+(proj.bomNewCount||0)+" → "+proj.bomRows.length+" 項差異":proj.bomRows.length?proj.bomRows.length+" 項差異":"尚無差異——先匯入或手動輸入";const tv=parseFloat(proj.targetSavePerUnit)||0;const pctRaw=tv>0?-C.target/tv*100:0;const pct=Math.max(0,Math.min(100,pctRaw));const heroPct=tv>0?'<span class="pct">達成 '+Math.round(pctRaw)+"%</span>":"";const heroProg=tv>0?'<div class="bxb-pb"><div class="bxb-pb-t"><div class="bxb-pb-f" style="width:'+pct.toFixed(0)+'%"></div></div><div class="bxb-pb-c"><span>目前 '+App._bomFmt(C.target)+"</span><span>目標 −"+tv.toLocaleString()+"/台</span></div></div>":"";const stL=proj.ecnType?["設變成本差額","採購降價貢獻","每台目標成本差額"]:["結構變更差額","同料價格差額","每台差異淨額"];const stats='<div class="bxb-stats"><div class="bxb-stat"><div class="l">'+stL[0]+'</div><div class="v '+dir(C.chg)+'">'+App._bomFmt(C.chg)+'<span class="u">'+U.esc(base)+'/台</span></div></div><div class="bxb-stat"><div class="l">'+stL[1]+'</div><div class="v '+dir(C.buy)+'">'+App._bomFmt(C.buy)+'<span class="u">'+U.esc(base)+'/台</span></div></div><div class="bxb-stat hero"><div class="l">'+stL[2]+heroPct+'</div><div class="v '+dir(C.target)+'">'+App._bomFmt(C.target)+'<span class="u">'+U.esc(base)+" / 台</span></div>"+heroProg+"</div></div>";const zones=App._BOM_ZONES.map(z=>App._bomZoneHtml(proj,z)).join("");const deadRows=proj.bomRows.filter(r=>App._bomDead(r)>0);const deadBar=C.dead>0?'<div class="bom-deadbar">⚠ 呆滯合計 <b>'+App._bomMoney(C.dead)+"</b>："+deadRows.map(r=>U.esc(r.partNoA||"")+"（"+(parseFloat(r.stockQty)||0)+"×"+(parseFloat(r.oldPrice)||0)+"="+App._bomMoney(App._bomDead(r))+"）").join("＋")+"，皆走「即刻切換」。改「漸進切換」可消呆滯。</div>":"";const allSeries=!proj.ecnType&&proj.bomModelIdx===-1&&(proj.bomModels||[]).length>0;const blk2=allSeries?'<div class="bxb-blk"><div class="bxb-ey"><span class="bxb-no">'+bno.board+'</span><span class="bxb-tt">世代差異比對看板</span><span class="bxb-note">全系列加權平均</span></div><div style="padding:32px 20px;text-align:center;color:var(--ink4);font-size:13px;line-height:1.7;background:var(--paper);border:1px dashed var(--rule);border-radius:var(--r8)">各機種的壓縮機、控制板料號與用量不同，無法做實體零件差異比對。<br><b style="color:var(--ink2)">請於上方型號列表點選單一機種</b>，以檢視該機種的零件變更明細。</div></div>':'<div class="bxb-blk"><div class="bxb-ey"><span class="bxb-no">'+bno.board+'</span><span class="bxb-tt">'+(proj.ecnType?"差異比對與成本分析看板":"世代差異比對看板")+'</span><span class="bxb-note">'+cnt+"</span></div>"+stats+App._bomBreakdownHtml(proj)+'<div style="display:flex;flex-direction:column;gap:9px">'+zones+"</div>"+App._bomAltPairHtml(proj)+deadBar+"</div>";const machineDelta=(proj.bomRows||[]).reduce((a,r)=>a+App._bomDiff(r),0);const WT=App._bomWholeTotals(proj);const mcfgLink=`<a class="bxb-mcfg" onclick="App._bomWholeWizard('`+pid+`')">調整計算基準</a>`;const phCell=(label,hint)=>'<div class="bxb-mcell"><div class="l">'+label+'</div><div class="v" style="color:var(--ink4);font-size:13px;font-weight:500">'+hint+"</div></div>";let machine;if(WT.ok){const methodLabel={level:"依階次葉節點",matCat:"依材料類別（M 料）",sumAll:"全部料加總",topRow:"金額欄頂列"}[WT.method]||"";machine='<div class="bxb-machine"><div class="mh">整機成本對照（幣別 '+U.esc(base)+"） · 舊/新整機總額＝完整 BOM 葉節點加總 · 基準："+methodLabel+"（舊 "+WT.oldLeaf+"／新 "+WT.newLeaf+" 個葉節點）"+mcfgLink+'</div><div class="bxb-mcell '+dir(WT.delta)+'"><div class="l">整機價差（新−舊）</div><div class="v">'+App._bomFmt(WT.delta)+'</div></div><div class="bxb-mcell"><div class="l">舊版整機總額</div><div class="v">'+App._bomMoney(WT.oldTotal)+'</div></div><div class="bxb-mcell"><div class="l">新版整機總額</div><div class="v">'+App._bomMoney(WT.newTotal)+"</div></div></div>"}else if(WT.noData){machine='<div class="bxb-machine"><div class="mh">整機成本對照（幣別 '+U.esc(base)+'） · 匯入舊/新版完整 BOM 並「差異比對」後，自動加總葉節點顯示整機總額</div><div class="bxb-mcell '+dir(machineDelta)+'"><div class="l">整機價差（差異行加總）</div><div class="v">'+App._bomFmt(machineDelta)+"</div></div>"+phCell("舊版整機總額","匯入後顯示")+phCell("新版整機總額","匯入後顯示")+"</div>"}else{const reasonMsg=WT.reason==="noprice"?"完整表無單價欄——只能核對物料/用量，無法加總整機金額":"無法自動判斷葉節點基準（找不到階次或材料類別欄）";machine='<div class="bxb-machine"><div class="mh">整機成本對照（幣別 '+U.esc(base)+"） · "+reasonMsg+mcfgLink+'</div><div class="bxb-mcell '+dir(machineDelta)+'"><div class="l">整機價差（差異行加總）</div><div class="v">'+App._bomFmt(machineDelta)+"</div></div>"+phCell("舊版整機總額","點右上選基準")+phCell("新版整機總額","點右上選基準")+"</div>"}const num=(label,field,val,ro,note)=>'<div class="bxb-rin"><label>'+label+(note||"")+'</label><input type="number" '+(ro?"disabled ":"")+'value="'+val+'"'+(ro?"":` onchange="App._bomSet('`+pid+"','"+field+`',this.value)"`)+"></div>";const roiParams='<div class="bxb-rins">'+num("年產台數（台）","annualVolume",proj.annualVolume)+num("評估年限（年）","evalYears",proj.evalYears)+(C.autoMold>0?`<div class="bxb-rin"><label>一次性 · 模具（自動加總 · <span style="color:var(--mat);cursor:pointer;text-decoration:underline" onclick="App._moldDetailPopup('`+pid+`')">明細</span>）</label><input value="`+App._bomMoney(C.autoMold)+'" disabled title="來自「物料→模具費用」分攤到本案·改分攤請去該頁"></div>'+num("一次性 · 模具（手動加項·試模等）","ot_mold",proj.oneTimeCost.mold||0):num("一次性 · 模具","ot_mold",proj.oneTimeCost.mold||0))+num("一次性 · 認證","ot_cert",proj.oneTimeCost.cert||0)+num("一次性 · 呆滯（自動）","",App._bomMoney(C.dead),true)+(forced?'<div class="bxb-rin"><label>目標成本調降 / 台<span style="color:var(--ink4);font-size:9.5px"> 不適用</span></label><input value="—" disabled></div>':num("目標成本調降 / 台","targetSavePerUnit",tv,false,'<span style="color:var(--amber);font-size:10px"> 餵進度條</span>'))+"</div>";const bigcard=(lbl,val,cls,formula)=>'<div class="bxb-bigcard '+cls+'"><div class="bl">'+lbl+'</div><div class="bv '+cls+'">'+val+"</div>"+(formula?'<div class="bf">'+formula+"</div>":"")+"</div>";const roiOut=forced?'<div class="bxb-bigout forced">'+bigcard("合規／變更總代價（"+proj.evalYears+" 年）",App._bomMoney(C.forcedCost)+" <small>"+U.esc(base)+"</small>","cost","每台差額 "+App._bomFmt(C.target)+" × 年產 "+(proj.annualVolume||0).toLocaleString()+" × "+proj.evalYears+" 年 ＋ 一次性 "+App._bomMoney(C.oneTime))+'<div class="bxb-rnote"><b>回本期不適用</b>——被迫型不是為了賺錢，是為了合規/續產。目標＝把總代價壓到最小：改<b>漸進切換</b>消呆滯、壓低模具/認證一次性。</div></div>':'<div class="bxb-bigout">'+bigcard("年效益",App._bomMoney(C.annual)+" <small>"+U.esc(base)+"/年</small>","good","每台差額 "+App._bomFmt(-C.target).replace("+","")+" × 年產 "+(proj.annualVolume||0).toLocaleString())+bigcard("回本期",C.payback!=null?Math.round(C.payback*10)/10+" <small>月</small>":"—","good","一次性 "+App._bomMoney(C.oneTime)+" ÷ 月效益")+bigcard(proj.evalYears+" 年淨效益",App._bomMoney(C.netN)+" <small>"+U.esc(base)+"</small>","good","年效益 × "+proj.evalYears+" − 一次性")+bigcard("設變降本達成率",tv>0?Math.round(-C.target/tv*100)+" <small>%</small>":"—","good",tv>0?"目標調降 "+tv.toLocaleString()+" ｜ 實際調降 "+App._bomMoney(-C.target):"開案未填目標調降")+"</div>";const exportBar=proj.ecnType?'<div class="bxb-expbar"><div class="bxb-exp-note"><b>匯出 Excel 報告</b>：'+((proj.bomModels||[]).length>1?"「多機種比較」＝型號比較總表＋各機種差異明細＋換料對照；「本機種設變分析」＝目前機種三頁（公式可改）。":"選呈現模式（對照／替換）與欄位 → 三頁報表（總覽與效益評估／成本差異明細／設變對照 BOM，公式全串·可改黃格自動重算）。")+"系統不存 Excel，從結構化資料現場重生。</div>"+((proj.bomModels||[]).length>1?`<button class="bxb-cta ghost" onclick="App.exportBomModels(App.getProj('`+pid+`'))">📊 多機種比較</button>`:"")+`<button class="bxb-cta" onclick="App._bomExportWizard('`+pid+`')">📤 `+((proj.bomModels||[]).length>1?"本機種設變分析":"匯出 Excel 報告")+"</button></div>":"";const typeTag='<span style="font-weight:400;color:var(--ink4);font-size:11px">（'+(forced?"被迫型":"效益型")+"·於頁首切換）</span>";const bLock=proj.ecnType&&App._ecnLocked(proj);const lockWrap=html=>bLock?'<div class="bom-locked">'+html+"</div>":html;let blk3;if(proj.ecnType){blk3='<div class="bxb-blk"><div class="bxb-ey"><span class="bxb-no">'+bno.roi+'</span><span class="bxb-tt">成本效益結算與匯出</span><span class="bxb-note">'+(forced?"被迫型 · 追合規總代價壓最小":"效益型 · 結案時比對新舊 BOM 結算成本調降")+"</span></div>"+lockWrap(machine+'<div class="bxb-sech">💰 效益試算參數 '+typeTag+"</div>"+roiParams)+'<div class="bxb-sech">📊 結算輸出 <span style="font-weight:400;color:var(--ink4);font-size:11px">— 這次變更值不值得做的最終答案</span></div>'+roiOut+exportBar+"</div>"}else if(allSeries){const A=App._bomAllSeriesStats(proj);const mfmtA=n=>{const a=Math.abs(n);return a>=1e6?Math.round(n/1e5)/10+"M":Math.round(n).toLocaleString()};const heroA='<div class="bxb-bigcard hero"><div class="bl">全系列加權達成率'+(A.achv!=null?A.achv>=100?' <span class="bxb-badge good">整體達標</span>':' <span class="bxb-badge bad">整體超支</span>':"")+'</div><div class="bv '+(A.achv==null?"":A.achv>=100?"good":"cost")+'">'+(A.achv!=null?A.achv+" <small>%</small>":"—")+'</div><div class="bf">'+(A.anyTarget?"加權實際 "+App._bomMoney(A.actUnit)+" vs 目標 "+App._bomMoney(A.tgtUnit)+" · 全系列年產 "+Math.round(A.volSum).toLocaleString()+" 台":"各機種填目標成本＋年產後計算")+"</div></div>";const profitCard='<div class="bxb-bigcard"><div class="bl">專案總毛利 <span style="font-weight:400;color:var(--ink4)">/ 年</span></div><div class="bv '+(A.anySell?"good":"")+'">'+(A.anySell?mfmtA(A.profitYr)+" <small>"+U.esc(base)+"</small>":"—")+'</div><div class="bf">'+(A.anySell?"加權毛利 × 各機種年產加總":"各機種填售價＋年產後計算")+"</div></div>";const mpctCard='<div class="bxb-bigcard"><div class="bl">總毛利率</div><div class="bv '+(A.marginPct!=null?"good":"")+'">'+(A.marginPct!=null?Math.round(A.marginPct*10)/10+" <small>%</small>":"—")+'</div><div class="bf">加權售價扣除加權成本</div></div>';const saveCard='<div class="bxb-bigcard"><div class="bl">全系列年省總額 <span style="font-weight:400;color:var(--ink4)">/ 年</span></div><div class="bv '+(A.anyTarget?A.saveYr>=0?"good":"cost":"")+'">'+(A.anyTarget?mfmtA(A.saveYr)+" <small>"+U.esc(base)+"</small>":"—")+'</div><div class="bf">各機種（目標−實際）× 年產加總</div></div>';blk3='<div class="bxb-blk"><div class="bxb-ey"><span class="bxb-no">'+bno.roi+'</span><span class="bxb-tt">成本目標與毛利結算</span><span class="bxb-note">全系列加權綜合 · 依各機種年產量加權（共 '+A.modelCount+' 機種）</span></div><div class="bxb-sech">📊 結算輸出 · 全系列綜合</div><div class="bxb-bigout">'+heroA+profitCard+mpctCard+saveCard+"</div></div>"}else{const tuc=parseFloat(proj.targetUnitCost)||0;const usp=parseFloat(proj.unitSellPrice)||0;const act=WT.ok?WT.newTotal:null;const oldGen=WT.ok?WT.oldTotal:null;const vol=parseFloat(proj.annualVolume)||0;const achv=tuc>0&&act?Math.round(tuc/act*100):null;const save=tuc>0&&act!=null?tuc-act:null;const yr=save!=null?save*vol:0;const margin=usp>0&&act!=null?usp-act:null;const marginPct=usp>0&&margin!=null?margin/usp*100:null;const gen=oldGen!=null&&act!=null?act-oldGen:null;const mfmt=n=>{const a=Math.abs(n);return a>=1e6?Math.round(n/1e5)/10+"M":Math.round(n).toLocaleString()};const dispVal=v=>v!=null?App._bomMoney(v):"—";const rinRO=(label,val)=>'<div class="bxb-rin"><label>'+label+'</label><input value="'+val+'" disabled></div>';const npiParams='<div class="bxb-rins">'+num("目標成本 / 台","targetUnitCost",tuc,false,'<span style="color:var(--ink4);font-size:9.5px"> 立案設定</span>')+rinRO("實際整機成本 / 台",dispVal(act))+rinRO("目標差異",act!=null&&tuc>0?App._bomFmt(act-tuc):"—")+num("基準售價（選填）","unitSellPrice",usp)+num("年產台數","annualVolume",vol)+rinRO("上一代成本",dispVal(oldGen))+'</div><div style="font-size:11px;color:var(--ink4);margin:7px 0 2px">目標成本／基準售價由立案設定同步（此頁即時試算） · '+mcfgLink+"</div>";const okAchv=save!=null&&save>=0;const pbHtml=achv!=null?'<div class="bxb-pb"><div class="bxb-pb-t"><div class="bxb-pb-f" style="width:'+Math.max(0,Math.min(100,act>0?Math.round(tuc/act*100):0))+'%"></div></div><div class="bxb-pb-c"><span>實際 '+App._bomMoney(act)+"</span><span>目標 "+tuc.toLocaleString()+"</span></div></div>":"";const heroCard='<div class="bxb-bigcard hero"><div class="bl">成本目標達成率'+(save!=null?okAchv?' <span class="bxb-badge good">已達標</span>':' <span class="bxb-badge bad">超支</span>':"")+'</div><div class="bv '+(achv==null?"":okAchv?"good":"cost")+'">'+(achv!=null?achv+" <small>%</small>":"—")+"</div>"+pbHtml+'<div class="bf">'+(save!=null?(save>=0?"每台省 ":"每台超支 ")+App._bomMoney(Math.abs(save))+" · 年"+(save>=0?"省 ":"增 ")+mfmt(Math.abs(yr))+" "+U.esc(base):"填目標成本／匯入 BOM 後計算")+"</div></div>";const marginCard='<div class="bxb-bigcard"><div class="bl">單台毛利</div><div class="bv '+(margin==null?"":"good")+'">'+(margin!=null?App._bomFmt(margin).replace("+","")+" <small>"+U.esc(base)+"</small>":"—")+'</div><div class="bf">'+(usp>0?"基於預估售價 "+usp.toLocaleString():"填基準售價以啟用毛利")+"</div></div>";const marginPctCard='<div class="bxb-bigcard"><div class="bl">預估毛利率</div><div class="bv '+(marginPct==null?"":"good")+'">'+(marginPct!=null?Math.round(marginPct*10)/10+" <small>%</small>":"—")+'</div><div class="bf">'+(usp>0?"售價扣除整機成本後":"填基準售價以啟用")+"</div></div>";const genCard=gen!=null?'<div class="bxb-bigcard"><div class="bl">世代成本效益</div><div class="bv '+(gen<=0?"good":"cost")+'">'+App._bomFmt(gen)+" <small>"+U.esc(base)+'</small></div><div class="bf">'+(gen<=0?"較上一代降本":"較上一代增加")+"</div></div>":"";const npiCards='<div class="bxb-bigout">'+heroCard+marginCard+marginPctCard+genCard+"</div>";blk3='<div class="bxb-blk"><div class="bxb-ey"><span class="bxb-no">'+bno.roi+'</span><span class="bxb-tt">成本目標與毛利結算</span><span class="bxb-note">新品 · 實際整機成本 vs 目標成本／台</span></div><div class="bxb-sech">💰 結算參數</div>'+npiParams+'<div class="bxb-sech">📊 結算輸出 <span style="font-weight:400;color:var(--ink4);font-size:11px">— 這個新品成本站不站得住</span></div>'+npiCards+"</div>"}const mfmtS=n=>{const a=Math.abs(n);return a>=1e6?Math.round(n/1e5)/10+"M":Math.round(n).toLocaleString()};const stkSeg=(l,v,cls)=>'<span class="bom-stick-m"><span class="l">'+l+'</span><span class="v '+(cls||"")+'">'+v+"</span></span>";let stkInner="";if(!proj.ecnType&&(proj.bomModels||[]).length){if(allSeries){const A=App._bomAllSeriesStats(proj);stkInner='<span class="bom-stick-md">全系列加權</span>'+stkSeg("達成率",A.achv!=null?A.achv+"%":"—",A.achv!=null&&A.achv>=100?"good":"")+stkSeg("總毛利/年",A.anySell?mfmtS(A.profitYr):"—",A.anySell?"good":"")+stkSeg("年省/年",A.anyTarget?mfmtS(A.saveYr):"—","")}else{const tucS=parseFloat(proj.targetUnitCost)||0,uspS=parseFloat(proj.unitSellPrice)||0,actS=WT.ok?WT.newTotal:null;const achvS=tucS>0&&actS?Math.round(tucS/actS*100):null,marS=uspS>0&&actS!=null?uspS-actS:null;stkInner='<span class="bom-stick-md">'+U.esc((proj.bomModels[proj.bomModelIdx]||{}).name||"本機種")+"</span>"+stkSeg("達成率",achvS!=null?achvS+"%":"—",achvS!=null?achvS>=100?"good":"cost":"")+stkSeg("單台毛利",marS!=null?App._bomMoney(marS):"—",marS!=null?"good":"")+stkSeg("目標差",actS!=null&&tucS>0?App._bomFmt(actS-tucS):"—","")}}else if(proj.ecnType){stkInner='<span class="bom-stick-md">'+(forced?"被迫型":"效益型")+"</span>"+(forced?stkSeg("變更總代價",App._bomMoney(C.forcedCost),"cost"):stkSeg("年效益",App._bomMoney(C.annual),"good")+stkSeg("回本",C.payback!=null?Math.round(C.payback*10)/10+"月":"—","")+stkSeg("達成率",tv>0?Math.round(-C.target/tv*100)+"%":"—",tv>0?"good":""))}const stickyHtml=stkInner?'<div class="bom-stick">'+stkInner+"</div>":"";const bomHead=(proj.ecnType?lockWrap(roiModeBar):"")+'<div class="bomm-head">'+flow+App._bomNpiBar(proj)+"</div>";const drift=App._bomPriceDrift(proj);const cinfo=drift.length?'<div class="bom-cinfo"><span class="bom-cinfo-ic">📣</span><div class="bom-cinfo-tx"><b>系統商情提示</b>：料號主檔偵測到 '+(drift[0].vendor?"供應商 "+U.esc(drift[0].vendor)+" ":"")+drift.slice(0,3).map(function(d){return U.esc(d.partNo)}).join("、")+(drift.length>3?" 等":"")+" 共 <b>"+drift.length+`</b> 筆物料有新報價，目前 BOM 仍用舊價計算。</div><button class="bom-cinfo-b ghost" onclick="App._bomDriftDetail('`+pid+`')">查看單價差異</button><button class="bom-cinfo-b" onclick="App._bomApplyMasterPrices('`+pid+`')">一鍵套用最新成本</button></div>`:"";const subTab=App._bomSubTab==="detail"?"detail":"settle";const stbtn=(id,name,cue)=>'<button class="bom-subtab'+(subTab===id?" on":"")+`" onclick="App._bomSetSubTab('`+id+`')">`+name+'<span class="cue">'+cue+"</span></button>";const subtabsBar='<div class="bom-subtabs">'+stbtn("settle","結算","值不值 · 財務結論")+stbtn("detail","明細","改了哪些料")+"</div>";const pane=subTab==="detail"?lockWrap(blk2):bomHead+blk3;return'<div class="bom-wrap">'+cinfo+stickyHtml+subtabsBar+'<div class="bom-subpane">'+pane+"</div></div>"};App._bomZoneHtml=function(proj,z){App._bomZoneOpen=App._bomZoneOpen||{};const open=App._bomZoneOpen[z.kind]!==false;const rows=(proj.bomRows||[]).filter(r=>r.changeKind===z.kind);const zSum=rows.filter(r=>r.includeInTarget!==false).reduce((a,r)=>a+App._bomDiff(r),0);const npi=!proj.ecnType;const zName=npi?{add:"新增",del:"刪除",rev:"改量 · 換料",priceOnly:"同料跨代價差"}[z.kind]:z.name;const zSub=npi?{add:"這代新拉的料",del:"這代取消的料",rev:"規格／用量調整、替代料",priceOnly:"同料號、同用量、只單價不同（供應商報價變動）"}[z.kind]:z.sub;const heads=(npi?{add:["納入","品號","新 價×量","價差",""],del:["納入","品號","舊 價×量","價差",""],rev:["納入","品號 A","替代 B","舊 價×量","新 價×量","價差",""],priceOnly:["納入","品號（同料·同量）","量","舊單價","新單價","價差×量",""]}:{add:["納入","品號","新 價×量","切換","價差",""],del:["納入","品號","舊 價×量","切換","庫存","呆滯",""],rev:["納入","品號 A","替代 B","舊 價×量","新 價×量","價差","切換","庫存","呆滯",""],priceOnly:["納入","品號（同料·同量）","量","舊單價","新單價","價差×量",""]})[z.kind];const inp=(r,f,w)=>'<input class="bom-in '+(w||"")+'" type="'+(f==="partNoA"||f==="replacePartNoB"?"text":"number")+'" value="'+U.esc(r[f]!=null?String(r[f]):"")+`" onchange="App._bomRowSet('`+proj.id+"','"+r.id+"','"+f+`',this.value)">`;const pq=(r,pf,qf)=>'<span class="bom-pq">'+inp(r,pf,"n")+"×"+inp(r,qf,"n")+"</span>";const sw=r=>'<select class="bom-sw '+(r.switchMode==="immediately"?"imm":"run")+`" onchange="App._bomRowSet('`+proj.id+"','"+r.id+`','switchMode',this.value)"><option value="running"`+(r.switchMode!=="immediately"?" selected":"")+'>漸進</option><option value="immediately"'+(r.switchMode==="immediately"?" selected":"")+">即刻</option></select>";const chk=r=>'<input type="checkbox" '+(r.includeInTarget!==false?"checked ":"")+`onchange="App._bomRowSet('`+proj.id+"','"+r.id+`','includeInTarget',this.checked)">`;const del=r=>`<button class="bom-x" title="刪除此行" onclick="App._bomRowDel('`+proj.id+"','"+r.id+`')">×</button>`;const dcell=r=>{const d=App._bomDead(r);return d>0?'<b class="bom-deadv">'+App._bomMoney(d)+"</b>":"—"};const diff=r=>{const d=App._bomDiff(r);return'<b class="'+(d>0?"up":"dn")+'">'+App._bomFmt(d)+"</b>"};const tds=r=>{if(npi){if(z.kind==="add")return[chk(r),inp(r,"partNoA","w"),pq(r,"newPrice","newQty"),diff(r),del(r)];if(z.kind==="del")return[chk(r),inp(r,"partNoA","w"),pq(r,"oldPrice","oldQty"),diff(r),del(r)];if(z.kind==="rev")return[chk(r),inp(r,"partNoA","w"),inp(r,"replacePartNoB","w"),pq(r,"oldPrice","oldQty"),pq(r,"newPrice","newQty"),diff(r),del(r)];return[chk(r),inp(r,"partNoA","w"),inp(r,"newQty","n"),inp(r,"oldPrice","n"),inp(r,"newPrice","n"),diff(r),del(r)]}if(z.kind==="add")return[chk(r),inp(r,"partNoA","w"),pq(r,"newPrice","newQty"),sw(r),diff(r),del(r)];if(z.kind==="del")return[chk(r),inp(r,"partNoA","w"),pq(r,"oldPrice","oldQty"),sw(r),inp(r,"stockQty","n"),dcell(r),del(r)];if(z.kind==="rev")return[chk(r),inp(r,"partNoA","w"),inp(r,"replacePartNoB","w"),pq(r,"oldPrice","oldQty"),pq(r,"newPrice","newQty"),diff(r),sw(r),inp(r,"stockQty","n"),dcell(r),del(r)];return[chk(r),inp(r,"partNoA","w"),inp(r,"newQty","n"),inp(r,"oldPrice","n"),inp(r,"newPrice","n"),diff(r),del(r)]};const body=rows.length?rows.map(r=>"<tr>"+tds(r).map(c=>"<td>"+c+"</td>").join("")+"</tr>").join(""):'<tr><td colspan="'+heads.length+'" class="bom-empty">此區尚無差異行（用「＋手動輸入」建立，或等差異比對自動帶入）</td></tr>';const allChk=rows.length&&rows.every(r=>r.includeInTarget!==false);return`<div class="bom-zone"><div class="bom-zhead" onclick="App._bomZoneToggle('`+z.kind+`')"><span class="bom-zchev">`+(open?"▾":"▸")+"</span><b>"+zName+'</b><span class="bom-zcnt">'+rows.length+" 項</span>"+(zSub?'<span class="bom-zsub">'+zSub+"</span>":"")+(rows.length?'<label class="bom-zall" onclick="event.stopPropagation()"><input type="checkbox" '+(allChk?"checked ":"")+`onchange="App._bomZoneAll('`+proj.id+"','"+z.kind+`',this.checked)">整區納入</label>`:"")+'<span class="bom-zsum '+(zSum>0?"up":"dn")+'">'+(rows.length?App._bomFmt(zSum):"")+"</span></div>"+(open?'<div class="bom-zscroll"><table class="bom-tbl"><thead><tr>'+heads.map(h=>"<th>"+h+"</th>").join("")+"</tr></thead><tbody>"+body+"</tbody></table></div>"+(z.kind==="priceOnly"&&rows.length?'<div class="bom-znote">'+(npi?"同料號、同用量、只單價不同（供應商跨代報價變動），可整批或逐行決定是否納入成本比對。":"同料號、同用量、只單價不同（採購談降價）＝採購 performance，非設變本體，可整批或逐行決定是否納入。")+"</div>":""):"")+"</div>"};App._bomGuard=function(projId){const p=App.getProj(projId);if(p&&p.ecnType&&App._ecnLocked(p)){U.toast("已結案（唯讀）——如需變更請點右上「翻案重啟」","warning",{soft:true});return true}return false};App._bomSet=function(projId,field,val){if(App._roGuard())return;if(App._bomGuard(projId))return;const p=App.getProj(projId);if(!p)return;App._bomEnsure(p);if(field==="ot_mold")p.oneTimeCost.mold=parseFloat(val)||0;else if(field==="ot_cert")p.oneTimeCost.cert=parseFloat(val)||0;else if(field==="bomQuoteCurrency"||field==="bomBaseCurrency"||field==="roiType")p[field]=val;else p[field]=parseFloat(val)||0;if(p.bomModels&&p.bomModels[p.bomModelIdx]&&(field==="targetUnitCost"||field==="unitSellPrice"||field==="annualVolume"||field==="targetSavePerUnit")){p.bomModels[p.bomModelIdx][field]=p[field]}Store.projects.save();App._bomRerender(p)};App._bomRowSet=function(projId,rowId,field,val){if(App._roGuard())return;if(App._bomGuard(projId))return;const p=App.getProj(projId);if(!p)return;const r=(p.bomRows||[]).find(x=>x.id===rowId);if(!r)return;if(field==="includeInTarget")r.includeInTarget=!!val;else if(field==="partNoA"||field==="replacePartNoB"||field==="switchMode")r[field]=val;else{r[field]=parseFloat(val)||0;if(r.changeKind==="priceOnly"&&field==="newQty")r.oldQty=r.newQty}App._bomSyncDead(p);Store.projects.save();App._bomRerender(p)};App._bomRowDel=function(projId,rowId){if(App._roGuard())return;if(App._bomGuard(projId))return;const p=App.getProj(projId);if(!p)return;p.bomRows=(p.bomRows||[]).filter(x=>x.id!==rowId);App._bomSyncDead(p);Store.projects.save();App._bomRerender(p)};App._bomZoneToggle=function(kind){App._bomZoneOpen=App._bomZoneOpen||{};App._bomZoneOpen[kind]=App._bomZoneOpen[kind]===false;const p=App.getProj(App.currentProjectId);if(p)App._bomRerender(p)};App._bomZoneAll=function(projId,kind,checked){if(App._roGuard())return;if(App._bomGuard(projId))return;const p=App.getProj(projId);if(!p)return;(p.bomRows||[]).forEach(r=>{if(r.changeKind===kind)r.includeInTarget=checked});Store.projects.save();App._bomRerender(p)};App._bomSyncDead=function(p){App._bomEnsure(p);p.oneTimeCost.deadStock=(p.bomRows||[]).reduce((a,r)=>a+App._bomDead(r),0)};App._bomDriftPart=function(r){if(!r||r.changeKind==="del")return"";if(r.changeKind==="rev")return String(r.replacePartNoB||"").trim()||String(r.partNoA||"").trim();return String(r.partNoA||"").trim()};App._bomPriceDrift=function(proj){const parts=typeof DATA!=="undefined"&&DATA.parts||[];if(!parts.length||!proj||!(proj.bomRows||[]).length)return[];const seen={},out=[];proj.bomRows.forEach(function(r){const pn=App._bomDriftPart(r);if(!pn||seen[pn])return;const mp=parts.find(function(p){return p.partNo===pn});if(!mp)return;const master=parseFloat(mp.unitPrice)||0;if(master<=0)return;const bomPrice=parseFloat(r.newPrice)||0;if(Math.abs(master-bomPrice)<.005)return;seen[pn]=1;out.push({rowId:r.id,partNo:pn,bomPrice,masterPrice:master,vendor:mp.vendor||"",diff:master-bomPrice})});return out};App._bomDriftDetail=function(projId){const p=App.getProj(projId);if(!p)return;const drift=App._bomPriceDrift(p);if(!drift.length){U.toast("目前無單價差異");return}const base=p.bomBaseCurrency||"";const rows=drift.map(function(d){return'<tr><td class="dq-lead">'+U.esc(d.partNo)+"</td><td>"+(U.esc(d.vendor)||"—")+'</td><td class="r">'+App._bomMoney(d.bomPrice)+'</td><td class="r">'+App._bomMoney(d.masterPrice)+'</td><td class="r"><b class="'+(d.diff>0?"up":"dn")+'">'+App._bomFmt(d.diff)+"</b></td></tr>"}).join("");App.openModal({title:"主檔商情 vs BOM 記錄價",wide:true,body:'<div class="drift-box"><div class="drift-note">下列料號的料號主檔最新單價與目前 BOM 記錄的新價不同（同基準幣 '+(U.esc(base)||"—")+' 比較）。「一鍵套用」會把主檔價寫進 BOM 的「新價」欄並重算成本／毛利。</div><div class="dq-scroll"><table class="dq-mx drift-tbl"><thead><tr><th class="dq-lead">料號</th><th>廠商</th><th class="r">BOM 記錄價</th><th class="r">主檔最新價</th><th class="r">差額</th></tr></thead><tbody>'+rows+"</tbody></table></div></div>",footer:`<div style="flex:1"></div><button class="btn-ghost" onclick="App.closeModal()">關閉</button><button class="bxb-cta" onclick="App._bomApplyMasterPrices('`+projId+`')">一鍵套用最新成本</button>`})};App._bomApplyMasterPrices=function(projId){if(App._roGuard&&App._roGuard())return;if(App._bomGuard(projId))return;const p=App.getProj(projId);if(!p)return;const drift=App._bomPriceDrift(p);if(!drift.length){U.toast("目前無單價差異");return}App.confirmModal({title:"套用主檔最新成本？",msg:"將把 "+drift.length+" 筆料號的主檔最新單價寫進本案 BOM 的「新價」，並重算成本與毛利（改了無法還原先前值）。",okText:"確認套用",cancelText:"再想想",onConfirm:function(){const byId={};drift.forEach(function(d){byId[d.rowId]=d.masterPrice});(p.bomRows||[]).forEach(function(r){if(byId[r.id]!=null)r.newPrice=byId[r.id]});App._bomSyncDead(p);Store.projects.save();App.closeModal();App._bomRerender(p);U.toast("✓ 已套用 "+drift.length+" 筆最新成本 · 已重算成本／毛利")}})};App._bomAddManual=function(projId){if(App._roGuard())return;if(App._bomGuard(projId))return;App.openModal({title:"手動輸入差異行 — 選差異類型",body:'<div class="bom-pick">'+App._BOM_ZONES.map(z=>`<button class="bom-pick-btn" onclick="App._bomCreateRow('`+projId+"','"+z.kind+`')"><b>`+z.name+"</b><span>"+{add:"料號只在新版（增料）",del:"料號只在舊版（刪料，呆滯大來源）",rev:"同料改量／A 換 B 替代料",priceOnly:"同料同量、只單價變（採購降價）"}[z.kind]+"</span></button>").join("")+'</div><div class="field-hint">建立後直接在該區行內填品號/價量/切換/庫存；「納入」預設勾選。'+(((App.getProj(projId)||{}).bomModels||[]).length?"多機種模式：寫入「檢視機種明細」目前選中的機種。":"")+"</div>",footer:'<button class="tb-action ghost" onclick="App.closeModal()">取消</button>'})};App._bomCreateRow=function(projId,kind){if(App._bomGuard(projId))return;const p=App.getProj(projId);if(!p)return;App._bomEnsure(p);p.bomRows.push({id:U.id(),changeKind:kind,partNoA:"",replacePartNoB:"",oldPrice:0,oldQty:kind==="add"?0:1,newPrice:0,newQty:kind==="del"?0:1,switchMode:"running",stockQty:0,includeInTarget:true,approveStatus:""});App._bomZoneOpen=App._bomZoneOpen||{};App._bomZoneOpen[kind]=true;Store.projects.save();App.closeModal();App._bomRerender(p)};App._bomExportWizard=function(projId){const p=App.getProj(projId);if(!p)return;if(!p.bomSheets||!p.bomSheets.new||!p.bomSheets.old){U.toast("⚠ 尚無完整 BOM 資料——先「匯入舊版＋匯入新版 → 差異比對」，之後隨時可匯出","warning");return}const headers=p.bomSheets.new.grid[0]||[];const saved=p.bomExportCols?p.bomExportCols.slice():null;const isOn=h=>{if(!saved)return true;const k=saved.indexOf(String(h));if(k===-1)return false;saved.splice(k,1);return true};this.openModal({wide:true,title:"匯出 BOM · ROI — 選呈現模式與欄位",body:'<div class="bxw"><div class="imap-sect">① BOM 分頁呈現模式</div><div class="bom-pick"><button class="bom-pick-btn bxw-mode on" data-mode="compare" onclick="App._bxwMode(this)"><b>對照模式（標準作法）</b><span>以舊版全表為底：舊料灰底、新料黃底插在所屬位置下方，不刪舊料、自行比對</span></button><button class="bom-pick-btn bxw-mode" data-mode="replace" onclick="App._bxwMode(this)"><b>替換模式</b><span>以新版全表為準：變更列黃底、舊料不顯示</span></button></div><div class="imap-sect">② 成本差異明細呈現方式</div><div class="bom-pick"><button class="bom-pick-btn bxw-dl'+(p.bomDiffLayout!=="raw"?" on":"")+'" data-dl="curated" onclick="App._bxwDlayout(this)"><b>系統建議 · 精簡對照（較好閱讀）</b><span>剔除來源重複的單價/用量/金額欄，改用「舊用量｜新用量、舊單價｜新單價」成對並排，一眼看懂差異</span></button><button class="bom-pick-btn bxw-dl'+(p.bomDiffLayout==="raw"?" on":"")+'" data-dl="raw" onclick="App._bxwDlayout(this)"><b>維持原欄位 · 後方附加</b><span>保留你在下方勾選的所有原始欄位，ROI 欄（舊/新單價·用量·價差）附加在最後（舊行為）</span></button></div><div class="imap-sect">③ 要保留的欄位（'+headers.length+' 欄，ERP 帶出的用不到就取消勾）<span class="bxw-all"><a onclick="App._bxwAll(true)">全選</a>｜<a onclick="App._bxwAll(false)">全不選</a></span></div><div class="bxw-cols">'+headers.map((h,i)=>h==null||String(h).trim()===""?"":'<label class="bxw-col"><input type="checkbox" class="bxw-chk" value="'+i+'"'+(isOn(h)?" checked":"")+"> "+U.esc(toTrad(String(h)))+"</label>").join("")+'</div><div class="field-hint">勾選的欄位將套用至匯出的 Excel 報表，黃底輸入格會自動計算。勾選會記住，下次直接匯。</div></div>',footer:`<button class="tb-action ghost" onclick="App.closeModal()">取消</button><button class="tb-action" onclick="App._bxwGo('`+projId+`')">匯出 Excel</button>`})};App._bxwMode=function(btn){document.querySelectorAll(".bxw-mode").forEach(b=>b.classList.remove("on"));btn.classList.add("on")};App._bxwDlayout=function(btn){document.querySelectorAll(".bxw-dl").forEach(b=>b.classList.remove("on"));btn.classList.add("on")};App._bxwAll=function(on){document.querySelectorAll(".bxw-chk").forEach(c=>{c.checked=on})};App._bxwGo=function(projId){const p=App.getProj(projId);if(!p)return;const mode=(document.querySelector(".bxw-mode.on")||{}).dataset?document.querySelector(".bxw-mode.on").dataset.mode:"compare";const diffLayout=(document.querySelector(".bxw-dl.on")||{}).dataset?document.querySelector(".bxw-dl.on").dataset.dl:"curated";const colIdx=[...document.querySelectorAll(".bxw-chk")].filter(c=>c.checked).map(c=>parseInt(c.value,10));if(!colIdx.length){U.toast("⚠ 至少勾選一個欄位","warning");return}const headers=p.bomSheets.new.grid[0]||[];p.bomExportCols=colIdx.map(i=>String(headers[i]));p.bomDiffLayout=diffLayout;Store.projects.save();App.closeModal();App.exportBomRoi(p,{mode,colIdx,diffLayout})};App._bomWholeWizard=function(projId){if(App._bomGuard(projId))return;const p=App.getProj(projId);if(!p)return;const bs=p.bomSheets;if(!bs||!bs.new||!bs.new.grid){U.toast("⚠ 先匯入舊/新版並「差異比對」，才有完整 BOM 可設定加總基準","warning");return}const headers=bs.new.grid[0]||[];const opts=headers.map(h=>h==null||String(h).trim()===""?"":'<option value="'+U.esc(String(h))+'">'+U.esc(toTrad(String(h)))+"</option>").join("");const cur=p.bomWholeMethod||"";const wc=p.bomWholeCols||{};App._bwmMethod=cur;const selHtml=(id,label,val)=>'<div class="bom-cfield"><label>'+label+'</label><select id="'+id+'"><option value="">— 自動偵測 / 不指定 —</option>'+headers.map(h=>h==null||String(h).trim()===""?"":'<option value="'+U.esc(String(h))+'"'+(String(val||"")===String(h)?" selected":"")+">"+U.esc(toTrad(String(h)))+"</option>").join("")+"</select></div>";const mbtn=(mk,title,desc)=>'<button class="bom-pick-btn'+(cur===mk?" on":"")+'" data-wm="'+mk+'" onclick="App._bwmPick(this)"><b>'+title+"</b><span>"+desc+"</span></button>";App.openModal({title:"整機總額計算基準 — 選葉節點認定方式",body:'<div class="field-hint">不同 BOM 檔結構不一。系統預設<b>自動判斷</b>（有階次欄→取無子件的葉節點；否則材料類別 M 料）。判不準或想改基準時在此手動指定，選擇會記住隨專案同步。</div><div class="bom-pick">'+mbtn("","自動偵測","階次欄優先 → 材料類別；判不出會提示手動")+mbtn("level","依階次葉節點","取「下一列階次未更深」的無子件料號，Σ 單價×用量")+mbtn("matCat","依材料類別","只加總「材料/採購件（M）」列，排除組合/加工品（A）")+mbtn("sumAll","全部料加總","不判階層，所有料號 Σ 單價×用量（結構單純時用）")+mbtn("topRow","用金額欄頂列","BOM 頂層已滾算總成本時，直接取指定金額欄第一列")+'</div><div class="bom-wm-cols">'+selHtml("bwm-level","階次欄（依階次葉節點時可指定）",wc.level)+selHtml("bwm-matcat","材料類別欄（依材料類別時可指定）",wc.matCat)+selHtml("bwm-amount","金額欄（用金額欄頂列時指定）",wc.amount)+'</div><div class="field-hint">未指定欄位＝沿用系統自動偵測到的欄；找不到才需在此手動選。</div>',footer:`<button class="tb-action ghost" onclick="App.closeModal()">取消</button><button class="tb-action" onclick="App._bomWholeApply('`+projId+`')">套用</button>`})};App._bwmPick=function(btn){document.querySelectorAll(".bom-pick-btn[data-wm]").forEach(b=>b.classList.remove("on"));btn.classList.add("on");App._bwmMethod=btn.dataset.wm};App._bomWholeApply=function(projId){if(App._roGuard())return;if(App._bomGuard(projId))return;const p=App.getProj(projId);if(!p)return;p.bomWholeMethod=App._bwmMethod||null;const gv=id=>{const e=document.getElementById(id);return e&&e.value?e.value:null};const cols={level:gv("bwm-level"),matCat:gv("bwm-matcat"),amount:gv("bwm-amount")};p.bomWholeCols=cols.level||cols.matCat||cols.amount?cols:null;Store.projects.save();App.closeModal();App._bomRerender(p)};App._ecnTabCHtml=function(proj){const ev=proj.ecnEvents||[];if(!ev.length)return'<div class="ecn-ph"><div class="ecn-ph-ico">🕓</div><b>事件時間軸</b><p>尚無事件紀錄。</p></div>';return'<div class="ecn-timeline">'+ev.map(e=>'<div class="ecn-tl-item"><span class="ecn-tl-dot"></span><span class="ecn-tl-date">'+U.esc(e.date||"")+'</span><span class="ecn-tl-label">'+U.esc(e.label||e.type||"")+"</span>"+(e.cause?'<span class="ecn-tl-cause">'+U.esc(e.cause)+"</span>":"")+"</div>").join("")+"</div>"};
